/**
 * Company contact details — single source of truth for the phone / WhatsApp
 * number used across the app (Call Us cards, legal pages, contact emails, and
 * the WhatsApp support widget). Update here to change it everywhere.
 */

/** Display form shown to users. */
export const COMPANY_PHONE_DISPLAY = "+91-777-888-9674";

/** E.164 form for `tel:` links. */
export const COMPANY_PHONE_E164 = "+917778889674";

/** Digits-only (country code + number) for wa.me WhatsApp deep links. */
export const COMPANY_WHATSAPP_DIGITS = "917778889674";

/** Support hours line shown alongside the phone number. */
export const COMPANY_SUPPORT_HOURS = "10AM to 6PM (IST), Monday - Saturday";
