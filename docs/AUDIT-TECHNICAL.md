# Rescan-4 — Whole-Project Audit (2026-05-22)

Fresh audit after rescans 1–3 closed every known H/M/L bug. Different mandate: architecture, code-quality, frontend, schema drift, operational, and DX concerns that prior bug-hunting passes deprioritised.

All four HIGH findings have been verified against the actual code, not just trusted from the audit agent.

---

## 🎯 In-flight & open work

Surfaced at the top so the active surface is visible without scrolling through the ~200-row history table below.

### 🔄 Pending deploy

Live revision: **`dms-00124-bjk`** at https://dms-5itdvlx2va-ew.a.run.app (deployed 2026-06-12 07:25Z, includes slice 7hn)

Committed slices NOT yet in the live revision:

_(none — fully caught up)_

### 🎯 Currently working on

- **Fully caught up through `7hn`** (live at `dms-00124-bjk`). 40 rescan-4 route-handler slices `7ga–7hn` shipped 2026-06-08 + 2026-06-10 + 2026-06-11 + 2026-06-12 across 23 deploys.
- **Pattern**: read source → mock all dependencies → pin security gates (rate-limit, cache contract, fallback paths) + error mapping → focused vitest → full suite + tsc → commit + audit MD row in bullet format.

### 📋 Backlog (with assigned batch numbers)

Each item below has a tentative batch number from the next-available sequence (after `7g6`). Sub-tasks broken into trackable bullets — tick them off as slices ship.

#### 🔄 Route-handler test slices (continuing the 7g* sequence)

- [x] ~~**Batch 7g7** — `/api/admin/hosting/provision` POST~~ ✅ live `dms-00100-tfj`
- [x] ~~**Batch 7g8** — `/api/admin/hosting/packages` GET + POST + PATCH~~ ✅ live `dms-00100-tfj`
- [x] ~~**Batch 7g9** — `/api/admin/pending-domains` GET (LIST) — 324 lines~~ ✅ live `dms-00100-tfj`
- [x] ~~**Batch 7ga** — `/api/admin/order-management` route — large admin surface~~ ✅ live `dms-00101-xr2`
- [x] ~~**Batch 7gb** — `/api/domains/pricing` route (customer-facing, RC cache + per-TLD live bypass)~~ ✅ live `dms-00101-xr2`
- [x] ~~**Batch 7gc** — `/api/user/hosting/stats` route — 365 lines, user-facing mirror of 7g6~~ ✅ live `dms-00102-7q5`
- [x] ~~**Batch 7gd** — Email-change flow (POST request + GET confirm) — account-takeover surface~~ ✅ live `dms-00102-7q5`
- [x] ~~**Batch 7ge** — Customer nameserver-change `/api/user/domains/nameservers` POST (authz / IDOR guard)~~ ✅ live `dms-00103-dmg`
- [x] ~~**Batch 7gf** — Public contact form `/api/contact` POST (reCAPTCHA + anti-XSS sanitization)~~ ✅ live `dms-00103-dmg`
- [x] ~~**Batch 7gg** — Customer domain-watch `/api/user/domains/watch` (GET+POST+DELETE)~~ ✅ live `dms-00103-dmg`
- [x] ~~**Batch 7gh** — Razorpay webhook `/api/webhooks/razorpay` POST — 5-layer defense (HMAC + replay window + Redis nonce)~~ ✅ live `dms-00103-dmg`
- [x] ~~**Batch 7gi** — Customer dashboard `/api/user/dashboard` GET (aggregation feed; auth dual-path, IDOR scope, sync cooldown, stats math)~~ ✅ live `dms-00104-7qx`
- [x] ~~**Batch 7gj** — Subscription create + cancel pair (`/api/payments/create-subscription` + `/api/payments/cancel-subscription`) — IDOR scope + Razorpay error masking~~ ✅ live `dms-00105-8mw`
- [x] ~~**Batch 7gk** — Customer DNS-management list `/api/user/domains/dns` GET (registered-only filter + cross-order dedupe + IDOR scope)~~ ✅ live `dms-00106-fw8`
- [x] ~~**Batch 7gl** — Customer support tickets `/api/user/support` GET + POST (rate-limit + anti-XSS admin email + attachment validation order)~~ ✅ live `dms-00107-fgm`
- [x] ~~**Batch 7gm** — Public status endpoints 4-route bundle: health, maintenance, captcha, account-status (fail-open vs fail-closed + auto-expire + deliberate-enumeration)~~ ✅ live `dms-00107-fgm`
- [x] ~~**Batch 7gn** — Customer order endpoints 3-route bundle: `/api/orders` list + `/api/orders/[id]` + `/api/user/orders/[id]` (auth dual-path + IDOR scope + customer-safe field allow-list)~~ ✅ live `dms-00109-tjv`
- [x] ~~**Batch 7go** — Admin list endpoints 3-route bundle: `/api/admin/users/deactivated` + `/api/admin/support-tickets` + `/api/admin/hosting/pending` (admin-gate variants + page clamp quirk + error-leak inconsistency pinned)~~ ✅ live `dms-00110-k29`
- [x] ~~**Batch 7gp** — Client log-forwarder `/api/log` POST + admin IP-status `/api/admin/ip-status` GET (anti-DoS bounds + anti-logging-loop silent fail + connectDB ordering quirk)~~ ✅ live `dms-00111-d6x`
- [x] ~~**Batch 7gq** — 3-route bundle: `/api/admin/users/no-hosting` + `/api/public/hosting-test-plan` + `/api/admin/invoices` (PII leak guard + strict-boolean feature flag + NaN-quirk pair)~~ ✅ live `dms-00112-6x7`
- [x] ~~**Batch 7gr** — Pending-hosting admin actions pair: `/api/admin/hosting/pending/[id]` DELETE + `/api/admin/hosting/pending/[id]/retry` POST (cron-path argshape lock + dropped vs provisioned message branch + error-leak pattern)~~ ✅ live `dms-00113-tkg`
- [x] ~~**Batch 7gs** — 2-route bundle: `/api/admin/resellerclub/balance` GET + `/api/user/invoices/sync` POST (Prepaid/NoBilling branch + secret-leak guards on RC + Zoho error paths)~~ ✅ live `dms-00113-tkg`
- [x] ~~**Batch 7gt** — Admin invoice-PDF + clear-invoice-number pair (binary PDF response shape + idempotent no-op contract)~~ ✅ live `dms-00114-hh6`
- [x] ~~**Batch 7gu** — Admin change-package + customer renew-info (DA typed-outcome 5-branch dispatch + 1-year-only renewal pricing rule + response-curation leak guard)~~ ✅ live `dms-00115-xbl`
- [x] ~~**Batch 7gv** — Admin user-management pair: `/api/admin/users/reset-2fa` POST + `/api/admin/users/reactivate` POST (admin-protection guard + isDeleted precondition + TOTP-secret leak guard)~~ ✅ live `dms-00115-xbl`
- [x] ~~**Batch 7gw** — Cron-hosting-expiry + admin-log-error (dual/triple auth + per-item failure isolation + anti-recursive-log-storm console-on-fail)~~ ✅ live `dms-00115-xbl`
- [x] ~~**Batch 7gx** — Admin reset-password (step-up re-auth + bcrypt) + admin domains-sync (single-vs-batch + 100-cap + 500ms anti-rate-limit pause)~~ ✅ live `dms-00115-xbl`
- [x] ~~**Batch 7gy** — TOTP setup (GET status + POST enrollment) + user hosting auto-renew PATCH (already-enabled guard + pending-secret pattern + active-only precondition)~~ ✅ live `dms-00115-xbl`
- [x] ~~**Batch 7gz** — Auth-me (boolean has-password, no hash leak) + user-invoice-PDF (10/min rate, sentinel-id guard, MongoDB-before-Zoho IDOR, inline disposition)~~ ✅ live `dms-00116-blx`
- [x] ~~**Batch 7ha** — Public resend-activation (rate-limit-first + anti-enumeration) + admin nameserver-change (admin-scope variant of 7ge)~~ ✅ live `dms-00116-blx`
- [x] ~~**Batch 7hb** — TOTP confirm (8 backup codes, plaintext-once + hashed-at-rest) + TOTP disable (password-FIRST step-up + TOTP-or-backup-code)~~ ✅ live `dms-00116-blx`
- [x] ~~**Batch 7hc** — User invoices list (self-heal inline-retry) + cancel-trial (3-step termination with failure isolation; one-trial-per-user policy preserved)~~ ✅ live `dms-00117-f7f`
- [x] ~~**Batch 7hd** — Auth-activate (rate-limit-first + expired-vs-unknown branch + token-clear-before-JWT) + domain-verify-status (RC typed-outcome 3-branch dispatch + strict 'Active' match)~~ ✅ live `dms-00117-f7f`
- [x] ~~**Batch 7he** — Invoice-pay (IDOR with anti-enumeration 404 + anti-double-pay) + admin re-sync-invoice (stuck-status reset + Zoho soft-failure path)~~ ✅ live `dms-00118-m5v`
- [x] ~~**Batch 7hf** — Auth-reset-password (5-layer defense incl. CRITICAL admin-block) + hosting-check-eligibility (GET+POST shared helper + one-trial-per-user via 2 sources)~~ ✅ live `dms-00118-m5v`
- [x] ~~**Batch 7hg** — Trial-OTP send (rate-limit + phone normalisation + 502 SMS-provider signal) + verify (single-use consume + signed token, NOT raw OTP)~~ ✅ live `dms-00119-6z5`
- [x] ~~**Batch 7hh** — Admin service-users overview (session-auth + aggregation+fallback dedup + no-live-DA) + pending-domains bulk verify (100-cap + 3-branch outcome dispatch)~~ ✅ live `dms-00119-6z5`
- [x] ~~**Batch 7hi** — Admin DA diagnostics (Promise.allSettled fan-out + optional cleanup) + bulk DA-cleanup (typed-outcome 4-branch + 100-cap + failure isolation)~~ ✅ live `dms-00119-6z5`
- [x] ~~**Batch 7hj** — Deep health-check (Mongo+Redis 3s-bounded + 503-on-fail) + test-automation status (env-gated admin + service-type+expiry-field branch + simulated-now)~~ ✅ live `dms-00120-4j5`
- [x] ~~**Batch 7hk** — Hosting SSO (cross-tenant guard + suspension block + browser/JSON dual response) + upgrade-info (upgrades-only filter + prorated ₹100-floor formula + expired guard)~~ ✅ live `dms-00121-fh2`
- [x] ~~**Batch 7hl** — Admin manual hosting-assign (admin-gate first + DA username gen 8+4 base36 ≤14 + DNS update best-effort swallow + `already exists` 409) + cron-check-unprovisioned (dual auth + CONCURRENCY=5 chunked retry fan-out via `Promise.allSettled` + 30-min stuck-orders watchdog + email-send swallow)~~ ✅ live `dms-00122-x8p`
- [x] ~~**Batch 7hm** — Admin DNS-activate (admin gate + status='registered' guard + already-active 400 with `force=true` override + RC `activateDNSManagement` failure swallow + local-mark survives RC throw + no-resellerOrderId warn-and-skip + legacy top-level RC orderId fallback) + test-automation-trigger (admin-OR-non-prod auth + `ENABLE_TIME_SIMULATION` 403 + zod-enum service-type guard + hosting/domain fan-out + force-eligible mutation + scheduler fetch w/ `x-cron-secret` + `x-simulated-time` headers; family-quirk raw-msg leak pinned)~~ ✅ live `dms-00123-kp2`
- [x] ~~**Batch 7hn** — Auth-forgot-password (7-layer defense: CSRF → rate-limit-BEFORE-body-parse → zod → reCAPTCHA w/ clientIP fallback chain → anti-enumeration triple-indistinguishable response → admin-via-public-flow block → `crypto.randomBytes(32).toString('hex')` 1h token + `isFirstTimeSetup = user.isGuest === true` strict-truth branch; token-saved-BEFORE-email pinned for reissue path) + check-ip (4-service hard-coded fan-out + JSON/text dispatch + per-service try/catch failure isolation + all-fail null-fallback + 4-field server-info echo lock with cookie/auth negative leak guard; FAMILY-QUIRK: dedup-before-vote means "most common" picks last-distinct-IP)~~ ✅ live `dms-00124-bjk`
- [ ] **Batch 7ho** — More untested route handlers (sweep continues: chat / register / complete-profile / domains/* / etc.)

#### 🔄 M14 component-test remainders

- [ ] **Batch 7m14-1** — `MultiStageRegisterForm` (multi-step + Zod field-error tree); the audit's largest remaining component
- [ ] **Batch 7m14-2** — Admin/dashboard layout shells (UserLayout / AdminLayout / DashboardSidebar — mostly presentational)
- **Out of scope by design**: payment-handler / SSE / cross-origin / JWT-boundary flows stay un-tested

#### 🔄 axe-core runtime a11y

- [ ] **Batch 7axe-1** — Wire axe-core into CI as a vitest matcher (now unblocked — the M14 harness is in place)
- [ ] **Batch 7axe-2** — Per-component a11y remediation as failures surface (depends on 7axe-1)

#### ⏸ M8 — PendingDomain._id ObjectId (deferred)

- [ ] **Batch 7m8-pre** — Prod data audit: scan PendingDomain collection for non-ObjectId `_id` values (1-time read query)
- [ ] **Batch 7m8** — Tighten `_id: Schema.Types.Mixed` → `ObjectId` (depends on audit being clean) + migration script for any leftovers

#### ⏸ M4 — 5 page components > 1000 lines (refactor, not test)

These are refactor work, not test work. Multi-day each. Risk-vs-reward of splitting depends on which page; recommend tackling in priority order:

- [ ] **Batch 7m4-1** — `app/admin/user-management/page.tsx` (1410 lines)
- [ ] **Batch 7m4-2** — `app/dashboard/dns-management/page.tsx` (1379 lines)
- [ ] **Batch 7m4-3** — `app/admin/hosting/page.tsx` (1314 lines)
- [ ] **Batch 7m4-4** — `app/admin/system-settings/page.tsx` (1250 lines)
- [ ] **Batch 7m4-5** — `app/admin/dns-management/page.tsx` (1047 lines)

#### ⏸ M6 deeper — Per-component client→server refactor

- [ ] **Batch 7m6-sweep** — Per-component review of the remaining ~110 `'use client'` components. The easy leaf demotions shipped in 7x. The remaining group genuinely need client features (framer-motion / event-handler props / forwardRef-with-refs / Radix), so further M6 progress requires component-splitting refactors, not one-shot demotions. Recommended approach: identify the top-N most-imported `'use client'` components and split their interactive bits into smaller client islands.

---

## Status

| Batch | Findings | Status | Commit(s) |
|---|---|---|---|
| Batch 7a | H1 — cart store NextAuth migration | ✅ Closed | `c4aec87` |
| Batch 7b | H2 — PendingDomain compound unique + prod migration | ✅ Closed | `45e8e85`, `caece03` |
| Batch 7c | H3 — Bearer-token cleanup (38 files, −917 lines) | ✅ Closed | `6bdd43b` |
| Batch 7d | H4 — Mongo `maxPoolSize` 10 → 50 + Cloud Run pairing | ✅ Closed | `9b60a9f` |
| Batch 7e | M2 + M3 + M5 + M7 + M9 + M10 + M11 + M12 + M15 + L2 | ✅ Closed | `8aee422` |
| Batch 7f | L3 + L4 + L5 + L6 + L7 + L9 + L10 + L11 | ✅ Closed | `5ee9c94` |
| Batch 7g | M1 slice 1 — RC `registerDomain` anti-corruption (13 tests) | ✅ Closed | `67390e4` |
| Batch 7h | M1 slice 2 — DA `createUser` anti-corruption (7 tests) | ✅ Closed | `6391de2` |
| Batch 7i | M1 slice 3 — RC `renewDomain` + /domains/renew 202-on-pending | ✅ Closed | `2fca12a` |
| Batch 7j | M1 slice 4 — inner/outer RC fragment vocab unified | ✅ Closed | `c31bf1e` |
| Batch 7k | M1 slice 5 — DA `suspendUser` + expiry-worker not-found skip | ✅ Closed | `292802c` |
| Batch 7l | M1 slice 6 — DA `unsuspendUser` + 4 callsite migrations | ✅ Closed | `90ac494` |
| Batch 7m | M1 slice 7 — RC `transferDomain` + user-actionable 400 on registry-reject | ✅ Closed | `facfb87` |
| Batch 7n | M1 slice 8 — DA `getUserConfig` + sync-worker inline-parser removed | ✅ Closed | `23fda8e` |
| Batch 7o | M1 slice 9 — DA `deleteUser` + admin/hosting/actions sweep | ✅ Closed | `ba451f2` |
| Batch 7p | M1 slice 10 — RC `getDomainOrderId` + `getDomainDetails` (4 callsites, 18 new tests) | ✅ Closed | `c76ce35` |
| Batch 7q | M1 slice 11 — RC `getDNSRecords` (2 callsites, 9 new tests) | ✅ Closed | `a4b4422` |
| Batch 7r | M1 slice 12 — DA `changePackage` + upgrade-flow + admin route (7 new tests) | ✅ Closed | `26d201f` |
| Batch 7s | M13 partial — delete dead `error.message.includes` chain in verification-error.ts (-65 LOC) | ✅ Closed | `299f84c` |
| Batch 7t | L1 — typed Razorpay SDK client (10 `as unknown as` casts → 1) | ✅ Closed | `9b6d018` |
| Batch 7u | M14 partial — extract pure cart-validation helpers + 21 unit tests | ✅ Closed | `0011477` |
| Batch 7v | M14 continued — store-level cart tests (addItem/removeItem cascade, getters, syncWithServer) — 20 new tests | ✅ Closed | `12d2cbd` |
| Batch 7w | L8 — React error boundaries (FloatingCart + ChatWidget + CartPage) | ✅ Closed | `2339a2a` |
| Batch 7w.2 | L12 partial — enable `plugin:jsx-a11y/recommended` (lint surface, 144 warnings flagged) | ✅ Closed | `7c466b5` |
| Batch 7x | M6 partial — demote 14 leaf components from `'use client'` to server | ✅ Closed | `0f35e5f` |
| Batch 7y | S2 — pre-commit hooks (husky + lint-staged + tsc --noEmit) | ✅ Closed | `2660cfe` |
| Batch 7z | S3 partial — `validatedBody` / `validatedQuery` helpers + 5-route sweep (10 new tests) | ✅ Closed | `067da79` |
| Batch 7z.2 | S3 continued — 6 more routes validated (payments/create-subscription, hosting/renew, hosting/cancel-trial, hosting/auto-renew, domains/nameservers, settings/change-email) | ✅ Closed | `e87ad6a` |
| Batch 7z.3 | S3 continued — 5 more routes (admin/razorpay-mode discriminated-union, hosting/upgrade, contact, user/support, contact `.sanitized` typing fix) | ✅ Closed | `8b4aac3` |
| Batch 7z.4 | S3 continued — 6 more routes (user/support/[id], admin/support-tickets/[id], trial-otp send + verify, trial-eligibility, chat) | ✅ Closed | `1dfc073` |
| Batch 7z.5 | S3 continued — 6 admin write routes (hosting/change-package, hosting/actions discriminated-union, hosting/assign, users/reactivate, users/reset-password, log-error) | ✅ Closed | `a978849` |
| Batch 7z.6 | S3 continued — 6 more admin write routes (domains/nameservers, domains/activate-dns, domains/sync, diag-da/cleanup, reset-password, tld-pricing/cache) | ✅ Closed | `60cf32f` |
| Batch 7z.7 | S3 continued — 6 admin routes (hosting/test-plan, hosting/packages POST + PATCH, hosting/provision, backup, users/[id] PUT, pending-domains/verify) | ✅ Closed | `ba4f8d5` |
| Batch 7z.8 | S3 continued — 6 routes (admin/settings, admin/pending-domains POST, auth/activate, workers/sync-zoho-invoice, workers/sync-hosting-status, workers/process-hosting-expiry) | ✅ Closed | `d524fd9` |
| Batch 7z.9 | S3 continued — 6 routes (workers/process-service-expiry, auth/check-account-status, totp/confirm, totp/disable, resend-activation, admin/domains/dns POST + DELETE) | ✅ Closed | `33ee8ff` |
| Batch 7z.10 | S3 continued — 6 user-facing domain routes (search, activate-dns, renew GET + POST, booking-status GET + POST, verify-status, transfer) | ✅ Closed | `993272a` |
| Batch 7z.11 | S3 continued — 6 routes (log, domains/bulk-search, domains/nameservers, cart, test/automation/trigger, admin/pending-domains/[id]) + latent bug fix | ✅ Closed | `a9317ec` |
| Batch 7z.12 | S3 complete — 3 payment routes (verify, guest/verify, guest/create-order) — **73/73 routes validated** | ✅ Closed | `5355331` |
| Batch 7aa | S1 partial — typed frontend `apiClient` (`ApiResult<T, ApiError>` + Zod-aware) + 3 migrated callsites + 12 new tests | ✅ Closed | `bd4edbf` |
| Batch 7ab | S1 continued — 5 more frontend callsites migrated to `apiClient` (AdminPasswordReset, ProfileCompletionForm, DomainBookingProgress, DomainCrossSell, GoogleRecaptcha) | ✅ Closed | `6d6b714` |
| Batch 7ac | S1 continued — 4 more frontend callsites (ResetPasswordForm, LivePricingIndicator, TrialOtpModal ×2, DomainSetup) | ✅ Closed | `e9c5458` |
| Batch 7ad | S1 continued — 3 more components, 6 fetches (MultiStageRegisterForm, DomainSearch watch, InvoiceDiagnostics ×4) | ✅ Closed | `ba0602c` |
| Batch 7ae | S1 continued — first app/ pages (maintenance, activate ×3) + `cache` option added to apiClient | ✅ Closed | `cbe3301` |
| Batch 7af | S1 continued — 2 admin pages (invoices list-GET, pricing-management GET + DELETE) | ✅ Closed | `02b2d7f` |
| Batch 7ag | S1 continued — 3 pages (domains/transfer, domains/bulk-search, admin/domains GET + sync POST) | ✅ Closed | `d390f38` |
| Batch 7ah | S1 continued — dashboard support pages (create-ticket POST, ticket close PATCH + reply POST) | ✅ Closed | `9bd22ea` |
| Batch 7ai | S1 continued — admin support-tickets pages (auth/me + list/detail GETs, reply POST, status + priority PATCH) | ✅ Closed | `c7ed08f` |
| Batch 7aj | S1 continued — admin hosting pending + packages pages (list GETs, retry POST, delete, package PATCH) | ✅ Closed | `9a64032` |
| Batch 7ak | S1 continued — admin order-management (orders GET + delete/archive + unarchive PATCH) + payment-management (payments GET) | ✅ Closed | `9e6f75e` |
| Batch 7al | S1 continued — admin settings/security 2FA page (totp setup GET + POST, confirm POST, disable POST) | ✅ Closed | `525f521` |
| Batch 7am | S1 continued — admin hosting main page (stats GET, provision-deps 2×GET, provision/actions/change-package POSTs, details GET) | ✅ Closed | `ccbef5e` |
| Batch 7an | S1 continued — admin user-management (3×GET parallel, reset-pw/reactivate/reset-2fa POSTs, deactivate + permanent-delete) + apiClient.delete body support | ✅ Closed | `bdd905f` |
| Batch 7ao | S1 continued — admin pending-domains (balance GET, auth/me GET, list GET, register/verify POSTs, archive + permanent DELETEs, resolve + retry PUTs) | ✅ Closed | `a8f5b17` |
| Batch 7ap | S1 continued — admin system-settings (12 fetches: razorpay-mode GET/POST×2, settings GET×3 + POST×4, check-ip GET; backup blob stays raw) | ✅ Closed | `c2646f5` |
| Batch 7aq | S1 continued — dashboard settings (8 fetches: user-settings GET + PUT×2, auth/me GET, totp setup GET/POST + confirm/disable POST; nominatim geocode stays raw) | ✅ Closed | `bb55a93` |
| Batch 7ar | S1 continued — admin dns-management (10 fetches: domains/dns/nameservers GETs, NS default/custom POSTs, activate-dns POST, record add POST, delete + edit DELETEs-with-body) | ✅ Closed | `702c9a8` |
| Batch 7as | S1 continued — dashboard dns-management (12 fetches: services-status + domains + dns + nameservers GETs, NS default/custom POSTs w/ polling, record add POST, delete + edit DELETEs-with-body + restore POST, activate-dns POST) | ✅ Closed | `402fd5d` |
| Batch 7at | S1 continued — admin settings (22 fetches: 9 GET loaders incl. paired cache load, cache PUT + DELETE, 2 check-ip GETs, 8 settings save POSTs, test-plan POST; removed dead authHeaders helper) | ✅ Closed | `547ab37` |
| Batch 7au | S1 continued — dashboard hosting (cancel-trial POST, auto-renew PATCH) + dashboard domains/[id] (services-status + domains + nameservers GETs, NS update POST) | ✅ Closed | `0e8b093` |
| Batch 7av | S1 continued — public hosting page (trial-eligibility POST, test-plan GET) + dashboard home (domains/sync POST) + dashboard invoices (invoices/sync POST) | ✅ Closed | `ac60d54` |
| Batch 7aw | S1 continued — useDomainSearch hook (3 search POSTs: Phase-1 quick, Phase-2 fire-and-forget suggestions, load-more) — one of the two 7ad-deferred items | ✅ Closed | `036f161` |
| Batch 7ax | S1 continued — RegisterForm register POST (the last 7ad-deferred holdout; its 3 cross-origin geocoding fetches stay raw by design) | ✅ Closed | `4d3ac2c` |
| Batch 7ay | M14 continued — first React component tests: `@testing-library/react` harness + `<EmptyCart>` (2) + `<CartOrderSummary>` (10) | ✅ Closed | `f3a3cf9` |
| Batch 7az | M14 continued — `<CartItemCard>` (14): domain/hosting fork, period-label formatting, .ai min-period annotation, billing-cycle lock, price math, multi-year + annual-hosting badges, callbacks | ✅ Closed | `56cc55d` |
| Batch 7ba | M14 continued — CheckoutPage redirect gating (9): admin → /admin/dashboard, incomplete profile → /cart + toast, non-ok auth/me → /login, fetch throw → /login, strict-true check for profileCompleted, loading-status no-op, completed-profile no-redirect; plus the empty-cart-after-load → /dashboard replace | ✅ Closed | `a3e9cde` |
| Batch 7bb | M14 continued — `<ProfileCompletionWarning>` (12): show/hide gating, session+local merge precedence, missing-field grammar (singular vs "X and Y are"), trim check, Complete-now redirect with/without returnUrl, Dismiss hide, profileUpdated event-driven re-check | ✅ Closed | `e243609` |
| Batch 7bc | M14 continued — `<HostingUpsell>` (5): card render (heading + ₹/strike-through/Save-50%/features), add-to-cart CartItem shape (period 12 + yearly + hostingPlan.id), auto-link to existing domain via `linkedDomain`, dedupe with error toast when same plan already in cart, legacy-domain-no-itemType still auto-links | ✅ Closed | `77c745a` |
| Batch 7bd | M14 continued — `<DomainCrossSell>` (8): disabled-when-empty submit, no-dot auto-append `.com`, available/taken/success-false panels, apiClient-failure error toast, exact-match preference, typing clears result, add-to-cart CartItem shape (period 12, itemType domain) | ✅ Closed | `ea272ac` |
| Batch 7be | M14 continued — `<DomainSetup>` (11): heading + plan-name fallback; Link tab — required + invalid-format errors, typing clears error, valid-link 500ms-delay onUpdateDomain + success toast; Buy tab — tab switch, .com auto-append, available/taken panels, apiClient failure, Add & Link fires both onAddDomainToCart (period 1) + onUpdateDomain and clears search state | ✅ Closed | `1dde2fc` |
| Batch 7bf | M14 continued — admin invoice-diagnostics subtree (21 tests / 3 files): `<DiagnosticsHeader>` (6) — issue-vs-clear icon + copy, singular/plural grammar, row-toggle vs refresh-chip stopPropagation, loading spinner; `<ConflictsTable>` (7) — empty null-render, group header, per-row fields, isDeleted tag, zohoInvoiceId truncation + title tooltip, Clear # callback, pendingId disable; `<StuckOrdersTable>` (8) — empty null-render, missing-zoho fallback, single-order hides Re-sync-all, multi-order shows '(N)' + onResyncAll, per-row onResync, bulkProgress bar/counts/label, pendingId disable | ✅ Closed | `b2c3a62` |
| Batch 7bg | M14 continued — auth password-reset forms (11 / 2 files): `<AdminPasswordReset>` (5) — empty/mismatch disable, < 8 chars toast, payload + success-clears-form, route's error.message; `<ResetPasswordForm>` (6) — default vs isSetup headings & button labels, mismatch + length toasts, `{token, password, recaptchaToken}` payload, success-screen swap (Password Reset Complete vs Account Activated copy) | ✅ Closed | `cf4b7be` |
| Batch 7bh | M14 continued — public-facing forms (10 / 2 files): `<ContactForm>` (4) — render, happy-path payload + success-swap, "Send Another Message" return, route's error toast; `<ForgotPasswordForm>` (6) — default vs isSetup headings & button labels, prefilledEmail pre-population, happy-path payload + sent-screen mirror, isSetup-flavoured sent copy/toast, failure inline + toast | ✅ Closed | `83f1620` |
| Batch 7bi | M14 continued — `<LivePricingIndicator>` (5): mount-fetch + price extraction from `data[tld].customer.addnewdomain["1"]` + onPriceUpdate, "Live" badge gating, "Live pricing not available" (success-but-tld-missing) vs "Failed to fetch live pricing" (apiClient !ok), Refresh re-fetch | ✅ Closed | `f1667d6` |
| Batch 7bj | M14 continued — `<TrialOtpModal>` (10): isOpen render gate, defaultPhone normalisation, 10-digit gate on Send OTP, /send + stage-swap on success vs error toast on failure, /verify + sessionStorage token stash + onVerified, 2xx-no-token treated as failure, Change-number resets stage + clears code, Close → onClose | ✅ Closed | `7857dc2` |
| Batch 7bk | M14 continued — operational status leaves (10 / 2 files): `<OutboundIPBadge>` (5) — loading badge, single-IP green vs multi-IP orange + "*" + title-tooltip, success:false + apiClient !ok both surface "IP Error"; `<DomainBookingProgress>` (5) — loading, empty-status fallback, progress % + step messages, domain_registered → onComplete + success panel highlighting domainName, Refresh re-fetch | ✅ Closed | `8515648` |
| Batch 7bl | M14 continued — `<ProfileCompletionForm>` (6): disabled name/email render, forced phoneCc=+91 + address.country=IN on submit, localStorage merge + profileUpdated window event, /dashboard fallback vs `?returnUrl=` honoured, failure-path error toast with no navigation, GST input upper-casing | ✅ Closed | `d39ef58` |
| Batch 7bm | M14 continued — `<SearchInput>` (9): value mirroring, onChange per keystroke, onSearch via button click + Enter-in-container, empty/searching disable rules, isSearching disables input + spinner+label swap, "We'll check .com, .net, .in and more for …" prompt visibility in multi mode / suppression after first search / suppression in single mode | ✅ Closed | `1729a96` |
| Batch 7bn | M14 continued — `<SearchResults>` (10): AnimatePresence pre-render gate, isSearching loading splash, HeroResultCard for results[0] + CompactResultCards for the rest, single-result omits "Other Popular Extensions", inline error banner, Clear Search callback, suggestions skeleton while loading, category-tab filtering of suggestions (All vs Tech etc.), Load More button + isLoadingMore disabled+"Loading More…" swap | ✅ Closed | `38f7587` |
| Batch 7bo | M14 continued — `<HeroResultCard>` + `<CompactResultCard>` (10): available vs taken render forks, Intl en-IN INR price formatting (incl. the 1.5× strike-through on hero), onAdd / onWatch wiring with onWatch optional / N/A disabled fallback, "SAVE N%" computation from originalPrice, "Live Price" badge when pricingSource==='live' | ✅ Closed | `ab35b6c` |
| Batch 7bp | M14 continued — `<MultiStageRegisterForm>` (8): rendering, client validation gate (empty + password-mismatch), apiClient payload (no confirmPassword), happy-path with requiresActivation true/false + returnUrl propagation, Zod field-error tree mapped to per-field inline errors, generic toast fallback, typing clears the inline error | ✅ Closed | `81c84a0` |
| Batch 7bq | M14 continued — small leaves (10 / 2 files): `<FAQItem>` (4) — render, default-closed, uncontrolled toggle on click + chevron rotate, controlled mode via onToggle + isOpen prop; `<MessageAttachments>` (6) — null/empty render-nothing, anchor shape (href/download/target/rel + title with filename+size-in-KB), inner img alt, multiple attachments, align-prop → justify-class mapping incl. default 'left' | ✅ Closed | `c02ce4d` |
| Batch 7br | M14 continued — `<AttachmentPicker>` (10): trigger + helper-text render, custom-label prop, thumbnail-grid + remove button + index-correct removal, MAX_FILES count gate + non-image + wrong-mime + > MAX_BYTES validations each fire a toast and skip onChange, happy-path upload (real FileReader → data: URL) shapes the new attachment, capacity disables trigger | ✅ Closed | `ca8c5ca` |
| Batch 7bs | M14 continued — admin display leaves (18 / 3 files): `<AdminStatsCard>` (7) — title/value, increase/decrease/neutral change variants + 'vs last month', change-prop optional, colour-variant background incl. default; `<AdminQuickActions>` (3) — empty render, per-action anchor (href + heading + description), colour-variant classes; `<ActionMenu>` (7) — isOpen render gate, per-item button render, item-click fires onClick + onClose, click-outside via mouseDown → onClose, Escape → onClose (other keys ignored), danger-variant divider only for non-first danger items | ✅ Closed | `8cd99c0` |
| Batch 7bt | M14 continued — `<AdminDataTable>` (10): title + columns + rows + custom column.render, "No data available" empty + loading-row, client-side search filter, sortable header click toggles asc/desc and reorders rows, client-side pagination (page-2 swaps the visible window), server-side mode (totalItems + currentPage + onPageChange used instead of client math; Showing N to M of T copy), onSearch fires per keystroke in server-side mode, searchable=false hides the input, right-click row → onRowContextMenu with the row data | ✅ Closed | `9bbeb5d` |
| Batch 7bu | M14 continued — `<ErrorBoundary>` (7): children passthrough when nothing throws, default inline fallback (role=alert + copy + Try-again button), ReactNode `fallback` prop override, function `fallback` form receiving (error, reset), labelled console.error on capture, default-label fallback | ✅ Closed | `cba6262` |
| Batch 7bv | M14 continued — loading + skeleton primitives (23 / 2 files): `<Skeleton>` + prebuilt SkeletonText/Card/Table/List (10) — variant routing (wave overlay vs pulse class vs none), default 100%/200px box vs 40px circle, explicit width/height overrides, prebuilt composition counts; `<LoadingSpinner>` + `<PageLoading>` + `<DataLoading>` (13) — message + size + fullScreen wrapping, page→message lookup with 'content' fallback for unknown keys, DataLoading type=table/card/list count routing | ✅ Closed | `ea5035e` |
| Batch 7bw | M14 continued — small UI leaves (21 / 4 files): `<ClientOnly>` (3) — hydration-deferred render + null default fallback; `<EmptyState>` (4) — title + description, optional action button, className passthrough; `<RefreshButton>` (5) — default Refresh label + icon, showText=false icon-only, isLoading spin + disabled, title attribute default vs custom; `<ExpiryBadge>` (9) — null/NaN render-nothing, three-tier copy + colour fork (green > 60d / yellow 15-60d / red ≤ 14d) with the "Renew Now" suffix + animate-pulse on 1-14 days, expired path with no pulse, singular 'day' for exactly 1 day, button-vs-span fork on onRenew | ✅ Closed | `8a5bbae` |
| Batch 7bx | M14 continued — admin display + brand chrome (17 / 3 files): `<AdminCard>` (5) — title/value, three change-variant colour classes + no-change hide; `<AdminTabs>` (4) — tab list with icons, active-tab class, per-tab onClick, no-onClick crash-safety; `<Logo>` (8) — Next/Link wrapping at default '/' href, custom href, unlinked path when href=''  is passed, showText label, variant light vs dark (asset + text colour), size→class mapping | ✅ Closed | `3867d4e` |
| Batch 7by | M14 continued — layout + form primitives (21 / 4 files): `<Section>` (6) — children + id + default white-bg/lg-padding, three other backgrounds + xl padding; `<Header>` (3) — fixed-top wrapper + z-50 + className passthrough; `<Textarea>` (6) — label, helperText vs error mutual-exclusion with red-ring class on error, ref forwarding, props passthrough; `<Badge>` (6) — default + secondary/destructive/outline variants + className merging | ✅ Closed | `a8898d9` |
| Batch 7bz | M14 continued — public chrome + ui primitives (23 / 4 files): `<ContactInfo>` (5) — 'Get in touch' intro + three contact cards (call/email/visit), className passthrough; `<ScrollToTop>` (4) — > 300px scroll-visibility threshold, hide-on-scroll-back, smooth scroll on click; `<Switch>` (5) — Radix role + aria-checked binding, onCheckedChange wiring + disabled gate + className merging; `<Button>` (9) — default + 5 variant classes, 4 sizes, asChild + Radix Slot composition (anchor-as-button retains button variant classes), ref forwarding, disabled state | ✅ Closed | `39d84e4` |
| Batch 7ca | M14 continued — tiny presentational leaves (12 / 5 files): `<SkeletonBase>` (3) — default bg + animate-pulse, animate=false opt-out, className via cn(); `<SkeletonCard>` (2) — 5-placeholder composition incl. the circular icon; `<SkeletonStats>` (2) — 4 cards × 4 placeholders = 16 in a grid; `<RupeeIcon>` (3) — SVG render, default + custom className, ₹ glyph in content; `<PageTransition>` (2) — children passthrough + className on the motion.div | ✅ Closed | `9384632` |
| Batch 7cb | M14 continued — loading + skeleton variants (13 / 4 files): `<LoadingSpinner>` simple primitive (4) — default md `h-8 w-8`, sm `h-4 w-4`, lg `h-12 w-12` size variants, className passthrough on the outer wrapper; `<SkeletonHero>` (2) — 4 SkeletonBase placeholders (icon + title + subtitle + search box) and the circular icon placeholder; `<SkeletonContact>` (2) — 2-column md-grid form/info shell with 21 SkeletonBase children (9 form + 12 info); `<SkeletonSection>` (5) — default render with title block + 3 cards in a 3-col grid (4 + 15 = 19 placeholders), `title={false}` skips the title block, `cards` prop controls the rendered card count (5 × 5 = 25), `columns=4` → `md:grid-cols-4`, `columns=2` → `sm:grid-cols-2` | ✅ Closed | `aa2ade9` |
| Batch 7cc | M14 continued — public-page primitives + motion shell (16 / 4 files): `<Card>` (7) — rounded-lg wrapper, three variant classes (default/elevated/outlined), three padding sizes (sm/md/lg), hover-classes opt-in, `animate=false` skips the motion.div wrapper (bare inner div is root) vs `animate=true` wraps it; `<HeroSection>` (5) — primary gradient default + secondary/dark variants, `background='solid'` drops the gradient, `backgroundImage` prop applies an inline `background-image: url(...)` style; `<FeatureCard>` (3) — icon + h4 title + description render, className passthrough to the underlying Card, default hover=true picks up `hover:shadow-xl`; `<MotionProvider>` (1) — children render passthrough through framer-motion's MotionConfig | ✅ Closed | `a708475` |
| Batch 7cd | M14 continued — shadcn card primitives + admin/stats + consent (24 / 4 files): shadcn `ui/card` set (8) — Card div with rounded-lg/border/bg-card, CardHeader flex-col p-6, CardTitle h3 + text-2xl, CardDescription p + text-sm/text-muted-foreground, CardContent p-6 pt-0, CardFooter flex items-center, ref forwarding + cn() className merging; `<AdminTable>` (5) — loading spinner + no table, default 'No data available' + custom emptyMessage, key→value cell mapping, `column.render(value, row)` override; `<StatsCard>` (5) — value/label/icon render, three trend variants (up=green↗ / down=red↘ / neutral=gray→), the **both-trend-and-trendValue-required** gating (neither alone renders the trend row); `<CookieConsentBanner>` (6) — visibility gates: anon + no consent → banner shows, prior consent → hidden, **authenticated → auto-accept** (writes to localStorage), Accept & X buttons both persist consent + hide, **defensive `useSession` undefined race** doesn't throw | ✅ Closed | `1ce672c` |
| Batch 7ce | M14 continued — Modal + custom Button + custom Input (27 / 3 files): `<Modal>` (6) — isOpen=false renders nothing, title + children + h3 heading, close button + overlay-click both fire onClose, `closeOnOverlayClick=false` opt-out, size→max-w class map (sm/lg/xl); custom `<Button>` (8) — distinct from shadcn ui/button (7bz), default primary gradient + md size, danger/outline variant gradients, sm size padding scale, fullWidth, **loading state disables + shows spinner svg**, disabled suppresses onClick, animate=false renders plain `<button>` (no motion wrapper) with click still firing; custom `<Input>` (13) — label + helperText render, **error vs helperText mutual-exclusion** (error wins + adds `border-red-300`), ref forwarding, **autocomplete inference** (email→email, type=password default→current-password, name contains 'confirm'→new-password, type=password + 'create' placeholder→new-password, name=phone→tel, explicit autoComplete prop overrides), icon→pl-10 padding, typing updates value | ✅ Closed | `90bb20f` |
| Batch 7cf | M14 continued — contact/loading + order timeline + floating cart (19 / 4 files): `<ContactMap>` (4) — 'Find Us' heading + address, **iframe src embeds the Rohini coords** (lat 28.7406 / lon 77.0884), Get-Directions external link (target=_blank + rel=noopener), pre-load 'Loading map...' indicator; `<LoadingPage>` (3) — default 'Loading...' + dots, custom message, `showDots=false` omits the dots span; `<OrderTimeline>` (5) — base 2-step (Payment + Confirmation), `hasHosting` inserts the Hosting step with '2–5 minutes', `hasDomains` inserts the Domain step with 'Up to 24 hours', **both flags renders payment→hosting→domain→email in that order**, userEmail interpolated into the confirmation step detail; `<FloatingCart>` (7) — renders cart link for non-admin on public route, badge with count when > 0, **'9+' cap above 9**, no badge at 0, **four hide gates**: admin route, admin user (localStorage role=admin), invoice /view page, plus the SSR-mounted guard. Mocks `next/navigation`, `@/lib/storage`, and `@/store/cartStore` via vi.hoisted | ✅ Closed | `057c94c` |
| Batch 7cg | M14 continued — register subforms + hosting-upgrade ConfirmStep (14 / 3 files): `<CredentialsSection>` (4) — Password + Confirm-password fields masked by default, eye-icon toggles type=password ↔ type=text **independently** for each field, onChange forwards the correct `name`; `<PersonalInfoSection>` (4) — 5 visible fields (first/last name, email, company, phone), email→type=email + phone→type=tel, **fixed Indian country code** display + hidden `phoneCc=+91` form payload field, onChange wiring; `<ConfirmStep>` (6) — target plan name + From + remaining days, prorated charge via `formatIndianCurrency` (₹1,234), **subscription-cancel amber warning gated by `upgradeInfo.hasSubscription`**, Back + Pay button callbacks, Pay button label includes the formatted charge | ✅ Closed | `c9dcae1` |
| Batch 7ch | M14 continued — session provider + SelectPlanStep + AddressSection (14 / 3 files): `<SessionProvider>` (1) — wraps children in the next-auth SessionProvider (mocked via vi.hoisted) — pins the auth-library coupling so a swap surfaces; `<SelectPlanStep>` (7) — step 1 of the hosting-upgrade modal: current plan name/price/remaining days, `hasSubscription` warning gate, eligible-plan list with prorated charges, **feature pills capped at 3** per plan, plan-button click → `onSelectPlan(plan)` with the full plan object, Cancel → `onCancel`; `<AddressSection>` (6) — register-form address subform: line1/city/state/zipcode fields populated from `formData.address.*`, **fixed Indian country display + hidden `address.country=IN` payload field**, the INDIAN_STATES set is rendered in the state select, Auto-fill button fires `onDetectLocation` and is disabled + shows the loading spinner when `isDetectingLocation=true`, dotted-path field names (`address.city`, `address.state`) forwarded on change | ✅ Closed | `0ed0ae0` |
| Batch 7ci | M14 continued — PricingCard + CenteredLoading + CustomToast (21 / 3 files): `<PricingCard>` (6) — title/subtitle/price/currency/period, original-price line-through + renewalPrice + discountBadge optional rows, isPopular adds 'Most Popular' banner + indigo border, **button-vs-link fork** keyed on onButtonClick presence, feature list with included + highlight class branches; `<CenteredLoading>` (6) + `<InlineLoader>` (2) — default 'Loading...' + dots, custom message, showMessage=false omits text, size→spinner-class map (sm/md/lg/xl), fullScreen=true wraps in `fixed inset-0` overlay vs the inline `min-h-[200px]` wrapper, InlineLoader size map + className passthrough; `<CustomToast>` imperative API (7) — `toast.custom` mocked via vi.hoisted, the captured render fn re-mounted standalone to assert success-green / error-red / loading-blue border + per-type icon (svg vs spinner), title rendering, **dismiss button calls toast.dismiss(t.id)**, `showLoadingToast` + `showPermanentToast` pin `duration=Infinity`, `dismissible=false` omits the X button | ✅ Closed | `af899b4` |
| Batch 7cj | M14 continued — EnhancedToast + Razorpay iframe (16 / 2 files): `<EnhancedToast>` (8) + `<ToastContainer>` (1) — title + optional message, **per-type icon + colour palette** (success=green / error=red / warning=yellow / info=blue), close button → onClose(id) after 300ms exit, **auto-dismiss at `duration + 300ms`** (fake timers), `duration=0` disables auto-dismiss, ToastContainer maps `toasts` array; `useRazorpayCheckout` (8) — the postMessage protocol contract: Frame() null until `open()`, then iframe overlay renders; iframe `'ready'` → parent posts `{type:'open', options}` to contentWindow; `'success'` resolves the open Promise with the payload; `'dismiss'` rejects with `{kind:'dismissed'}`; `'error'` rejects with `{kind:'error', message}`; **a second `open()` while active rejects immediately**; **foreign-origin postMessages are ignored** (CSP defense — verified by sending a `dismiss` from `https://evil.example.com` and confirming the Promise hadn't settled) | ✅ Closed | `ad58212` |
| Batch 7ck | M14 continued — Footer + AuthShell (13 / 2 files): `<Footer>` (6) — company intro + 5 Quick Links with hrefs (Home/About/Contact/Login/Register), Services section (Domain Registration / DNS Management / SSL Certificates + Web Hosting→/hosting), four policy links in bottom strip (/privacy, /terms-and-conditions, /data-deletion, /cancellation-refund), current-year copyright line, social media aria-labels (Facebook/Instagram/LinkedIn), className passthrough; `<AuthShell>` (7) — form-side title as `<h1>`, subtitle optional render gate, children inside form panel, default panelEyebrow ('Anutech Digital') + panelTitle ('The clean way to own your online identity') vs override props, current-year footer copyright, brand-panel Logo links back to `/` | ✅ Closed | `bab0d5e` |
| Batch 7cl | M14 continued — SocialLoginButtons + skeleton primitives + admin skeleton (17 / 3 files): `<SocialLoginButtons>` (7) — next-auth `signIn` + react-hot-toast mocked via vi.hoisted: renders only Google by default (FACEBOOK/GITHUB env gated off), 'Or continue with' divider, click calls `signIn('google', {redirect:false, callbackUrl:'/dashboard'})`, `result.error='OAuthSignin'` → toast.error + onError, **`result.error='AccessDenied'` → `showAccountDeactivated` branch** (different copy, no toast.error), `result.ok=true` → toast.success + onSuccess (under fake timers to dodge the 2s redirect setTimeout), uncaught signIn throw → generic toast.error in catch; skeleton `_primitives` (7) — Sk renders div + skeleton class + className passthrough, PageHeader 3 placeholders (2 title + 1 action) + `wide=true` swaps `w-48` → `w-64`, TableSkeleton placeholder math (toolbar 2 + header cols + rows×cols data) defaults to 6×5=37 total, FormSection title=true vs false toggles the title bar; skeleton `AdminLayout` (3) — AdminTableRowsSkeleton cell-count math (header + per-row 8-cell signature: icon tile + 2 lines + middle + 2-button tail), AdminLayoutSkeleton renders sidebar logo + 8 nav rows + topbar shell and slots children into `<main>` | ✅ Closed | `5401a1f` |
| Batch 7cm | M14 continued — Dashboard skeleton family + SkeletonCart (14 / 2 files): `DashboardSkeletons` (11) — SkeletonBar default w-full/h-4 + width/height overrides, SectionCardSkeleton title bar + N body rows (defaults 1+3=4), TableSkeleton 1 header + rows×cols (defaults 1+30=31), ListSkeleton 2 bars per item, FormSkeleton 8 fields × 2 + submit = 17, DNSManagementSkeleton composition (3 + 21), OrdersSkeleton 6×6 + DomainsSkeleton 6×5 + AdminTableSkeleton 8×6 delegate to TableSkeleton, default export `<DashboardSkeletons />` is intentionally null; `<SkeletonCart>` (3) — 55-placeholder shell snapshot, 4-col trust strip + 6/7/8-col main content grid pinned via Tailwind class selectors | ✅ Closed | `426c33e` |
| Batch 7cn | M14 continued — barrel re-exports + payment skeletons + example component (12 / 4 files): `skeletons/PageSkeletons` barrel (4) — re-exports a sampling from each topical file (AdminLayout, AdminPages, UserDashboard, PaymentPages) so a future rename surfaces here; `components/DomainSearch` barrel (1) — backwards-compat shim; the default export is **referentially identical** to `components/domain-search/DomainSearch`'s default; `PaymentPages` skeletons (3) — CheckoutPageSkeleton renders > 10 .skeleton placeholders + the `lg:grid-cols-6` content grid, PaymentSuccessPageSkeleton renders the `min-h-screen` success shell, CartPageSkeleton renders a non-empty placeholder grid; `<DomainRequirementsExample>` (4) — trigger button render, the **`requiresVerification` gate** (false → modal absent even when isModalOpen=true), requiresVerification=true + isModalOpen=true → modal renders with `domain="anutech" tld=".au"`, button click → openModal callback. Hook + modal both mocked via vi.hoisted | ✅ Closed | `8c7f2e4` |
| Batch 7co | M14 continued — UserDashboard + AdminPages skeleton families (22 / 2 files): `skeletons/UserDashboard` (13) — DashboardLayoutSkeleton slots children into `<main>` and renders > 15 .skeleton placeholders in the sidebar/topbar shell; 11 named page-skeleton exports (DashboardHomeSkeleton, OrdersPageSkeleton, DomainsPageSkeleton, InvoicesPageSkeleton, HostingPageSkeleton, SupportPageSkeleton, ReferralsPageSkeleton, SettingsPageSkeleton, DNSPageSkeleton, TicketDetailPageSkeleton, DetailPageSkeleton) each render > 5 placeholders (it.each table); `skeletons/AdminPages` (9) — 9 named admin-page exports (AdminUsersPageSkeleton, AdminPaymentsPageSkeleton, AdminSupportPageSkeleton, AdminPendingDomainsPageSkeleton, AdminHostingPageSkeleton, AdminPricingPageSkeleton, AdminGenericPageSkeleton, AdminDashboardSkeleton, AdminSettingsPageSkeleton) each render > 5 placeholders (it.each table). Smoke-level coverage — the goal is to catch a future refactor accidentally yielding an empty skeleton, not to pin individual placeholder counts | ✅ Closed | `b5c271a` |
| Batch 7cp | M14 continued — admin AdminLayout + InvoiceDiagnostics orchestration (19 / 2 files): `admin/AdminLayout` (10) — 11-item nav rendered, children slot into main content, **active link routing** (link active class on the matching pathname), **two special cases**: `/admin` (no /dashboard) treats Dashboard as active, `/admin/dns-management` treats Domains as active; user initials display (firstName+lastName chars → 'AL'), 'AU' fallback when user=null; onLogout prop renders + fires the Logout button, absence hides it; mobile menu button labelled 'Open navigation menu'; `admin/InvoiceDiagnostics` (9) — apiClient + confirmDialog + toast helpers + 3 subcomponents (DiagnosticsHeader, ConflictsTable, StuckOrdersTable) all vi.hoisted mocked: initial loading skeleton, empty-issues fetch leaves panel collapsed, with-issues fetch auto-expands, clear-invoice-number declined → no POST, clear-invoice-number confirmed → POST + success toast + refetch, re-sync single → POST + success toast, **re-sync all empty list → no confirm**, re-sync all all-succeed → bulk-success toast with count, re-sync all partial → mixed-error toast | ✅ Closed | `d87551d` |
| Batch 7cq | M14 continued — DomainRequirementsModal + DomainSelectionModal (16 / 2 files): `<DomainRequirementsModal>` (8) — isOpen gate, intro + Important Notice copy, Required Information list, **Restrictions per-type colour fork** (error=red / warning=orange / info=blue), Alternative Options list + `onSelectAlternative(domain)` callback with the exact domain string, alt.available true/false swaps the trailing icon, Close fires onClose, **Contact Support opens `mailto:?subject=Domain Registration Support` via window.open**, defensive `Array.isArray()` guards (non-array `requirements`/`restrictions` props don't crash); `<DomainSelectionModal>` (8) — cartStore + react-hot-toast mocked: isOpen gate, empty input → 'Please enter a domain name' error, invalid format → 'Please enter a valid domain name', valid domain + no existingDomainName → `addItem({domainName: 'mysite.com', price, currency:'INR', registrationPeriod:12, itemType:'hosting', hostingPlan:{name, period:12, features}})` with **lowercased domain**, `existingDomainName='hosting-abc'` placeholder leaves input empty vs user-supplied value pre-fills, **existingDomainName + matching cart item → `updateItem(oldDomain, ...)` (not addItem)** to swap the placeholder, typing after an error clears the error message | ✅ Closed | `3ac519c` |
| Batch 7cr | M14 continued — `<HostingUpgradeModal>` orchestration (11 / 1 file): the full step machine `loading → select → confirm → paying → verifying → success` (and the `↘ error` branches). Heavy mock setup — `global.fetch`, `next-auth/react.useSession`, `next/navigation.useRouter`, `safeSessionStorage`, `useRazorpayCheckout` (open + Frame), and both `SelectPlanStep` + `ConfirmStep` subcomponents replaced with thin mocks. Pins: isOpen gate, initial fetch to `/api/v1/user/hosting/upgrade-info?domainName=…&credentials:include`, success + eligiblePlans>0 → 'select' step (SelectPlanStep mounted), **eligiblePlans=0 → 'error' step with 'highest available plan' copy**, HTTP error → 'error' step with server message, fetch throws → generic 'Failed to load upgrade options' error, pick-plan transitions to 'confirm' step, back from confirm → select, **`razorpay.open()` rejecting with `{kind:'dismissed'}` is a soft-cancel back to 'confirm'**, upgrade order POST fail → 'error' step with server message, Retry from error step refetches upgrade-info | ✅ Closed | `5727de8` |
| Batch 7cs | M14 continued — UserLayout + GoogleRecaptcha (21 / 2 files): `user/UserLayout` (14) — 6-item nav (Dashboard/Domains/Hosting/Invoices/Support/Account Settings, Invoices link's accessible name includes the RupeeIcon ₹ glyph), active-link blue text+border classes, top-bar heading reflects the active nav (falls back to 'Dashboard' when no match), user-info shows name+email when user present vs 'Loading...' when user=null; **three logout-button states** — active when onLogout + user, disabled when onLogout + user=null, inactive 'No logout handler' block when onLogout absent — pinned via the component's existing `data-testid` attrs; click awaits async onLogout, ProfileCompletionWarning rendered (mocked), isLoading=true swaps children for DataLoading skeleton, hideFloatingButtons gates the floating Home link; `<GoogleRecaptcha>` (7) — RecaptchaClient + apiClient + env stubbed: **captcha-status `{enabled:false}` → onSuccess('captcha-disabled') with no widget render**, missing/placeholder site key → onSuccess('manual-pass'), happy path renders widget with the supplied theme + size and forwards tokens via the widget callback, expired-callback fires onExpire + resets widget, error-callback fires onError + shows visible error, **'already been rendered' render error is swallowed** (no onError, no error message — important because StrictMode double-mounts can fire this), other render errors → onError + 'Failed to render verification widget' visible message | ✅ Closed | `0a64dd5` |
| Batch 7ct | M14 continued — `<DomainRenewalModal>` (9 / 1 file): fetch + react-hot-toast mocked, `vi.setSystemTime` fixes "now" so the `daysUntilExpiry` math is deterministic. Pins: isOpen=false renders nothing + no fetch; isOpen=true → loading copy + fetches `/api/v1/domains/renew?domainName=...&years=1` with `credentials:'include'`; success render shows Current Status + Renewal Cost (₹1,500 via `formatIndianCurrency`) + all **6 renewal-period buttons** (1/2/3/4/5/10 with proper Year/Years pluralisation); HTTP error → toast.error with server message; expiry ≤ 30 days surfaces the red **'expiring soon! renew now'** warning block; selecting a year refetches with `?years=N`; Cancel button fires onClose; successful renew POST → success toast + onClose; failed renew POST → toast.error + modal stays open | ✅ Closed | `c4e83d5` |
| Batch 7cu | M14 continued — `<HostingRenewalModal>` (10 / 1 file): the hosting variant of DomainRenewalModal (7ct), but uses the **Razorpay iframe checkout** flow rather than a mock paymentId. Mocks: fetch, useSession, useRouter, safeSessionStorage, useRazorpayCheckout (open + Frame), react-hot-toast. Coverage on the full load+renew step machine: isOpen=false renders nothing + no fetch; isOpen=true → renew-info fetch with credentials, header rendered; successful renew-info → plan name + ₹12,000 price visible; renew-info HTTP error → toast.error + onClose; renew-info fetch throws → generic toast.error + onClose; **Renew → POST + razorpay dismissed → soft-cancel (no toast)**; Renew → POST fail → toast.error with server message (no razorpay.open call); Renew → razorpay generic error (non-dismissed) → toast.error with the tagged.message; **happy path** order POST → razorpay success → verify success → success toast + `safeSessionStorage.paymentResult` written + onClose + `router.push('/payment-success')`; verify failure → toast.error + modal stays open | ✅ Closed | `fc9230b` |
| Batch 7cv | M14 continued — public `<Navigation>` (11 / 1 file): 354-line component covering three variants (default public-site / dashboard / admin). Mocks usePathname + useSession (typed with the undefined-race safe-destructure) + cartStore. Default variant (7): 4 desktop nav links (Home/Hosting/About Us/Contact Us — `getAllByRole` because mobile menu also renders them), unauthenticated → Login link + no Dashboard link, authenticated → Dashboard link + no Login, active-link blue class via `google-blue` className match, cart-count badge appears once > 0, mobile menu toggle button labelled, **defensive `useSession()` undefined race** doesn't throw (falls through to unauthenticated branch); dashboard/admin variants (4): onLogout button fires the callback, **role chip blue=user vs red=admin** swap (color encodes risk in the chrome), Shopping Cart link has the aria-label + href + count badge, public-site nav links absent on dashboard/admin variants | ✅ Closed | `821ae97` |
| Batch 7cw | M14 continued — `<NameServerManagement>` (11 / 1 file): the localStorage-backed nameserver CRUD panel. Mocks `safeLocalStorage` (with a Map-backed `vi.hoisted` store) + `react-hot-toast`. Pins: on mount, hydrates `nameserverConfigs` from storage **prepending the hard-coded RESELLERCLUB default** (default is NOT persisted — only customs are); 'Add Custom' opens the inline editor + Cancel closes it; Save with empty name → toast.error 'Please enter a configuration name'; Save with all-empty servers → toast.error 'at least one nameserver'; **Save success persists only the customs to localStorage** (RESELLERCLUB_DEFAULT is hard-coded, asserted absent from the persisted JSON) + toast.success + closes form; 'Add Another Nameserver' grows the input list (ns1/ns2/ns3 placeholder progression); Delete on a custom row removes it + persists; **Delete button is NOT rendered on the default row** (assertion via `getAllByTitle` length); Set Active flips `isActive` across configs + persists; Reset to Default Nameservers restores default-active state; **malformed localStorage JSON** gracefully falls back to the default-only list (caught by the try/catch in the load effect) | ✅ Closed | `20f13ee` |
| Batch 7cx | M14 continued — `<LoginForm>` (15 / 1 file): the 391-line credentials-login orchestration component. Mocks signIn, useRouter+useSearchParams, toast helpers, safeLocalStorage, logger; GoogleRecaptcha + AuthShell + SocialLoginButtons replaced with light shims. Pins: email/password/Remember-me/Forgot-password rendered; 'Create an account' link respects `?returnUrl=`; `?message=…` shows green activation banner; `?returnUrl=/checkout` shows the 'sign in to complete your purchase' banner; **`?error=AccessDenied`** surfaces the deactivated-account message + cleans up the URL via `history.replaceState`; password eye toggles type=password ↔ text; submit calls `signIn('credentials', {redirect:false, email, password, callbackUrl})`; **`CredentialsSignin` → 'Invalid email or password'**; **`TotpRequired`** reveals the 2FA step WITHOUT an error toast (correct credentials, just missing 2FA); **`InvalidTotpCode`** → 'Invalid authenticator code' toast; **`AccountNotActivated`** → toast.error + 1s-delayed `router.push('/activate?email=…')`; **`AccountDeactivated`** → `showAccountDeactivated` helper; ok=true → success toast + 100ms-delayed `window.location.href = safeReturnUrl`; **Remember-me checked** → `setItem('rememberMe','true')` + `setItem('savedEmail', email)`; unchecked → `removeItem` both; **open-redirect defence** — `?returnUrl=https://evil.example.com/steal` falls back to `/dashboard` (the `startsWith('/')` guard) | ✅ Closed | `3bade40` |
| Batch 7cy | M14 continued — `<DomainSearch>` orchestrator (14 / 1 file): the 292-line search-page parent. Subcomponents (SearchInput / SearchResults / DomainRequirementsModal) all have standalone tests; here we focus on the orchestration callbacks. Mocks: cartStore, useRouter, toast helpers, apiClient.post, domainRequirements helpers, useDomainSearch hook, and the 3 subcomponents replaced with thin stubs exposing onAddToCart/onShowRequirements/onWatch via test buttons. Pins: SearchInput + SearchResults + hero copy render; `showHeroText=false` hides the hero; **addToCart paths** — standard domain → cartStore.addItem + success toast + 1s router.push('/cart'); requires-additional-details TLD → opens requirements modal (no cart write); unsupported domain → error toast; missing price/available → 'missing required data' toast; handleShowRequirements opens the modal directly; modal close hides it; **handleWatch result paths** — 200 → 'we'll email you when X becomes available' success toast, 401 → opens **sign-in prompt with returnUrl pointing back to the search**, 409 → 'Already watching' error toast (no sign-in prompt), 400 → error toast with server message, default → generic 'Failed to watch domain' toast; sign-in + create-account anchors in the prompt both carry the returnUrl with `q=<base>` (the dot-split first segment) | ✅ Closed | `7f2df32` |
| Batch 7cz | M14 continued — `<RegisterForm>` (11 / 1 file): the 486-line registration orchestration. Subform sections (PersonalInfo / Address / Credentials) all have standalone tests (slices 7cg/7ch); this slice focuses on the **4-step state machine + submit paths**. Mocks: useRouter, react-hot-toast, safeLocalStorage, apiClient.post, GoogleRecaptcha + Card + Logo + Button + SocialLoginButtons shimmed. **The three subform sections render real `<input>` elements** (testid-prefixed) so `user.type()` drives one change event per character — necessary because the source's `handleChange` uses non-functional `setFormData`, so multiple synchronous onChange calls would clobber each other. Pins: heading + sign-in link + 3 sections render; submit on step 1 with empty fields → 'fill in all required fields' toast (no advance); **password length 1–7 disables Create-account, ≥8 re-enables** (the gating that catches half-typed passwords); 4-step advance via fill+submit chain → final apiClient.post body has the full register payload (firstName/lastName/email/password/phone/phoneCc/companyName/address.{line1,city,state,country:IN,zipcode}); successful register → token + user persisted to localStorage, **token cookie set with SameSite=Lax** (assertion via `document.cookie`), success toast, registerFormData cleared, 100ms-delayed router.push('/dashboard'); **password mismatch on final step → toast.error + NO API call** (so a typo doesn't burn a registration attempt); apiClient fail with server message → that exact message in toast.error; apiClient fail with status=0 (network) → generic 'An error occurred' (avoids leaking 'Failed to fetch' to the user); registerFormData persists to localStorage as the form fills (passwords excluded); **rehydrates on mount** (saved data NOT wiped by the initial empty state); detectLocation: geolocation unsupported → 'not supported' toast | ✅ Closed | `652ab74` |

| Batch 7da | M14 **COMPLETE** — `<ChatWidget>` (10 / 1 file): the 260-line streaming-SSE chat widget; the last untested component in `components/`. Mocks `global.fetch` with a hand-rolled `ReadableStream` shim that yields SSE-style data chunks; `Element.prototype.scrollIntoView` stubbed. Pins: closed by default; toggle button opens panel with the greeting message; **sessionStorage hydration restores prior conversation** on mount, **trailing-empty-assistant placeholder dropped** (interrupted-stream defence); Close (X) hides panel (uses `getAllByRole` because the toggle button also carries `aria-label='Close chat'` when open); Send button disabled when input is empty; **streaming flow** — POST `/api/v1/chat`, then `data: {"text":"..."}` chunks accumulate into the trailing assistant message and `data: [DONE]` terminates; **malformed chunks are silently skipped** (a `data: this-is-not-json` line interleaved with valid chunks doesn't crash the stream); network failure (`ok:false`) → assistant fallback 'Sorry, something went wrong… contact support@anutech.in'; sessionStorage persists each settled exchange (user + assistant accumulated reply); **Enter (no shift) submits; Shift+Enter does not** | ✅ Closed | `1c74c24` |
| Batch 7db | M14 follow-up — top-level hooks coverage (18 / 3 files): `useModalScroll` (6) + `useScrollToTop` — isOpen=true locks body scroll (overflow=hidden, position=fixed, top=-scrollY, width=100%), isOpen=false leaves styles cleared, transitioning open→closed restores styles (note: scrollTo restore doesn't fire on transition because the cleanup wipes top before the false-branch reads it — pinned as a known quirk), isOpen=false with a pre-existing body.style.top DOES scrollTo, unmount-while-open cleanup restores styles, useScrollToTop scrolls (0,0) on mount; `useDomainRequirements` (4) — non-restricted TLD → empty arrays + requiresVerification=false (data fns NOT called), restricted TLD → arrays populated from getDomainRequirements + generateAlternativeDomains, openModal/closeModal toggle isModalOpen, handleSelectAlternative closes the modal; `useUser` (8) — status='loading' → isLoading=true + user=null, unauthenticated + no token → router.push('/login'), unauthenticated WITH legacy token → no push, authenticated session before SWR resolves → fallback to sessionUser derived from session.user fields, /api/auth/me data wins over sessionUser fallback, admin role → window.location.replace('/admin/dashboard'), localStorage 'user' is the legacy fallback (when no session), **defensive `useSession()` undefined race** doesn't crash + treats as loading | ✅ Closed | `9dc57b5` |
| Batch 7dc | M14 follow-up — nested domain-search hooks (24 / 2 files): `useDomainPricing` (3) — INR formatting with ₹ glyph + Indian-locale grouping (1,00,000 for lakh), custom currency override (USD), zero fraction digits; `useDomainSearch` (21) — the 342-line state machine + pure helpers. validateDomainInput pure: 'example.com' → base+suggestedTld, 'example.co.uk' → multi-segment TLD, bare 'example' → suggestedTld=null, 'ab' minimum length, single 'a' / 'foo!' invalid, whitespace stripped; getSuggestedTlds pulls from TOP_TLDS filtered by isRestrictedTLD; state machine — initial state, **localStorage rehydration** from `domainSearchState`, `initialSearchTerm` + `autoSearch=true` triggers a search after 100ms, invalid input → toast.error + no API, **redirectOnSearch=true** routes via `router.push('/domains/search?q=…')` instead of fetching, happy path Phase-1 quick → setResults + Phase-2 suggestions fetch, **quick `restricted_tld` error** → setError + toast.error (NO Phase-2 fetch — wastes a request), quick network error → 'Network error' toast, handleInputChange branches (bare → searchMode='multiple'+baseDomain, with-TLD → 'single', invalid → empty), clearSearch resets all state | ✅ Closed | `e8b5c55` |

| Batch 7dd | M14 follow-up — small lib utilities (21 / 5 files): `lib/utils` (6) — `cn()` Tailwind class merger joins, filters falsy, supports clsx object form, dedupes Tailwind conflicts via tailwind-merge (`p-4 + p-2` → `p-2`); `getSupportEmail()` returns env var or falls back to `support@anutech.in`; `lib/fetcher` (4) — SWR fetcher calls fetch with `credentials:'include'` + JSON Content-Type, returns parsed JSON on success, throws typed Error with `.status` + `.info` on non-2xx, non-JSON body → `.info={}` fallback; `lib/constants` (4) — INDIAN_STATES has exactly 36 entries (28 states + 8 UTs), contains all canonical names, alphabetical ordering (drives register-form dropdown); `lib/tld-min-periods` (3) — TLD_MIN_PERIODS only includes TLDs with `minYears>1` (asserted bidirectionally against `TLD_POLICIES`), `getMinRegistrationPeriod` delegates to `getMinYears`; `lib/security-headers` barrel (3) — three exports referentially identical to `lib/security/headers` originals | ✅ Closed | `2eeeca0` |
| Batch 7de | M14 follow-up — request-id + auth cookies + axios client + 2 integration barrels (23 / 5 files): `lib/request-id` (6) — Cloud Run X-Cloud-Trace-Context priority over x-request-id, TRACE_ID extraction (`abc/SPAN_ID;o=1` → `abc`), x-request-id fallback, **fresh UUID v4 fallback** (asserted via the 8-4-4-4-12 hex pattern + ≠ between successive calls); `lib/auth-config/cookies` (6) — 3 cookie groups (sessionToken/callbackUrl/csrfToken), sessionToken+csrfToken httpOnly, **callbackUrl NOT httpOnly** (client reads it for post-OAuth redirects), `__Secure-`/`__Host-` prefix gating on `useSecureCookies`, csrfToken uses **`__Host-` prefix** (path=/ + no Domain) not `__Secure-`; `lib/zohobooks/axios-client` (3) — 30s timeout default mirroring ResellerClub budget, axios verbs available, defaults object exposed; `lib/integrations/resellerclub` barrel (4) — 6 ops + 6 classify fns + matchesAny + 5 fragment-keyword lists (BALANCE_PENDING, PROCESSING_LOCK, etc.) re-exported, matchesAny lower-case substring match; `lib/integrations/directadmin` barrel (4) — 6 ops + 6 classify error fns + 3 fragment lists (USERNAME_COLLISION/USER_NOT_FOUND/PACKAGE_NOT_FOUND) re-exported, matchesAny shared with RC. **RC client throws at module-load on missing env vars** — stubbed via `vi.hoisted` before the static import | ✅ Closed | `4fc5c1a` |
| Batch 7df | M14 follow-up — protocol consts + time service + cron auth + hosting dates + guest JWT (43 / 5 files): `lib/razorpay-checkout-protocol` (6) — RAZORPAY_FRAME_PATH='/razorpay-checkout', RazorpayCheckoutOptions accepts minimum {key} + pass-through unknown keys, RazorpaySuccessPayload requires payment_id+signature, ParentToFrame open + FrameToParent ready/success/dismiss/error compile-time checks; `lib/time-service` (10) — TimeService.now resolution priority (explicit param > x-simulated-time header > SIMULATED_TIME env > real new Date()), production guard requires ENABLE_TIME_SIMULATION=true to enable simulation, TimeService.daysUntil with future/same-day/past inputs + **uses floor (no rounding up)** so 9d23h → 8 whole days; `lib/cron-auth` (6) — CRON_SECRET unset → false, missing header → false, length mismatch → false (no timingSafeEqual call), matching secret → true via constant-time compare, ASCII secret exact-bytes match; `lib/hosting-dates` (10) — calculateHostingDates default 'months' uses setMonth (12 → +1 year), 'days' uses setDate (7 → +1 week), 'minutes' uses setMinutes (45 → +45min for test plans), registeredAt+expiresAt are distinct Date objects, validateRegistrationPeriod only accepts 12 (everything else → 1); `lib/guest-token` (6) — sign+verify round-trip preserves payload, 1h TTL baked in (exp-iat ≈ 3600), tampered signature → null, wrong purpose → null, garbage input → null, **alg=none forgery → null** (algorithm pinned in verify) | ✅ Closed | `78c151b` |
| Batch 7dg | M14 follow-up — auth-secret + mongoose alias + 4 thin DB services (18 / 6 files): `lib/auth-secret` (3) — exports AUTH_SECRET trimmed from NEXTAUTH_SECRET, **throws at module-load when unset** (fail-fast vs silent broken signatures); `lib/mongoose` (2) — `connectToDatabase` thin alias for connectDB from `@/lib/mongodb`, propagates errors; `lib/services/domains` (4) — listDomainsForUser queries Domain.find({userId}).sort({createdAt:-1}), getDomainById delegates to Domain.findById + returns null when missing; `lib/services/payments` (2) — createPaymentInTransaction calls Payment.create([data], {session}) (the **only** Mongoose signature that attaches to an in-flight transaction), unwraps the array result, propagates write-conflict errors; `lib/services/system-logs` (3) — recordSystemLog defaults level='error' + source='Unknown' + user=null, user-supplied fields override defaults, optional fields pass through verbatim or stay undefined; `lib/services/ip-checks` (4) — recordIPCheck stamps `checkedAt: new Date()` server-side + supports success/error paths, **getLatestIPCheck lazy-imports User model** for the populate call's typed 4th arg (Mongoose typed overload doesn't include it but runtime needs it — pinned so a future "cleanup" doesn't drop the arg) | ✅ Closed | `9329459` |
| Batch 7dh | M14 follow-up — RC read-ops + DA suspend/unsuspend wrappers (20 / 5 files): 3 RC reads + 2 DA writes — each a thin wrapper that delegates to the underlying SDK and maps results onto a typed Outcome union. `getDomainOrderId` (4): RC success with order-id → `found`; **success with data stringifying to 'null'** → not_found + info log (defensive — RC sometimes returns `"null"` as a literal); 'no matching' error message → not_found; generic error → hard_failure + error log. `getDomainDetails` (3): success → found, 'no orders found' message → not_found + info, generic error → hard_failure + error. `getDNSRecords` (4): forwards both `domainName` + `customerId` args, success → found, '404'/'not found' messages → not_found + info, auth-failed → hard_failure + error. `suspendUser` (5): delegates with optional reason arg, USER_NOT_FOUND_FRAGMENTS match → user_not_found + warn log, **DA status=503 → da_unreachable** (mapped specifically because that's DA's nginx-up-backend-down signature), generic Error → hard_failure. `unsuspendUser` (4): mirror of suspendUser but **logs user_not_found at ERROR level** (not warn) because the renewal flow has already collected payment — the asymmetry is a deliberate operational signal | ✅ Closed | `6eb12b5` |
| Batch 7di | M14 follow-up — DA delete-user + get-user-config + change-package wrappers (13 / 3 files): closes the 3 remaining DA user-op wrappers. `deleteUser` (4): happy `deleted` → no log, **user_not_found logged at INFO** ('already gone, treating as success at callsite') — distinct from get-user-config's WARN because cleanup intent + already-deleted = success, status=503 → da_unreachable + warn, generic Error → hard_failure + error. `getUserConfig` (4): happy → `{kind:'found', config}` with the full DA config payload; user_not_found logged at WARN (terminal — caller marks Hosting row orphaned); 503 → da_unreachable + warn (transient — retry); generic → hard_failure + error. `changePackage` (5): forwards both `username` + `newPackage` args, the wrapper has a **5th outcome `package_not_found`** (the only DA wrapper with that branch) for config/seeding errors → ERROR log; user_not_found → ERROR log (upgrade flow already paid); 503 → da_unreachable + warn; generic → hard_failure + error. With 7di + 7dh, **all 6 DA user-op wrappers (createUser was already covered in earlier slice 7e/da-create-user.test.ts) and all 6 RC operation wrappers now have unit-test coverage** | ✅ Closed | `a77753f` |
| Batch 7dj | M14 follow-up — RC renewDomain + auth-config helpers + field-encryption (28 / 3 files): `renewDomain` wrapper (6) — RC success → outcome from classify, hard_failure response → error log, **thrown error matching BALANCE_PENDING_FRAGMENTS** ('insufficient balance' literal) → `balance_pending` (short-circuit so the cron-retry path still works when the SDK throws instead of returning {status:'error'}), thrown non-balance error → hard_failure with the message preserved, non-Error throws (string) still classified (String coercion), forwards years arg through to SDK; `auth-config/helpers` (10) — extractSocialName per-provider name parsing: google uses given_name/family_name (falls back to user.name split), github prefers profile.name → login → user.name (login alone has no space → entire string is firstName), facebook + unknown providers split user.name only, missing fields → empty strings never throws; SOCIAL_PROVIDERS=['google','facebook','github'] shape pinned; **useSecureCookies** module-load constant — true when NEXTAUTH_URL starts with `https://`, false for http://, false when unset (defensive nullish chain); `field-encryption` (12) — AES-256-GCM round-trip preserves plaintext, output starts with 'enc:' prefix + iv:tag:ciphertext layout (24+32 hex chars for IV+authTag), **two encryptions of same input produce different ciphertexts** (fresh IV per call), re-encrypting an already-encrypted value is a no-op, decrypting plaintext returns it verbatim (legacy migration), empty strings pass through, key validation requires 64-char hex (throws otherwise), isEncrypted detects the prefix, **tampered authTag fails decryption** (GCM auth check), malformed payload throws 'Invalid encrypted field format' | ✅ Closed | `2cf378d` |
| Batch 7dk | M14 follow-up — RC transferDomain + redis cache + settings-service shim (30 / 3 files): RC `transferDomain` wrapper (9) — forwards all 4 args (domainName + authCode + customerId + optional contacts), success → outcome via classify, **transfer_rejected response logs at WARN (not error)** because rejection is the customer's problem (bad EPP code / transfer lock / 60-day window) not ours, hard_failure response logs at ERROR, thrown error matching BALANCE_PENDING_FRAGMENTS → balance_pending short-circuit, thrown error matching TRANSFER_REJECTED_FRAGMENTS ('auth code' literal, hyphenated 'auth-info' does NOT match — fragments use 'auth code'/'auth-code'/'authcode') → transfer_rejected, other throws → hard_failure, String coercion for non-Error throws — **closes all 6 RC operation wrappers**; `lib/redis` (15) — REDIS_HOST unset → null + warn log on module-load + cache methods become no-ops; configured → ioredis instance constructed with `lazyConnect`+`enableOfflineQueue:false`+5s connect/3s command timeouts; redisCache.get JSON-parses, returns null on miss, catches connection errors + JSON-parse failures + logs; redisCache.set JSON-stringifies with default 120s TTL via 'EX' command, respects custom TTL; redisCache.del → redis.del(key); all 3 catch errors + log without throwing (callers can't break on cache outages); `lib/settings-service` (8) — class-style shim delegating to functional API at `@/lib/services/settings`: getSetting → getSettingValue(key, defaultValue=null), setSetting → upsertSetting(key, value, {description:'', category:'general', updatedBy:'system'} defaults), **clearCache is no-op + info log** (caching is deliberately disabled — pinned so a future "optimisation" that adds caching isn't silently masked by the shim), getAllSettings maps the docs to a flat array with all metadata fields | ✅ Closed | `1af0146` |
| Batch 7dl | M14 follow-up — RC registerDomain + DA createUser orchestration wrappers (15 / 2 files): closes 2 wrappers whose underlying classify functions were tested in earlier slices (rc-register-domain.test.ts and da-create-user.test.ts) but whose **orchestration layers** weren't. RC `registerDomain` (7): forwards all 6 args (domainName, years, customerId, optional nameServers, contacts, tldAttributes) verbatim or as `undefined`, success → outcome via classify, hard_failure response → error log, thrown error matching BALANCE_PENDING_FRAGMENTS short-circuits to balance_pending (the axios-throw-instead-of-typed-error fallback), other throws → hard_failure, non-Error throws → String coerced. DA `createUser` (8) — the username-collision retry loop: **empty candidate list → 'no username candidates' hard_failure immediately** (defensive — no SDK call), first-candidate success → `{kind:'created', username}` (no retries), first-candidate collision + second-candidate success → tries the second + warn log on each retry attempt; **all-candidates-collide → `username_collision_exhausted`** (final warn log with the count, not error — collisions are expected); **status=503 short-circuits → `da_unreachable` (no further candidates tried** — the DA backend is down, retrying other usernames just wastes calls); generic Error short-circuits → `hard_failure`; `ip` arg defaults to DA_SERVER_IP from the SDK module, custom `ip` overrides. **Closes all 6 RC + 6 DA operation orchestration wrappers** | ✅ Closed | `3d5cdf8` |
| Batch 7dm | M14 follow-up — request-context + DA error unwrap + email transporter (27 / 3 files): `lib/request-context` (12) — AsyncLocalStorage-backed per-request ctx propagation: outside-scope getRequestContext/getCurrentRequestId both undefined; withRequestContext binds ctx for sync + async work (Promise.resolve, setTimeout) inside the run; **two concurrent runs keep their own ctx (no bleed)**; ctx unbound after run completes; withRequestLogContext extracts x-request-id from Request, missing header → fresh UUID v4 (pattern asserted), extra handler args forwarded; **`__requestContextStorage` published on globalThis** so server-logger can read without re-importing this module (avoids pulling `node:async_hooks` into the Edge runtime); `lib/directadmin/types` (7) — unwrapDAError: AxiosError with response → {status, data, code, message} all populated, AxiosError without response (network failure) → status+data undefined but code+message kept, AxiosError with string body (HTML auth-bounce case) → data is raw string, generic Error → message only, non-Error string/object/null/undefined coerced; `lib/email/transporter` (8) — module-load throws on missing SMTP_HOST/FROM_EMAIL env, **SPF alignment warning when FROM_EMAIL domain differs from SMTP_USER domain**, getTransporter creates with right config + verify(), caches across calls, throws after verify() rejects, sendEmail rejects invalid recipient (returns false, no SDK call), success → sendMail with `"NAME" <FROM>` shape + true return, sendMail rejection swallowed → false return (no throw) | ✅ Closed | `bd949c1` |
| Batch 7dn | M14 follow-up — email barrel + client logger + device fingerprint (37 / 3 files): `lib/email/index` (23) — `EmailService` class shim re-exporting sendEmail + 22 per-topic helpers (welcome / password-reset / activation / purchase-order / order-confirmation / admin-notification / low-balance / domain-purchase / domain-registration + failure / renewal-invoice / domain-booking-status / service-reminder / expiry-today / suspension / grace-period / available / hosting-provisioned), all asserted via `it.each` tables; sendEmail referentially identical at module + class level; `lib/logger` (12) — client-side dev-only console + production /api/v1/log forwarding: dev NODE_ENV → all 5 methods write to console.*; prod → no console.log/info/debug; **error + warn forward to `/api/v1/log` via `navigator.sendBeacon`** (payload contains level + sanitized messages); log/info/debug do NOT forward (avoid noise); Error objects serialised to {message, stack, name}; **circular objects replaced with '[Circular Object]'** (defensive JSON.stringify); fetch fallback when sendBeacon is unavailable (`keepalive:true` for page-lifecycle safety); `lib/device-fingerprint` (5) — SHA-256-hashed client signals: 64-hex-char hash on first call, **cached across calls within a module load** (no re-paint of the canvas), different navigator profiles → different hashes (module reset between cases), **'fb_' prefixed non-crypto fallback** when subtle.digest throws, canvas-throws path still returns a full hash (canvas signal goes empty, others remain) | ✅ Closed | `b034c97` |
| Batch 7do | M14 follow-up — auth-config barrel + client error handler + cloud tasks (29 / 3 files): `lib/auth-config` (9) — backwards-compat barrel: wires providers/callbacks/cookies from submodules, pages.signIn/error='/login', session.strategy='jwt'+maxAge=30min, secret=AUTH_SECRET, debug tracks NODE_ENV (true=dev / false=prod), logger.error/warn/debug forward to serverLogger with NextAuth Error:/Warning:/Debug: prefixes + JSON-stringified metadata; `lib/client-error-handler` (13) — parseApiResponse: JSON 200 → parsed body, JSON 200 + success:false → ApiError (using data.error || data.message || 'Request failed' fallback chain), JSON 400 → ApiError with status, **non-JSON 503 → 'Service Unavailable' with SERVICE_UNAVAILABLE code**, non-JSON 500 → 'Server Error (500)' with SERVER_ERROR code, unreadable body → details='Could not read response body'; getUserFriendlyErrorMessage — DA-server-down codes (DA_SERVER_DOWN/503/504/SERVICE_UNAVAILABLE) → 'Hosting Server is currently unreachable', NO_HOSTING → empty string (UI empty-state), 'Failed to fetch'/TypeError → 'Connection failed', err.message fallback then generic; `lib/cloud-tasks` (7) — createHttpTask: throws on missing GCP_PROJECT_ID, **CloudTasksClient lazy-imported AND cached** across calls (asserted by Ctor mock called once across 3 invocations), parent path uses GCP_QUEUE_LOCATION default 'us-central1', task body **base64-encoded JSON** with x-cron-secret header carrying CRON_SECRET (or empty string when unset — defensive default, not absent), scheduledTime → task.scheduleTime.seconds, createTask rejection → error log + rethrow | ✅ Closed | `0c01d6b` |
| Batch 7dp | M14 follow-up — safe storage + API response wrapper (20 / 2 files): `lib/storage` (7) — SafeStorage's privacy-mode fallback: happy path delegates to window.localStorage/sessionStorage (round-trip + length + clear + key/index iteration); safeLocalStorage and safeSessionStorage are **independent instances** (writes don't leak between them); **window.localStorage access throwing at probe time → falls back to MemoryStorage** + warn log (the privacy-mode / blocked-cookie path that broke production logins before this wrapper existed); setItem failure on the real storage (e.g. QuotaExceededError) is caught and logged WITHOUT throwing (caller code keeps working — important for the cart-save path that survives quota errors); `lib/api-response-wrapper` (13) — withSecurityHeaders delegates to addSecurityHeaders, secureJsonResponse builds NextResponse.json + applies headers (default status 200), secureErrorResponse message-masking: **500 in production masks the real error to 'An internal server error occurred'** (defence-in-depth against internal-disclosure), 500 in dev keeps the real message, **503 in production keeps the real error** (operational status, not internal disclosure — the DA-server-down message must reach the user), 4xx keeps the real error in production (validation/auth messages are user-facing), **400 with errorDetails embeds them in body** (other statuses do NOT — never leak internals), timestamp on every error body, code defaults to 'ERROR'; logging: 5xx → serverLogger.error, <5xx → serverLogger.warn; withSecureHeaders HOF applies headers after handler resolves | ✅ Closed | `7568855` |
| Batch 7dq | M14 follow-up — trial-OTP + cached MongoDB connection (21 / 2 files): `lib/trial-otp` (15) — redis-backed phone OTP + HMAC-signed proof tokens: generateOtp 6-digit format, normalisePhone strips non-digits + leading 91 country code, storeOtp writes {code, attempts:0} with 10-min TTL, consumeOtp **5-attempt lockout** (delete + 'Too many incorrect attempts') / wrong code increments attempts / missing → 'OTP expired' / correct → delete + ok:true (single-use), signOtpToken+verifyOtpToken round-trip, tampered signature → 'Invalid signature', expectedPhone mismatch → 'Token does not match phone', **expired token** (Date.now > e, asserted with vi.useFakeTimers advancing 31min past 30min TTL) → 'Token expired', wrong version 'v0' → 'Unsupported token version', malformed input (empty, no dot, non-string, invalid JSON in payload) → 'Malformed token' never throws; `lib/mongodb` (6) — cached connection with documented pool options: module-load throws on missing MONGODB_URI, first call invokes mongoose.connect with `maxPoolSize:50`+`minPoolSize:2`+`maxConnecting:5`+`waitQueueTimeoutMS:10000` etc. (pinned because pool sizing is tied to Cloud Run's --concurrency=80 — a future "make it 10 again" would silently re-introduce the latency floor that bumping to 50 fixed), subsequent calls return cached connection (single mongoose.connect across multiple calls), **failed connect clears cached promise** so next call retries (otherwise the rejected promise would be returned forever), cache published on globalThis.mongoose to survive Next.js hot-reloads | ✅ Closed | `d37bc1c` |
| Batch 7dr | M14 follow-up — hosting-provisioned email + DA server ops (18 / 2 files): `lib/email/hosting` (9) — sendHostingProvisionedEmail: sendEmail boundary mocked, asserts the assembled HTML — recipient + subject 'Hosting Account Provisioned Successfully' + userName greeting, domain + serverIp embedded, **planName preferred over packageName when both supplied** (planName='Starter' beats packageName='basic' in the rendered table), packageName fallback when planName absent, each nameserver rendered as an `<li>`, NEXTAUTH_URL-anchored `/dashboard/hosting` button, SUPPORT_EMAIL mailto link from the transporter module, sendEmail false propagates to caller, empty nameservers array → empty ul (no crash); `lib/directadmin/server` (9) — three server-level DA ops (getServerInfo / listResellers / getLicenseInfo). All three GET against their respective `CMD_API_*` endpoint with basic auth + DEFAULT_TIMEOUT_MS=30s. **DA in-band error detection on BOTH object form (`data.error === '1'`) AND string body starting with 'error=1'** — DA returns either depending on the endpoint. listResellers: `list[]` array, single-string `list` coerced to a 1-element array, falsy entries filtered out. Each op calls `executeRequest(fn, opName)` for the standard retry wrapper — the test mock's pass-through invocation pins the op-name (GetServerInfo / ListResellers / GetLicenseInfo) for log correlation | ✅ Closed | `794766b` |
| Batch 7ds | M14 follow-up — SMS provider + settings service (22 / 2 files): `lib/sms` (13) — provider-pluggable SMS abstraction. Default provider is **'console'** (logs only, no SMS credits burned) — happy path produces success=true + provider='console' + `console_${Date.now()}` messageId. **Phone numbers masked in logs** — only last 4 digits visible (`***1111` not `9999911111`); short numbers (<4 digits) mask as `***`. `SMS_PROVIDER='msg91'` selects the MSG91 adapter. MSG91 env validation (missing AUTH_KEY/TEMPLATE_ID → success:false + no fetch). 10-digit Indian numbers normalised to 91-prefixed (`9999911111` → `919999911111`). Template variables forwarded as recipient fields. **`authkey` header** for authentication (not Bearer). Non-OK response / `type:'error'` in-band / fetch throw → all surface as success:false with the right error message; `lib/services/settings` (9) — functional settings API. getSettingValue swallows DB errors → returns defaultValue + logs (feature-flag fallback to conservative default). getSetting returns full doc with metadata. getSettingsMap empty input → empty map (no DB call — defensive short-circuit), one-round-trip $in batch read. listSettings sorts by category then key (admin-panel ordering). upsertSetting with defaults (`updatedBy='system'`) + omits description/category when not supplied (no overwriting prior values with undefined). deleteSetting → `deleteOne({key})` | ✅ Closed | `adb4a7e` |
| Batch 7dt | M14 follow-up — disposable-email blocklist + pending-domains service (20 / 2 files): `lib/disposable-emails` (8) — hand-curated blocklist: known domains (tempmail, mailinator, 10minutemail, guerrillamail, yopmail, trashmail) flagged via `it.each`; **subdomain match via parent-domain walk** (`foo.mailinator.com` and `deep.nested.mailinator.com` both hit `mailinator.com`); case-insensitive; **malformed input never throws** (empty / no-@ / `user@` / `@example.com` / no-TLD / null / undefined / number all return false — caller handles email-format validation separately); guerrillamail family with multiple TLDs (.com/.net/.de/.biz + guerrillamailblock + sharklasers) all blocked; `getDisposableDomains` returns full list, mutating the returned array **does NOT affect future calls** (fresh array per invocation — defence against attacker-injected mutation); `lib/services/pending-domains` (12) — admin half-state CRUD: `getPendingDomainById` with **dual `_id` matching** ($or with both raw-string and ObjectId form when the input is ObjectId-valid; only raw-string match for legacy non-hex IDs — pinned because the legacy collection has rows with string _id that won't match ObjectId() coercion); `populateUser=true` populates 'userId' with the admin projection `firstName lastName email phone companyName`; `getPendingDomainByName` delegates to `findOne({domainName})`; `listActivePendingDomainsForUser` filters `isArchived: {$ne:true}` (admins archive resolved/abandoned rows — they shouldn't appear in user UI) + sorts newest-first; `listAllPendingDomainNames` returns the lean `{domainName:1}` projection only | ✅ Closed | `c1c1580` |
| Batch 7du | M14 follow-up — hosting-plans service + billing helpers (36 / 2 files): `lib/services/hosting-plans` (12) — getPlanByPlanId default filters by planId only; `activeOnly:true` adds isActive:true; getPlanById delegates to findById; getPlanByRazorpaySubscriptionPlanId uses **$or on both `razorpayPlans.monthly` AND `razorpayPlans.yearly`** (subscription.charged webhook needs to route both monthly + yearly Razorpay plan IDs back to the same local plan); listActivePlans default = price ASC (`{price:1}`) — matches the upgrade UI's "next-higher tier" ordering, 'price-desc' inverts, **sort:false skips sorting entirely** (insertion order); getPlanByPlanIdLean adds `.lean()`; setPlanActive returns `{matched, modified}` from updateOne with **defensive `?? 0` fallback** when older mongoose drivers omit the count fields; upsertPlanByPlanId uses $set + upsert:true + new:true + **always includes `planId` in the $set** so upsert-insert has the key field; `lib/billing` (24) — cart-item type inference + expiration math. **isHostingItem priority order**: itemType='hosting' first, then `hostingPlan.name/serverPackage` data, then `'hosting-'` placeholder domainName — note the **placeholder check runs BEFORE the itemType='domain' negative check** to catch mislabeled trial-items (a 'domain' label with 'hosting-x' name still classifies as hosting). inferPeriodUnit: explicit periodUnit wins, hosting+registrationPeriod=10 → 'minutes' (the test-cycle convention), hosting else → 'months', domain → 'years'. calculateItemExpiration: hosting + months/days, domain + years, **duration > 10 forces monthly** even for itemType='domain' — defence against the 2038-style bug where `12 months` registered as `12 YEARS` would land past the Unix-time limit. validateCartItem: missing hostingPlan / domainName / price → string errors; valid items → null | ✅ Closed | `1a742d4` |
| Batch 7dv | M14 follow-up — renewal-payments service + suggestion generator (17 / 2 files): `lib/services/renewal-payments` (9) — idempotency-guard for the Razorpay `subscription.charged` webhook (4-step lock protocol). recordRenewalPayment inserts with `processed:false` + `status:'success'` + the full payload shape; **Mongo 11000 duplicate-key errors propagate** (callers catch as 'already seen'); subscriptionId forwarded when supplied. claimRenewalPayment is the **atomic flip**: filter `{providerPaymentId, processed:false}` + `$set processed:true + processedAt:Date` + `new:true` (returns updated doc — null when another worker already claimed). releaseRenewalClaim unwinds: `$set processed:false + $unset processedAt`. attachOrderToRenewal: $set orderId only (no claim transition); `lib/suggestion-generator` (8) — alternative-domain generator. Groups candidates by base name so RC calls use `searchDomainWithTlds` (one network call per unique base, not per candidate); only `available=true` results pass through; **TLD→category mapping** (com/net/in/co.in/org → Popular; tech/ai/dev/io/digital → Tech; biz/solutions/inc/corp → Business; else → All); **originalPrice = round(price * 1.5)** for the UI strike-through; final results capped at 20 (categorisation tabs); per-base RC errors swallowed — other bases still surface their results | ✅ Closed | `9496c10` |
| Batch 7dw | M14 follow-up — ResellerClubAPI + DirectAdminService class barrels (60 / 2 files): `lib/resellerclub` (27) — ResellerClubAPI static facade re-exports **all 30 methods** from the 5 topical submodules (search:6 / customers:8 / registration:5 / dns:8 / renewal-transfer:3), verified via `it.each` — pins each function exists + is referentially identical (`searchDomain === search.searchDomain`) so a future submodule rename surfaces here; `lib/directadmin` (33) — DirectAdminService static facade re-exports **22 methods + 4 validators + 3 constants** from packages/users/dns/server submodules, including the client-level `DirectAdminError` class + `DA_SERVER_IP` constant + `NAMESERVERS` + `KNOWN_PACKAGES` class statics, and the 4 validator functions (validateUsername / normalizePackageName / validatePackageName / logDebugCredentials). Both shims preserve the static-only surface deliberately (`@version 2.0.0` source comments warn against stateful initialization in serverless requests) — these tests pin the contract so the call sites' 100+ `ResellerClubAPI.X` / `DirectAdminService.X` invocations don't silently break on a future "ESM-ify the API" PR | ✅ Closed | `56655bc` |
| Batch 7dx | M14 follow-up — logout helpers + domain-watches service (15 / 2 files): `lib/logout` (8) — performLogout + logoutUser. Pins: **isLoggingOut flag set BEFORE signOut** (AuthSync sees the flag before re-sync races a stale session); signOut called with `redirect:false` so we control the navigation; clears the 4 documented localStorage auth fields **without touching unrelated keys** (token/user/rememberMe/savedEmail — but a 'other' key survives, asserted explicitly); clears all 6 cookie variants (token + 5 NextAuth flavours) on both `path:/` and `path:/+domain=hostname` (cross-domain coverage); toast.success + 500ms-delayed `window.location.replace('/login')` for performLogout, immediate replace for logoutUser; signOut throw on happy path is logged + logout still completes; outer-catch fallback (forced via Array.forEach throw) still triggers full clear + redirect; `lib/services/domain-watches` (7) — DomainWatch CRUD: `listWatchesForUser` lean projection + createdAt desc sort, `countWatchesForUser` pre-check before upsert (avoids over-count past MAX_WATCHES), upsertUserWatch with `setDefaultsOnInsert:true` + insert default `lastStatus:'unknown'` (cron picks up new rows on next tick), removeUserWatch returns `deletedCount > 0` → bool (false → route 404), listWatchesForCron populates userId with `email/firstName/lastName` projection + limit + lean, recordWatchCheck stamps lastCheckedAt+lastStatus, removeWatchById is the one-shot post-notify cron delete | ✅ Closed | `494c14a` |
| Batch 7dy | M14 follow-up — Razorpay payments service + ResellerClub axios client (24 / 2 files): `lib/razorpay-payments` (16) — RazorpayPaymentsService static facade over `razorpayClient.payments`. Pins getAllPayments forwards `{count, skip}` (defaults 100/0); **getPaymentsByDateRange converts JS Date → Unix-second epoch via `Math.floor(d.getTime()/1000)`** (Razorpay's wire format takes seconds, not ms — a 1000× mistake would silently send queries far outside the actual range); SDK throws are logged + rethrown for both list methods; filterDomainPayments OR-combines 4 signals (description contains `ord_`, notes carry `domainName/orderId/domains`, amount in paise range ₹100–₹10000, OR failed-status payments with any of those — the failed branch lets refund-tracing surface non-captured orders); payments with zero signals (sub-₹100 amount + no notes/desc) are excluded; getDomainPaymentDetails extracts orderId via the **`ord_\d+_\w+`** regex from description, falls back to notes.orderId; domainNames preference order is `notes.domainNames > notes.domainName (wrapped) > notes.domains`; customerEmail falls back to payment.email **when notes.customerEmail is absent** but notes.customerEmail wins when present; payment object returned by-reference (same identity); `lib/resellerclub/client` (8) — axios facade + interceptors. Pins **module-load env validation throws** when any of `RESELLERCLUB_API_URL / RESELLERCLUB_ID / RESELLERCLUB_SECRET` is missing (caught via `expect(import(...)).rejects` with `vi.resetModules` per test); axios.create called with the right baseURL + 30s timeout + UA header; request-interceptor masks `api-key + auth-userid` in the serverLogger payload then INJECTS the real `auth-userid / api-key / reseller-id` into params (only the log line is masked, the wire credentials are intact); response success interceptor logs status + url + passes the response through unchanged; **response error interceptor is the credential-leak guard — logs `path` only, NEVER `config.params`** (asserted via `JSON.stringify(meta) not.toContain("leaky_secret")` — a future "log full request context" PR breaks this test before it lands in prod) | ✅ Closed | `25d8fa1` |
| Batch 7dz | M14 follow-up — RC renewal/transfer + Zoho org gate (15 / 2 files): `lib/resellerclub/renewal-transfer` (8) — 3 RC wire wrappers. getRenewalPricing GETs `/api/domains/renewal-price.json` with `{domain-name, years}`; renewDomain POSTs `/api/domains/renew.json` with null body + `order-id/years/exp-date/invoice-option:NoInvoice` (exp-date stamped through as the existing Unix-second registry-expiry — RC requires it to compute new expiry server-side, not "now+years*365d"); renewDomain error message **falls through err.response.data.message > raw .data (string body) > err.message > 'Failed to renew domain'** so a registry response like `{message:'Domain locked'}` reaches the user verbatim, but a raw-string body also passes through; transferDomain base call includes domain-name/auth-code/customer-id/NoInvoice; with-contacts variant fans out to 4 fields with **reg-contact-id mirroring admin** (RC requires registrant=admin for transferred domains — a contacts={admin,tech,billing} caller doesn't need to repeat admin as registrant); all 3 RC throws wrap into `{status:'error', message}` sentinel; `lib/zohobooks/org` (7) — getOrganizationDetails. Pins **early-return null when `_hasRefreshToken()` is false** (lazy init — callers never crash on missing-OAuth-config, they get null and degrade gracefully); finds organization via **`String(o.organization_id) === String(self._orgId)`** on both sides (Zoho returns numeric IDs as strings sometimes — a strict `===` against a numeric `_orgId` config would miss); falls back to orgs[0] for single-org tenants (the common case — admin-token-less reseller setups); `response.data.code !== 0` → null; missing `organizations` array → null; axios throw → unwrapZohoError + logger.error + null (never bubbles to the caller); **HTTP call routed through `self._idempotentRetry`** so transient 503s from Zoho's billing-cluster get retried at the service level | ✅ Closed | `0e6453c` |
| Batch 7ea | M14 follow-up — Zoho credit-notes (refund flow) + recurring-invoice profiles (19 / 2 files): `lib/zohobooks/credit-notes` (8) — createCreditNote. Pins **typed ZohoError throw with MISSING_REFRESH_TOKEN sentinel** when refresh token is absent (refund handler relies on this — without typed sentinel, downstream axios ENOTFOUND would be unrecoverable); paise → rupees via /100 + `self._roundAmount` (defence against the float-precision case where /100 produces .999999...); reference_number = `REFUND-{razorpayRefundId}` so Zoho dedupes on the natural key — a re-fired webhook can no-op safely; line_items: 1 row, quantity:1, rate=rupees, name=`Refund — Order {orderId}`; **2-step protocol**: POST `/creditnotes` THEN POST `/creditnotes/{id}/invoices` to apply against the original invoice (Zoho doesn't auto-apply on creation); apply uses the same rupee amount (no double-rounding); **step-1 fail throws `CREDITNOTE_CREATE_FAILED`, step-2 fail throws `CREDITNOTE_APPLY_FAILED`** with distinct sentinel codes so the caller can distinguish 'orphan credit-note in Zoho' (apply-fail) from 'no credit-note at all' (create-fail); both HTTP calls wrapped in `self._idempotentRetry` so transient 503s retry; `lib/zohobooks/recurring` (11) — createRecurringInvoice (Razorpay-subscription → Zoho RIP per hosting line). Pins null-token returns `[]` (lazy-init); **contact get-or-create**: getContactByEmail null → createContact, hit → updateContactDetails (re-sync edited user info — the second branch matters: a customer that updated their billing-address in the user UI gets the change pushed to Zoho on next renewal cycle); non-hosting items filtered out; **`registrationPeriod<12` → monthly profile (recurrence_frequency='months', create_days_before=7, start_date=today+1mo); `>=12` → yearly (frequency='years', cdb=30, start_date=today+1yr)** — these constants drive the renewal-notice email window AND the first-charge date; `is_never_expiring:true` (cancel via UI, never auto-stop); line item name: hostingPlan.name > 'Hosting Service' fallback for the synthetic 'hosting-XYZ' placeholder; rate routes through `self._roundAmount` (defence against a buggy price like 1023.456 hitting Zoho as the full float); **per-item try/catch independent of others** — 1 item's profile-creation failure doesn't abort the loop, the bad one is recorded `success:false` and the next still processes (1-to-many hosting orders matter); top-level catch on contact-failure → returns `[]` (the rest of the flow can't proceed without a Zoho contact ID) | ✅ Closed | `fe59514` |
| Batch 7eb | M14 follow-up — Zoho contact CRUD + GST fallbacks (17 / 1 file): `lib/zohobooks/contacts` — covers all 7 exported functions. Pins getContactByEmail/getContactByName **propagate `MISSING_REFRESH_TOKEN` ZohoError** instead of swallowing (caller-side distinction between 'not found' and 'broken auth' — a swallow-and-return-null would let the refund flow silently double-create); both functions accept code:0+contacts:[] as null; axios throws fall through to null after logging. createContact: **GST sanitization** trims whitespace + strips internal spaces + uppercases before sending (Zoho's GST validator is strict — a `"  07aabcu9603r 1zm  "` field hits the wire as `"07AABCU9603R1ZM"`); no-GST users get `gst_treatment:'consumer'` (no gst_no key); **GST-validation fallback** — when Zoho rejects with `code:2` + `'gst'` in message, the function strips gst_no + retries the POST as `consumer`, surfacing the contact successfully (defence against admin re-entering a partially-valid GST without re-checking the validator); **duplicate-name fallback** — Zoho `code:3062` triggers `getContactByName(contact_name)` to find the pre-existing contact and return it (Zoho enforces unique contact_name across the org — first-time race or admin imports can dupe). updateContactDetails: cascades to **updateContactPerson** via `getContactPersons` + `find(p => p.is_primary_contact)` (Zoho uses the contact-person's name for the invoice "Bill To" line — main-contact update alone leaves stale names on the PDF); **person-update failure logs WARN but main update still succeeds** (returns true — the invoice header is the more important field); main-update throw → false (degrade gracefully, caller proceeds). getContactPersons returns the array on code:0 / [] on no-token / [] on throw. updateContactPerson PUTs to `/contacts/contactpersons/{id}` with **phone mirrored to both `phone` and `mobile`** (Zoho's contact-person schema separates them but our user model has only one phone field). updateContactToConsumer: the explicit invoice-blocker fallback — `gst_treatment:'consumer'` + `gst_no:''` (clears the GST number on the contact entirely; tests pass AxiosError instances to trigger the `unwrapZohoError` `instanceof AxiosError` branch — plain rejected objects don't trigger `errorData?.code` lookup) | ✅ Closed | `5605810` |
| Batch 7ec | M14 follow-up — Domain-requirements registry + edge-runtime security headers (33 / 2 files): `lib/domainRequirements` (18) — RESTRICTED_TLDS derived at module-load from TLD_POLICIES (source-of-truth pattern — any addition to TLD_POLICIES surfaces here automatically); isRestrictedTLD accepts both `.com` AND `com` shapes (DB rows store TLDs in both flavours) + case-insensitive; 4 pre-canned country TLDs (.au/.co.uk/.ca/.de) with the .au ABN/ACN field pinned as the killer requirement; unknown TLDs return `{requirements:[], restrictions:[]}` not throw; **generateAlternativeDomains excludes the INPUT TLD** from the 5-common-TLD list (no `.com → .com` echo; input not in common-5 → all 5 returned); all entries marked `available:true` with the in-code note that the caller is responsible for actual RC checks; requiresAdditionalDetails is a backward-compat alias asserted via referential identity (`===`); isDomainSupported is the literal inverse; `lib/security/headers` (15) — `addCorsHeaders` SKIPS `/api/webhooks/*` routes (third-party callers don't send/check CORS — a wildcard `*` here would be wrong); matching `Origin` header **echoed back when it equals APP_ORIGIN**, otherwise falls back to APP_ORIGIN (defence against CSRF-via-Origin — a fetched `https://evil.example` Origin doesn't get echoed); buildPreflightResponse returns 204 + Max-Age:86400 + the CORS headers; `addSecurityHeaders` always-on (X-Content-Type-Options/X-Frame-Options/X-XSS-Protection/Referrer-Policy); **HSTS + CSP gated on `NODE_ENV==='production'`** (dev gets neither); **skipCSP suppresses CSP even in prod** (legacy admin pages); **strictCSP OMITS `'unsafe-inline'` + `'unsafe-eval'` from script-src** (the nonce-only model for API routes + static legal/marketing/error pages — an XSS hole there can't pivot via eval/new Function); nonce option injects `'nonce-XYZ'` into script-src; X-Powered-By stripped if upstream set it; **handler-set headers WIN over the security defaults** (X-Frame-Options:DENY set by handler survives — uses `headers.has()` guard); CSP carries Razorpay/reCAPTCHA/Cloudflare-turnstile allow-list + frame-ancestors:'self' + object-src:'none'. Test setup unmocks next/server (the global setup.ts mocks NextResponse to stubs) and uses `vi.importActual` to get the real Headers behaviour | ✅ Closed | `6a3ec3d` |

| Batch 7ed | M14 follow-up — reCAPTCHA server-verify + Razorpay SDK singleton (18 / 2 files): `lib/recaptcha` (9, server-side) — RecaptchaServer.verifyToken + isCaptchaEnabled. Pins **isCaptchaEnabled DEFAULTS TO TRUE on any error** (settings-table outage MUST NOT silently disable captcha — security default is "on"); DB setting accepted as either boolean true OR string 'true' (the settings store accepts both); **kill-switch is checked BEFORE the missing-secret-key warning, the token check, AND the network call** so an admin who disabled captcha sees zero noise even with placeholder keys; missing/placeholder secret key path returns success:true + WARN log (dev convenience — `your-recaptcha-secret-key` literal hits this); empty-token → success:false with error 'reCAPTCHA token is required'; **literal sentinel `'captcha-disabled'` → success:false** (the explicit-bypass form sent by the frontend when captcha-off — different from kill-switch path because settings says enabled but the frontend lied); siteverify POST'd to `https://www.google.com/recaptcha/api/siteverify` with URL-encoded params secret+response+remoteip (remoteip omitted when not supplied — not just empty); !data.success → returns the Google `error-codes` array verbatim for caller display; network throw → success:false + serverLogger.error (never crashes the auth handler); `lib/razorpay-client` (9) — module-load gate. Pins **throws at module-load when RAZORPAY_KEY_ID or KEY_SECRET missing** (fail-fast — preferable to 401 on first payment attempt because the error surfaces at boot, not under traffic); SDK ctor called with `{key_id, key_secret}` from env; **`api.defaults.timeout = 30_000`** (the L1 fix: the official SDK has no default timeout and a hung Razorpay slot would otherwise stall payment-verify indefinitely — mirrors the resolved H3 Zoho axios timeout); both `razorpay` AND `razorpayClient` exports point at the SAME singleton SDK instance (the L1 collapse — prior to this module 10 callsites each constructed their own SDK with their own escape-hatch `as unknown` casts). Test pattern: `vi.resetModules()` + dynamic import to re-exercise the module-load gate per test; Razorpay ctor mocked with a function (not arrow) that **explicitly returns the sdkInstance** so `new RazorpayCtor()` evaluates to that singleton (an arrow lacks `[[Construct]]`; an unreturned plain function would yield a fresh `this` object each call) | ✅ Closed | `6b5d747` |
| Batch 7ee | M14 follow-up — structured server-logger + admin-audit log (36 / 2 files): `lib/server-logger` (21) — the Cloud-Logging-aware facade. Pins **production output is ONE JSON line per call** with severity + message + time (Cloud Logging auto-parses single-line JSON into structured entries — multi-line breaks dashboards); **severity enum strings exactly match Google's LogSeverity** (INFO/WARNING/ERROR/DEBUG) so the web UI colour-codes correctly; dev output is `[SEVERITY] ...args` with native console rendering (no pre-stringify — stack traces stay readable in the terminal); **path sanitization** replaces `process.cwd()` with `<PROJECT_ROOT>` (defence vs leaking internal paths in Stack-Overflow-pasted logs); plain object args MERGED into the top-level JSON (`{requestId, orderId}` becomes top-level fields — Cloud Logging filters can query them directly), NOT serialised into the message string; Error args: message → `message` field, stack + errorName → meta; unserialisable objects (circular refs) → `[unserializable object]` placeholder, never throws; **ambient `requestId` pulled from `globalThis.__requestContextStorage.getStore()`** (the AsyncLocalStorage hop — request-context.ts uses node:async_hooks which Webpack rejects in the Edge runtime, so the globalThis hop keeps middleware's import path clean); explicit `requestId` arg WINS over ambient (call-site override); ambient-storage throw is swallowed; **`error()` ALSO fires remoteLog** (fire-and-forget POST to `/api/v1/admin/log-error` with `x-cron-secret` header + 2s `AbortSignal.timeout` — info/warn/debug do NOT); remoteLog NO-OPs when neither `NEXTAUTH_URL` nor `APP_URL` is set; `APP_URL` is the fallback; fetch throw silently swallowed; remoteLog stack is path-sanitized too; `lib/audit-log` (15) — admin-action audit logger. Pins logAdminAction never bubbles a DB write failure (logging MUST NOT break the request); logAPIRequest derives a structured `METHOD_RESOURCE` action (GET → VIEW_X, POST → CREATE_X, PUT/PATCH → UPDATE_X, DELETE → DELETE_X); **request-body sanitization redacts ALL 9 sensitive fields** to `"[REDACTED]"` before insert (password/token/secret/apiKey/accessToken/refreshToken/creditCard/cvv/ssn) — non-sensitive fields preserved; `responseStatus >= 400` → `success:false` AND auto-fills `error:"HTTP {status}"` (explicit error arg wins); **getClientIP precedence: x-forwarded-for (first comma-split) > x-real-ip > request.ip > 'unknown'** (the standard CF/nginx proxy chain); user.id missing → falls back to `_id.toString()` (hydrated Mongoose docs); queryAuditLogs builds {userId/action/ip/createdAt:{$gte,$lte}} filter; sort createdAt:-1 + default limit 100 + skip 0 + lean; DB throw → returns []; getAuditStats aggregates actionsByType counts, actionsByDay sorted ASC, topIPs top-10 DESC, **errorRate = errors/total*100 (divide-by-zero guard: 0 when totalActions=0)**; DB throw → returns the zero-shape object never bubbles | ✅ Closed | `7852b7f` |

| Batch 7ef | M14 follow-up — DA + RC integration classifiers (59 / 2 files): `lib/integrations/directadmin/classify` (29) — pure DA-error → typed-outcome classifiers for the 6 user-ops (createUser, suspendUser, unsuspendUser, getUserConfig, deleteUser, changePackage). Pins the **3 shared vocabularies** byte-for-byte: USERNAME_COLLISION (1 fragment), USER_NOT_FOUND (6 wordings — unable-to-find/no-such/does-not-exist/not-found/unknown/cannot-find), PACKAGE_NOT_FOUND (6 wordings). matchesAny is case-insensitive substring + undefined-safe + empty-safe. **Priority order for all 6 classifiers**: vocabulary-matched-kind → 503-unreachable → hard-failure-with-op-specific-default-reason. **changePackage uniquely has a 4-branch order**: USER_NOT_FOUND CHECKED FIRST then PACKAGE_NOT_FOUND (DA can emit both fragments when both fields are wrong — user dominates so the caller doesn't try to fix the wrong field); `lib/integrations/resellerclub/classify` (30) — pure RC-response → typed-outcome classifiers for register / renew / getDomainOrderId / getDomainDetails / getDNSRecords / transferDomain. **5 vocabularies pinned**: BALANCE_PENDING (7 including the surprising 'please contact support' — RC sometimes wraps balance issues in a generic support-contact message so we treat it as pending and let the auto-retry cron drain it), PROCESSING_LOCK (3), ALREADY_IN_PROGRESS (3), READ_NOT_FOUND (9), TRANSFER_REJECTED (11 incl. clientTransferProhibited / 60-day / EPP / authcode variants). **Register status priority**: success+orderid → registered, success-no-orderid → registered_no_order_id, pending → balance_pending, message fragments override status:'error'. **Renew price coercion**: numeric pass-through, string → Number(), non-numeric → undefined via Number.isFinite. **getDomainOrderId guards literal-string sentinels**: 'undefined' / 'null' / empty → not_found (RC's inner wrapper returns success+stringified-undefined when the lookup misses). **getDNSRecords**: empty records IS 'found' (not 'not_found' — that's reserved for 'domain not under DNS management'); defensive Array.isArray guard returns [] when records missing. **transferDomain**: balance fragment → balance_pending (NOT transfer_rejected — different recovery flow); EPP/lock/60-day fragments → transfer_rejected; entityid stringified to entityId | ✅ Closed | `c973347` |

| Batch 7eg | M14 follow-up — SecurityValidator + DirectAdmin client pure helpers (64 / 2 files): `lib/security/validator` (30) — SecurityValidator static surface. Pins containsMaliciousPatterns flags SQL keywords, NoSQL operators ($where/$ne/$regex/...), XSS (<script>, javascript:, on*=), command injection, path-traversal (../, %2f variants), null byte (\x00), control chars; **sanitized field is progressive multi-pattern strip** (the SQL pattern eats the literal word 'SCRIPT' then the XML <[^>]*> pattern eats the bare angle brackets — defence-in-depth, not a hermetic strip — inner chars between matched fragments survive); validateFileUpload rejects 18 dangerous extensions verbatim + **short-circuits on >10MB content BEFORE pattern-scanning** (the in-code warning notes the regex stack-overflows on huge inputs — content already over-budget); sanitizeInput honours maxLength=1000 default + allowHtml strips <tags> + allowSpecialChars limits to alnum/space/-_.; validateEmailSecurity lowercases + trims + rejects '..' / '@.' / '.@' patterns + >254 chars; **validatePasswordSecurity strength tiers**: <8 weak+error, 8-11 medium, 12+ strong; rejects 9 common patterns (123456/password/admin/user/qwerty/abc123/letmein/welcome/login); tests use `#` not `!` as the password's special char because `!` matches the LDAP-injection class and would fail the malicious-content check. **validateCSRF dual-layer defense**: GET/HEAD/OPTIONS bypass; Origin match wins (mismatch fails 'CSRF: Origin mismatch'); Referer fallback when no Origin (startsWith check on authorizedUrl); **PRODUCTION with no Origin AND no Referer → fails** (head-less request guard against curl-style scripted attacks); DEV without headers passes (curl-friendly); NEXTAUTH_URL/APP_URL unset → 'CSRF: NEXTAUTH_URL is not configured'; `lib/directadmin/client` (34) — pure helpers (executeRequest skipped — global queue + circuit breaker keep module-level state). Pins **DirectAdminError sets 4 properties** (message, context, status, response — the typed-error contract every classifier reads); **DA_SERVER_IP env-driven with literal fallback** (first-boot safety — production deploys MUST set DIRECTADMIN_IP or a forgotten staging env would silently provision new accounts onto the prod DA box); NAMESERVERS = the two ns1/ns2 entries DA hands clients; KNOWN_PACKAGES derived from HOSTING_PLANS.serverPackage at module-load (single source of truth); MIN_REQUEST_INTERVAL_MS=500ms rate-limit floor; DEFAULT_TIMEOUT_MS=8000ms per-request budget. **parseDAError handles 4 input shapes**: {error}/{text}/{details} object → that string, URL-encoded `error=1&text=...&details=...` → **DECODED concat with '+' → space** (DA emits `Failed%20to+create` → "Failed to create"), HTML → tag-stripped + collapsed-whitespace (under 500 chars), raw string under 500 chars → as-is, anything else → "Complex or empty error response from DirectAdmin"; parseResponseData URL-encoded string → object; **repeated keys (`list[]=a&list[]=b`) merge into string[]** (DA's array-fan-out wire format); validateUsername 3-16 chars + must start with [a-z] + alnum after (no `_` or `-` allowed — DA's strict identifier rules); validatePackageName allows [A-Za-z0-9_-]+ (looser since admins seed packages); **normalizePackageName case-corrects against KNOWN_PACKAGES + logs only when actually corrected** (already-canonical input is a silent no-op — pinned to avoid log spam); unknown package passed through unchanged (validation is separate); empty input passed through; **logDebugCredentials emits first 2 chars + key length only — NEVER the full key** (tested via `expect(log).not.toContain(secret)` — credential-leak guard); test pattern uses vi.resetModules + dynamic re-import to re-exercise module-load env-capture per test | ✅ Closed | `1975c9f` |

| Batch 7eh | M14 follow-up — RC wrapper facade + JWT admin/user auth (29 / 2 files): `lib/resellerclub-wrapper` (15) — legacy ResellerClubWrapper static facade (pre-dates the typed `lib/integrations/resellerclub/` layer). Pins **every method delegates to ResellerClubAPI.X with args passed verbatim** (search/searchDomainWithTlds/getDomainDetails/getDomainOrderId/getDNSRecords/setDefault+CustomNameservers/activateDNSManagement/transferDomain/addDNSRecord/updateDNSRecord/deleteDNSRecord/getNameservers/deleteDomainOrder); **registerDomain re-shapes** the caller's `contacts={admin, tech, billing}` to the flat `{adminContactId, techContactId, billingContactId}` form that ResellerClubAPI.registerDomain expects; contacts omitted → all 3 IDs undefined (RC will use the customer-id defaults); **renewDomain orchestrates 2 lookups before the renew call**: getDomainOrderId → resolve order-id, getDomainExpiry → resolve Unix-second expiry, THEN renewDomain(orderId, years, expDate); **endtime field takes precedence over the 'expiry-date' alias** (RC has used both in different API revisions); falls back to 'expiry-date' when endtime missing; early-return error WITHOUT calling later steps when orderId lookup fails, expiry lookup fails, or expDate parses as 0; `lib/admin-auth` (14) — verifyAdminAuth + verifyUserAuth JWT gating. Pins token cookie missing → 'No authentication token'; **jwt.verify throws → GENERIC 'Invalid or expired authentication token'** (the actual JWT error message NEVER leaks to the client — defense vs token-enumeration / signature-format probing); decoded.userId missing → 'Invalid token format'; user not in DB → 'User not found'; user.isActive=false → 'Account is deactivated' on admin path; **USER path enriches with `message + supportEmail + isDeactivated:true`** (drives the client-side 'contact support' banner — admin doesn't get this because admins can't deactivate themselves); user.role !== 'admin' on admin path → 'Access denied. Admin privileges required.'; admin happy-path returns **SANITISED user shape** (id/email/firstName/lastName/role — NEVER password hash or token state) + fires `void logAdminAction(...)` audit entry (non-blocking — caller doesn't await); action stamped as `{METHOD}_{LAST_PATH_SEGMENT}` (DELETE /api/admin/users/del → DELETE_DEL); IP-resolution chain matches audit-log (XFF first-split > x-real-ip > req.ip > 'unknown'); SUPPORT_EMAIL env falls back to 'support@anutech.in'; **verifyUserAuth does NOT call logAdminAction** (only admin actions are audited) | ✅ Closed | `cf52074` |

| Batch 7ei | M14 follow-up — API security middleware + admin-security re-auth gate (42 / 2 files): `lib/api-security-middleware` (30) — route-level security primitives. Pins validateRequestSize 1MB default ceiling + custom max override + no-content-length is valid (streams ok); **sanitizeErrorMessage in PRODUCTION returns GENERIC 'An error occurred. Please try again later.'** (NEVER leaks Error.message — defense vs DB connection string / stack leakage); DEV returns .message verbatim; **string-input redaction of password/token/secret kvs to [REDACTED]** is case-insensitive; non-Error/non-string falls back to generic sentinel. createErrorResponse stamps {error, code, timestamp} + applies security headers; validateMethod enumerates allowed methods in the error message. **getRequestMetadata IP chain DIFFERS from audit-log** — request.ip wins FIRST (not last) — followed by XFF (first comma-split) > x-real-ip > 'unknown' (the API-security layer trusts the runtime's own IP detection more than headers; documented inconsistency with audit-log's XFF-first chain). isAllowedOrigin: null origin → true (same-origin); DB cors_protection_enabled gate, DB allowed_origins string OR array OR comma-list, wildcard `*.example.com` → regex coercion; DB query throws → falls back to ALLOWED_ORIGINS env list (logged + non-fatal). createCORSHeaders: not-allowed → empty {}; allowed → 4-header set with Allow-Credentials:true ONLY when Origin header present. handleOPTIONS returns 204 + CORS + security headers; `lib/admin-security` (12, exported helpers only — full verifyAdminSecurity pipeline is integration-tested elsewhere). Pins getClientIP audit-log-compatible chain (XFF first-split-trimmed > x-real-ip > request.ip > 'unknown'); **requireReAuth FAIL-CLOSED invariant** for high-stakes re-auth (sensitive admin operations): missing x-reauth-token → required:true/passed:false; user-lookup failure → fail-closed; user has no password field → fail-closed; bcrypt.compare(currentPw, user.password) returns true → passed:true only when the bcrypt result is literally true; **DB throw or bcrypt engine error → ALSO fail-closed** (verification errors MUST NOT grant access — this is the non-negotiable invariant) | ✅ Closed | `daa855d` |

| Batch 7ej | M14 follow-up — payment post-tasks + idempotency guard (23 / 2 files): `lib/services/payment/post-tasks` (14) — createZohoInvoice + runPostPaymentTasks. Pins **2-attempt outer retry with 1.5s spacer** (handles cold-start + short Zoho hiccups so the user's first payment usually lands without background self-heal); custom maxAttempts + retryDelayMs honoured; on final failure throws the LAST error (not the first — caller surfaces with the latest cause). **Claim-contention guard**: claimOrderForZohoInvoice null → returns `{invoiceId:"", invoiceNumber:null}` WITHOUT calling zohoService (another worker is already on it). **Failure releases the claim**: createInvoice throw → releaseZohoInvoiceClaim BEFORE rethrow so the next retry can re-claim; no-invoice-id sentinel → release + throw with the 'possible validation error' message (caller surfaces the GST hint). cartItems mapped through inferPeriodUnit before Zoho. runPostPaymentTasks fires admin notification + domain-booking email in PARALLEL with **errors caught internally** (caller's payment response NEVER blocked by transient email failures); hosting-only order → no domain-booking email; ADMIN_EMAIL falls back to 'sales@anutech.in'; `lib/services/payment/idempotency` (9) — handleAlreadyProcessedPayment. Pins null existingOrder + null payment-id-lookup → returns null (caller proceeds with normal verify flow). **F13 trust-DB-cart override**: when existingOrder.domains is non-empty, trusted DB rows REPLACE client-supplied cartItems (defense vs client-tampered cart on a re-fired verify). Zoho-recovery branch: claimOrderForZohoInvoice with **staleClaimAfterMs:5*60*1000** (allows a 5-minute-old stalled claim to be re-stolen); claim null → skip Zoho but still return 200; createInvoice success → recordZohoInvoiceForOrder; **createInvoice resolves with no invoice_id → releaseZohoInvoiceClaim (orphan-claim guard)**; **createInvoice throw → releaseClaim AND outer catch swallows** (Zoho failure NEVER blocks the already-processed response — user always sees their 200); existingOrder with zohoInvoiceId already set → fast-path skip (no claim/createInvoice call); hosting items get plan-name enrichment via getPlanByPlanId (best-effort, plan errors swallowed). Response includes orderId/invoiceNumber/registrationResults/successfulDomains projected from the trusted Order doc | ✅ Closed | `0cc0f9e` |

| Batch 7ek | M14 follow-up — Hosting CRUD service + Order Zoho-claim protocol (40 / 2 files): `lib/services/hostings` (24) — Hosting use-case helpers. Pins **getHostingById.populateUser projects only 6 user fields** (email/firstName/lastName/whatsappNumber/phone/phoneCc — cron batch sizes can't afford the full User doc); findUserHosting starts with {userId} + optional {domainName} narrowing; **userHasAnyHosting uses select('_id').lean() + returns boolean** (cheap existence check — no full doc haul); listHostingsForUser default limit 50 + createdAt:-1; **limit≤0 returns ALL** (dashboard sync needs everything); findUserHostingById scopes by BOTH _id AND userId (ownership pattern for `/api/user/hosting/[id]/*` routes); **listDueServiceHostingCandidates $or covers 3 processing_until forms** (null / not-exists / expired) AND status $nin failed/terminated + slim projection (`_id domainName`); **lockHostingForScheduler uses `new:false`** (returns PRE-update doc so caller can tell 'newly locked' vs 'extended'); **releaseHostingSchedulerLock GUARDED by lockExpiry** (concurrent worker can't release someone else's lock); upsertHostingFromDirectAdminStats uses $set + $setOnInsert + upsert:true; listExpiredActiveHostings filter status:active + expiryDate $lt cutoff + $ne null + slim projection; deleteHostingsByIdOrUsername filters by id OR username + returns {deletedCount, matchedHostings} for caller logging; **deletedCount falls back to 0** (older mongoose driver guard); `lib/services/orders` Zoho-claim + pending-claim (16) — focused subset. **claimOrderForZohoInvoice atomic protocol** uses findOneAndUpdate with $or of unclaimed forms (not-exists / empty-string); opts.allowNull adds {null}; opts.allowFailed adds the 'creation_failed' sentinel (retry cron); **staleClaimAfterMs adds the 'steal stalled claim' form** (pending_creation + updatedAt before cutoff — required for the 5-min recovery window); positive-only check (0 means no steal); sets zohoInvoiceId:'pending_creation' + new:true; lost race → null. **recordZohoInvoiceForOrder E11000 fallback**: duplicate-key collision on invoiceNumber → retry WITHOUT invoiceNumber (Zoho's own idempotency layer may attribute the same number to two local Orders; View/Download still works without the local number — preserves local unique index); non-11000 errors rethrow. **releaseZohoInvoiceClaim guarded by 'pending_creation'** + $unset (concurrent worker can't accidentally clear someone else's claim). markZohoInvoiceCreationFailed transitions pending_creation → 'creation_failed' sentinel. **claimPendingOrderForProcessing atomic pending→processing transition** guarded by status:'pending' (so /verify and /razorpay/webhook converge on the same row EXACTLY once — mutually exclusive even under concurrent calls); razorpayPaymentId/Signature/paymentVerification all stamped into $set when supplied | ✅ Closed | `e79580c` |

| Batch 7el | M14 follow-up — DomainVerificationService + auth email templates (24 / 2 files): `lib/domain-verification` (11) — pure helpers + verifyDomainRegistration. Pins **isPendingRegistration** is a literal predicate on registrationStatus==='pending' (NOT on isAvailable — the registrationStatus field is the source of truth across all callers); **getVerificationSummary buckets**: successful/pending/failed counts + pendingDomains list pushes pending names in input order; empty input → all zero. **verifyDomainRegistration domain-parse**: 'foo.example.com' → base='foo' + tld='example.com' for the RC search; case-insensitive exact match; **exact-match + available:true → 'pending' with the insufficient-balance heuristic reason** (RC accepted the order but wallet can't fund it, domain still resolves as available); **exact-match + available:false → ALSO 'pending'** (conservative — might be success OR queued); partial-match fallback → 'pending'; no match → 'pending' with 'manual verification' reason (NEVER 'failed' — avoids spurious refunds); **searchDomainWithTlds THROW is the ONLY path that produces 'failed'** (with the Verification failed: {message} reason — DB/integration writes can decide whether to retry); checkedAt stamped as Date on every result; `lib/email/auth` (13) — auth-flow email templates. Pins subject lines (drives the user's inbox preview — copy changes have UX-visible consequences); recipient = userEmail param (no silent CC/BCC); **sendPasswordResetEmail isSetup branch** flips subject 'Set up your account password' (vs 'Password Reset Request'), button label 'Set Password' (vs 'Reset Password'), AND **appends `&setup=1` to the URL** (the guest→full-account flow distinct from password recovery); sendPasswordChangeNotificationEmail isFirstTimeSet flips between 'Password Set Successfully' and 'Password Changed Successfully'. **sendActivationEmail fallback chain APP_URL > NEXTAUTH_URL > literal 'https://app.anutech.in'** (activation has a different fallback chain than other emails — supports marketing-site activations where APP_URL points elsewhere); token + userName both rendered into the HTML; sendEmail return propagates (true/false — callers decide whether to surface failure) | ✅ Closed | `0ef1936` |

| Batch 7em | M14 follow-up — billing + domain email templates (28 / 2 files): `lib/email/billing` (14) — **sendPurchaseOrderEmail 3-way subject branch**: paymentStatus:'success' + registrationFailed:true → "Purchase Order - PO123 (Registration Failed)"; paymentStatus:'success' + registrationFailed:false → bare "Purchase Order - PO123"; paymentStatus:'failed' → "(Payment Failed)" suffix; **sendOrderConfirmationEmail 4-way subject branch** based on the successful + pending domain mix: all-successful → "Order Confirmation"; pending-only OR pending+successful → "Payment Successful"; mix that includes 'failed' → fallback "Payment Received"; sendAdminNotification prepends **'[Admin] '** to subject (drives admin inbox filters); JSON-stringified data payload renders into HTML; **sendLowBalanceAlert threshold tiers**: balance < threshold*0.5 → '[CRITICAL]' prefix; else → '[Warning]'; balance amount in subject is .toFixed(2) format (₹XX.XX); optional reseller fields render into the body when present; `lib/email/domain` (14) — sendDomainPurchaseEmail/RegistrationEmail/RegistrationFailureEmail/BookingStatusEmail have fixed subject lines (drives inbox preview UX); **sendRenewalInvoiceEmail subject embeds domain name** ("Hosting Renewal Reminder - {domain}"); **sendServiceReminderEmail urgency-tiered subject**: daysRemaining<=1 → "🚨 URGENT: Your {type} {name} expires TODAY" (boundary edge at 1 is critical — the SAME treatment as today); else → "Renewal Reminder: {name} expires in {N} day{s}" with proper pluralization (1 day vs 2 days); sendServiceExpiryTodayEmail subject embeds serviceType + serviceName + TODAY; sendServiceSuspensionEmail "Account Suspended: {name}"; sendServiceGracePeriodEmail subject embeds en-IN-localized grace-end date; **sendDomainAvailableEmail subject 'Good news! {domain} is now available'** (the domain-watch cron's fire-and-notify email — different prefix to stand out in inbox); **note**: sendDomainAvailableEmail's signature is `(userEmail, domainName, userName?)` — userName is THIRD, not SECOND like the other emails — tests pin this against accidental positional-arg swap | ✅ Closed | `4019d20` |

| Batch 7en | M14 follow-up — NextAuth providers list + CredentialsProvider.authorize (21 / 1 file): `lib/auth-config/providers` — covers the providers array shape AND the heavy CredentialsProvider.authorize function (password-login path). Pins **optional providers gated on env**: Facebook + GitHub omitted from the list when BOTH client_id+secret unset (no crash, silent skip); when both set, they appear. Google + Credentials always present. **Google profile mapper** returns the 8-field shape (id/name/email/image/role:'user'/given_name/family_name/locale). **authorize generic-credential-error surface**: user-not-found AND password-mismatch both throw the SAME GENERIC 'Invalid email or password' (security — identical surface so attackers can't enumerate accounts via timing or text); AccountNotActivated + AccountDeactivated are DISTINCT typed errors (UX — user knows whether to check email or contact support). **Rate-limit keyed by `login:{email-lowercase}:{ip}`** (per-user AND per-IP attacks both caught); IP precedence XFF > x-real-ip > 'unknown'; **rate-limit exceeded throws 'TooManyRequests' BEFORE the DB lookup** (cheap rejection — no DB pressure under DDoS). **reCAPTCHA gate**: PRODUCTION + secret-key set + missing token → throws 'reCAPTCHA verification is required' BEFORE DB; verifyToken failure → throws 'reCAPTCHA verification failed'; **reCAPTCHA service NETWORK ERROR is SWALLOWED** (login proceeds when captcha service is down — defense vs Google-outage blocking all logins). **2FA flow**: totpEnabled=true + no totpCode → 'TotpRequired'; invalid TOTP + no matching backup hash → 'InvalidTotpCode'; **backup-code path** — TOTP secret doesn't verify BUT a backup hash matches → consumeUserBackupCode(matchedHash) + WARN log + login proceeds; matched hash list traversed in order; matched hash CONSUMED so subsequent attempts can't reuse; TOTP secret match short-circuits before backup-code traversal. Happy path returns sanitised user shape `{id, email, name: "First Last", role}` + updateLastActivity stamped | ✅ Closed | `b0e833c` |

| Batch 7eo | M14 follow-up — NextAuth callbacks: signIn + jwt + session (25 / 1 file): `lib/auth-config/callbacks` — the 3 lifecycle hooks NextAuth fires. Pins **signIn social-login gate hard rules**: admin role NEVER allowed via social (Google/Facebook/GitHub) → returns false (admins MUST use password — defense vs. compromised OAuth provider issuing a fresh token); inactive user → false; missing email from provider (GitHub private email) → false; DB lookup error → false (fail-CLOSED — never let a transient blip waive the admin gate). Credentials provider bypasses the social gate (auth was already validated in providers.authorize). **jwt token-refresh path** (no user/account but token.id present): user not found OR isActive=false → returns null (NextAuth v4 clears session); **sessionInvalidatedAt newer than token.iat → null** (the admin "force logout user" mechanism works — token.iat is in epoch seconds, compared via *1000 to ms); session timeout → null; happy-refresh stamps token.role + token.profileCompleted from DB + updates lastActivity. **Admin password rotation flag**: `token.passwordExpired = Date.now() - passwordChangedAt > PASSWORD_ROTATION_DAYS*24*60*60*1000` — non-admin users NEVER get the flag (only admin accounts are forced-rotated). DB error during refresh → returns the EXISTING token (NOT null — degrade gracefully so a transient blip doesn't kick everyone offline simultaneously). Credentials login path: stamps token.id/role/provider + loads profileCompleted from DB (the credentials authorize callback returns a minimal user so the rest is filled here). **session callback is the final DB gate**: re-validates isActive + sessionInvalidatedAt vs token.iat + timeout (defense-in-depth — jwt callback could have stamped a stale token); stamps token fields onto session.user (id/role/profileCompleted/provider/optional passwordExpired); role defaults to 'user' when token.role missing; DB throw during session check → STILL ALLOWS session (degrade gracefully) — symmetric with jwt-refresh behaviour | ✅ Closed | `ed0b965` |

| Batch 7ep | M14 follow-up — payment order-creator pure helpers (9 / 1 file): `lib/services/payment/order-creator` — covers `cartItemsFromOrderDomains` projection + `validateNoRestrictedDomains` gate (createCompletedOrder + finalizePendingOrder are heavy transaction code better exercised in integration tests). Pins cartItemsFromOrderDomains projects Order.domains → CartItem shape; **hostingPlan reshape**: `{planId, name, serverPackage}` (DB form) → `{id, name, serverPackage}` (cart form) — the downstream Zoho/email code reads `.id`; **isTrial defaults to false via STRICT `=== true` check** so undefined / false / truthy-non-bool ("yes" string) all yield false (no accidental coercion); hostingPlan null → undefined in result (no spurious empty object). validateNoRestrictedDomains: empty cart → ok:true; unrestricted TLDs (.com/.net/.io) → ok:true. **BONUS CATCH** (test documents a known limitation as a guard against regression): the gate's TLD lookup `requiresAdditionalDetails(item.domainName)` is keyed against the FULL domain string (e.g. "example.au"), but `DOMAIN_REQUIREMENTS` is keyed by bare TLD (".au"); the function call therefore never matches a real-world cart item. The gate is effectively a no-op as written — restricted-TLD orders pass through unblocked at this layer and rely on registrar-side rejection as the actual backstop. A bare-TLD input like `".au"` DOES trigger the gate (proves the underlying check works), so the fix is to parse the TLD first at the call site. Test pins both the no-op behaviour AND the works-with-bare-TLD case so any future fix surfaces here | ✅ Closed | `0a32e38` |
| Batch 7eq | M14 follow-up — RC customers + RC search/pricing wire helpers (36 / 2 files): `lib/resellerclub/customers` (17) — getCustomerId/createCustomer/createContact/modifyContact/getCustomerDetails/getCustomerDomains. Pins **getCustomerId 3-way state surface**: 'success' (customer found, parsed numeric customerId), 'error' (RC returns body but no customerid), 'not_found' (RC throws — the expected "customer doesn't exist" path treated as a non-error signal); parseInt converts RC's string customerid. createCustomer POSTs `/api/customers/signup.json` with full 13-param body in **kebab-case** (`address-line-1`, `phone-cc`, `lang-pref`) + `company` defaults to `name` + `reseller-id` from `RESELLERCLUB_RESELLER_ID` (falls back to `RESELLERCLUB_ID`); AxiosError extracts data.message → falls back to error.message → plain Error returns 'Unknown error occurred' sentinel. createContact uses the `type:'Contact'/'CaDomain'/'IrtContact'` enum (Canadian + Irish ccTLDs have special contact types); same kebab-case shape. **modifyContact builds params CONDITIONALLY** — only sets each kebab-case key when the input field is supplied (partial update doesn't overwrite untouched RC fields). getCustomerDetails/getCustomerDomains wrap RC into `{status:'success', data}` / `{status:'error', message}` sentinels. getCustomerDomains pagination: `no-of-records:500` + `page-no:1`; `lib/resellerclub/search` (19) — getDomainPricing + getTLDPricing + getResellerPricingForTLD + getResellerDetails. **getDomainPricing PARALLEL fetch** — customer + reseller pricing via Promise.all (single round-trip latency, not serial); returns `timestamp:ISO string` for cache invalidation; fetch throw wraps into generic 'Failed to fetch live domain pricing' (underlying HTTP error swallowed). **getTLDPricing TLD-variation lookup**: 9 fallback variants tried in order — tldMappings entry FIRST (highest priority — `com → dotcom`), then original/with-dot/upper/lower/dotXXX/domXXX/centralnic*; leading '.' stripped from input; TLDs with no matching variation omitted from result; reseller side `null` when mapping miss. **getResellerPricingForTLD parses addnewdomain[1] as parseFloat** + returns `{price, currency:'INR'}`; missing TLD/key → null; API throw → null (never crashes callers). **getResellerDetails unwraps nested `{status, data}` envelope** when RC returns the wrapped shape; flat response.data used directly otherwise; **AxiosError error-message chain**: `data.message > data.error > 'API Error: {status} {statusText}'`; AxiosError WITHOUT response (network error) → 'No response from ResellerClub API'; plain Error → error.message | ✅ Closed | `3001d1b` |

| Batch 7er | M14 follow-up — RC registration lifecycle (26 / 1 file): `lib/resellerclub/registration` — registerDomain + deleteDomainOrder + getDomainDetails/Expiry/OrderId. Pins **registerDomain defaults to the 4 hardcoded RC nameservers** (deepak1299294.{mercury,venus,earth,mars}.orderbox-dns.com) when caller doesn't supply ns; caller-supplied list wins. Wire shape uses URLSearchParams: domain-name/years (stringified)/customer-id/3 contact-id fields + **`reg-contact-id` MIRRORS `admin-contact-id`** (RC requires registrant=admin); `invoice-option:'NoInvoice'`; **nameservers appended as repeated `ns=...` params** (not a CSV). TLD-policy params (T&C accept + ccTLD attributes like .us Nexus) appended via getRegistrationParamPairs — fan-out into the wire so per-TLD compliance fields land correctly. **RC error mapping**: response.data.status:'error' OR data.error → wrapped; **balance/processing-lock/already-in-progress fragments → status:'pending'** (queue for retry, not surface as hard error); RC's `InvoicePaid` sentinel (webhook reconciliation pending) → also 'pending'; mapRegistrationError friendly message preferred over raw RC text when present (registry-policy errors get a clearer caller-facing string). **AxiosError HTTP-status mapping** (8 distinct codes): 401→auth, 403→forbidden, 400→invalid, 409→conflict, 429→rate-limit, 5xx→server, ECONNABORTED→timeout, ENOTFOUND/ECONNREFUSED→connection failed; unrecognised → generic 'Failed to register domain'. **deleteDomainOrder**: POSTs `/api/domains/delete.json` with `order-id`; **response.data.status === 'error' (case-insensitive)** → wrapped as error sentinel. getDomainDetails + getDomainExpiry both hit the SAME `/api/domains/details.json` endpoint (distinct caller aliases for clarity). getDomainOrderId hits the separate `/api/domains/orderid.json`. Each wire helper has its own error-message sentinel ('Failed to get domain details' / 'expiry' / 'fetch domain order ID') | ✅ Closed | `fd41daa` |

| Batch 7es | M14 follow-up — RC DNS records + nameservers (30 / 1 file): `lib/resellerclub/dns` — activateDNSManagement + getDNSRecords + 7-record-type addDNSRecord + updateDNSRecord + deleteDNSRecord + setDefaultNameservers + setCustomNameservers + getNameservers. Pins **getDNSRecords queries 7 record types SERIALLY** (A/AAAA/CNAME/MX/NS/TXT/SRV — one HTTP per type), individual-type failures SWALLOWED so a domain with partial records still returns them. **numbered-key collection**: RC returns `{1:{...}, 2:{...}, recsonpage, recsindb}` — the two count keys filtered OUT; **id-fallback chain**: recordid > recordId > 'record-id' > numbered key string; **field normalisation**: timetolive → ttl, host → name. **addDNSRecord 7-way endpoint switch**: each record type has its own RC endpoint (add-ipv4/add-ipv6/add-cname/add-mx/add-ns/add-txt/add-srv); **MX gets default priority=10**; **SRV gets priority=10 + weight=10 + port=443 defaults** (RC requires all three; we hardcode sensible defaults). **TTL FLOORED to 7200** (RC minimum — ttl=60 silently rewritten to 7200 not rejected). **host normalisation**: `@` → domainName, else passes through (CNAME/A records can use @ for the root). Unsupported type → error sentinel WITHOUT making a POST (don't waste the round-trip). AxiosError extracts `response.data.msg` field for caller. updateDNSRecord same `@` normalisation, priority passes through (even undefined — RC applies its own default). deleteDNSRecord echoes back host/value/type as RC requires for verification. **setDefaultNameservers DELEGATES to setCustomNameservers** with the 4 hardcoded deepak1299294.* (same as registration default). **setCustomNameservers paramsSerializer** is a custom URLSearchParams builder that fans the `ns:[...]` array into repeated `ns=...` wire params (axios default uses CSV). **getNameservers 2-step lookup**: getDomainOrderId → details.json with `options:'NsDetails'` (required to populate ns fields); collects ns1..ns13 dynamically with gap support; returns [] on any failure (never crashes callers) | ✅ Closed | `2632ecd` |

| Batch 7et | M14 follow-up — DA hosting-package CRUD (18 / 1 file): `lib/directadmin/packages` — listPackages/getPackageDetails/createPackage. Pins listPackages GETs CMD_API_PACKAGES_USER with basic auth + 8s timeout; **handles 3 response shapes** for the package list (`list[]` / `list` / `packages` — DA's wire returns whichever based on version + endpoint); HTML-login-page guard (response.data starts with `<` → throws, don't parse a login page as data); **DA in-band error** (`error=1` in URL-encoded string OR `{error:'1'}` object) → typed DirectAdminError; **Array.isArray normalisation** wraps single-string into [string]; falsy entries filtered out. getPackageDetails normalises package name via normalizePackageName + uses 'GetPackageDetails' context for the typed-error. **createPackage builds payload with sensible defaults**: quota:1000, bandwidth:10000, mysql:5, domainptr:5, ftp:5 + cgi/php/spam ON; options spread AFTER defaults so caller can override any. **validatePackageName runs SYNC before executeRequest** — the throw bypasses the `.catch()` wrapper, caller sees the raw 'Invalid package name' message (not wrapped in 'Failed to create hosting package'). DA in-band error during createPackage → DirectAdminError preserved (not wrapped); non-DA error → wrapped in `Failed to create hosting package: {message}`. Tests use URL-encoded string inputs (the actual DA wire format — source's `data.startsWith('error=1')` check requires strings) | ✅ Closed | `c6e78d5` |

| Batch 7eu | M14 follow-up — DA DNS records CRUD + disabled NS-update stub (17 / 1 file): `lib/directadmin/dns` — getDNSRecords/deleteDNSRecords/addDNSRecord + the intentionally-disabled updateDNSNameservers. Pins getDNSRecords validates username FIRST + auth uses `${ADMIN_USER}|{username}` form (DA's per-user proxy). **2 response-shape parsers**: numbered-key (`name0/value0/type0/ttl0/key0`, scanning until `nameN` is undefined) AND a **BIND zone-file fallback** for newer DA versions where action=view returns raw zone files; BIND detection looks for `$TTL` or `IN<TAB>NS` or `IN NS`; comment + directive lines ($ or ;) skipped; regex `Name [TTL] IN Type Value` parses to record shape. **key construction**: numbered-key parser uses `data[keyN]` when present, else **synthesises `name={name}&value={value}`** (required for the deletion API which takes select-value form). deleteDNSRecords **short-circuits on empty array/null/undefined** records (no HTTP call); POST payload: `action=select` + `selectN={key OR synthesised}` per record; Content-Type x-www-form-urlencoded. addDNSRecord defaults `ttl=14400`; empty name defaults to domain root; **NS-record-at-root special case** — empty name → `${domain}.` (trailing dot required by DA's zone-file parser for root-level NS records); explicit name skips the trailing-dot fix. All 3 fns throw DirectAdminError on `error=1` response with the right context. **updateDNSNameservers always throws** — intentionally disabled because DNS authority is decided at purchase time, not synced after | ✅ Closed | `7afa408` |

| Batch 7ev | M14 follow-up — DA user lifecycle (34 / 1 file): `lib/directadmin/users` — all 12 exported functions covered. Pins **getOneTimeLoginUrl auth-form switching**: admin user uses plain getAuth(); non-admin uses `${ADMIN_USER}|{username}` Login-As form (DA generates session in TARGET user's context, not key-owner's); SSO payload includes **5 select_deny entries** to block password-change / login-keys / 2FA / change-info from the SSO session, `max_uses:1` + `clear_key:yes` (SSO link cannot be re-used) + 1-hour epoch-seconds expiry_timestamp; 2 response shapes accepted (raw URL starting with `http` OR `{result: 'http...'}` JSON envelope from newer DA); unexpected shape → wrapped 'Failed to generate DirectAdmin SSO link'. createUser generates random temp password (passwd === passwd2), default `ip = DA_SERVER_IP` (override possible); validates username + normalises/validates package first; **friendly error rewrite**: NON-DirectAdminError containing "already exists" → 'User or domain already exists on the server.' (typed DA errors preserved as-is, friendly rewrite only on the .catch path). getUserConfig/getUserUsage hit distinct DA endpoints (CMD_API_SHOW_USER_CONFIG / SHOW_USER_USAGE) with user param + admin auth. **getUserDomains 3-way response handling**: list[] / list arrays passed through; **keys-as-domain-names fallback** (DA sometimes returns `{x.com: "stats", y.com: "stats"}` — filters out error/text/details meta-keys + requires `.` in key as heuristic). suspendUser / unsuspendUser / deleteUser all POST CMD_API_SELECT_USERS with their distinct action params (`dosuspend:Suspend` + reason / `dounsuspend:Unsuspend` / `delete:Delete + confirmed:Confirm` for DA's 2-step deletion). suspendUser default reason = 'Admin Action'. **domainExists FAIL-SAFE defaults to false** on empty/error/HTTP-throw (lets purchase proceed — DA rejects at create-user time if domain truly exists; safer than blocking a valid order). listUsers + getAllUserUsage return parsed URL-encoded responses; listUsers filters falsy entries | ✅ Closed | `6b70b27` |

| Batch 7ew | M14 follow-up — order-service readers + admin pagination + pure helpers (39 / 1 file): `lib/services/orders` reader subset —  + 3 pure helpers (findOrderDomain/mapOrderDomains/filterOrderDomainsByName) + 3 domain-keyed lookups. Pins **getOrderByIdOrOrderId branches on 24-hex regex** (NOT $or) — explicitly documented footgun avoidance: a hex string that happens to equal an orderId can't match the wrong row. findUserOrder uses ObjectId.isValid branch + ALWAYS scopes by userId + filters isDeleted (privacy-safe — returns null for other-user rows so route returns 404, indistinguishable from missing). **findInvoiceNumberConflicts aggregation pipeline pinned**: $match-exists → $group-by-invoiceNumber → $match count>1 → $sort desc → $limit 100 (E11000-conflict diagnostic). listOrdersByIds + listOrdersByRazorpayPaymentIds both short-circuit on empty array (no Mongo round trip). **listStuckZohoInvoiceOrdersAdmin $or covers 5 unclaimed forms** (not-exists / null / empty / creation_failed / pending_creation). listOrdersWithInFlightDomains 1000-row hard cap + populates owner with 5-field projection. clearOrderInvoiceNumber $unset for E11000 recovery. findOrderByRazorpayOrderIdOrInternalId: builds $or dynamically — both supplied → 2 entries, one supplied → 1 entry, NONE supplied → returns null WITHOUT Mongo call. **listOrdersForAdmin pagination**: page/perPage floored to ≥1 (no zero or negative); skip=(page-1)*perPage; archived flag flips isDeleted filter; includePending default false excludes 'pending' (checkout intents that haven't been paid). **HARD-DELETED-USER fallback**: when populated User is null, synthesises userId stub from on-order userName/userEmail snapshot (firstName/lastName split on first space, email from snapshot, _id:null, isDeleted:true) — keeps admin views renderable when user deleted. hasMore = skip + orders.length < total. Pure helpers: findOrderDomain ===-match by domainName; mapOrderDomains type-pre-annotated; filterOrderDomainsByName uses Set-membership. **findOrderByDomainForUser ALWAYS scopes userId+isDeleted** (privacy-safe); findOrderByDomain admin variant has NO userId but still filters isDeleted; **findOrdersByDomainName INCLUDES soft-deleted rows** (refund/audit views need them) | ✅ Closed | `2f45117` |

| Batch 7ex | M14 follow-up — order-service writers + renewal builder + remaining readers (27 / 1 file): `lib/services/orders` writer subset — . Pins listOrdersForUser default limit 50 + populateUser true + excludes pending+soft-deleted; limit≤0 returns all (no .limit() chain). softDeleteOrder sets isDeleted:true + Date(), permanentlyDelete uses findByIdAndDelete, unarchive $unset deletedAt. **createOrderInSession uses new Order(payload).save({session})** so insert participates in caller's withTransaction. **createRenewalOrder** key invariants: paise → rupees (/100); orderId = `ORD-RNW-{Date.now()}-{8-hex-chars}` via crypto.randomBytes(4) — M3-style fix vs prior Math.random()*1000 burst-collision pattern; payment.order_id falls back to subscriptionId when null (the webhook payload doesn't always carry order_id for subscription renewals); isMonthly drives registrationPeriod (1/12) + periodUnit (months/years); **razorpaySignature pinned to literal 'webhook_verified'** (audit-trail marker, not a real signature — the webhook handler already verified before calling); status:'completed' + orderType:'renewal' + successfulDomains pre-populated. **forceMarkZohoCreationFailed unconditional $set** (no pending_creation guard — used from catch-block where prior state is indeterminate; differs from markZohoInvoiceCreationFailed which is guarded). **listStuckZohoInvoiceOrders user-scoped variant intentionally has 4 $or forms** (not 5 like admin) — EXCLUDES 'pending_creation' because in-flight claim isn't "stuck" yet for user's own retry UI. listAllOrdersForAdminDomains excludes pending + populates 5-field owner. userHasPriorTrialOrder uses Order.exists (cheap existence) + boolean coercion. **findPriorHostingOrderForUser $or covers userEmail OR userId** (migration-window safety where order may pre-date user-account creation). listRecentCompletedOrdersForUser default 14 days + sort ASC (oldest first — caller de-dupes by name, newer entries overwrite). listUserInvoiceOrders: invoiceNumber $exists $ne null filter + slim projection (`invoiceNumber zohoInvoiceId amount currency status createdAt`) | ✅ Closed | `aeb92c2` |

| Batch 7ey | M14 follow-up — ResellerClubDNSSpecific 23-method class (47 / 1 file): `lib/resellerclub-dns-specific` — the parallel DNS API surface that splits each record-type into its own endpoint (RC's KB-1091 layout). 1 search + 7 add (IPv4/IPv6/CNAME/MX/NS/TXT/SRV) + 7 modify + 1 SOA-modify + 7 delete = 23 endpoints. Pins **searchDNSRecords distinct error message**: 404 → literal 'Request failed with status code 404' (legacy caller-grep contract — code elsewhere checks for this exact string before treating as "domain not under management"), non-404 → 'Failed to search DNS records'. **All 7 add methods** POST to `/api/dns/manage/add-{TYPE}-record.json` with `domain-name + host + value + ttl`; MX adds `priority`; SRV adds `priority + weight + port`. **All 7 modify methods** POST `/api/dns/manage/modify-{TYPE}-record.json` with `record-id` + the same per-type params. **All 7 delete methods** POST `/api/dns/manage/delete-{TYPE}-record.json` with ONLY `domain-name + record-id` (RC looks up the record by id; no need to echo value/ttl). **modifySOARecord is the 8-field special case** — no record-id (SOA is singleton per zone) + 8 params: ttl/primary_ns/resp_person/serial/refresh/retry/expire/minimum. Each method wraps any thrown error into its own typed sentinel message (`Failed to {action} {TYPE} record`) so callers can route on the exact string. Tests use `it.each` parametrised over the 21 add/modify/delete cases — each variant verified for endpoint + params + error sentinel | ✅ Closed | `0a71c05` |

| Batch 7ez | M14 follow-up — payment price-verifier (20 / 1 file): `lib/services/payment/price-verifier` — the attacker-bypass guard between client-displayed cart and Razorpay charge. Pins **hosting items SKIPPED** (hosting prices come from local HostingPlan, not RC); hosting-only cart → ok:true + serverTotal:0 + no RC fetch. **RC outage fallback**: live fetch throw OR malformed customerPricing → ok:true with `fellBackToClient:true` flag — refusing payment on transient RC outage would kill every checkout, accept the cached-validated client price instead. **UNVERIFIABLE-TLD REJECTION (the attacker-bypass guard)**: RC has data but no price for this specific TLD → ok:false with 'couldn't verify the live price' error (NEVER fall through to client price — conservative deny is the security default). **Rounding tolerance window** = max(₹1 absolute, 0.5% relative); neither arm fails a payment for noise. Per-domain mismatch over tolerance → mismatchedDomains list + typed error 'Prices have updated. Please refresh your cart — {domains} changed.' (first 3 domains + ellipsis). Total-level mismatch (accumulated rounding within per-domain tolerances) ALSO fails — message format `total ₹X vs ₹Y`. **domainToRcKey TLD resolution**: tldMappings entry FIRST (e.g. `com → domcno` — the legacy RC key shape kept in mappings); multi-level TLDs like `.co.in` get tldMappings hit; multi-level fallback `co.{ext}` → `codot{ext}` heuristic; new gTLDs not in mappings → `dot{tld}` heuristic (RC's modern convention). **extractCustomerPrice year fallback**: requested registrationPeriod year takes precedence; falls back to ANY non-zero year bucket for min-period TLDs like .ai (only some buckets populated); legacy `data.price` shape supported as final fallback | ✅ Closed | `bbe2385` |

| Batch 7fa | M14 follow-up — provisioner-verification phase (13 / 1 file): `lib/services/payment/provisioner-verification` — runDomainVerificationPhase. Post-loop verification + PendingDomain materialization. Pins **hosting items SKIPPED from verifyMultipleDomains** + only non-hosting items become PendingDomain rows; hosting-only cart → NO verify call. **Pending/failed non-hosting → PendingDomain bulk-write** with status:'pending'|'failed' + reason (defaults differ per status; orderDomain.error preferred when present) + verificationAttempts:0. **SILENT-FAILURE detection**: 'registered' rows the verifier flags as still-available → orderDomain.status FLIPPED to 'pending' IN-PLACE + Domain.deleteOne cleanup of the optimistic insert + PendingDomain queued with verificationAttempts:1; isPendingRegistration is the typed predicate that determines this branch (RC said success but registry still shows available → likely insufficient-funds at the registry). **Cleanup failure SWALLOWED** (Domain.deleteOne throws → logged + continue; never abort the verify phase on a cleanup blip). **Bulk upsert filter = (domainName, userId)** — NOT domainName alone (anti-regression: the previous bug let one user's failure overwrite another user's PendingDomain row, including the userId field, silently reassigning ownership in the admin dashboard). **PendingDomain.bulkWrite failure swallowed + logged** (never blocks payment-verify response). Empty queue → early return (no bulkWrite call). PendingDomain payload uses **ctx.customerResult.contactId for ALL 3 contact roles** (admin/tech/billing — single-contact convention for retries); user._id stringified for the userId field | ✅ Closed | `8c103fa` |

| Batch 7fb | M14 follow-up — per-item domain provisioner (15 / 1 file): `lib/services/payment/provisioner-domain` — provisionDomainItem switches on the M1-typed `RegisterDomainOutcome`. Pins **EXISTING-DOMAIN SHORT-CIRCUIT**: when a non-deleted Domain row already exists for the name → SKIPS RC call entirely, returns `kind:'already_registered'` + status:'registered' (defence vs re-registering a soft-deleted-then-repurchased domain); findOne filter is `{domainName, deletedAt: null}` — soft-deleted rows DON'T count as active. rcRegisterDomain shape: `contacts: {admin/tech/billing all = customerResult.contactId}` (single-contact convention); `nameServers: undefined` (RC defaults per current policy — caller's nameServers field ignored at this layer); tldAttributes pass-through. **'registered' outcome** → status:'pending' (locally — verify-phase confirms before flipping to registered) + writes Domain row with `next_action_at = expiresAt - FIRST_REMINDER_DAYS×24h` where FIRST_REMINDER_DAYS = max(AUTOMATION_CONFIG.REMINDER_DAYS) (renewal-reminder cron pre-stamp); **`registered` without orderId in response → calls `fetchOrderIdFallback`** (best-effort RC lookup); `registered_no_order_id` branch ALWAYS calls fallback; fetchOrderIdFallback throw SWALLOWED. **Domain.create failure SWALLOWED** (local-DB blip never fails the payment — registrar already accepted, the row can be reconciled later). **'balance_pending'** → status:'pending' + 'insufficient balance' user message + RC orderId fallback (the self-heal cron will flip to registered when ops tops up). **'already_in_progress'** → status:'pending' + 'being processed' message (prior duplicate order will complete via cron). **'hard_failure' → status:'failed'** with GENERIC user message — internal reason DISCARDED so registry-specific details (TLD policy codes, eligibility errors) don't leak to UI. hard-failure attaches `step:'domain_failed', progress:100` to bookingStatus. **Outer try/catch is last-line defence**: any unmapped exception (e.g. rcRegisterDomain itself throws — should never happen since integration layer catches, but future-regression guard) → synthesised hard_failure outcome | ✅ Closed | `98e1505` |

| Batch 7fc | M14 follow-up — per-item hosting provisioner (29 / 1 file): `lib/services/payment/provisioner-hosting` — provisionHostingItem covers DA-user creation + Hosting record + PendingHosting fallback. Pins **3-attempt username candidate pre-generation**: crypto.randomBytes(4) 5-hex suffix on a sanitised lowercased-alnum prefix from the domain (truncated to 5 chars); list handed verbatim to daCreateUser (which tries them in order, retrying on collisions). Empty prefix → 'user' fallback. **daCreateUser outcome dispatch**: 'created' → captures username + setUserDirectAdminUsername + email + Hosting row; 'username_collision_exhausted' → throws (rare; 3× username collisions against existing DA accounts); **'da_unreachable' → throws with `__daUnreachable:true` + status:503 attached** (signals handler to route to PendingHosting status:'pending' for cron retry); 'hard_failure' → plain throw routed to status:'failed'. **resolveDaPackageName 4-step fallback chain**: explicit hostingPlan.serverPackage → name-based inference (starter/standard/plus substring match — 'standard' wins over 'plus' in "Standard Plus" via priority order) → price-based via PRICE_TO_PACKAGE (derived from HOSTING_PLANS at module-load — no duplicated magic numbers) → DA_DEFAULT_PACKAGE env → literal 'Starter'. **Trial-item invariant**: isTrial:true → ALWAYS 15-day expiry regardless of caller's registrationPeriod/periodUnit (defence vs misconfigured trial item being treated as paid); message mentions '15 days free'. **periodUnit normalisation**: 'years' OR undefined → 'months' (hosting plans store monthly cycles); 'days'/'minutes' pass through. **Hosting record next_action_at = expiresAt - FIRST_REMINDER_DAYS×24h** (renewal-reminder cron pre-stamp); **autoRenew + billingType keyed on razorpay_subscription_id**: present → autoRenew:true + billingType:'subscription'; absent → false + 'manual'. setUserDirectAdminUsername mutation BEFORE Hosting.create so subsequent items can reuse the existing DA user. **sendHostingProvisionedEmail / createHosting / getPlanByPlanId failures ALL SWALLOWED** (DA is the source of truth — local rows + emails reconcile later, never fail the payment). **handleHostingProvisionError routing**: DA-unreachable → status:'pending' + 'queued ... temporarily unavailable' user msg + PendingHosting status:'pending' + bookingStep 'hosting_deferred' progress:50; everything else → status:'failed' + generic 'team has been notified' msg + PendingHosting status:'failed' + bookingStep 'domain_failed' progress:100. **Raw error.message stays in PendingHosting + serverLogger** for postmortem but NEVER reaches the user (registry-state fragments would leak account paths/retry tokens). createPendingHosting failure SWALLOWED — orderDomain.error still surfaces to caller | ✅ Closed | `997a7e7` |

| Batch 7fd | M14 follow-up — payment-verify top-level error handler (16 / 1 file): `lib/services/payment/verification-error` — handleVerificationError. Pins **cart-composition message branching**: hosting-only → 'Hosting provisioning is being processed'; domain-only (or untyped) → 'Domain registration is being processed'; bundle (both) → 'Service registration and provisioning is being processed' — UX matches what was actually charged. **Happy path (existingOrder in scope)**: Order.updateOne in place — preserves M4 post-provisioning state + real Razorpay tracking + doesn't duplicate the row; status:'processing' for admin / self-heal worker pickup; `paymentVerification.paymentStatus = 'captured_pending_support'` (filterable by support); razorpay_payment_id omitted → razorpayPaymentId NOT in $set (don't overwrite to undefined). Response stamps requiresSupport:true + paymentStatus:'success' + per-item pending registrationResults entries + pendingDomains list. **Fallback path (no existingOrder)**: synthetic Order with `ord_{ts}_{base36}` orderId; status:'processing' (NOT 'completed' — nothing was registered); preserves real Razorpay ids when available, else literal 'fallback_order' / 'fallback_payment' / 'fallback_signature' sentinels (still parseable, never confused with real prefixed razorpay ids); each domain row gets bookingStatus[0]=payment_verified progress:30; hosting periodUnit defaults to 'months', domain defaults to 'years'. **Last-resort paths**: user missing → returns 500 with SUPPORT_EMAIL embedded + errorType:'verification_error' (original error.message NEVER leaks to user); DB write throw during update or fallback save → ALSO returns last-resort 500 | ✅ Closed | `c363c9a` |

| Batch 7fe | M14 follow-up — hosting-plan upgrade handler (12 / 1 file): `lib/services/payment/upgrade` — handleUpgradePayment invoked from /verify when razorpay_order_id starts with 'upg_'. Pins **scoped order lookup**: getOrderByRazorpayOrderId is called with `orderType:'hosting_upgrade'` (prevents the regular Order verify path from accidentally being routed here). **Idempotency**: order.status !== 'pending' → returns success WITHOUT re-running DA flow + 'already processed' message + NO daChangePackage call. Hosting/plan not found → order.status:'failed' + saved + 404 (no payment captured yet for these branches). **Pre-DA-change subscription cancel**: hosting.subscriptionId present → `RazorpayService.cancelSubscription` + hosting.subscriptionId cleared + billingType→'manual' + autoRenew→false (upgrade switches billing model from subscription to manual). **Subscription cancel throw is NON-FATAL** — logged + proceeds with DA change (the upgrade itself should succeed even if Razorpay cancel fails; hosting still gets the manual flag locally). Payment fields (razorpayPaymentId/Signature) stamped on order BEFORE the DA call so audit trail is preserved even on DA failure. **daChangePackage outcome dispatch**: 'changed' → order.status:'completed' + domain.status:'registered' + hosting.planId/.name/.serverPackage updated; **non-'changed' (user_not_found/package_not_found/hard_failure/da_unreachable) → order.status:'paid' (NOT failed — payment was captured) + domain.status:'failed' + domain.error stamped with `kind + reason` internally + 500 response with GENERIC user-facing 'Payment was captured but the plan change on the server failed' message** (typed DA detail (`pawan42 does not exist` etc.) NEVER leaks to user) | ✅ Closed | `da5c60f` |
| Batch 7ff | M14 follow-up — Razorpay payment-verification pipeline (14 / 1 file): `lib/services/payment/verification` — verifyRazorpayPayment 4-step pipeline + F3 amount-mismatch fraud guard. Pins **signature check FIRST** (cheap local HMAC — no network call wasted on bad signatures); invalid → 400 'Invalid payment signature' WITHOUT fetching paymentDetails. **Status whitelist**: only 'captured' OR 'authorized' pass; anything else (failed/refunded/created) → 400 with the actual status named. **Order ID cross-check** (defence vs replay-from-different-order): when razorpay_order_id supplied, paymentDetails.order_id must match exactly. **Subscription ID cross-check** (same defence for subscription flow); when subscription_id supplied but paymentDetails LACKS one → WARN + proceed (subscription metadata sometimes arrives out-of-band on Razorpay's side, signature is the real authority); both present and DIFFERENT → 400. **F3 amount-mismatch guard** `validateOrderAmountMatchesRazorpay`: DB amount × 100 must === Razorpay order amount (paise — Razorpay's wire format); mismatch → 400 'Payment amount does not match order' (defence vs underpayment fraud where a client signs a real payment for less than the cart total). **Math.round() applied to DB amount** (float-precision drift defence — 500.001×100 → 50000); Razorpay amount string-coerced via Number() | ✅ Closed | `7fe97db` |

| Batch 7fg | M14 follow-up — Razorpay renewal-payment handler (20 / 1 file): `lib/services/payment/renewal` — handleRenewalPayment invoked from /verify when payment looks like a renewal. Pins **isRenewal detection (3 signals)**: razorpay_order_id starts with `ord_renew_` OR `rnw_` OR `paymentDetails.notes.type === 'invoice_payment'`; none → returns null (fall through to normal new-order flow). Order lookup: Razorpay-id first, internal orderId fallback (verify route may pass either). **Zoho invoice creation branch** (no existing invoice_id in notes): builds invoiceItems from renewalOrder.domains with periodUnit defaults (hosting→'months', domain→'years'); uses recordZohoInvoiceForOrder service helper (NOT direct order.save) so the E11000 invoice-number collision fallback path is the SAME helper used by /api/payments/verify; Zoho throw SWALLOWED. **Existing invoiceId branch**: applyPaymentToInvoice with amount paise→rupees via Math.round/100; createInvoice NOT called. **Hosting reactivation pre-fetches ALL user hostings ONCE** + maps by domainName (O(N+M), not N round-trips per item). daUnsuspendUser only called when status:'expired'|'suspended' AND directAdminUsername present (typed outcome, internal logs differentiate — callsite proceeds with DB update regardless: payment IS captured, DB is the source of truth). **Expiry-extension base**: future expiryDate → extend from THAT (stack, not reset to now); past → extend from now. **periodUnit dispatch**: minutes/days/years use the matching setX; default 'months' uses setUTCMonth (UTC-stable across DST). Stamps renewalInvoiceId + renewalStatus:'paid' via cast (typed shape doesn't include these — ops dashboards read them); clears last_reminder_sent; next_action_at = newExpiry - 15d. **Broad fallback**: no specific order → walks user hostings + reactivates suspended/expired ones with 1-month extension (safety net for legacy renewal flows). Reactivation throw + order.save throw both SWALLOWED — final response is ALWAYS 200 `{success:true, message:'services reactivated', orderId}` once renewal is detected (renewal flow never returns an error to the client — payment IS captured by the time we get here) | ✅ Closed | `ad30bdb` |

| Batch 7fh | M14 follow-up — cart-provisioning orchestrator (16 / 1 file): `lib/services/payment/provisioner` — provisionCartItems thin dispatcher after H2 decomposition (per-item branches in provisioner-{hosting,domain,verification}). Pins **setupRcCustomerAndContact called first** with user fields + address mapped to RC shape (address missing → undefined arg accepted); RC failure (status:'error' OR missing IDs) → THROWS 'Failed to get ResellerClub customer/contact IDs' (provisioning aborts — no point per-item work without the customer). **RC IDs persisted via setUserResellerClubIds**: conditional write (only when user lacks the field — `user.x ? undefined : customerResult.x` avoids overwriting existing IDs); **in-memory user mirrored** (`user.resellerClubCustomerId = customerResult.customerId`) so callers later in the request don't refetch; setUserResellerClubIds throw SWALLOWED (best-effort persistence). **Per-item fan-out via Promise.all** (NOT for-loop) — 5-item cart pays 1× RC+DA latency, not 5×; `cartItems.map` preserves output order. **Dispatch**: isHostingItem → provisionHostingItem (passes razorpay_subscription_id + customerResult); non-hosting → provisionDomainItem (with cartItems for the "is paired with hosting?" log line). **Placeholder hosting-domain short-circuit**: cart items whose domainName starts with `hosting-` (synthetic placeholder for the linked hosting slot) return synthetic success WITHOUT calling provisionDomainItem; `orderDomain:null` (rides on the linked hosting item's row, no duplicate). runDomainVerificationPhase called AFTER all per-item work with the accumulated orderDomains + customerResult. **finalSuccessfulDomains** computed from `status:'registered' AND itemType !== 'hosting'` (post-verification view — hosting items EXCLUDED even though they're 'registered', the variable name reflects domain-side success). pendingDomains + failedDomains computed post-verification. Logs delta when finalSuccessfulDomains count differs from initial successfulDomains count (silent-failure visibility). Returns 6-field result: registrationResults + successfulDomains + orderDomains + finalSuccessfulDomains + pendingDomains + failedDomains | ✅ Closed | `287cb8f` |

| Batch 7fi | M14 follow-up — Razorpay subscription webhook handlers (25 / 1 file): `lib/services/payment/webhook-handlers` — handleSubscriptionCharged + handleSubscriptionFailed. Pins **missing userId or domainName in subscription.notes → silent early return** (no idempotency anchor possible without these — webhook is silently dropped, Razorpay's retry will hit again). **HOSTING NOT FOUND → admin alert email** ('Subscription charge received but Hosting not found' — customer was charged but no Hosting record exists; manual refund or create required) + return; alert email failure swallowed. **recordRenewalPayment E11000 (dupe-key) IS EXPECTED on retries** — flow continues to idempotency check (the unique index on providerPaymentId is the idempotency anchor); other write errors → return (caller's retry will hit it again). **Idempotency chain**: getRenewalByProviderPaymentId → renewal.processed=true → skip; claimRenewalPayment returns null (another worker won) → skip; hosting/user missing after claim → releaseRenewalClaim (rollback for retry) + return. **Renewal logic ("Payment Always Wins")**: wasInactive (expired/failed/terminated) → HARD RESET expiry from now (NOT from stale expiryDate); else (active/expiring_soon) → ADD-ON extend from current expiryDate. **DA unsuspend only on status === 'expired'** (not on 'failed'/'terminated' which have no DA account to unsuspend); typed outcome, logged inside wrapper. **Trial → paid transition**: `hosting.isTrial:true` → flip false + **HARD-RESET expiry to now+1yr** (the trial's 15-day expiry MUST NOT be the base — would otherwise be a `15d + 1yr` regression). status:'active' + last_reminder_sent:null + processing_until:null reset; next_action_at = newExpiry - 15d; paymentId + subscriptionId stamped on hosting. **createRenewalOrder failure NON-CRITICAL** (service already renewed — audit gap only); attachOrderToRenewal NOT called when Order creation failed. **Zoho fire-and-forget**: createHttpTask after Order created; failure SWALLOWED (Cloud Tasks retries; service activation NEVER depends on Zoho). **handleSubscriptionFailed**: status:'expired' + billingType:'manual' (auto-renew failed, must be recovered manually) + next_action_at:undefined (prevent scheduler re-queuing); DA suspend called when username present; all errors caught (try/catch wraps body) — handler always resolves | ✅ Closed | `fe72780` |

| Batch 7fj | M14 follow-up — trial-abuse anti-fraud pipeline (29 / 1 file): `lib/trial-abuse` — evaluateTrialAbuse + recordTrialClaim + getClientIp/hashIp + isTrialOtpRequired. Pins **getClientIp proxy-chain** (x-forwarded-for first-split-trimmed > x-real-ip > 'unknown'). **hashIp privacy invariant**: HMAC-SHA256 with AUTH_SECRET (NEXTAUTH_SECRET fallback then literal sentinel) — **raw IP NEVER persisted**; empty/'unknown' input → empty string output so the model query is a no-op for IP-less requests; deterministic same-secret hash, secret-rotation breaks the mapping (DB-leak resilience without losing forward fraud signal). **evaluateTrialAbuse 5-step pipeline (cheapest first)**: (1) disposable email → DISPOSABLE_EMAIL instant, no other checks fire; (2) reCAPTCHA verification (only when token supplied) — failure OR v3 score < 0.5 → RECAPTCHA; **Google-unreachable throw SWALLOWED** (don't hard-fail on transient Google outage — same contract as login); (3) IP throttle: 30-day TrialClaim.exists({ipHash, createdAt:$gte}) hit → IP_THROTTLE; (4) device fingerprint throttle: same 30-day window → DEVICE_THROTTLE; (5) phone-OTP gate: only when isTrialOtpRequired() flag flipped — missing otpToken → OTP_REQUIRED; verifyOtpToken invalid → OTP_REQUIRED with verifier-supplied reason. All-clear → `{allowed:true}` only (no extra fields). **recordTrialClaim E11000 race tolerance**: duplicate-key from the partial unique index (ipHash, deviceFingerprint) → SWALLOWED + warn-logged — prior insert already records the claim; throwing here would inconvenience a user who already paid the ₹1 trial fee for a no-op race. Non-E11000 errors also swallowed (best-effort persistence; logged). userEmail lowercased before persist; empty ipHash → undefined (don't persist empty strings). **isTrialOtpRequired** strict check: setting value === true OR === 'true' string → true; everything else (false/undefined/null) → false (default-OFF gate) | ✅ Closed | `1001851` |

| Batch 7fk | M14 follow-up — TLD pricing Redis cache (20 / 1 file): `lib/tld-pricing-cache` — TLDPricingCacheService singleton + processed-view + raw-view keys. Pins **get/set/purge/getRaw/setRaw/isValid/getStatus/setTTL/getTTL** surface. **set() TTL contract**: defaultTTL is in MINUTES (60 default) but stored in Redis as SECONDS (× 60) — defence vs a copy-paste rewrite that would set 60-second TTLs and trash hit-rate. **purge deletes BOTH CACHE_KEY and RAW_CACHE_KEY together** (sync invariant — purging one without the other would leave the admin "processed" and customer "raw" views looking at different snapshots, breaking the share-the-upstream-fetch optimisation). **All Redis throws SWALLOWED** (cache is best-effort — never blocks the calling fetch path; get/getRaw return null on error, set/setRaw/purge resolve silently, isValid returns false). **getStatus**: cache miss → all-null fields + `isRedis` from `process.env.REDIS_HOST`; hit → cachedAt ISO + expiresAt (now + ttl seconds) + remainingTime + itemCount; redis-error inside status → safe miss-shape default. **setTTL mutates the in-memory default** (singleton — subsequent set() calls pick it up); getTTL returns the current minutes value | ✅ Closed | `ae6c1d3` |
| Batch 7fl | M14 follow-up — PendingHosting service + 6-step retry (18 / 1 file): `lib/services/pending-hostings` — 9 helpers + provisionPendingHosting. Pins **status:'pending' rows are AUTO-RETRYABLE** (DA was unreachable at checkout, soft-failed via the 2026-05-19 path); **'failed' rows stay MANUAL** (DA rejected for a logical reason — admin must inspect). listDeferredPendingHostings: filters status:'pending' ONLY + sort createdAt ASC (oldest first, fair queue) + cap 50 (predictable DA load per cron tick). createPendingHosting default status:'failed'. deletePendingHostingsByUsername $or BOTH legacy `username` AND new `daUsername` (migration-window compat). listStuckPendingHostings: cutoff-driven + status $in [pending, failed] + slim projection. **provisionPendingHosting 6-step retry**: (1) user not found → `{ok:false, error}` no mutation; (2) **user.directAdminUsername ALREADY SET → DROP the row + return `dropped:true`** (sibling row already provisioned the user — keeping this row would block future sweeps with a misleading ALREADY_HAS_HOSTING error); (3) daCreateUser throw → pending.error stamped + pending.save + `{ok:false}` (admin will see latest reason); (4) **DNS update FAILURE best-effort** — logged + retry proceeds; (5) user fields stamped (directAdminUsername/hostingCreatedAt/hostingExpiresAt = +1yr) + Hosting row created with `next_action_at = expiry - 15d` + billingType:'manual' + isTrial:false; **Hosting.create throw SWALLOWED** (DA account is live + user fields set — local audit reconciled later); (6) PendingHosting deleted on success + provision email sent; **email failure SWALLOWED** (DA account live, missing email is minor) | ✅ Closed | `1b0278f` |
| Batch 7fm | M14 follow-up — TLD policy registry (40 / 1 file): `lib/tld-policies` — TLD_POLICIES + extractTld + getTldPolicy + getMinYears + getMaxYears + isRestricted + validateDomainPeriod + getRegistrationParamPairs + getRequiredAttributes + getMissingAttributes + mapRegistrationError. Pins **TLD_POLICIES contract**: 7 big gTLDs (com/net/org/info/biz/xyz/me) at 1-10 years no T&C; **23 new gTLDs** (dev/app/page/new/how/soy/rsvp/zip/mov/foo/esq/prof/phd/nexus/shop/store/online/site/tech/space/website) require acceptTerms; **.ai 2-year minimum** (long-min gTLD); **.co caps at 5 years max** (bounded); 8 Indian ccTLDs allowed (in/co.in/net.in/org.in/firm.in/gen.in/ind.in/bharat); **15 ccTLDs restricted** (au/uk/co.uk/ca/de/fr/nl/es/it/jp/cn/br/mx/ru/za — local presence we can't fulfil). **extractTld multi-level priority**: 3+ segment domain checks the TWO-level TLD first (`shop.co.uk` → `co.uk`, NOT `uk`); falls back to last segment if not in registry (`foo.bar.com` → `com`); single-segment → '' empty. getTldPolicy merges DEFAULTS (1-10) with registry overrides. **validateDomainPeriod**: null when valid; restricted → 'requires local presence' message FIRST (period not checked); below min → 'minimum registration of N year(s)' with singular/plural; above max → 'at most N years'. **getRegistrationParamPairs T&C contract**: acceptTerms TLDs emit `attribute-name=acceptTerms` + `attribute-value=1` pair (registry rejects new gTLDs without it); non-acceptTerms TLDs emit no pairs; dynamic required-attributes get numbered `attr-name1/attr-value1, attr-name2/attr-value2` pairs starting at 1 with missing values silently skipped (caller-side enforcement via getMissingAttributes). **mapRegistrationError** 6 distinct branches (T&C + min-period + eligibility + contact-invalid + premium + unavailable) all case-insensitive; unknown → null | ✅ Closed | `f1168cc` |
| Batch 7fn | M14 follow-up — SupportTicket service (25 / 1 file): `lib/services/support-tickets` — findUserTicket/Lean + listTicketsForUser + listTicketsForUserSummary + getTicketById/Lean + listTicketsForAdmin + countOpenTickets + createSupportTicket + updateTicketByIdAsAdmin. **Ownership-gate foot-gun guard**: findUserTicket combines {_id, userId} in ONE findOne — a missing-userId filter could leak a foreign ticket, so both gates are baked in at the service layer (route layer still 404s either way). **listTicketsForUserSummary projection contract**: select() drops embedded messages array off the wire; computes messageCount + lastMessage (= messages.at(-1)) per row; absent updatedAt falls back to createdAt. **listTicketsForAdmin pagination**: page/perPage default 1/20, both Math.max(1, …) clamped; **status:'all' SKIPS the filter** (NOT stored as 'all' — anti-foot-gun) while status:'open' applies to BOTH find AND countDocuments (otherwise pages wouldn't match total); Promise.all parallelises the find + count; pages = Math.ceil(total/perPage). **countOpenTickets**: status $in ['open','in_progress'] — OPEN_STATUSES enum is the single source of truth for "active" (system-health card uses this); does NOT include resolved/closed | ✅ Closed | `f12fc10` |
| Batch 7fo | M14 follow-up — TLD mappings lookup table (49 / 1 file): `lib/tld-mappings` — static plain-TLD → RC-product-key registry consumed by the pricing cache. **Five `dom*` exceptions pinned by name** (NOT `dot*`): com→domcno, org→domorg, info→dominfo, biz→dombiz, us→domus. These are historical RC product codes that don't follow the `dot{tld}` pattern. Any future "harmonisation" PR that flips them silently breaks pricing lookups (cache layer would miss every .com price) — anti-regression pinned explicitly. **`za` anomaly**: za→'dotcoza' NOT 'dotza' (source comment notes dotza was broken; dotcoza exists). **`net` is the lone `dot*` in legacy big-gTLDs**: confirms the dom-exception set is exactly 5. **15 commented-out "Unsupported" ccTLDs** (ua/ug/uy/uz/va/ve/vg/vi/vn/vu/wf/ye/yt/zm/zw) stay OUT of the map → lookup returns undefined. Value contract: every value lowercase, no leading dot, no whitespace, length ≥ 5; every value starts with 'dot' or 'dom' (only two RC prefix shapes); ≥ 200 entries (massive ccTLD coverage) | ✅ Closed | `3dcec9e` |
| Batch 7fp | M14 follow-up — WhatsApp Cloud API sender (27 / 1 file): `lib/whatsapp` — WhatsAppService class with isConfigured + formatNumber + sendTemplate + sendServiceReminder + sendPaymentConfirmed + sendServiceSuspended. **isConfigured AND-gate**: both WHATSAPP_API_TOKEN and WHATSAPP_PHONE_NUMBER_ID must be set; missing either → sendTemplate short-circuits to `return false` BEFORE the fetch (don't blow up on undefined token in headers). **formatNumber E.164 contract**: 10-digit → `+91${digits}` (India assumption); 12-digit starting with 91 → `+${digits}` as-is; arbitrary length → `+${digits}` (no validation rejection); non-digits stripped via /\D/g. **15s AbortSignal.timeout** attached to fetch (worker hot path — a hung Graph slot must not stall the worker's Cloud Run slot). Bearer auth + Content-Type JSON on every send. **components shape**: empty bodyParams → components:[] (NOT empty body component — Meta rejects); non-empty → single body component with parameters wrapped as {type:'text', text}. Network/HTTP errors all SWALLOWED → return false (best-effort, never re-thrown). **Template fallback**: env WHATSAPP_TEMPLATE_REMINDER / _PAYMENT / _SUSPENDED override per-template name; `??` fallback fires ONLY on null/undefined (NOT empty string — production foot-gun pinned in test setup). sendPaymentConfirmed default currency 'INR'; sendServiceReminder/Suspended renewUrl = `${NEXTAUTH_URL}/dashboard` (empty NEXTAUTH_URL → just '/dashboard') | ✅ Closed | `3a90ed3` |
| Batch 7fq | M14 follow-up — Zoho invoice retry / self-heal (22 / 1 file): `lib/zoho-invoice-retry` — selfHealUserInvoices + syncUserInvoicesNow + internal retryOne. **5-min Redis throttle** (THROTTLE_SECONDS=300) keyed by `zoho-retry:${orderObjectId}` prevents tight-loop hammering when the user reloads the invoices page repeatedly; throttle-hit short-circuits BEFORE the claim attempt (no DB touch). **Atomic claim with relaxed gate** {allowNull:true, allowFailed:true} — terminal `creation_failed` orders DO get re-attempted (a Zoho outage shouldn't permanently quarantine the order). **Claim returns null → skipped:'already_done'** (concurrent caller or a paid invoice already attached; no double-create). **User-not-found → markFailed + skipped:'no_user'** (order points to a vanished user; permanent fail). **items projection defaults**: itemType=domain, period=1, periodUnit='years' for non-hosting / 'months' for hosting. **createInvoice signature**: (orderMeta, user, items, 'Razorpay', isPaid=true) — the trailing `true` pre-pays the invoice in Zoho so it doesn't go to the customer as a draft. **AxiosError-shaped throw**: err.response.data.message extracted preferentially → err.message → String(err) fallback chain. **markFailed catch-path is wrapped in `.catch(() => {})`** so an outer markFailed throw can't crash the retry loop (anti-cascade — bookkeeping failure during a Zoho-failure cleanup wouldn't fail the whole user's self-heal). **selfHealUserInvoices**: empty stuck → []; outer catch returns [] (fire-and-forget never throws). **syncUserInvoicesNow**: bypasses throttle via {skipThrottle:true}; NO outer catch (caller-initiated errors surface for "Sync now" UX) | ✅ Closed | `c366030` |
| Batch 7fr | M14 follow-up — Support attachment validator (47 / 1 file): `lib/support-attachments` — validateAttachments + sumExistingAttachmentBytes + checkTicketTotalCap + ATTACHMENT_MAX_FILES (4) + MAX_SIZE_BYTES (2MB) + TICKET_TOTAL_CAP (20MB) + ALLOWED_MIMES set. Defence-in-depth pipeline. **Allowlist explicitly EXCLUDES image/svg+xml** (SVG would allow embedded JS — XSS vector); pinned by name (`.size === 4` plus negative-test on svg). **Magic-byte verification** for all 4 allowed MIMEs: PNG (89 50 4E 47 0D 0A 1A 0A), JPEG (FF D8 FF), GIF87/89 (47 49 46 38 37/39 61), WEBP (RIFF + WEBP); first-byte-flipped → magic_mismatch reject (prevents disguised HTML/JS-as-fake-PNG). **Strict base64 charset** (`/^[A-Za-z0-9+/]+={0,2}$/`) — `@`, spaces, anything outside A-Za-z0-9+/= rejected. **Tight decoded-size tolerance** (±4 bytes — was 10% before, narrowed to defeat smuggling); declared-size LIE within tolerance → accepted but **decoded size overrides client-declared** in the CleanAttachment (we trust what we actually decoded). **Filename sanitisation**: control chars stripped, path separators → `_`, HTML chars (`<>"'`) stripped, leading dots stripped (no hidden files), capped at 100 chars; empty after sanitisation → `screenshot.<ext>` fallback (jpg/png/webp/gif/img). **Pipeline order rejects short-circuit** (array → count → filename → mime → mime-allowlist → size → size-cap → dataUrl → prefix → base64 → decode → decoded-size → magic). null/undefined/empty array → success with [] (no attachment is valid). **checkTicketTotalCap boundary inclusive at 20MB** (exactly at cap → null, exactly 1 over → error string) | ✅ Closed | `3c9501b` |
| Batch 7fs | M14 follow-up — ZohoBooks invoice ops (51 / 1 file): `lib/zohobooks/invoices` — createInvoice + getInvoicesByEmail + getInvoicePdf + getAllInvoices + getInvoiceById + applyPaymentToInvoice + getInvoicesByReferenceNumber. **Idempotency dedup**: createInvoice first calls getInvoicesByReferenceNumber(orderId) and returns the existing invoice if one is found (prevents duplicate invoices when retry layer fires concurrently with original creation). **Missing-refresh-token throws ZohoError MISSING_REFRESH_TOKEN** (createInvoice can't silently skip a write; getters return null/[]/false). **Contact upsert**: getContactByEmail null → createContact; found → updateContactDetails (re-sync on every invoice path so address changes flow). **GST tax routing**: customerState.toLowerCase() vs _ORG_STATE; inter-state → IGST18, intra-state → GST18; falsy state defaults to intra-state (safer than mis-IGST). **Test-hosting rate**: itemType:'hosting' AND periodUnit:'days' → rate=1, quantity=1 (₹1 trial); else rate=_roundAmount(price), quantity=duration. Hosting line-item name: plan.name → 'Hosting Service' fallback when domainName starts with 'hosting-'. Domain line-item name = domainName, description includes 'Domain Registration'. **GST-error fallback (code=2 + 'gst')**: updateContactToConsumer + retry; fix fails → rethrow original. **Tax-mismatch (code=3032)**: swap IGST↔GST on EVERY line_item and retry. **Subscription-expired (code=103001)**: outer catch throws ZohoError SUBSCRIPTION_EXPIRED (admin-visible). **status/sent failure SWALLOWED** (already-sent is fine; payment proceeds anyway). **Payment skip rules**: total <= 0 OR shouldApplyPayment=false → skip; payment failure does NOT throw (invoice is durable artefact). getInvoicesByEmail/getAllInvoices: DESC sort by invoice_number using NATURAL sort (INV-00014 ranks above INV-00009). **Code-57 probe path** on access-denied → bare list attempt to distinguish scope vs customer-ID issue. getInvoicePdf: Accept:'application/pdf' header + arraybuffer responseType + ?accept=pdf param | ✅ Closed | `8175c99` |
| Batch 7ft | M14 follow-up — ZohoBooks singleton service class (46 / 1 file): `lib/zohobooks` — ZohoBooksService + ZohoError. **Singleton invariant**: getInstance returns SAME instance (auth state + token cache must persist across all callers). **DC-based baseUrl routing**: ZOHO_DC strips leading dot then routes: 'com' → zohoapis.com, 'eu' → zohoapis.eu, default/'in'/unknown → zohoapis.in. Auth host follows the DC too (accounts.zoho.com vs .in/.eu). **_hasRefreshToken gates almost every public method**; reads env at construction time (NOT live — runtime env-var flip needs restart). **ZohoError shape**: name='ZohoError', title+code+details fields; message = `${title}: ${JSON.stringify(details)}`. **_roundAmount**: 2-decimal rounding via Math.round(v*100)/100 — avoids float noise being sent to Zoho. **_isValidGst**: 15-char Indian GSTIN regex (2 state digits + 5 letters + 4 digits + 1 letter + 1 alphanum + Z + 1 alphanum); trims/strips whitespace + uppercases before match. **_getHeaders contract**: 'Zoho-oauthtoken ${token}' Authorization + Content-Type JSON + X-com-zoho-books-organizationid header ONLY when orgId set (org-scope even when token has multi-org access). **Token caching**: cached until expiry-60s safety buffer (refresh BEFORE token dies — anti-edge-of-cliff that would otherwise hit a 401 in production). OAuth error response OR network throw → wrapped in ZohoError AUTH_ERROR. **_idempotentRetry policy**: 3 attempts default; retryable iff status falsy OR status ≥ 500 OR status === 429; 4xx (validation/auth) NOT retried — throws immediately. Exponential backoff delay * 2^i (1s → 2s → 4s). **103001 detection sets _subscriptionExpired flag** (one-shot guard prevents repeat DB writes) so health-check surfaces it without a write-level probe call. **_ORG_STATE throws if env missing** (GST tax-type calc would silently mis-route otherwise — fail-loud at access time). **_TAX_IDS** captured at class-load time (static-readonly fallbacks pinned; env-override requires restart) | ✅ Closed | `6913473` |
| Batch 7fu | M14 follow-up — User service (71 / 1 file): `lib/services/users` — 50+ thin Mongoose use-case wrappers. Tests focus on security-critical / contract-pinning paths rather than line coverage of every passthrough. **createUserWithCredentials role HARDCODED to 'user'** + provider:'credentials' + isActivated:false (registration CANNOT escalate; mass-assignment-proof). Optional profile fields (phone/phoneCc/companyName/gstNumber/address) use conditional spread `...(input.x ? { x } : {})` so omission lands schema defaults instead of undefined writes. **softDeleteUser MUST also stamp sessionInvalidatedAt=now** (otherwise the user's old JWT keeps working after deactivation). **applyUserPatch role whitelist**: only 'user' / 'admin' accepted (anti-mass-assignment foot-gun guard); isActive true→false sets sessionInvalidatedAt, false→true clears it, unchanged → NOT touched. **resetUser2FA REVOKES sessions** (admin-initiated recovery — stamps sessionInvalidatedAt); **disableTOTPForUser does NOT** (user-initiated — keep sessions valid). **setUserDirectAdminUsername is CAS-style**: filter requires _id AND (field unset / null / empty) so concurrent multi-hosting provisionings have a first-writer-wins on User; per-Hosting row still carries its own DA username so per-account mapping isn't lost. **findUserByActivationToken expiry-gate flip**: default future-only ($gt:now), onlyExpired:true → $lte:now (so UI shows 'token expired' instead of 'invalid token'). **findUserByResetToken / findUserByPendingEmailToken opt-in** to normally-select:false fields — the only places they're allowed back. **getUserByEmailForLogin**: opts in +password + maxTimeMS cap default 5000ms (NextAuth authorize has tight latency budget — slow primary can't stall login indefinitely). **findUsersByIds / findUsersByEmails empty-array short-circuit**: no DB call (anti-foot-gun even though $in:[] would match nothing). **setUserResellerClubIds empty-patch → NO DB call**. **getUserCart returns [] for missing user** (caller never null-checks). **invalidateUserSessionNow stamps BOTH sessionInvalidatedAt AND lastActivityAt** (latter so next legitimate sign-in starts a fresh window). **listUsers defaults**: filter excludes admins + deleted, page 1, limit 50, sort createdAt:-1, totalPages=ceil(total/limit)||0. **getUserForTokenRefresh + getUserForSessionCheck exact projections** (security-hot-path — runs on EVERY authenticated request). **permanentDeleteUser snapshots userName + userEmail onto orders that don't have userName BEFORE deleting** (audit trail must survive deletion; snapshot failure does NOT block deletion — admin already authorised) | ✅ Closed | `95358fd` |
| Batch 7fv | M14 follow-up — Zoho error-unwrap helper (20 / 1 file): `lib/zohobooks/types` — the file is mostly TypeScript type declarations (tsc verifies); only `unwrapZohoError` has runtime branches. Used in every Zoho catch block to pull out the API-error payload regardless of throw shape (AxiosError / plain Error / raw value). **3 branches pinned**: (1) AxiosError → extracts response.data + response.status + err.message verbatim (avoids `(err as any).response?.data` patterns scattered across ~10 callsites); (2) plain Error → { message: err.message } with NO data/status (caller's `.data?.code` checks short-circuit to undefined); (3) anything else → { message: String(err) } so the catch ALWAYS has something to log even when the throw target is `null`/`undefined`/a number/`[object Object]`. **AxiosError with NO response** (network error pre-headers): data + status BOTH undefined, message extracted — caller's `!status` check in retry layer returns true → retryable. **Retry-logic interop**: pins how the unwrapped shape feeds idempotentRetry's `!status || status >= 500 || status === 429` gate. **Code surfacing**: 103001 (subscription-expired), 2+gst (GST fallback), 3032 (tax-mismatch), 57 (access-denied probe) all surface via r.data.code | ✅ Closed | `42a9670` |
| Batch 7fw | **Edge middleware security boundary** (60 / 1 file): `middleware.ts` — THE security boundary every request flows through.<br>• HTTPS enforcement (production-only); x-forwarded-proto !== 'https' → 301 to https://<br>• 3-way HTTPS bypass: loopback (127.0.0.1/localhost/0.0.0.0) AND x-internal-maintenance-check:1 sentinel (anti-loop on maintenance-status fetch)<br>• OPTIONS preflight returns immediately via buildPreflightResponse — NO token fetch, NO CSRF check<br>• URI normalization: collapses `//` → `/` with 307 (NOT 301 — avoid permanent redirect cache)<br>• API v1 prefix rewrite: `/api/v1/x` classified as `/api/x` for prefix matching, log preserves v1<br>• HEAD bypass for public routes (monitoring tools don't have credentials)<br>• **CSRF gate on EVERY authenticated mutating /api/*** — NOT just admin (anti-foot-gun fix: previously only admin branch had it, leaving /api/user/* and /api/payments/* exposed to top-level POST navigations via sameSite:lax)<br>• 13-element PUBLIC_API_PREFIXES list pinned via parametric tests (auth/webhooks/public/health/cron/workers etc. exempt — non-cookie auth)<br>• Admin API: 401/403 JSON, NO redirects (REST clients can't follow page redirects)<br>• Admin pages: redirect to /403 NOT /login (info-leak guard: avoids 'this endpoint exists' via 401)<br>• Auth-page redirect by role: logged-in admin on /login → /admin/dashboard; user → /dashboard<br>• Admin-on-/dashboard guard: admin accessing /dashboard/* → /admin/dashboard (prevents role pollution of user surfaces)<br>• Guest checkout (/checkout/guest) is PUBLIC by design — no token fetch (guest JWT validated at route handler)<br>• CORS headers applied to all non-OPTIONS /api/* responses<br>• requestId echoed in response headers (Cloud Trace correlation) | ✅ Closed | `697c15c` |
| Batch 7fx | **Payment-verify route orchestrator** (40 / 1 file): `app/api/payments/verify/route.ts` — THE payment-verification entry point. Pure orchestrator pinning route-level integration contracts.<br>• Auth gate FIRST: getUserFromRequest null → 401 (no downstream side effects)<br>• Schema validation: refine requires order_id OR subscription_id; refine requires non-empty cartItems<br>• verifyRazorpayPayment failure passes response through verbatim (signature gate is THE security checkpoint — identity-equal NextResponse propagates)<br>• Defense-in-depth ownership check: existingOrder.userId !== user._id → 404 'Order not found' NOT 403 (info-leak guard)<br>• Already-completed early-exit: 'Order already completed.' (idempotent re-clicks don't double-charge)<br>• paid/processing → 'provisioning in progress' + domainRegistrationStatus:'processing'<br>• Amount-match check fires ONLY on pending orders with razorpay_order_id (anti-underpayment)<br>• Renewal short-circuit: handleRenewalPayment non-null returned directly (skips Zoho + provisioning)<br>• hosting_upgrade orderType → handleUpgradePayment (dynamic import)<br>• Idempotency guard BEFORE restricted-domain check (correct ordering)<br>• claimPendingOrderForProcessing claim failure → 'processing' response (anti-double-provision when webhook beat us)<br>• finalizePendingOrder vs createCompletedOrder fork: pending → finalize (DB-trusted cart); no pending → createCompleted<br>• **H1 anti-swap-domain**: finalizePendingOrder NOT passed cartItems (verified by absence of property)<br>• cartItemsFromOrderDomains used for Zoho line items (NOT request body — H1 mirror)<br>• Zoho failure SWALLOWED → invoiceCreationFailed:true, status 207 multi-status<br>• forceMarkZohoCreationFailed + recordSystemLog on Zoho throw (durable failure record)<br>• 207 vs 200 status differentiation<br>• Response message branching: 6 shape combinations (hosting+domain / hosting-only / domain-only × success/pending)<br>• handleVerificationError catch passes outer refs (razorpay_order_id, payment_id, existingOrder) | ✅ Closed | `afdaf12` |
| Batch 7fy | **Payment-create-order route** (38 / 1 file): `app/api/payments/create-order/route.ts` — the pre-payment gate. Pipeline: auth → schema → TLD policy → live price verification → trial gates → Razorpay create → pending Order → response.<br>• Auth gate FIRST (no user → 401, no downstream side effects)<br>• TLD policy validateDomainPeriod fires ONLY on domain items, NOT hosting; undefined itemType = legacy domain<br>• **Live price verification (anti-tampering)**: priceCheck.ok=false → 409 PRICE_CHANGED with serverTotal + clientTotal + mismatchedDomains payload<br>• fellBackToClient → warning logged, proceeds with client total<br>• Trial gate 1: non-yearly billing → 400 'Trial is only available for yearly hosting plans'<br>• Trial gate 2: settings hosting_trial_enabled=false → 400 'Free trials are currently unavailable'<br>• Trial gate 3: prior trial → 400 'You have already used your free trial'<br>• Trial gate 4: evaluateTrialAbuse denied → 400 with abuse code (defense-in-depth; eligibility is advisory)<br>• **recordTrialClaim called BEFORE subscription creation** (anti-bypass — throttle stamped even if Razorpay fails)<br>• Subscription createSubscription signature: (planId, userId, domain, true, 100, 15 for trial else undefined)<br>• Subscription creation failure falls back to one-time charge (no user-facing error)<br>• HostingPlan not found → falls through to one-time<br>• createOrder pending-row persisted BEFORE response; razorpayPaymentId/Signature='pending' placeholders<br>• DB write failure → 500 'Failed to initialise order' (better than orphaned Razorpay order)<br>• derivedOrderType: domain+hosting → 'bundle'; hosting → 'hosting'; domain → 'domain'<br>• periodUnit defaults: domain → 'years', hosting → 'months'<br>• bookingStatus[0] = 'payment_verified' progress 5<br>• Razorpay error mapping 5-branch: Invalid amount → 400; Amount too small → 400 'Minimum ₹1'; Amount too large → 400 'Maximum ₹10,00,000'; Network error → 503; Gateway error → 502; default → 500<br>• No payment target → throws → 500<br>• Response shape: razorpayOrderId, razorpaySubscriptionId, subscriptionPlan, amount, currency, hasSubscription, isTrial | ✅ Closed | `b7b6dd6` |
| Batch 7fz | **Guest payment-verify route** (41 / 1 file): `app/api/payments/guest/verify/route.ts` — guest-checkout variant of /verify with extra guards (unauthenticated actor).<br>• Rate-limit FIRST (5/min IP cap)<br>• Guest token validation → 401 'Guest session expired'<br>• Trial-block: cartItems with isTrial → 400 'Free trials require an account' (1-per-user-lifetime needs account)<br>• Signature verification verifyPayment false → 400 'Invalid payment signature'<br>• getPaymentDetails throw → 400 'Failed to verify payment status'<br>• non-captured/authorized status → 400 with status; both 'captured' AND 'authorized' accepted<br>• paymentDetails.order_id mismatch → 400 'Order ID mismatch'<br>• **Email ownership defense-in-depth**: existingOrder.userEmail vs guestEmail compared CASE-INSENSITIVE → 404 'Order not found' (anti-bind-to-victim-email)<br>• Already-completed → 'Order already completed' + isGuest:true; paid/processing → 'provisioning in progress'<br>• Amount-match check fires on pending orders<br>• Domain support check fires only on domain items (hosting SKIPS — doesn't go through RC)<br>• **Email-claim defense**: existing NON-guest user → 409 'sign in to continue' (last fence)<br>• createUser for new guest: random 64-hex unusable password, isGuest:true, provider:'credentials', country:'IN', phoneCc:'+91'<br>• Existing guest with !profileCompleted → backfills firstName/lastName/phone/address from token + save<br>• finalizePendingOrder NOT passed cartItems (H1 mirror — DB-trusted cart only)<br>• claimPendingOrderForProcessing failure → 'processing' response with isGuest<br>• Legacy path (no pending Order): provisionCartItems + mongoose.startSession.withTransaction (createOrderInSession + createPaymentInTransaction atomic)<br>• cartItemsFromOrderDomains used for Zoho (H1 mirror)<br>• Zoho failure SWALLOWED + recordSystemLog + forceMarkZohoCreationFailed; main response still 200 (NOT 207 like logged-in path)<br>• Setup-password email ONLY for new guests (isGuest=true) — resetToken 32-hex + 24h, 4th arg=true (setup mode), fire-and-forget<br>• Catch path fallback Order: createOrder with status:'processing' + 'contact support' error + paymentStatus 'captured_pending_support'<br>• getOrderByOrderId dedup BEFORE fallback createOrder (anti-duplicate-key)<br>• Catch path → 500 'Payment verification failed' | ✅ Closed | `407e893` |
| Batch 7g0 | **Guest payment-create-order route** (46 / 1 file): `app/api/payments/guest/create-order/route.ts` — pre-payment gate for guest checkout. Two modes: with guestToken (signed token is source of truth) vs without (deep validation + sign fresh token).<br>• Rate-limit FIRST (5/min IP cap)<br>• Existing-token mode: invalid → 401; valid → registrant from token NOT body (anti-tampering)<br>• Existing token reused verbatim (signGuestToken NOT called again)<br>• Fresh-token val 1: missing email → 400 'Email is required'<br>• Fresh-token val 2: validateEmail invalid → 400 'Invalid email address'<br>• Email normalized .trim().toLowerCase() BEFORE validation<br>• Fresh-token val 3: each of 7 required fields missing → 400 'Missing required field: <name>'<br>• Fresh-token val 4: phone !== 10 digits → 400 '10-digit number'<br>• Fresh-token val 5: zipcode !== 6 digits → 400 '6-digit number'; firstName/lastName > 50 → 400 'too long'<br>• Trial-block: cartItems.isTrial → 400 (mirrors verify)<br>• TLD policy validateDomainPeriod fires ONLY on domain items<br>• Disposable-email block → 400 code:'DISPOSABLE_EMAIL' (trial-abuse IP/device throttle NOT reused — paid checkout has its own friction)<br>• Live price verification: mismatch → 409 PRICE_CHANGED; fellBackToClient → client total<br>• totalAmount = priceCheck.serverTotal + hostingTotal (hosting uses HostingPlan DB, trusted as-is)<br>• totalAmount <= 0 → 400 'Invalid order amount'<br>• Razorpay receipt id starts with 'gst_' (distinct from logged-in 'ord_')<br>• **Email-claim defense**: existing NON-guest user → 409 'sign in to continue' (last fence)<br>• Existing guest user reused (NOT recreated — repeat abandoned-checkout is fine)<br>• createUser for new guest: random 64-hex unusable password, isGuest:true, provider:'credentials', country:'IN', phoneCc:'+91'<br>• createOrder pending row persisted BEFORE response; razorpayPaymentId/Signature='pending' placeholders<br>• orderId 'ord_<ts>_<rand>'; derivedOrderType domain/hosting/bundle<br>• periodUnit defaults: domain → 'years', hosting → 'months'; bookingStatus[0] progress 5<br>• userId pinned to guest user._id<br>• DB write failure → 500 'Failed to initialise order'<br>• Response: razorpayOrderId, amount, currency, guestToken, email<br>• Outer catch → 500 'Failed to create payment order' | ✅ Closed | `e2cdbd3` |
| Batch 7g1 | **Domain-search public route** (35 / 1 file): `app/api/domains/search/route.ts` — high-traffic public endpoint. Orchestrates rate-limit → schema → TLD parsing → restricted-TLD block → validateDomainName → Redis-cached parallel fan-out (RC search + suggestions + DA hostingExists).<br>• Rate-limit FIRST (20/min cap, 'slow down' message)<br>• Domain-with-TLD detection: split on `.`<br>• Multi-level TLD via parts.slice(1).join('.') ('shop.co.uk' → 'co.uk' NOT 'uk')<br>• 3+ segment domains preserve N-1 levels as TLD<br>• Single explicit restricted TLD → 'restricted_tld' response (NO RC call, NO validateDomainName — short-circuits)<br>• Multi-TLD array form strips leading dots; string form (legacy) comma-splits + strips<br>• Default ['com'] when neither tlds nor explicit TLD<br>• All-TLDs-restricted → 'all_tlds_restricted' with list<br>• Some-restricted → filter out, continue with allowed<br>• validateDomainName invalid → 400 errors.join(', ') (full list, not first)<br>• **Quick mode**: returns ONLY availability (no suggestions, no hosting check); cache hit → skip RC; cache miss → fetch + set 600s<br>• Empty results NOT cached (anti-cache-pollution)<br>• Full mode parallel fan-out: Promise.all over RC search + suggestions + DA hostingExists<br>• Suggestions error SWALLOWED → empty array<br>• hostingExists error SWALLOWED → false<br>• Hosting check ONLY when domainToCheck set; skipped entirely otherwise<br>• Cache TTL 600s (10 min) for both results + suggestions<br>• searchQuery.userEnteredDomain null when base-only; set when user typed full domain<br>• Response: success + results + suggestions + hostingExists + searchQuery + cached:{results, suggestions} + requestId + responseTime<br>• Outer catch → 500 with error.message OR generic | ✅ Closed | `7a556e8` |
| Batch 7g2 | **User-settings GET+PUT route** (29 / 1 file): `app/api/user/settings/route.ts` — THE place users mutate their own account state. 5 explicit defense-in-depth security layers.<br>• GET auth gate FIRST (no user → 401 UNAUTHORIZED)<br>• GET RC sync best-effort (errors don't block response)<br>• GET RC fields BACKFILL ONLY when local empty (don't overwrite manual edits)<br>• **GET response data-minimization**: 13-field whitelist (firstName, lastName, email, phone, phoneCc, whatsappNumber, address, city, state, country, zipCode, company, gstNumber)<br>• NEVER leaks password / role / resetToken / TOTP secrets / _id<br>• GET defaults phoneCc='+91', country='IN' when local empty<br>• PUT auth gate FIRST<br>• PUT profile schema via Schemas.profileUpdate.safeParse → 400 VALIDATION_ERROR on fail<br>• **Anti-mass-assignment shield**: ONLY parsed.data fields written to user (a payload setting role:'admin' is dropped — role isn't in the schema)<br>• Each field updated only when defined; nested address initialized when absent<br>• profileCompleted recomputed from updated state<br>• PUT password strength check fail → 400 WEAK_PASSWORD with FIRST error<br>• PUT password: getUserWithPassword to refetch with hash (auth-loaded doc has select:false)<br>• **Anti-ATO 3-tier**: existing password requires currentPassword (400 MISSING_PASSWORD); wrong → 401 INVALID_PASSWORD; same as current → 400 SAME_PASSWORD<br>• OAuth-only user (no password hash) SKIPS currentPassword check<br>• **PUT response data-minimization**: 6-field user projection (id, email, firstName, lastName, profileCompleted, role)<br>• NEVER includes password / resetToken / TOTP / address<br>• resellerClubSynced + zohoBooksSynced flags exposed<br>• PUT side-effects all SWALLOWED (RC modifyCustomer, Zoho sync, email notifications)<br>• isFirstTime password detection: !hadPasswordBefore AND provider !== 'credentials'<br>• Outer catch → 500 SERVER_ERROR | ✅ Closed | `28477c2` |
| Batch 7g3 | **Domain-nameservers GET+POST route** (31 / 1 file): `app/api/domains/nameservers/route.ts` — domain-NS lookup (GET) + update (POST). Security gates + RC-API happy path (RDAP/DNS fallback + custom-mode DNS-resolve excluded — util.promisify(dns.X) doesn't compose with vi.fn).<br>• GET auth gate FIRST (no user → 401)<br>• GET param validation: missing domainName → 400; invalid format → 400 (regex anti-traversal/anti-injection)<br>• GET method='api' happy path: registrar='Anutech Digital'<br>• Nameserver cleanup: dedup lowercase + filter invalid format + drop 'name'-containing entries (registrar placeholders)<br>• Default-vs-custom detection (4 patterns): registrar-servers.com, orderbox-dns.com, resellerclub.com, publicdomainregistry.com → 'default'; cloudflare/awsdns → 'custom'<br>• GET response: success + domainName + nameservers + count + method + whoisData + lastChecked<br>• POST auth gate FIRST<br>• POST schema: domainName regex, method enum, custom refine requires ≥2 valid NSs<br>• **3-tier ownership defense**: findOrderByDomainForUser null → 404 'Domain not found for this user'<br>• findOrderDomain null → 404 'Domain not found in order'<br>• Missing resellerClubOrderId → 404 'contact support' (anti-foreign-domain — user can't update NSs they don't own)<br>• POST method='default' → setDefaultNameservers with RC order id<br>• effectiveNameservers fixed pair: ['ns1.registrar-servers.com', 'ns2.registrar-servers.com']<br>• POST RC failure → 502 (upstream gateway error) with RC message or generic<br>• Domain.updateOne with {domainName, deletedAt:null} filter<br>• DB write failure SWALLOWED (registrar already updated — non-fatal)<br>• POST outer catch → 500 'Failed to update nameservers' | ✅ Closed | `b180e96` |
| Batch 7g4 | **Admin pending-domain CRUD route** (38 / 1 file): `app/api/admin/pending-domains/[id]/route.ts` — admin-only GET + PUT + DELETE. Coordinates registrar cancellation + Order-collection sync + failure-email notifications.<br>• **All 3 handlers gate on AuthService.getAdminFromRequest** (NOT getUserFromRequest — admin-only)<br>• null admin → 401 (no DB lookup, no RC call, no email)<br>• GET: getPendingDomainById({populateUser:true}); null → 404<br>• PUT schema: status enum (pending/processing/failed/completed), adminNotes max 5000, reason max 2000<br>• Each PUT field updated only if defined; pendingDomain.save() once<br>• **PUT order-sync H1 fix**: status='completed' → order.domains[i].status='registered' (legacy code compared against 'registered' which never matched — every sync silently fell through to 'failed' even when admin marked completed)<br>• status='completed' bookingStatus 'domain_registered' progress 100<br>• ANY OTHER status → 'failed' + bookingStatus 'domain_failed' progress 0<br>• reason copied to order.domains[i].error when status='failed'<br>• Matching domain NOT found in order.domains → no mutation<br>• Order sync failure SWALLOWED (admin action shouldn't fail because of bookkeeping)<br>• **DELETE permanent vs soft-archive fork** via ?permanent=true query<br>• DELETE permanent: RC orderId fallback search via rcGetDomainOrderId (safety net for stale rows)<br>• RC cancel success → registrarStatus:'cancelled'<br>• RC error captured but deletion proceeds (local state must converge regardless of registrar reachability)<br>• order.domains[i].status='cancelled' + bookingStatus message includes 'Confirmed with registrar' when RC succeeded<br>• Domain.deleteMany cleanup {domainName, orderId} (premature Domain records)<br>• Cleanup failure SWALLOWED<br>• DELETE archive (soft): findOneAndUpdate isArchived:true + archivedAt + archivedBy=admin.email + status:'failed'<br>• Archive does NOT call RC (archive ≠ permanent)<br>• Archive order.domains[i].status='failed' + error stamped with reason + bookingStatus appended<br>• sendPurchaseOrderEmail when customer.email present (registrationFailed=true after successful payment)<br>• Email failure SWALLOWED; missing populated email → email skipped (no crash on raw ObjectId)<br>• DELETE outer catch → 500 'Failed to process pending domain: <msg>' (exposes specific error to admin UI) | ✅ Closed | `ad0c6bc` |
| Batch 7g5 | **Admin TLD-pricing dashboard route** (34 / 1 file): `app/api/admin/tld-pricing/route.ts` — admin TLD pricing comparison (customer vs reseller margin). Redis-cached; raw cache shared with customer `/api/domains/pricing`.<br>• Admin auth gate FIRST (no admin → 401, no DB connect, no cache get, no RC call)<br>• Cache settings: `tld_pricing_cache_enabled` DEFAULTS TO TRUE when absent<br>• Cache disabled (=== false) → cacheGet/cacheSet NOT called<br>• `tld_pricing_cache_ttl` applied via setTTL when present<br>• Cache hit happy path: returns cached data with `pricingSource + ' (Cached from Redis)'` suffix + cached:true + cachedAt ISO; NO RC call<br>• **Cache auto-purge anti-stale-empty**: cachedData.totalCount === 0 OR empty tldPricing array OR missing tldPricing → purge + fall through to fresh fetch<br>• Raw cache populated via setRaw on miss (shared with customer-facing /api/domains/pricing endpoint to avoid duplicate RC fetches)<br>• setRaw skipped when caching disabled<br>• **TLD key conversion**: 'dotXXX' → 'XXX' (dotio → io, dotcom → com)<br>• 'codotXXX' → 'co.XXX' multi-level reconstruction (codotuk → co.uk, codotin → co.in)<br>• 'privacy-protection' AND 'premium_dns' SKIPPED (service entries)<br>• Non-dot/non-codot keys SKIPPED (obscure internal codes)<br>• **Price extraction tolerates 4 shapes** per side: nested `addnewdomain.1`, top-level `'1'`, `.price` fallback, raw string<br>• Reseller also supports nested `data['0'].pricing.addnewdomain['1']` (legacy shape)<br>• NaN-producing input → coerced to 0<br>• Both prices 0 → TLD SKIPPED (no entry in response)<br>• **Margin calc**: (customer - reseller) / customer * 100; only when BOTH > 0 (no /0 risk)<br>• Output sorted alphabetically by tld ('.ai' before '.com')<br>• getTldCategory: known TLD → 'Generic'/'Country Code'/'New Generic'; unknown → 'Other'<br>• getTldDescription: known TLD → specific; unknown → 'Domain extension'<br>• **Cache write contract**: non-empty + enabled → .set() with full shape; empty → REFUSES to cache + .purge() (anti-cache-pollution — empty cache from transient RC outage would persist 'no TLDs' until next miss)<br>• Response: success + tldPricing + totalCount + lastUpdated + pricingSource ('ResellerClub API (Indian Pricing)') + cached:false<br>• Outer catch → 500 'Failed to fetch TLD pricing' | ✅ Closed | `2aadf77` |
| Batch 7g6 | **Admin hosting-stats route** (27 / 1 file): `app/api/admin/hosting/stats/route.ts` — admin hosting-stats dashboard. Two-mode contract: LIVE (DA available) iterates DA users + maps to local; FALLBACK DB (DA unreachable) iterates Hosting collection.<br>• Admin auth gate FIRST: AuthService.isAdmin false → 403 FORBIDDEN (NOT 401 — anti-info-leak); NO DB, NO DA call<br>• **5s DA timeout** via Promise.race against setTimeout-reject (Error('DA_TIMEOUT')) — anti-Cloud-Run-slot-stall; DA being slow must not block admin panel >5s<br>• DA_TIMEOUT message branch: 'Connection attempt to DirectAdmin timed out (5s limit)' vs raw err.message for other failures<br>• DA failure (any) → fallback DB mode: isDaAvailable=false + daError captured<br>• LIVE mode happy: source='live', isDaConnected=true, daMode='Live', warning=null; user{name,email} from local; usage + package + phpVersion from DA<br>• 'unlimited' bandwidth/quota → 'Unlimited' display string<br>• PHP version resolution: daConfig.php_version (when not 'Default') → serverInfo.php (when daConfig.php='ON') → 'Default'<br>• **Email-fallback linking**: local user NOT found by daUsername → try by daConfig.email → linkedByEmail=true (handles legacy users with DA account but missing user.directAdminUsername field)<br>• Email lookup SKIPPED when username already matched (no extra DB call)<br>• Neither username nor email lookup → isUnlinked=true; user.name='Unlinked Account', user.email=daConfig.email<br>• **Best-match hosting record (3-tier)**: (1) exact username match, (2) status:'active' match, (3) latest (createdAt:-1 sort → [0])<br>• **Local DB status overrides DA status (anti-stale-DA)**: hostingRecord.status='suspended' OR 'terminated' wins over daConfig-computed status (so if cron updated DB but DA hasn't synced, admin sees correct state)<br>• 'active' DB status does NOT override DA — DA suspended='yes' still wins (override only when DB is suspended/terminated)<br>• **Per-user DA fetch failure → error row (NOT crash)**: getUserConfig throw → row with status='error', domain='Error fetching', error=message<br>• Hosting.find throw → empty [] (inner IIFE catches; doesn't crash the request)<br>• DB MODE: maps from hostingRecords; usage placeholders ('0' / 'Unknown'); status from h.status directly<br>• DB MODE hostingRecord with no matching local user → user='Unknown User'<br>• **daMode detection**: 'Live' (connected) / 'Disconnected' (DA failure regardless of URL) / 'Local' (DIRECTADMIN_URL contains localhost / 127.0.0.1 / host.docker.internal)<br>• Response shape: success + data + source (live/db) + isDaConnected + daError + daMode + warning (null when live)<br>• Outer-catch fallback (2nd-tier resilience path) excluded from coverage by design — interactions between dynamic imports + multiple async chains are flaky to pin; documented in test header | ✅ Closed | `3867ef6` |
| Batch 7g7 | **Admin hosting-provision POST route** (40 / 1 file): `app/api/admin/hosting/provision/route.ts` — admin manual hosting-provision endpoint. Coordinates DA createUser + DNS NS update + Order row + Zoho invoice + Hosting row + email + PendingHosting save-on-failure.<br>• **Schema validation FIRST** (BEFORE admin check — deliberate so outer catch can reference parsed body when provisioning fails later)<br>• Schema: userId ObjectId; domain trim+lowercase min 3 max 253; daUsername min 1 max 14 (DA limit); price non-negative; periodUnit enum (months/days/minutes)<br>• Admin gate: AuthService.isAdmin false → 403 FORBIDDEN<br>• User lookup: getUserById null → 404 USER_NOT_FOUND<br>• **Period clamping**: validityPeriod === 1 ? 1 : 12 (only 1 stays 1; anything else — 6/12/24/undefined — becomes 12; route only accepts monthly or yearly)<br>• periodUnit default 'months'; 'days'/'minutes' pass through for test/QA plans<br>• **DA package name lowercased BEFORE createUser** (DA requires lowercase regardless of admin typing 'PRO'/'Mixed-Case')<br>• createUser signature: (daUsername, email, domain, daPackageNameLowercased)<br>• **DNS update failure SWALLOWED** (doesn't fail provisioning — user.directAdminUsername is the primary commit; DNS is follow-up that admin can retry from dashboard)<br>• **user.directAdminUsername CAS-style first-set**: only assigned when not already set (multi-account-per-user support)<br>• First account stamps hostingCreatedAt + hostingExpiresAt; subsequent accounts do NOT touch those primary fields (per-Hosting row tracks dates instead)<br>• user.save() ALWAYS called (regardless of whether DA fields changed)<br>• Order pending-row contracts: orderId 'ORD-<ts>-<3char-upper>'; paymentId 'ADMIN-PAY-<ts>'; paymentMethod 'admin_provision'; status 'completed'; razorpayPaymentId 'ADMIN-PAY-<ts>'; razorpayOrderId 'ADMIN-RZP-<ts>'<br>• bookingStatus[0]: step 'domain_registered' progress 100 message 'Hosting provisioned by admin'<br>• Order.domains[0]: itemType 'hosting' + dnsProvider 'directadmin' + status 'registered'<br>• **Price resolution 3-tier**: manualPrice > plan.price > 0 (manual provided → wins; absent → plan.price; absent both → 0)<br>• Order.domains[0].hostingPlan reference populated when plan found; undefined when plan NOT found<br>• Zoho createInvoice with paymentMode 'Admin Provision' (distinct from 'Razorpay')<br>• **Bookkeeping side-effects ALL SWALLOWED** (Order create, Zoho invoice, Hosting record, email — each in own try/catch; DA + user already committed, bookkeeping failures don't undo provisioning)<br>• createHosting payload: userId + domainName + planId + name + serverPackage + status:'active' + dates + directAdminUsername + orderId + paymentId<br>• sendHostingProvisionedEmail with (email, firstName, {domainName, packageName, serverIp:DA_SERVER_IP, nameservers:DA.NAMESERVERS})<br>• user.firstName fallback to 'User' when undefined<br>• **DA error 503 OR code 'DA_SERVER_DOWN'** → 503 with 'DA_SERVER_DOWN' code (admin-visible — distinguishes upstream outage from logic bugs); does NOT save to PendingHosting<br>• **Other provisioning failures**: createPendingHosting row stamped (userId/domain/package/daUsername/error/status:'failed') + 200 with savedToPending:true (anti-loss: even when DA call fails, admin gets a queueable record to retry later)<br>• PendingHosting save failure → 500 PROVISION_FAILED (terminal — admin must retry the form) | 🔄 Committed, deploy pending | `70d6219` |
| Batch 7g8 | **Admin hosting-packages GET+POST+PATCH route** (40 / 1 file): `app/api/admin/hosting/packages/route.ts` — three handlers: GET (list DA + sync to DB), POST (create both DA + DB), PATCH (update DB + rotate Razorpay plans on renewal-price change).<br>• GET admin gate → 403 FORBIDDEN<br>• GET 5s DA timeout via Promise.race; DA failure → fallback DB-only (HostingPlan.find({isActive:true}))<br>• GET new DA package not in DB → HostingPlan.create with config-matched id+name+price (matches HOSTING_PLANS by serverPackage)<br>• GET new DA package with NO config match → raw package name + price 0<br>• GET 'unlimited' quota/bandwidth (case-insensitive) → -1 sentinel; other string → parseInt or 0<br>• GET existing DB plan: updates quota/bandwidth/details; preserves price > 0<br>• **GET price-recovery**: DB plan.price === 0 → re-look-up config and use config.price (legacy rows created before pricing was wired in)<br>• GET price=0 + no config match → stays 0 (no spurious override)<br>• GET per-package getPackageDetails failure SWALLOWED (other packages still processed)<br>• GET response: success + data + source ('live' vs 'db') + warning (null when live)<br>• GET outer catch: tries DB-only fallback ONCE more; if that ALSO fails → 500 PACKAGES_FETCH_FAILED<br>• POST admin gate → 403<br>• POST schema: packageName min 1 max 100; quota/bandwidth union(string|number); .passthrough() for arbitrary DA flags<br>• POST createPackage signature: (packageName, {quota:str, bandwidth:str, ...otherOptions}) — both coerced to string at boundary<br>• POST HostingPlan.create: planId=name=packageName, currency='INR', description default "", features default [], price default 0, quota/bandwidth parsed via parseInt<br>• POST DA createPackage throw → 500 PACKAGE_CREATION_FAILED (DB skipped)<br>• PATCH admin gate → 403; findById null → 404 NOT_FOUND<br>• PATCH schema: id ObjectId required; name max 200; description max 2000; price/renewalPrice non-negative<br>• PATCH each field updated ONLY when defined (anti-overwrite-with-undefined); isActive=false is the only safe falsy update<br>• **PATCH Razorpay rotation triggers**: renewalPrice CHANGED OR razorpayPlans missing OR razorpayPlans.monthly missing OR razorpayPlans.yearly missing<br>• PATCH monthly plan: createPlan(`${plan.name} - Monthly`, ..., plan.renewalPrice, 'monthly')<br>• **PATCH yearly plan: createPlan with plan.renewalPrice × 12** (matches current pricing logic — no yearly discount applied)<br>• PATCH razorpayPlans = {monthly: monthlyPlan.id, yearly: yearlyPlan.id}<br>• **PATCH Razorpay failure SWALLOWED** — plan.renewalPrice + other updates still saved locally (warning state, not failure)<br>• PATCH plan.save() ALWAYS called<br>• PATCH outer catch → 500 UPDATE_FAILED | 🔄 Committed, deploy pending | `pending` |

**All four HIGHs + 11 MEDIUMs + 11 LOWs + 2 architectural suggestions (S2 + S3) cleared, M6 / L12 cleared, **M14 COMPLETE** (every component file + every hook has a test); S1 effectively closed.** 206 vertical slices shipped across batches 7a–7gi (6 RC ops + 6 DA ops + vocab unification + M13 cleanup + L1 Razorpay typed-client + fifty-nine M14 slices + 71 lib-utility slices + 1 middleware-security slice + 4 payment-route slices + 3 user-data route slices + 5 admin-route slices + L8 error boundaries + L12 jsx-a11y lint + M6 leaf demotion + S2 pre-commit hooks + S3 complete twelve-slice sweep + twenty-four S1 frontend-apiClient slices + **9 new rescan-4 route-handler slices 7ga–7gi shipped 2026-06-08**: admin orders, public TLD pricing, customer hosting stats, email-change flow, nameserver IDOR, public contact form, watch-list, **Razorpay webhook 5-layer**, customer dashboard). Bonus catches:
- 7e: M3's tightened `BookingStep` type uncovered a real save-validation bug — `provisioner-hosting.ts:379` emits `step: "hosting_deferred"` but the schema enum didn't include it.
- 7f: L6's `Redis | null` typing exposed 3 latent null-deref sites (rate-limit, razorpay webhook, tld-pricing-cache) — each guarded.
- 7g–7r: M1 vertical slices establish the `lib/integrations/{resellerclub,directadmin}/` pattern. ~35 `toLowerCase().includes()` chains removed from app code (down from ~50); the inner/outer classification layers now share one fragment vocabulary; payment + admin + cron paths all branch on typed outcomes instead of message strings.
- 7s: With the typed outcomes in place, the 7-arm `error.message.includes` chain in verification-error.ts turned out to be matching strings nobody throws — deleted (−65 LOC).
- 7ep: `lib/services/payment/order-creator.validateNoRestrictedDomains` calls `requiresAdditionalDetails(item.domainName)` with the FULL domain string (e.g. `"example.au"`), but `DOMAIN_REQUIREMENTS` keys are bare TLDs (`".au"`). The lookup therefore never matches real cart items — the gate is currently a no-op and the restricted-TLD enforcement relies on registrar-side rejection as the actual backstop. Tests pin both the no-op behaviour AND the works-with-bare-TLD case so any future call-site fix (parse the TLD first) surfaces here.

Remaining work: 2 MEDIUMs (M4 1000+ line page components, M6 deeper client-component refactors; M8 deferred pending prod data audit). **S1 effectively closed** (46/~58 frontend files on `apiClient`; rest is by-design raw fetch). **M14 substantially addressed** — cart-validation helpers + cartStore + 14 component/page suites across the cart-page subtree, checkout redirect gating, admin invoice-diagnostics triage, auth password-reset forms, public-facing forms, operational status badges, profile-completion form, and the full domain-search subtree (775 tests across 48 files); remaining gaps are the large multi-step `MultiStageRegisterForm`, the admin/dashboard layout shells, and the deliberately-out-of-scope payment-handler / SSE flows.

---

## Production-impacting (HIGH) — RESOLVED

### ✅ [H1] Cart store server-sync silently broken for NextAuth users
**File:** [store/cartStore.ts](store/cartStore.ts) — lines 158, 190, 205, 214, 296, 328, 356, 413
**Problem:** Every server-sync path (`addItem`, `removeItem`, `updateItem`, `clearCart`, `loadFromServer`, `saveToServer`, `mergeWithServerCart`, the rehydrate hook) gates on `safeLocalStorage.getItem("token")`. After Batch 6d's NextAuth-only auth migration, `token` is never written by the credentials login path — only by `activate/page.tsx` and `RegisterForm.tsx`. Users who log in via `/login` have `token === null`, so their cart never persists server-side even though `/api/cart` happily accepts the NextAuth cookie.
**Fix:** Replace the localStorage gate with `useSession().status === "authenticated"`, or just always attempt the fetch with `credentials: "include"` and fail-soft on 401.
**Effort:** ~1 hour.

### ✅ [H2] PendingDomain unique index is global-per-domainName but the L5 upsert was scoped to (domainName, userId)
**File:** [models/PendingDomain.ts](models/PendingDomain.ts) — `index({ domainName: 1 }, { unique: true, partialFilterExpression: { isArchived: { $ne: true } } })` + [lib/services/payment/provisioner-verification.ts:151-154](lib/services/payment/provisioner-verification.ts)
**Problem:** Rescan-1's L5 fix scoped the bulk-upsert filter to `(domainName, userId)` so two users' failed registrations stay as separate audit rows. But the schema's unique index was left global. So when user B's registration for the same name fails, the upsert filter doesn't find user A's row, tries to insert a new one, and the unique index throws E11000 — a worse outcome than the pre-fix silent overwrite. The fix is half-wired.
**Fix:** Change the partial unique index to `{ domainName: 1, userId: 1 }` (still partial on `isArchived: {$ne: true}`).
**Effort:** ~10 min + migration to drop/recreate the index.

### ✅ [H3] Dead Bearer-token plumbing across 25+ frontend files
**Files:** Rough list — `app/checkout/page.tsx`, `app/dashboard/{page,dns-management,orders,settings,domains/[id],hosting}/page.tsx`, `app/cart/page.tsx`, `app/admin/{support-tickets,support-tickets/[id],dns-management,hosting,hosting/pending,hosting/packages,domains,settings,system-settings,pending-domains,invoices/[id]/view}/page.tsx`, `components/{HostingUpgradeModal,HostingRenewalModal,DomainRenewalModal,ProfileCompletionForm}.tsx`, `lib/fetcher.ts`.
**Problem:** Batch 6d's L2 only swept 5 admin pages. The remaining files still construct `Authorization: Bearer ${safeLocalStorage.getItem("token")}` headers. For NextAuth-credential-logged-in users, `token` is null → the header is literally `Bearer null`. Auth still works because `AuthService.getUserFromRequest` falls through to the NextAuth cookie, so it's cargo-cult — but it masks any future bug where the cookie fallback breaks.
**Fix:** Sweep the remaining files, dropping the localStorage-token branch and standardising on `credentials: "include"`.
**Effort:** ~3 hours.

### ✅ [H4] MongoDB `maxPoolSize: 10` is too low for Cloud Run concurrency
**File:** [lib/mongodb.ts:48](lib/mongodb.ts)
**Problem:** Cloud Run defaults to 80 concurrent requests per instance. Every concurrent request that hits Mongo queues behind a 10-connection pool. Under steady load the latency floor for any DB call becomes `(queue depth × avg query duration)`.
**Fix:** Bump `maxPoolSize` to 30–50 and document the relationship with Cloud Run `--concurrency`.
**Effort:** ~15 min + a load test.

---

## Real flaws (MEDIUM)

### Architecture

#### ✅ [M1] No anti-corruption layer between routes/services and DirectAdmin / ResellerClub wire shapes — CLOSED (12 slices shipped, batches 7g–7r)
**Files:** 57 callsites of `DirectAdminService.*` and 65 of `ResellerClubAPI.*`/`ResellerClubWrapper.*` directly inside `app/api/**` + `lib/services/payment/`. 51 `toLowerCase().includes(...)` chains parsing upstream English across the codebase.
**Problem:** Routes and provisioners read raw upstream response shapes (`result.status === "success"`, `result.message.toLowerCase().includes("insufficient balance")`). Any wording change at ResellerClub silently flips success → failed.
**Fix:** Introduce `lib/integrations/{resellerclub,directadmin}/` modules that translate raw responses into typed `Result<Outcome, ErrorVariant>` unions. Callers branch on the variant, not the message text. Multi-week effort; can be incremental starting with the provisioner-domain helpers.

### TypeScript / Schema drift

#### ✅ [M2] Schema drift on `User.cart` and `Order.domains.hostingPlan`
**Files:** [models/User.ts:60-72](models/User.ts) (interface) vs `:291-330` (schema); [models/Order.ts](models/Order.ts) vs [lib/types.ts:31](lib/types.ts).
**Problem:** TS interface declares `cart: {…}` while the schema accepts `periodUnit` and `linkedDomain` that the interface omits. Three different `hostingPlan` shapes across cart (`{name, period, features, serverPackage?}`), Order subdoc (`{planId, name, serverPackage}`), and `CartItem` in `lib/types.ts`.
**Fix:** Single `CartItem`/`HostingPlan` type defined once, shared by Mongoose schema, `lib/types.ts`, and the cart store. ~2-3 hours.

#### ✅ [M3] `IOrder.domains.bookingStatus.step` is a tight enum in the schema but `string` in OrderDomain
**Files:** [models/Order.ts:31-39](models/Order.ts) (8-variant enum) vs [lib/services/payment/provisioner.ts:48](lib/services/payment/provisioner.ts) (`step: string`).
**Fix:** Define `BookingStep` once, share with both. ~30 min.

### Frontend

#### [M4] Five page components have crossed the 1000-line threshold
**Files:** `app/dashboard/dns-management/page.tsx` (1597), `app/admin/user-management/page.tsx` (1520), `app/admin/system-settings/page.tsx` (1387), `app/admin/hosting/page.tsx` (1375), `app/admin/dns-management/page.tsx` (1192), `app/admin/settings/page.tsx` (1008). Each carries 30-35 `useState` hooks.
**Fix:** Extract per-domain hooks + sub-components, starting with `dns-management/page.tsx` as the template. ~1 day per page.

#### ✅ [M5] `<Invoice />` is dead but static-imports jsPDF + html2canvas (~500KB)
**File:** [components/Invoice.tsx:5-6](components/Invoice.tsx)
**Problem:** Zero importers exist. 250-line dead component pulling 500KB of vendor code into any page that transitively touches `components/index.ts`. Same class as the M4 orphan-model deletion from rescan-1.
**Fix:** Delete the file. If retained for future use, dynamic-import jsPDF/html2canvas only on click.

#### 🔄 [M6] 78% of components are `'use client'` — PARTIALLY ADDRESSED (slice 7x demoted 14 leaves)
**Problem:** 134 of 172 page/component .tsx files are client-rendered, defeating App Router's perf model. Navigation header, footer, FAQ-item, EmptyState, Card, StatsCard, Skeletons could mostly render on the server.
**Fix:** Audit `components/*.tsx` for `'use client'` directives that don't actually need state/effects; demote the leaves. ~1 day.
### Database

#### ✅ [M7] Redundant indexes in three models
**Files:**
- [models/Domain.ts:143](models/Domain.ts) (`{ next_action_at: 1 }`) is a prefix of `:147` (`{ next_action_at: 1, processing_until: 1 }`)
- [models/Domain.ts:142](models/Domain.ts) (`{ userId: 1, status: 1 }`) is a prefix of `:150` (`{ userId: 1, status: 1, expiresAt: 1 }`)
- [models/SystemLog.ts:29](models/SystemLog.ts) (`service: { index: true }`) is a prefix of `:44` (`{ service: 1, createdAt: -1 }`)
**Fix:** Drop the prefix-redundant single-field indexes. ~30 min + migration.

#### ⏸ [M8] `PendingDomain._id` is `Schema.Types.Mixed` — DEFERRED (needs prod data audit before backfill)
**File:** [models/PendingDomain.ts:34](models/PendingDomain.ts)
**Problem:** Accepts both ObjectId and String. Every read/write must remember which kind of id this row has.
**Fix:** Pick ObjectId; backfill any String-keyed rows. ~2 hours + migration.

### Operational

#### ✅ [M9] `/api/health` is shallow — passes when Mongo is unreachable
**File:** [app/api/health/route.ts](app/api/health/route.ts)
**Problem:** Returns 200 immediately with no DB/Redis/DA/RC probe. Cloud Run keeps promoting a process whose deps are broken.
**Fix:** Add `/api/health/deep` with `connectDB()` ping + Redis `PING` + last-DA-heartbeat-age. Point readiness probes at deep, keep `/api/health` shallow for liveness.

#### ✅ [M10] `AuthService.getUserFromRequest` emits `warn` on every unauthenticated request
**File:** [lib/auth.ts](lib/auth.ts) — `serverLogger.warn` lines on session fallthrough.
**Fix:** Demote to `debug` or remove — route layer already returns 401.

#### ✅ [M11] Hardcoded prod DA IP `136.115.64.54` as fallback default in 5 places
**Files:** `lib/services/payment/{renewal,provisioner-hosting}.ts`, `lib/services/pending-hostings.ts`, `lib/directadmin/users.ts:109`, `app/api/admin/hosting/provision/route.ts:265`
**Problem:** Forgotten env in staging/dev silently provisions onto the prod DA box.
**Fix:** Single `DA_DEFAULT_IP` constant in `config/constants.ts`; throw if missing in production. ~15 min.

### Code Quality

#### ✅ [M12] Webhook renewal-order does `new Order(...).save()` directly + `Math.random()` suffix
**File:** [lib/services/payment/webhook-handlers.ts:253](lib/services/payment/webhook-handlers.ts)
**Problem:** Last `new Order(...)` outside the service layer (audit said H1 closed this class). `orderId = \`ORD-RNW-${Date.now()}-${Math.floor(Math.random() * 1000)}\`` — same Math.random class that batch 1's [M3] fixed for invoice numbers but only ~1000 suffixes, will collide under burst.
**Fix:** `createRenewalOrder(input)` helper + `crypto.randomBytes(4).toString("hex")`. ~30 min.

#### ✅ [M13] String-message-based error dispatch in `verification-error.ts` — CLOSED (slice 7s removed the dead chain)
**File:** [lib/services/payment/verification-error.ts:219-266](lib/services/payment/verification-error.ts)
**Problem:** `else if (error.message.includes("Invalid payment signature"))` × 7. Any upstream wording change flips errors into the generic 500. 20 such `error.message.includes` patterns across `lib/`.
**Fix:** Throw typed `PaymentError("signature_error")` / `PaymentError("amount_mismatch")` from inner helpers; dispatch on `.code`. ~3 hours.
### Testing

#### 🔄 [M14] Zero component / page / cart-store tests — SUBSTANTIALLY ADDRESSED (783 tests / 49 files: cart-validation + cartStore + 15 component suites incl. the full domain-search subtree, cart-page subtree, admin invoice-diagnostics, auth + registration forms, public-facing forms, status badges; remaining: layout shells)
**Problem:** 588 tests are all model/service/integration. 0 of 172 .tsx files have a sibling `.test.tsx`. 1500-line admin pages and the cart store (more complex than several services by line count) are entirely unverified.
**Fix:** Start with the cart store (pure logic) and the checkout `useEffect` redirect logic. Vitest + @testing-library/react is already transitively available. ~3 hours harness + incremental tests.
#### ✅ [M15] Integration test suite runs serially (`fileParallelism: false`)
**File:** [vitest.integration.config.ts:30](vitest.integration.config.ts)
**Problem:** 159 tests across 16 files run serially. Will scale linearly as coverage grows.
**Fix:** Per-file `beforeAll` spinning up a fresh `MongoMemoryServer` on a random port. ~2 hours.

---

## Cleanups (LOW)

### ✅ [L1] Razorpay SDK escape-hatch casts repeated 10× — CLOSED in batch 7t
**Files:** `lib/razorpay.ts:41,223,244,257,303,338,354`, `lib/razorpay-payments.ts:54,77,93`
**Fix:** Wrap once in a typed `lib/razorpay-client.ts`; collapse the 10 casts to 1.
**Resolution:** New `lib/razorpay-client.ts` constructs the SDK once, sets the 30s timeout, and exposes a strongly-typed `TypedRazorpayClient` facade (`orders` / `payments` / `subscriptions` / `plans` with our `RazorpayPaymentDetails` etc. on the return types). The single `as unknown as TypedRazorpayClient` cast at module scope is the only type-bridging cast left; all seven callsites in `razorpay.ts` and two of the three in `razorpay-payments.ts` are now clean. The third callsite (`getPaymentById`) had zero consumers and was deleted.

### ✅ [L2] Three orphan/duplicate types in `lib/types.ts`
**File:** [lib/types.ts:1-11](lib/types.ts) (`User` duplicates `IUser` and is wrong), `:65-76` (`Payment` — `IPayment` is used directly), `:69-78` (`DNSRecord` — model deleted in M4 batch).
**Fix:** Delete all three; add ESLint rule to flag duplicate `I*`-name interfaces.

### ✅ [L3] Magic numbers `=== 10`, `=== 12` for hosting billing cycle inference (5 sites)
**Files:** `lib/services/payment/post-tasks.ts:68`, `app/checkout/page.tsx:486`, `app/checkout/guest/page.tsx:446`, `app/api/payments/create-order/route.ts:182`, `store/cartStore.ts:44-47`
**Fix:** `lib/billing.ts` exports `inferPeriodUnit(item: CartItem)` returning a strict enum.

### ✅ [L4] `puppeteer` in `dependencies` but only used by test scripts
**File:** `package.json` (puppeteer ^24.28.0, used only in `tests/browser/`)
**Problem:** Adds ~200MB to production node_modules + cold-start working set.
**Fix:** Move to `devDependencies`.

### ✅ [L5] Two parallel client-side loggers with different signatures
**Files:** `lib/logger.ts` (27 consumers, `(...args)` signature) and `lib/client-logger.ts` (2 consumers, `(message, details)` signature). Both POST to `/api/v1/log`.
**Fix:** Pick one shape; migrate; delete the loser. ~2 hours.

### ✅ [L6] `Redis = null as unknown as Redis` when REDIS_HOST is unset
**File:** [lib/redis.ts:22](lib/redis.ts)
**Problem:** Structural lie — every caller of `redis.foo()` will NPE. Currently OK because all callers are inside try/catch, but fragile.
**Fix:** Export typed `Redis | null`; force callers to narrow.

### ✅ [L7] `findUserOrder` still uses `$or` despite Batch 4 [L4] saying it was fixed
**File:** [lib/services/orders.ts:276-281](lib/services/orders.ts)
**Problem:** Sibling `getOrderByIdOrOrderId` was correctly fixed with a single-filter branch; this one was missed. Behaviour fine due to `userId` scope, but description/code disagree.
**Fix:** Mirror `getOrderByIdOrOrderId`. ~10 min.

### ✅ [L8] React error boundaries — CLOSED in batch 7w
**Problem:** A single React render error inside (say) the cart UI tears the whole page tree down to the root `error.tsx`. No mid-tree isolation.
**Fix:** Add `<ErrorBoundary>` around cart, chat widget, floating cart count. ~3 hours.
**Resolution:** Generic `<ErrorBoundary>` (class component, since React's error lifecycles only exist on class components) added at `components/ErrorBoundary.tsx`. Three integrations: `FloatingCart` in the root layout (fallback=null — silent failure preferred to a visible warning on every page), `ChatWidget` on the homepage (fallback=null), and the full cart-page body in `app/cart/page.tsx` (default fallback — the visible reload prompt, since the cart is the primary content). Navigation and Footer stay outside the boundary so a cart crash doesn't tear those down too.

### ✅ [L9] `app/error.tsx` still reads the dead localStorage `token` for "Was user logged in" display
**File:** [app/error.tsx:49-50](app/error.tsx)
**Fix:** Read from `useSession()` (client component, available).

### ✅ [L10] `Order.paymentId` `required:true, unique:true` is write-only/fallback-only after H1 migration
**File:** [models/Order.ts:14,16,122-126](models/Order.ts)
**Problem:** Every renewal-webhook construction fabricates a unique `paymentId` (using Razorpay payment id) just to satisfy the index. `razorpayPaymentId` is the real source of truth and is already indexed.
**Fix:** Drop `required` + `unique`; reads use `razorpayPaymentId ?? paymentId` defensively. ~30 min + migration.

### ✅ [L11] `getUserByIdSafe` is functionally identical to `getUserById` after `password: select:false`
**File:** [lib/services/users.ts:22,33-37](lib/services/users.ts)
**Problem:** Comment claims one is dangerous; in reality neither returns password without explicit `.select("+password")`. Misleads new contributors.
**Fix:** Delete the duplicate; pick the surviving name. ~30 min.

### 🔄 [L12] Accessibility coverage is thin — 10/62 components carry any aria/role attribute — PARTIALLY ADDRESSED (lint setup landed in 7w.2)
**Problem:** No keyboard navigation tests; modals likely lack focus traps + `aria-modal`.
**Fix:** Add `eslint-plugin-jsx-a11y`; run axe-core in CI. ~1 day for lint setup; remediation per component.
---

## Architectural Suggestions (no current bug, structural improvement)

### ✅ [S1] No shared API client in the frontend — 58 files do raw `fetch("/api/...")` — EFFECTIVELY CLOSED (helper in 7aa + 24-slice sweep 7aa–7ax; 46/~58 files migrated, remainder is by-design raw fetch)
**Problem:** Only 8 files use `lib/fetcher.ts`; the rest hand-roll. Every endpoint addition touches N callsites; response error handling is per-file; no central place to attach request-id / tracing.
**Suggestion:** Thin typed `apiClient.get(url, schema)` returning a `Result<T, ApiError>`. Migrate incrementally; pair with Zod schemas at the boundary.
### ✅ [S2] No pre-commit hooks (husky/lefthook); CI is the only gate — CLOSED in batch 7y
**Suggestion:** Husky + lint-staged running `tsc --noEmit` on staged files and `eslint --fix`. ~30 min.
**Resolution:** `husky` + `lint-staged` installed as devDeps; `prepare` script wired so `npm install` auto-installs the git hook. `.husky/pre-commit` runs `npx lint-staged` (eslint --fix on staged `.ts/.tsx/.js/.jsx` per the `lint-staged` config in `package.json`) followed by `npx tsc --noEmit` (project-wide typecheck). Skipping with `--no-verify` is supported but discouraged — the same checks gate CI.

### ✅ [S3] Most API routes parse JSON bodies with no validation — CLOSED (helper + 12-slice sweep across batches 7z–7z.12, all 73 routes validated)
**Files:** `app/api/admin/users/route.ts`, `app/api/domains/dns/route.ts`, `app/api/admin/users/reset-2fa/route.ts` use `Schemas.*.safeParse`. The other 130+ routes do `const { x, y } = await request.json()`.
**Suggestion:** `validatedBody<T>(req, schema)` helper. Sweep highest-risk routes (user mutations, admin writes, payment webhooks) first. Helper ~30 min; sweep is route-by-route.
---

## What surprised me

**Half-finished NextAuth migration.** Batch 6d closed 5 admin pages but ~30% of the frontend still wires Bearer tokens that nobody writes. Three callsites are load-bearing: cart-store (H1), `app/error.tsx`, `app/checkout/page.tsx`'s "credentials login uses JWT token" branch. The system limps along because every API route falls through to a NextAuth-cookie path that hides the bug.

**The PendingDomain L5 fix is half-wired.** App-layer filter was scoped correctly, DB-layer unique index was not — worse outcome than the pre-fix silent overwrite.

**Zero React tests.** 588 tests, 0 component/page/store tests. The 1500-line admin pages and the cart store (more complex than several services) have no automated coverage at all.

**The anti-corruption-layer gap** is structurally the most expensive piece. 51 `toLowerCase().includes(...)` chains + 122 direct upstream-SDK callsites. Every prior rescan flagged some flavor of "RC/DA error leaked / string-typed". The per-finding fixes are correct but they're a Hydra. A proper `lib/integrations/` boundary would close that class of bug permanently.

---
