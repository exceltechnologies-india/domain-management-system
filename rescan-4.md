# Rescan-4 — Whole-Project Audit (2026-05-22)

Fresh audit after rescans 1–3 closed every known H/M/L bug. Different mandate: architecture, code-quality, frontend, schema drift, operational, and DX concerns that prior bug-hunting passes deprioritised.

All four HIGH findings have been verified against the actual code, not just trusted from the audit agent.

---

## Status

| Batch | Findings | Status | Commit(s) |
|---|---|---|---|
| Batch 7a | H1 — cart store NextAuth migration | ✅ Closed | `c4aec87` |
| Batch 7b | H2 — PendingDomain compound unique + prod migration | ✅ Closed | `45e8e85`, `caece03` |
| Batch 7c | H3 — Bearer-token cleanup (38 files, −917 lines) | ✅ Closed | `6bdd43b` |
| Batch 7d | H4 — Mongo `maxPoolSize` 10 → 50 + Cloud Run pairing | ✅ Closed | `9b60a9f` |
| Batch 7e | M2 + M3 + M5 + M7 + M9 + M10 + M11 + M12 + M15 + L2 | ✅ Closed | `8aee422` |
| Batch 7f | L3 + L4 + L5 + L6 + L7 + L9 + L10 + L11 | ✅ Closed | `5ee9c94` |
| Batch 7g | M1 slice 1 — RC `registerDomain` anti-corruption (13 tests) | ✅ Closed | `67390e4` |
| Batch 7h | M1 slice 2 — DA `createUser` anti-corruption (7 tests) | ✅ Closed | `6391de2` |
| Batch 7i | M1 slice 3 — RC `renewDomain` + /domains/renew 202-on-pending | ✅ Closed | `2fca12a` |
| Batch 7j | M1 slice 4 — inner/outer RC fragment vocab unified | ✅ Closed | `c31bf1e` |
| Batch 7k | M1 slice 5 — DA `suspendUser` + expiry-worker not-found skip | ✅ Closed | `292802c` |
| Batch 7l | M1 slice 6 — DA `unsuspendUser` + 4 callsite migrations | ✅ Closed | `90ac494` |
| Batch 7m | M1 slice 7 — RC `transferDomain` + user-actionable 400 on registry-reject | ✅ Closed | `facfb87` |
| Batch 7n | M1 slice 8 — DA `getUserConfig` + sync-worker inline-parser removed | ✅ Closed | `23fda8e` |
| Batch 7o | M1 slice 9 — DA `deleteUser` + admin/hosting/actions sweep | ✅ Closed | `ba451f2` |
| Batch 7p | M1 slice 10 — RC `getDomainOrderId` + `getDomainDetails` (4 callsites, 18 new tests) | ✅ Closed | `c76ce35` |
| Batch 7q | M1 slice 11 — RC `getDNSRecords` (2 callsites, 9 new tests) | ✅ Closed | `a4b4422` |
| Batch 7r | M1 slice 12 — DA `changePackage` + upgrade-flow + admin route (7 new tests) | ✅ Closed | `26d201f` |
| Batch 7s | M13 partial — delete dead `error.message.includes` chain in verification-error.ts (-65 LOC) | ✅ Closed | `299f84c` |
| Batch 7t | L1 — typed Razorpay SDK client (10 `as unknown as` casts → 1) | ✅ Closed | `9b6d018` |
| Batch 7u | M14 partial — extract pure cart-validation helpers + 21 unit tests | ✅ Closed | `0011477` |
| Batch 7v | M14 continued — store-level cart tests (addItem/removeItem cascade, getters, syncWithServer) — 20 new tests | ✅ Closed | `12d2cbd` |
| Batch 7w | L8 — React error boundaries (FloatingCart + ChatWidget + CartPage) | ✅ Closed | `2339a2a` |
| Batch 7w.2 | L12 partial — enable `plugin:jsx-a11y/recommended` (lint surface, 144 warnings flagged) | ✅ Closed | `7c466b5` |
| Batch 7x | M6 partial — demote 14 leaf components from `'use client'` to server | ✅ Closed | `0f35e5f` |
| Batch 7y | S2 — pre-commit hooks (husky + lint-staged + tsc --noEmit) | ✅ Closed | `2660cfe` |
| Batch 7z | S3 partial — `validatedBody` / `validatedQuery` helpers + 5-route sweep (10 new tests) | ✅ Closed | `067da79` |
| Batch 7z.2 | S3 continued — 6 more routes validated (payments/create-subscription, hosting/renew, hosting/cancel-trial, hosting/auto-renew, domains/nameservers, settings/change-email) | ✅ Closed | `e87ad6a` |
| Batch 7z.3 | S3 continued — 5 more routes (admin/razorpay-mode discriminated-union, hosting/upgrade, contact, user/support, contact `.sanitized` typing fix) | ✅ Closed | `8b4aac3` |
| Batch 7z.4 | S3 continued — 6 more routes (user/support/[id], admin/support-tickets/[id], trial-otp send + verify, trial-eligibility, chat) | ✅ Closed | `1dfc073` |
| Batch 7z.5 | S3 continued — 6 admin write routes (hosting/change-package, hosting/actions discriminated-union, hosting/assign, users/reactivate, users/reset-password, log-error) | ✅ Closed | `a978849` |
| Batch 7z.6 | S3 continued — 6 more admin write routes (domains/nameservers, domains/activate-dns, domains/sync, diag-da/cleanup, reset-password, tld-pricing/cache) | ✅ Closed | `60cf32f` |
| Batch 7z.7 | S3 continued — 6 admin routes (hosting/test-plan, hosting/packages POST + PATCH, hosting/provision, backup, users/[id] PUT, pending-domains/verify) | ✅ Closed | `ba4f8d5` |
| Batch 7z.8 | S3 continued — 6 routes (admin/settings, admin/pending-domains POST, auth/activate, workers/sync-zoho-invoice, workers/sync-hosting-status, workers/process-hosting-expiry) | ✅ Closed | `d524fd9` |
| Batch 7z.9 | S3 continued — 6 routes (workers/process-service-expiry, auth/check-account-status, totp/confirm, totp/disable, resend-activation, admin/domains/dns POST + DELETE) | ✅ Closed | `33ee8ff` |
| Batch 7z.10 | S3 continued — 6 user-facing domain routes (search, activate-dns, renew GET + POST, booking-status GET + POST, verify-status, transfer) | ✅ Closed | `993272a` |
| Batch 7z.11 | S3 continued — 6 routes (log, domains/bulk-search, domains/nameservers, cart, test/automation/trigger, admin/pending-domains/[id]) + latent bug fix | ✅ Closed | `a9317ec` |
| Batch 7z.12 | S3 complete — 3 payment routes (verify, guest/verify, guest/create-order) — **73/73 routes validated** | ✅ Closed | `5355331` |
| Batch 7aa | S1 partial — typed frontend `apiClient` (`ApiResult<T, ApiError>` + Zod-aware) + 3 migrated callsites + 12 new tests | ✅ Closed | `bd4edbf` |
| Batch 7ab | S1 continued — 5 more frontend callsites migrated to `apiClient` (AdminPasswordReset, ProfileCompletionForm, DomainBookingProgress, DomainCrossSell, GoogleRecaptcha) | ✅ Closed | `6d6b714` |
| Batch 7ac | S1 continued — 4 more frontend callsites (ResetPasswordForm, LivePricingIndicator, TrialOtpModal ×2, DomainSetup) | ✅ Closed | `e9c5458` |
| Batch 7ad | S1 continued — 3 more components, 6 fetches (MultiStageRegisterForm, DomainSearch watch, InvoiceDiagnostics ×4) | ✅ Closed | `ba0602c` |
| Batch 7ae | S1 continued — first app/ pages (maintenance, activate ×3) + `cache` option added to apiClient | ✅ Closed | `cbe3301` |
| Batch 7af | S1 continued — 2 admin pages (invoices list-GET, pricing-management GET + DELETE) | ✅ Closed | `02b2d7f` |
| Batch 7ag | S1 continued — 3 pages (domains/transfer, domains/bulk-search, admin/domains GET + sync POST) | ✅ Closed | `d390f38` |
| Batch 7ah | S1 continued — dashboard support pages (create-ticket POST, ticket close PATCH + reply POST) | ✅ Closed | `9bd22ea` |
| Batch 7ai | S1 continued — admin support-tickets pages (auth/me + list/detail GETs, reply POST, status + priority PATCH) | ✅ Closed | `c7ed08f` |
| Batch 7aj | S1 continued — admin hosting pending + packages pages (list GETs, retry POST, delete, package PATCH) | ✅ Closed | `9a64032` |
| Batch 7ak | S1 continued — admin order-management (orders GET + delete/archive + unarchive PATCH) + payment-management (payments GET) | ✅ Closed | `9e6f75e` |
| Batch 7al | S1 continued — admin settings/security 2FA page (totp setup GET + POST, confirm POST, disable POST) | ✅ Closed | `525f521` |
| Batch 7am | S1 continued — admin hosting main page (stats GET, provision-deps 2×GET, provision/actions/change-package POSTs, details GET) | ✅ Closed | `ccbef5e` |
| Batch 7an | S1 continued — admin user-management (3×GET parallel, reset-pw/reactivate/reset-2fa POSTs, deactivate + permanent-delete) + apiClient.delete body support | ✅ Closed | `bdd905f` |
| Batch 7ao | S1 continued — admin pending-domains (balance GET, auth/me GET, list GET, register/verify POSTs, archive + permanent DELETEs, resolve + retry PUTs) | ✅ Closed | `a8f5b17` |
| Batch 7ap | S1 continued — admin system-settings (12 fetches: razorpay-mode GET/POST×2, settings GET×3 + POST×4, check-ip GET; backup blob stays raw) | ✅ Closed | `c2646f5` |
| Batch 7aq | S1 continued — dashboard settings (8 fetches: user-settings GET + PUT×2, auth/me GET, totp setup GET/POST + confirm/disable POST; nominatim geocode stays raw) | ✅ Closed | `bb55a93` |
| Batch 7ar | S1 continued — admin dns-management (10 fetches: domains/dns/nameservers GETs, NS default/custom POSTs, activate-dns POST, record add POST, delete + edit DELETEs-with-body) | ✅ Closed | `702c9a8` |
| Batch 7as | S1 continued — dashboard dns-management (12 fetches: services-status + domains + dns + nameservers GETs, NS default/custom POSTs w/ polling, record add POST, delete + edit DELETEs-with-body + restore POST, activate-dns POST) | ✅ Closed | `402fd5d` |
| Batch 7at | S1 continued — admin settings (22 fetches: 9 GET loaders incl. paired cache load, cache PUT + DELETE, 2 check-ip GETs, 8 settings save POSTs, test-plan POST; removed dead authHeaders helper) | ✅ Closed | `547ab37` |
| Batch 7au | S1 continued — dashboard hosting (cancel-trial POST, auto-renew PATCH) + dashboard domains/[id] (services-status + domains + nameservers GETs, NS update POST) | ✅ Closed | `0e8b093` |
| Batch 7av | S1 continued — public hosting page (trial-eligibility POST, test-plan GET) + dashboard home (domains/sync POST) + dashboard invoices (invoices/sync POST) | ✅ Closed | `ac60d54` |
| Batch 7aw | S1 continued — useDomainSearch hook (3 search POSTs: Phase-1 quick, Phase-2 fire-and-forget suggestions, load-more) — one of the two 7ad-deferred items | ✅ Closed | `036f161` |
| Batch 7ax | S1 continued — RegisterForm register POST (the last 7ad-deferred holdout; its 3 cross-origin geocoding fetches stay raw by design) | ✅ Closed | `4d3ac2c` |
| Batch 7ay | M14 continued — first React component tests: `@testing-library/react` harness + `<EmptyCart>` (2) + `<CartOrderSummary>` (10) | ✅ Closed | `f3a3cf9` |
| Batch 7az | M14 continued — `<CartItemCard>` (14): domain/hosting fork, period-label formatting, .ai min-period annotation, billing-cycle lock, price math, multi-year + annual-hosting badges, callbacks | ✅ Closed | `56cc55d` |
| Batch 7ba | M14 continued — CheckoutPage redirect gating (9): admin → /admin/dashboard, incomplete profile → /cart + toast, non-ok auth/me → /login, fetch throw → /login, strict-true check for profileCompleted, loading-status no-op, completed-profile no-redirect; plus the empty-cart-after-load → /dashboard replace | ✅ Closed | `a3e9cde` |
| Batch 7bb | M14 continued — `<ProfileCompletionWarning>` (12): show/hide gating, session+local merge precedence, missing-field grammar (singular vs "X and Y are"), trim check, Complete-now redirect with/without returnUrl, Dismiss hide, profileUpdated event-driven re-check | ✅ Closed | `e243609` |
| Batch 7bc | M14 continued — `<HostingUpsell>` (5): card render (heading + ₹/strike-through/Save-50%/features), add-to-cart CartItem shape (period 12 + yearly + hostingPlan.id), auto-link to existing domain via `linkedDomain`, dedupe with error toast when same plan already in cart, legacy-domain-no-itemType still auto-links | ✅ Closed | `77c745a` |
| Batch 7bd | M14 continued — `<DomainCrossSell>` (8): disabled-when-empty submit, no-dot auto-append `.com`, available/taken/success-false panels, apiClient-failure error toast, exact-match preference, typing clears result, add-to-cart CartItem shape (period 12, itemType domain) | ✅ Closed | `ea272ac` |
| Batch 7be | M14 continued — `<DomainSetup>` (11): heading + plan-name fallback; Link tab — required + invalid-format errors, typing clears error, valid-link 500ms-delay onUpdateDomain + success toast; Buy tab — tab switch, .com auto-append, available/taken panels, apiClient failure, Add & Link fires both onAddDomainToCart (period 1) + onUpdateDomain and clears search state | ✅ Closed | `1dde2fc` |
| Batch 7bf | M14 continued — admin invoice-diagnostics subtree (21 tests / 3 files): `<DiagnosticsHeader>` (6) — issue-vs-clear icon + copy, singular/plural grammar, row-toggle vs refresh-chip stopPropagation, loading spinner; `<ConflictsTable>` (7) — empty null-render, group header, per-row fields, isDeleted tag, zohoInvoiceId truncation + title tooltip, Clear # callback, pendingId disable; `<StuckOrdersTable>` (8) — empty null-render, missing-zoho fallback, single-order hides Re-sync-all, multi-order shows '(N)' + onResyncAll, per-row onResync, bulkProgress bar/counts/label, pendingId disable | ✅ Closed | `b2c3a62` |
| Batch 7bg | M14 continued — auth password-reset forms (11 / 2 files): `<AdminPasswordReset>` (5) — empty/mismatch disable, < 8 chars toast, payload + success-clears-form, route's error.message; `<ResetPasswordForm>` (6) — default vs isSetup headings & button labels, mismatch + length toasts, `{token, password, recaptchaToken}` payload, success-screen swap (Password Reset Complete vs Account Activated copy) | ✅ Closed | `cf4b7be` |
| Batch 7bh | M14 continued — public-facing forms (10 / 2 files): `<ContactForm>` (4) — render, happy-path payload + success-swap, "Send Another Message" return, route's error toast; `<ForgotPasswordForm>` (6) — default vs isSetup headings & button labels, prefilledEmail pre-population, happy-path payload + sent-screen mirror, isSetup-flavoured sent copy/toast, failure inline + toast | ✅ Closed | `83f1620` |
| Batch 7bi | M14 continued — `<LivePricingIndicator>` (5): mount-fetch + price extraction from `data[tld].customer.addnewdomain["1"]` + onPriceUpdate, "Live" badge gating, "Live pricing not available" (success-but-tld-missing) vs "Failed to fetch live pricing" (apiClient !ok), Refresh re-fetch | ✅ Closed | `f1667d6` |
| Batch 7bj | M14 continued — `<TrialOtpModal>` (10): isOpen render gate, defaultPhone normalisation, 10-digit gate on Send OTP, /send + stage-swap on success vs error toast on failure, /verify + sessionStorage token stash + onVerified, 2xx-no-token treated as failure, Change-number resets stage + clears code, Close → onClose | ✅ Closed | `7857dc2` |
| Batch 7bk | M14 continued — operational status leaves (10 / 2 files): `<OutboundIPBadge>` (5) — loading badge, single-IP green vs multi-IP orange + "*" + title-tooltip, success:false + apiClient !ok both surface "IP Error"; `<DomainBookingProgress>` (5) — loading, empty-status fallback, progress % + step messages, domain_registered → onComplete + success panel highlighting domainName, Refresh re-fetch | ✅ Closed | `8515648` |
| Batch 7bl | M14 continued — `<ProfileCompletionForm>` (6): disabled name/email render, forced phoneCc=+91 + address.country=IN on submit, localStorage merge + profileUpdated window event, /dashboard fallback vs `?returnUrl=` honoured, failure-path error toast with no navigation, GST input upper-casing | ✅ Closed | `d39ef58` |
| Batch 7bm | M14 continued — `<SearchInput>` (9): value mirroring, onChange per keystroke, onSearch via button click + Enter-in-container, empty/searching disable rules, isSearching disables input + spinner+label swap, "We'll check .com, .net, .in and more for …" prompt visibility in multi mode / suppression after first search / suppression in single mode | ✅ Closed | `1729a96` |
| Batch 7bn | M14 continued — `<SearchResults>` (10): AnimatePresence pre-render gate, isSearching loading splash, HeroResultCard for results[0] + CompactResultCards for the rest, single-result omits "Other Popular Extensions", inline error banner, Clear Search callback, suggestions skeleton while loading, category-tab filtering of suggestions (All vs Tech etc.), Load More button + isLoadingMore disabled+"Loading More…" swap | ✅ Closed | `38f7587` |
| Batch 7bo | M14 continued — `<HeroResultCard>` + `<CompactResultCard>` (10): available vs taken render forks, Intl en-IN INR price formatting (incl. the 1.5× strike-through on hero), onAdd / onWatch wiring with onWatch optional / N/A disabled fallback, "SAVE N%" computation from originalPrice, "Live Price" badge when pricingSource==='live' | ✅ Closed | `ab35b6c` |
| Batch 7bp | M14 continued — `<MultiStageRegisterForm>` (8): rendering, client validation gate (empty + password-mismatch), apiClient payload (no confirmPassword), happy-path with requiresActivation true/false + returnUrl propagation, Zod field-error tree mapped to per-field inline errors, generic toast fallback, typing clears the inline error | ✅ Closed | `81c84a0` |
| Batch 7bq | M14 continued — small leaves (10 / 2 files): `<FAQItem>` (4) — render, default-closed, uncontrolled toggle on click + chevron rotate, controlled mode via onToggle + isOpen prop; `<MessageAttachments>` (6) — null/empty render-nothing, anchor shape (href/download/target/rel + title with filename+size-in-KB), inner img alt, multiple attachments, align-prop → justify-class mapping incl. default 'left' | ✅ Closed | `c02ce4d` |
| Batch 7br | M14 continued — `<AttachmentPicker>` (10): trigger + helper-text render, custom-label prop, thumbnail-grid + remove button + index-correct removal, MAX_FILES count gate + non-image + wrong-mime + > MAX_BYTES validations each fire a toast and skip onChange, happy-path upload (real FileReader → data: URL) shapes the new attachment, capacity disables trigger | ✅ Closed | `ca8c5ca` |
| Batch 7bs | M14 continued — admin display leaves (18 / 3 files): `<AdminStatsCard>` (7) — title/value, increase/decrease/neutral change variants + 'vs last month', change-prop optional, colour-variant background incl. default; `<AdminQuickActions>` (3) — empty render, per-action anchor (href + heading + description), colour-variant classes; `<ActionMenu>` (7) — isOpen render gate, per-item button render, item-click fires onClick + onClose, click-outside via mouseDown → onClose, Escape → onClose (other keys ignored), danger-variant divider only for non-first danger items | ✅ Closed | `8cd99c0` |
| Batch 7bt | M14 continued — `<AdminDataTable>` (10): title + columns + rows + custom column.render, "No data available" empty + loading-row, client-side search filter, sortable header click toggles asc/desc and reorders rows, client-side pagination (page-2 swaps the visible window), server-side mode (totalItems + currentPage + onPageChange used instead of client math; Showing N to M of T copy), onSearch fires per keystroke in server-side mode, searchable=false hides the input, right-click row → onRowContextMenu with the row data | ✅ Closed | `9bbeb5d` |
| Batch 7bu | M14 continued — `<ErrorBoundary>` (7): children passthrough when nothing throws, default inline fallback (role=alert + copy + Try-again button), ReactNode `fallback` prop override, function `fallback` form receiving (error, reset), labelled console.error on capture, default-label fallback | ✅ Closed | `cba6262` |
| Batch 7bv | M14 continued — loading + skeleton primitives (23 / 2 files): `<Skeleton>` + prebuilt SkeletonText/Card/Table/List (10) — variant routing (wave overlay vs pulse class vs none), default 100%/200px box vs 40px circle, explicit width/height overrides, prebuilt composition counts; `<LoadingSpinner>` + `<PageLoading>` + `<DataLoading>` (13) — message + size + fullScreen wrapping, page→message lookup with 'content' fallback for unknown keys, DataLoading type=table/card/list count routing | ✅ Closed | `ea5035e` |
| Batch 7bw | M14 continued — small UI leaves (21 / 4 files): `<ClientOnly>` (3) — hydration-deferred render + null default fallback; `<EmptyState>` (4) — title + description, optional action button, className passthrough; `<RefreshButton>` (5) — default Refresh label + icon, showText=false icon-only, isLoading spin + disabled, title attribute default vs custom; `<ExpiryBadge>` (9) — null/NaN render-nothing, three-tier copy + colour fork (green > 60d / yellow 15-60d / red ≤ 14d) with the "Renew Now" suffix + animate-pulse on 1-14 days, expired path with no pulse, singular 'day' for exactly 1 day, button-vs-span fork on onRenew | ✅ Closed | `8a5bbae` |
| Batch 7bx | M14 continued — admin display + brand chrome (17 / 3 files): `<AdminCard>` (5) — title/value, three change-variant colour classes + no-change hide; `<AdminTabs>` (4) — tab list with icons, active-tab class, per-tab onClick, no-onClick crash-safety; `<Logo>` (8) — Next/Link wrapping at default '/' href, custom href, unlinked path when href=''  is passed, showText label, variant light vs dark (asset + text colour), size→class mapping | ✅ Closed | `3867d4e` |
| Batch 7by | M14 continued — layout + form primitives (21 / 4 files): `<Section>` (6) — children + id + default white-bg/lg-padding, three other backgrounds + xl padding; `<Header>` (3) — fixed-top wrapper + z-50 + className passthrough; `<Textarea>` (6) — label, helperText vs error mutual-exclusion with red-ring class on error, ref forwarding, props passthrough; `<Badge>` (6) — default + secondary/destructive/outline variants + className merging | ✅ Closed | `a8898d9` |
| Batch 7bz | M14 continued — public chrome + ui primitives (23 / 4 files): `<ContactInfo>` (5) — 'Get in touch' intro + three contact cards (call/email/visit), className passthrough; `<ScrollToTop>` (4) — > 300px scroll-visibility threshold, hide-on-scroll-back, smooth scroll on click; `<Switch>` (5) — Radix role + aria-checked binding, onCheckedChange wiring + disabled gate + className merging; `<Button>` (9) — default + 5 variant classes, 4 sizes, asChild + Radix Slot composition (anchor-as-button retains button variant classes), ref forwarding, disabled state | ✅ Closed | `39d84e4` |
| Batch 7ca | M14 continued — tiny presentational leaves (12 / 5 files): `<SkeletonBase>` (3) — default bg + animate-pulse, animate=false opt-out, className via cn(); `<SkeletonCard>` (2) — 5-placeholder composition incl. the circular icon; `<SkeletonStats>` (2) — 4 cards × 4 placeholders = 16 in a grid; `<RupeeIcon>` (3) — SVG render, default + custom className, ₹ glyph in content; `<PageTransition>` (2) — children passthrough + className on the motion.div | ✅ Closed | `9384632` |
| Batch 7cb | M14 continued — loading + skeleton variants (13 / 4 files): `<LoadingSpinner>` simple primitive (4) — default md `h-8 w-8`, sm `h-4 w-4`, lg `h-12 w-12` size variants, className passthrough on the outer wrapper; `<SkeletonHero>` (2) — 4 SkeletonBase placeholders (icon + title + subtitle + search box) and the circular icon placeholder; `<SkeletonContact>` (2) — 2-column md-grid form/info shell with 21 SkeletonBase children (9 form + 12 info); `<SkeletonSection>` (5) — default render with title block + 3 cards in a 3-col grid (4 + 15 = 19 placeholders), `title={false}` skips the title block, `cards` prop controls the rendered card count (5 × 5 = 25), `columns=4` → `md:grid-cols-4`, `columns=2` → `sm:grid-cols-2` | ✅ Closed | `aa2ade9` |
| Batch 7cc | M14 continued — public-page primitives + motion shell (16 / 4 files): `<Card>` (7) — rounded-lg wrapper, three variant classes (default/elevated/outlined), three padding sizes (sm/md/lg), hover-classes opt-in, `animate=false` skips the motion.div wrapper (bare inner div is root) vs `animate=true` wraps it; `<HeroSection>` (5) — primary gradient default + secondary/dark variants, `background='solid'` drops the gradient, `backgroundImage` prop applies an inline `background-image: url(...)` style; `<FeatureCard>` (3) — icon + h4 title + description render, className passthrough to the underlying Card, default hover=true picks up `hover:shadow-xl`; `<MotionProvider>` (1) — children render passthrough through framer-motion's MotionConfig | ✅ Closed | `a708475` |
| Batch 7cd | M14 continued — shadcn card primitives + admin/stats + consent (24 / 4 files): shadcn `ui/card` set (8) — Card div with rounded-lg/border/bg-card, CardHeader flex-col p-6, CardTitle h3 + text-2xl, CardDescription p + text-sm/text-muted-foreground, CardContent p-6 pt-0, CardFooter flex items-center, ref forwarding + cn() className merging; `<AdminTable>` (5) — loading spinner + no table, default 'No data available' + custom emptyMessage, key→value cell mapping, `column.render(value, row)` override; `<StatsCard>` (5) — value/label/icon render, three trend variants (up=green↗ / down=red↘ / neutral=gray→), the **both-trend-and-trendValue-required** gating (neither alone renders the trend row); `<CookieConsentBanner>` (6) — visibility gates: anon + no consent → banner shows, prior consent → hidden, **authenticated → auto-accept** (writes to localStorage), Accept & X buttons both persist consent + hide, **defensive `useSession` undefined race** doesn't throw | ✅ Closed | `1ce672c` |
| Batch 7ce | M14 continued — Modal + custom Button + custom Input (27 / 3 files): `<Modal>` (6) — isOpen=false renders nothing, title + children + h3 heading, close button + overlay-click both fire onClose, `closeOnOverlayClick=false` opt-out, size→max-w class map (sm/lg/xl); custom `<Button>` (8) — distinct from shadcn ui/button (7bz), default primary gradient + md size, danger/outline variant gradients, sm size padding scale, fullWidth, **loading state disables + shows spinner svg**, disabled suppresses onClick, animate=false renders plain `<button>` (no motion wrapper) with click still firing; custom `<Input>` (13) — label + helperText render, **error vs helperText mutual-exclusion** (error wins + adds `border-red-300`), ref forwarding, **autocomplete inference** (email→email, type=password default→current-password, name contains 'confirm'→new-password, type=password + 'create' placeholder→new-password, name=phone→tel, explicit autoComplete prop overrides), icon→pl-10 padding, typing updates value | ✅ Closed | `90bb20f` |
| Batch 7cf | M14 continued — contact/loading + order timeline + floating cart (19 / 4 files): `<ContactMap>` (4) — 'Find Us' heading + address, **iframe src embeds the Rohini coords** (lat 28.7406 / lon 77.0884), Get-Directions external link (target=_blank + rel=noopener), pre-load 'Loading map...' indicator; `<LoadingPage>` (3) — default 'Loading...' + dots, custom message, `showDots=false` omits the dots span; `<OrderTimeline>` (5) — base 2-step (Payment + Confirmation), `hasHosting` inserts the Hosting step with '2–5 minutes', `hasDomains` inserts the Domain step with 'Up to 24 hours', **both flags renders payment→hosting→domain→email in that order**, userEmail interpolated into the confirmation step detail; `<FloatingCart>` (7) — renders cart link for non-admin on public route, badge with count when > 0, **'9+' cap above 9**, no badge at 0, **four hide gates**: admin route, admin user (localStorage role=admin), invoice /view page, plus the SSR-mounted guard. Mocks `next/navigation`, `@/lib/storage`, and `@/store/cartStore` via vi.hoisted | ✅ Closed | `057c94c` |
| Batch 7cg | M14 continued — register subforms + hosting-upgrade ConfirmStep (14 / 3 files): `<CredentialsSection>` (4) — Password + Confirm-password fields masked by default, eye-icon toggles type=password ↔ type=text **independently** for each field, onChange forwards the correct `name`; `<PersonalInfoSection>` (4) — 5 visible fields (first/last name, email, company, phone), email→type=email + phone→type=tel, **fixed Indian country code** display + hidden `phoneCc=+91` form payload field, onChange wiring; `<ConfirmStep>` (6) — target plan name + From + remaining days, prorated charge via `formatIndianCurrency` (₹1,234), **subscription-cancel amber warning gated by `upgradeInfo.hasSubscription`**, Back + Pay button callbacks, Pay button label includes the formatted charge | ✅ Closed | `c9dcae1` |
| Batch 7ch | M14 continued — session provider + SelectPlanStep + AddressSection (14 / 3 files): `<SessionProvider>` (1) — wraps children in the next-auth SessionProvider (mocked via vi.hoisted) — pins the auth-library coupling so a swap surfaces; `<SelectPlanStep>` (7) — step 1 of the hosting-upgrade modal: current plan name/price/remaining days, `hasSubscription` warning gate, eligible-plan list with prorated charges, **feature pills capped at 3** per plan, plan-button click → `onSelectPlan(plan)` with the full plan object, Cancel → `onCancel`; `<AddressSection>` (6) — register-form address subform: line1/city/state/zipcode fields populated from `formData.address.*`, **fixed Indian country display + hidden `address.country=IN` payload field**, the INDIAN_STATES set is rendered in the state select, Auto-fill button fires `onDetectLocation` and is disabled + shows the loading spinner when `isDetectingLocation=true`, dotted-path field names (`address.city`, `address.state`) forwarded on change | ✅ Closed | `0ed0ae0` |
| Batch 7ci | M14 continued — PricingCard + CenteredLoading + CustomToast (21 / 3 files): `<PricingCard>` (6) — title/subtitle/price/currency/period, original-price line-through + renewalPrice + discountBadge optional rows, isPopular adds 'Most Popular' banner + indigo border, **button-vs-link fork** keyed on onButtonClick presence, feature list with included + highlight class branches; `<CenteredLoading>` (6) + `<InlineLoader>` (2) — default 'Loading...' + dots, custom message, showMessage=false omits text, size→spinner-class map (sm/md/lg/xl), fullScreen=true wraps in `fixed inset-0` overlay vs the inline `min-h-[200px]` wrapper, InlineLoader size map + className passthrough; `<CustomToast>` imperative API (7) — `toast.custom` mocked via vi.hoisted, the captured render fn re-mounted standalone to assert success-green / error-red / loading-blue border + per-type icon (svg vs spinner), title rendering, **dismiss button calls toast.dismiss(t.id)**, `showLoadingToast` + `showPermanentToast` pin `duration=Infinity`, `dismissible=false` omits the X button | ✅ Closed | `af899b4` |
| Batch 7cj | M14 continued — EnhancedToast + Razorpay iframe (16 / 2 files): `<EnhancedToast>` (8) + `<ToastContainer>` (1) — title + optional message, **per-type icon + colour palette** (success=green / error=red / warning=yellow / info=blue), close button → onClose(id) after 300ms exit, **auto-dismiss at `duration + 300ms`** (fake timers), `duration=0` disables auto-dismiss, ToastContainer maps `toasts` array; `useRazorpayCheckout` (8) — the postMessage protocol contract: Frame() null until `open()`, then iframe overlay renders; iframe `'ready'` → parent posts `{type:'open', options}` to contentWindow; `'success'` resolves the open Promise with the payload; `'dismiss'` rejects with `{kind:'dismissed'}`; `'error'` rejects with `{kind:'error', message}`; **a second `open()` while active rejects immediately**; **foreign-origin postMessages are ignored** (CSP defense — verified by sending a `dismiss` from `https://evil.example.com` and confirming the Promise hadn't settled) | ✅ Closed | `ad58212` |
| Batch 7ck | M14 continued — Footer + AuthShell (13 / 2 files): `<Footer>` (6) — company intro + 5 Quick Links with hrefs (Home/About/Contact/Login/Register), Services section (Domain Registration / DNS Management / SSL Certificates + Web Hosting→/hosting), four policy links in bottom strip (/privacy, /terms-and-conditions, /data-deletion, /cancellation-refund), current-year copyright line, social media aria-labels (Facebook/Instagram/LinkedIn), className passthrough; `<AuthShell>` (7) — form-side title as `<h1>`, subtitle optional render gate, children inside form panel, default panelEyebrow ('Anutech Digital') + panelTitle ('The clean way to own your online identity') vs override props, current-year footer copyright, brand-panel Logo links back to `/` | ✅ Closed | `bab0d5e` |
| Batch 7cl | M14 continued — SocialLoginButtons + skeleton primitives + admin skeleton (17 / 3 files): `<SocialLoginButtons>` (7) — next-auth `signIn` + react-hot-toast mocked via vi.hoisted: renders only Google by default (FACEBOOK/GITHUB env gated off), 'Or continue with' divider, click calls `signIn('google', {redirect:false, callbackUrl:'/dashboard'})`, `result.error='OAuthSignin'` → toast.error + onError, **`result.error='AccessDenied'` → `showAccountDeactivated` branch** (different copy, no toast.error), `result.ok=true` → toast.success + onSuccess (under fake timers to dodge the 2s redirect setTimeout), uncaught signIn throw → generic toast.error in catch; skeleton `_primitives` (7) — Sk renders div + skeleton class + className passthrough, PageHeader 3 placeholders (2 title + 1 action) + `wide=true` swaps `w-48` → `w-64`, TableSkeleton placeholder math (toolbar 2 + header cols + rows×cols data) defaults to 6×5=37 total, FormSection title=true vs false toggles the title bar; skeleton `AdminLayout` (3) — AdminTableRowsSkeleton cell-count math (header + per-row 8-cell signature: icon tile + 2 lines + middle + 2-button tail), AdminLayoutSkeleton renders sidebar logo + 8 nav rows + topbar shell and slots children into `<main>` | ✅ Closed | `5401a1f` |
| Batch 7cm | M14 continued — Dashboard skeleton family + SkeletonCart (14 / 2 files): `DashboardSkeletons` (11) — SkeletonBar default w-full/h-4 + width/height overrides, SectionCardSkeleton title bar + N body rows (defaults 1+3=4), TableSkeleton 1 header + rows×cols (defaults 1+30=31), ListSkeleton 2 bars per item, FormSkeleton 8 fields × 2 + submit = 17, DNSManagementSkeleton composition (3 + 21), OrdersSkeleton 6×6 + DomainsSkeleton 6×5 + AdminTableSkeleton 8×6 delegate to TableSkeleton, default export `<DashboardSkeletons />` is intentionally null; `<SkeletonCart>` (3) — 55-placeholder shell snapshot, 4-col trust strip + 6/7/8-col main content grid pinned via Tailwind class selectors | ✅ Closed | `426c33e` |
| Batch 7cn | M14 continued — barrel re-exports + payment skeletons + example component (12 / 4 files): `skeletons/PageSkeletons` barrel (4) — re-exports a sampling from each topical file (AdminLayout, AdminPages, UserDashboard, PaymentPages) so a future rename surfaces here; `components/DomainSearch` barrel (1) — backwards-compat shim; the default export is **referentially identical** to `components/domain-search/DomainSearch`'s default; `PaymentPages` skeletons (3) — CheckoutPageSkeleton renders > 10 .skeleton placeholders + the `lg:grid-cols-6` content grid, PaymentSuccessPageSkeleton renders the `min-h-screen` success shell, CartPageSkeleton renders a non-empty placeholder grid; `<DomainRequirementsExample>` (4) — trigger button render, the **`requiresVerification` gate** (false → modal absent even when isModalOpen=true), requiresVerification=true + isModalOpen=true → modal renders with `domain="anutech" tld=".au"`, button click → openModal callback. Hook + modal both mocked via vi.hoisted | ✅ Closed | `8c7f2e4` |
| Batch 7co | M14 continued — UserDashboard + AdminPages skeleton families (22 / 2 files): `skeletons/UserDashboard` (13) — DashboardLayoutSkeleton slots children into `<main>` and renders > 15 .skeleton placeholders in the sidebar/topbar shell; 11 named page-skeleton exports (DashboardHomeSkeleton, OrdersPageSkeleton, DomainsPageSkeleton, InvoicesPageSkeleton, HostingPageSkeleton, SupportPageSkeleton, ReferralsPageSkeleton, SettingsPageSkeleton, DNSPageSkeleton, TicketDetailPageSkeleton, DetailPageSkeleton) each render > 5 placeholders (it.each table); `skeletons/AdminPages` (9) — 9 named admin-page exports (AdminUsersPageSkeleton, AdminPaymentsPageSkeleton, AdminSupportPageSkeleton, AdminPendingDomainsPageSkeleton, AdminHostingPageSkeleton, AdminPricingPageSkeleton, AdminGenericPageSkeleton, AdminDashboardSkeleton, AdminSettingsPageSkeleton) each render > 5 placeholders (it.each table). Smoke-level coverage — the goal is to catch a future refactor accidentally yielding an empty skeleton, not to pin individual placeholder counts | ✅ Closed | `b5c271a` |
| Batch 7cp | M14 continued — admin AdminLayout + InvoiceDiagnostics orchestration (19 / 2 files): `admin/AdminLayout` (10) — 11-item nav rendered, children slot into main content, **active link routing** (link active class on the matching pathname), **two special cases**: `/admin` (no /dashboard) treats Dashboard as active, `/admin/dns-management` treats Domains as active; user initials display (firstName+lastName chars → 'AL'), 'AU' fallback when user=null; onLogout prop renders + fires the Logout button, absence hides it; mobile menu button labelled 'Open navigation menu'; `admin/InvoiceDiagnostics` (9) — apiClient + confirmDialog + toast helpers + 3 subcomponents (DiagnosticsHeader, ConflictsTable, StuckOrdersTable) all vi.hoisted mocked: initial loading skeleton, empty-issues fetch leaves panel collapsed, with-issues fetch auto-expands, clear-invoice-number declined → no POST, clear-invoice-number confirmed → POST + success toast + refetch, re-sync single → POST + success toast, **re-sync all empty list → no confirm**, re-sync all all-succeed → bulk-success toast with count, re-sync all partial → mixed-error toast | ✅ Closed | `d87551d` |
| Batch 7cq | M14 continued — DomainRequirementsModal + DomainSelectionModal (16 / 2 files): `<DomainRequirementsModal>` (8) — isOpen gate, intro + Important Notice copy, Required Information list, **Restrictions per-type colour fork** (error=red / warning=orange / info=blue), Alternative Options list + `onSelectAlternative(domain)` callback with the exact domain string, alt.available true/false swaps the trailing icon, Close fires onClose, **Contact Support opens `mailto:?subject=Domain Registration Support` via window.open**, defensive `Array.isArray()` guards (non-array `requirements`/`restrictions` props don't crash); `<DomainSelectionModal>` (8) — cartStore + react-hot-toast mocked: isOpen gate, empty input → 'Please enter a domain name' error, invalid format → 'Please enter a valid domain name', valid domain + no existingDomainName → `addItem({domainName: 'mysite.com', price, currency:'INR', registrationPeriod:12, itemType:'hosting', hostingPlan:{name, period:12, features}})` with **lowercased domain**, `existingDomainName='hosting-abc'` placeholder leaves input empty vs user-supplied value pre-fills, **existingDomainName + matching cart item → `updateItem(oldDomain, ...)` (not addItem)** to swap the placeholder, typing after an error clears the error message | ✅ Closed | `3ac519c` |
| Batch 7cr | M14 continued — `<HostingUpgradeModal>` orchestration (11 / 1 file): the full step machine `loading → select → confirm → paying → verifying → success` (and the `↘ error` branches). Heavy mock setup — `global.fetch`, `next-auth/react.useSession`, `next/navigation.useRouter`, `safeSessionStorage`, `useRazorpayCheckout` (open + Frame), and both `SelectPlanStep` + `ConfirmStep` subcomponents replaced with thin mocks. Pins: isOpen gate, initial fetch to `/api/v1/user/hosting/upgrade-info?domainName=…&credentials:include`, success + eligiblePlans>0 → 'select' step (SelectPlanStep mounted), **eligiblePlans=0 → 'error' step with 'highest available plan' copy**, HTTP error → 'error' step with server message, fetch throws → generic 'Failed to load upgrade options' error, pick-plan transitions to 'confirm' step, back from confirm → select, **`razorpay.open()` rejecting with `{kind:'dismissed'}` is a soft-cancel back to 'confirm'**, upgrade order POST fail → 'error' step with server message, Retry from error step refetches upgrade-info | ✅ Closed | `5727de8` |
| Batch 7cs | M14 continued — UserLayout + GoogleRecaptcha (21 / 2 files): `user/UserLayout` (14) — 6-item nav (Dashboard/Domains/Hosting/Invoices/Support/Account Settings, Invoices link's accessible name includes the RupeeIcon ₹ glyph), active-link blue text+border classes, top-bar heading reflects the active nav (falls back to 'Dashboard' when no match), user-info shows name+email when user present vs 'Loading...' when user=null; **three logout-button states** — active when onLogout + user, disabled when onLogout + user=null, inactive 'No logout handler' block when onLogout absent — pinned via the component's existing `data-testid` attrs; click awaits async onLogout, ProfileCompletionWarning rendered (mocked), isLoading=true swaps children for DataLoading skeleton, hideFloatingButtons gates the floating Home link; `<GoogleRecaptcha>` (7) — RecaptchaClient + apiClient + env stubbed: **captcha-status `{enabled:false}` → onSuccess('captcha-disabled') with no widget render**, missing/placeholder site key → onSuccess('manual-pass'), happy path renders widget with the supplied theme + size and forwards tokens via the widget callback, expired-callback fires onExpire + resets widget, error-callback fires onError + shows visible error, **'already been rendered' render error is swallowed** (no onError, no error message — important because StrictMode double-mounts can fire this), other render errors → onError + 'Failed to render verification widget' visible message | ✅ Closed | `0a64dd5` |
| Batch 7ct | M14 continued — `<DomainRenewalModal>` (9 / 1 file): fetch + react-hot-toast mocked, `vi.setSystemTime` fixes "now" so the `daysUntilExpiry` math is deterministic. Pins: isOpen=false renders nothing + no fetch; isOpen=true → loading copy + fetches `/api/v1/domains/renew?domainName=...&years=1` with `credentials:'include'`; success render shows Current Status + Renewal Cost (₹1,500 via `formatIndianCurrency`) + all **6 renewal-period buttons** (1/2/3/4/5/10 with proper Year/Years pluralisation); HTTP error → toast.error with server message; expiry ≤ 30 days surfaces the red **'expiring soon! renew now'** warning block; selecting a year refetches with `?years=N`; Cancel button fires onClose; successful renew POST → success toast + onClose; failed renew POST → toast.error + modal stays open | ✅ Closed | `c4e83d5` |
| Batch 7cu | M14 continued — `<HostingRenewalModal>` (10 / 1 file): the hosting variant of DomainRenewalModal (7ct), but uses the **Razorpay iframe checkout** flow rather than a mock paymentId. Mocks: fetch, useSession, useRouter, safeSessionStorage, useRazorpayCheckout (open + Frame), react-hot-toast. Coverage on the full load+renew step machine: isOpen=false renders nothing + no fetch; isOpen=true → renew-info fetch with credentials, header rendered; successful renew-info → plan name + ₹12,000 price visible; renew-info HTTP error → toast.error + onClose; renew-info fetch throws → generic toast.error + onClose; **Renew → POST + razorpay dismissed → soft-cancel (no toast)**; Renew → POST fail → toast.error with server message (no razorpay.open call); Renew → razorpay generic error (non-dismissed) → toast.error with the tagged.message; **happy path** order POST → razorpay success → verify success → success toast + `safeSessionStorage.paymentResult` written + onClose + `router.push('/payment-success')`; verify failure → toast.error + modal stays open | ✅ Closed | `fc9230b` |
| Batch 7cv | M14 continued — public `<Navigation>` (11 / 1 file): 354-line component covering three variants (default public-site / dashboard / admin). Mocks usePathname + useSession (typed with the undefined-race safe-destructure) + cartStore. Default variant (7): 4 desktop nav links (Home/Hosting/About Us/Contact Us — `getAllByRole` because mobile menu also renders them), unauthenticated → Login link + no Dashboard link, authenticated → Dashboard link + no Login, active-link blue class via `google-blue` className match, cart-count badge appears once > 0, mobile menu toggle button labelled, **defensive `useSession()` undefined race** doesn't throw (falls through to unauthenticated branch); dashboard/admin variants (4): onLogout button fires the callback, **role chip blue=user vs red=admin** swap (color encodes risk in the chrome), Shopping Cart link has the aria-label + href + count badge, public-site nav links absent on dashboard/admin variants | ✅ Closed | `821ae97` |
| Batch 7cw | M14 continued — `<NameServerManagement>` (11 / 1 file): the localStorage-backed nameserver CRUD panel. Mocks `safeLocalStorage` (with a Map-backed `vi.hoisted` store) + `react-hot-toast`. Pins: on mount, hydrates `nameserverConfigs` from storage **prepending the hard-coded RESELLERCLUB default** (default is NOT persisted — only customs are); 'Add Custom' opens the inline editor + Cancel closes it; Save with empty name → toast.error 'Please enter a configuration name'; Save with all-empty servers → toast.error 'at least one nameserver'; **Save success persists only the customs to localStorage** (RESELLERCLUB_DEFAULT is hard-coded, asserted absent from the persisted JSON) + toast.success + closes form; 'Add Another Nameserver' grows the input list (ns1/ns2/ns3 placeholder progression); Delete on a custom row removes it + persists; **Delete button is NOT rendered on the default row** (assertion via `getAllByTitle` length); Set Active flips `isActive` across configs + persists; Reset to Default Nameservers restores default-active state; **malformed localStorage JSON** gracefully falls back to the default-only list (caught by the try/catch in the load effect) | ✅ Closed | `20f13ee` |
| Batch 7cx | M14 continued — `<LoginForm>` (15 / 1 file): the 391-line credentials-login orchestration component. Mocks signIn, useRouter+useSearchParams, toast helpers, safeLocalStorage, logger; GoogleRecaptcha + AuthShell + SocialLoginButtons replaced with light shims. Pins: email/password/Remember-me/Forgot-password rendered; 'Create an account' link respects `?returnUrl=`; `?message=…` shows green activation banner; `?returnUrl=/checkout` shows the 'sign in to complete your purchase' banner; **`?error=AccessDenied`** surfaces the deactivated-account message + cleans up the URL via `history.replaceState`; password eye toggles type=password ↔ text; submit calls `signIn('credentials', {redirect:false, email, password, callbackUrl})`; **`CredentialsSignin` → 'Invalid email or password'**; **`TotpRequired`** reveals the 2FA step WITHOUT an error toast (correct credentials, just missing 2FA); **`InvalidTotpCode`** → 'Invalid authenticator code' toast; **`AccountNotActivated`** → toast.error + 1s-delayed `router.push('/activate?email=…')`; **`AccountDeactivated`** → `showAccountDeactivated` helper; ok=true → success toast + 100ms-delayed `window.location.href = safeReturnUrl`; **Remember-me checked** → `setItem('rememberMe','true')` + `setItem('savedEmail', email)`; unchecked → `removeItem` both; **open-redirect defence** — `?returnUrl=https://evil.example.com/steal` falls back to `/dashboard` (the `startsWith('/')` guard) | ✅ Closed | `3bade40` |
| Batch 7cy | M14 continued — `<DomainSearch>` orchestrator (14 / 1 file): the 292-line search-page parent. Subcomponents (SearchInput / SearchResults / DomainRequirementsModal) all have standalone tests; here we focus on the orchestration callbacks. Mocks: cartStore, useRouter, toast helpers, apiClient.post, domainRequirements helpers, useDomainSearch hook, and the 3 subcomponents replaced with thin stubs exposing onAddToCart/onShowRequirements/onWatch via test buttons. Pins: SearchInput + SearchResults + hero copy render; `showHeroText=false` hides the hero; **addToCart paths** — standard domain → cartStore.addItem + success toast + 1s router.push('/cart'); requires-additional-details TLD → opens requirements modal (no cart write); unsupported domain → error toast; missing price/available → 'missing required data' toast; handleShowRequirements opens the modal directly; modal close hides it; **handleWatch result paths** — 200 → 'we'll email you when X becomes available' success toast, 401 → opens **sign-in prompt with returnUrl pointing back to the search**, 409 → 'Already watching' error toast (no sign-in prompt), 400 → error toast with server message, default → generic 'Failed to watch domain' toast; sign-in + create-account anchors in the prompt both carry the returnUrl with `q=<base>` (the dot-split first segment) | ✅ Closed | `7f2df32` |
| Batch 7cz | M14 continued — `<RegisterForm>` (11 / 1 file): the 486-line registration orchestration. Subform sections (PersonalInfo / Address / Credentials) all have standalone tests (slices 7cg/7ch); this slice focuses on the **4-step state machine + submit paths**. Mocks: useRouter, react-hot-toast, safeLocalStorage, apiClient.post, GoogleRecaptcha + Card + Logo + Button + SocialLoginButtons shimmed. **The three subform sections render real `<input>` elements** (testid-prefixed) so `user.type()` drives one change event per character — necessary because the source's `handleChange` uses non-functional `setFormData`, so multiple synchronous onChange calls would clobber each other. Pins: heading + sign-in link + 3 sections render; submit on step 1 with empty fields → 'fill in all required fields' toast (no advance); **password length 1–7 disables Create-account, ≥8 re-enables** (the gating that catches half-typed passwords); 4-step advance via fill+submit chain → final apiClient.post body has the full register payload (firstName/lastName/email/password/phone/phoneCc/companyName/address.{line1,city,state,country:IN,zipcode}); successful register → token + user persisted to localStorage, **token cookie set with SameSite=Lax** (assertion via `document.cookie`), success toast, registerFormData cleared, 100ms-delayed router.push('/dashboard'); **password mismatch on final step → toast.error + NO API call** (so a typo doesn't burn a registration attempt); apiClient fail with server message → that exact message in toast.error; apiClient fail with status=0 (network) → generic 'An error occurred' (avoids leaking 'Failed to fetch' to the user); registerFormData persists to localStorage as the form fills (passwords excluded); **rehydrates on mount** (saved data NOT wiped by the initial empty state); detectLocation: geolocation unsupported → 'not supported' toast | ✅ Closed | `652ab74` |

| Batch 7da | M14 **COMPLETE** — `<ChatWidget>` (10 / 1 file): the 260-line streaming-SSE chat widget; the last untested component in `components/`. Mocks `global.fetch` with a hand-rolled `ReadableStream` shim that yields SSE-style data chunks; `Element.prototype.scrollIntoView` stubbed. Pins: closed by default; toggle button opens panel with the greeting message; **sessionStorage hydration restores prior conversation** on mount, **trailing-empty-assistant placeholder dropped** (interrupted-stream defence); Close (X) hides panel (uses `getAllByRole` because the toggle button also carries `aria-label='Close chat'` when open); Send button disabled when input is empty; **streaming flow** — POST `/api/v1/chat`, then `data: {"text":"..."}` chunks accumulate into the trailing assistant message and `data: [DONE]` terminates; **malformed chunks are silently skipped** (a `data: this-is-not-json` line interleaved with valid chunks doesn't crash the stream); network failure (`ok:false`) → assistant fallback 'Sorry, something went wrong… contact support@anutech.in'; sessionStorage persists each settled exchange (user + assistant accumulated reply); **Enter (no shift) submits; Shift+Enter does not** | ✅ Closed | `1c74c24` |
| Batch 7db | M14 follow-up — top-level hooks coverage (18 / 3 files): `useModalScroll` (6) + `useScrollToTop` — isOpen=true locks body scroll (overflow=hidden, position=fixed, top=-scrollY, width=100%), isOpen=false leaves styles cleared, transitioning open→closed restores styles (note: scrollTo restore doesn't fire on transition because the cleanup wipes top before the false-branch reads it — pinned as a known quirk), isOpen=false with a pre-existing body.style.top DOES scrollTo, unmount-while-open cleanup restores styles, useScrollToTop scrolls (0,0) on mount; `useDomainRequirements` (4) — non-restricted TLD → empty arrays + requiresVerification=false (data fns NOT called), restricted TLD → arrays populated from getDomainRequirements + generateAlternativeDomains, openModal/closeModal toggle isModalOpen, handleSelectAlternative closes the modal; `useUser` (8) — status='loading' → isLoading=true + user=null, unauthenticated + no token → router.push('/login'), unauthenticated WITH legacy token → no push, authenticated session before SWR resolves → fallback to sessionUser derived from session.user fields, /api/auth/me data wins over sessionUser fallback, admin role → window.location.replace('/admin/dashboard'), localStorage 'user' is the legacy fallback (when no session), **defensive `useSession()` undefined race** doesn't crash + treats as loading | ✅ Closed | `9dc57b5` |
| Batch 7dc | M14 follow-up — nested domain-search hooks (24 / 2 files): `useDomainPricing` (3) — INR formatting with ₹ glyph + Indian-locale grouping (1,00,000 for lakh), custom currency override (USD), zero fraction digits; `useDomainSearch` (21) — the 342-line state machine + pure helpers. validateDomainInput pure: 'example.com' → base+suggestedTld, 'example.co.uk' → multi-segment TLD, bare 'example' → suggestedTld=null, 'ab' minimum length, single 'a' / 'foo!' invalid, whitespace stripped; getSuggestedTlds pulls from TOP_TLDS filtered by isRestrictedTLD; state machine — initial state, **localStorage rehydration** from `domainSearchState`, `initialSearchTerm` + `autoSearch=true` triggers a search after 100ms, invalid input → toast.error + no API, **redirectOnSearch=true** routes via `router.push('/domains/search?q=…')` instead of fetching, happy path Phase-1 quick → setResults + Phase-2 suggestions fetch, **quick `restricted_tld` error** → setError + toast.error (NO Phase-2 fetch — wastes a request), quick network error → 'Network error' toast, handleInputChange branches (bare → searchMode='multiple'+baseDomain, with-TLD → 'single', invalid → empty), clearSearch resets all state | ✅ Closed | `e8b5c55` |

| Batch 7dd | M14 follow-up — small lib utilities (21 / 5 files): `lib/utils` (6) — `cn()` Tailwind class merger joins, filters falsy, supports clsx object form, dedupes Tailwind conflicts via tailwind-merge (`p-4 + p-2` → `p-2`); `getSupportEmail()` returns env var or falls back to `support@anutech.in`; `lib/fetcher` (4) — SWR fetcher calls fetch with `credentials:'include'` + JSON Content-Type, returns parsed JSON on success, throws typed Error with `.status` + `.info` on non-2xx, non-JSON body → `.info={}` fallback; `lib/constants` (4) — INDIAN_STATES has exactly 36 entries (28 states + 8 UTs), contains all canonical names, alphabetical ordering (drives register-form dropdown); `lib/tld-min-periods` (3) — TLD_MIN_PERIODS only includes TLDs with `minYears>1` (asserted bidirectionally against `TLD_POLICIES`), `getMinRegistrationPeriod` delegates to `getMinYears`; `lib/security-headers` barrel (3) — three exports referentially identical to `lib/security/headers` originals | ✅ Closed | `2eeeca0` |
| Batch 7de | M14 follow-up — request-id + auth cookies + axios client + 2 integration barrels (23 / 5 files): `lib/request-id` (6) — Cloud Run X-Cloud-Trace-Context priority over x-request-id, TRACE_ID extraction (`abc/SPAN_ID;o=1` → `abc`), x-request-id fallback, **fresh UUID v4 fallback** (asserted via the 8-4-4-4-12 hex pattern + ≠ between successive calls); `lib/auth-config/cookies` (6) — 3 cookie groups (sessionToken/callbackUrl/csrfToken), sessionToken+csrfToken httpOnly, **callbackUrl NOT httpOnly** (client reads it for post-OAuth redirects), `__Secure-`/`__Host-` prefix gating on `useSecureCookies`, csrfToken uses **`__Host-` prefix** (path=/ + no Domain) not `__Secure-`; `lib/zohobooks/axios-client` (3) — 30s timeout default mirroring ResellerClub budget, axios verbs available, defaults object exposed; `lib/integrations/resellerclub` barrel (4) — 6 ops + 6 classify fns + matchesAny + 5 fragment-keyword lists (BALANCE_PENDING, PROCESSING_LOCK, etc.) re-exported, matchesAny lower-case substring match; `lib/integrations/directadmin` barrel (4) — 6 ops + 6 classify error fns + 3 fragment lists (USERNAME_COLLISION/USER_NOT_FOUND/PACKAGE_NOT_FOUND) re-exported, matchesAny shared with RC. **RC client throws at module-load on missing env vars** — stubbed via `vi.hoisted` before the static import | ✅ Closed | `4fc5c1a` |
| Batch 7df | M14 follow-up — protocol consts + time service + cron auth + hosting dates + guest JWT (43 / 5 files): `lib/razorpay-checkout-protocol` (6) — RAZORPAY_FRAME_PATH='/razorpay-checkout', RazorpayCheckoutOptions accepts minimum {key} + pass-through unknown keys, RazorpaySuccessPayload requires payment_id+signature, ParentToFrame open + FrameToParent ready/success/dismiss/error compile-time checks; `lib/time-service` (10) — TimeService.now resolution priority (explicit param > x-simulated-time header > SIMULATED_TIME env > real new Date()), production guard requires ENABLE_TIME_SIMULATION=true to enable simulation, TimeService.daysUntil with future/same-day/past inputs + **uses floor (no rounding up)** so 9d23h → 8 whole days; `lib/cron-auth` (6) — CRON_SECRET unset → false, missing header → false, length mismatch → false (no timingSafeEqual call), matching secret → true via constant-time compare, ASCII secret exact-bytes match; `lib/hosting-dates` (10) — calculateHostingDates default 'months' uses setMonth (12 → +1 year), 'days' uses setDate (7 → +1 week), 'minutes' uses setMinutes (45 → +45min for test plans), registeredAt+expiresAt are distinct Date objects, validateRegistrationPeriod only accepts 12 (everything else → 1); `lib/guest-token` (6) — sign+verify round-trip preserves payload, 1h TTL baked in (exp-iat ≈ 3600), tampered signature → null, wrong purpose → null, garbage input → null, **alg=none forgery → null** (algorithm pinned in verify) | ✅ Closed | `78c151b` |
| Batch 7dg | M14 follow-up — auth-secret + mongoose alias + 4 thin DB services (18 / 6 files): `lib/auth-secret` (3) — exports AUTH_SECRET trimmed from NEXTAUTH_SECRET, **throws at module-load when unset** (fail-fast vs silent broken signatures); `lib/mongoose` (2) — `connectToDatabase` thin alias for connectDB from `@/lib/mongodb`, propagates errors; `lib/services/domains` (4) — listDomainsForUser queries Domain.find({userId}).sort({createdAt:-1}), getDomainById delegates to Domain.findById + returns null when missing; `lib/services/payments` (2) — createPaymentInTransaction calls Payment.create([data], {session}) (the **only** Mongoose signature that attaches to an in-flight transaction), unwraps the array result, propagates write-conflict errors; `lib/services/system-logs` (3) — recordSystemLog defaults level='error' + source='Unknown' + user=null, user-supplied fields override defaults, optional fields pass through verbatim or stay undefined; `lib/services/ip-checks` (4) — recordIPCheck stamps `checkedAt: new Date()` server-side + supports success/error paths, **getLatestIPCheck lazy-imports User model** for the populate call's typed 4th arg (Mongoose typed overload doesn't include it but runtime needs it — pinned so a future "cleanup" doesn't drop the arg) | ✅ Closed | `9329459` |
| Batch 7dh | M14 follow-up — RC read-ops + DA suspend/unsuspend wrappers (20 / 5 files): 3 RC reads + 2 DA writes — each a thin wrapper that delegates to the underlying SDK and maps results onto a typed Outcome union. `getDomainOrderId` (4): RC success with order-id → `found`; **success with data stringifying to 'null'** → not_found + info log (defensive — RC sometimes returns `"null"` as a literal); 'no matching' error message → not_found; generic error → hard_failure + error log. `getDomainDetails` (3): success → found, 'no orders found' message → not_found + info, generic error → hard_failure + error. `getDNSRecords` (4): forwards both `domainName` + `customerId` args, success → found, '404'/'not found' messages → not_found + info, auth-failed → hard_failure + error. `suspendUser` (5): delegates with optional reason arg, USER_NOT_FOUND_FRAGMENTS match → user_not_found + warn log, **DA status=503 → da_unreachable** (mapped specifically because that's DA's nginx-up-backend-down signature), generic Error → hard_failure. `unsuspendUser` (4): mirror of suspendUser but **logs user_not_found at ERROR level** (not warn) because the renewal flow has already collected payment — the asymmetry is a deliberate operational signal | ✅ Closed | `6eb12b5` |
| Batch 7di | M14 follow-up — DA delete-user + get-user-config + change-package wrappers (13 / 3 files): closes the 3 remaining DA user-op wrappers. `deleteUser` (4): happy `deleted` → no log, **user_not_found logged at INFO** ('already gone, treating as success at callsite') — distinct from get-user-config's WARN because cleanup intent + already-deleted = success, status=503 → da_unreachable + warn, generic Error → hard_failure + error. `getUserConfig` (4): happy → `{kind:'found', config}` with the full DA config payload; user_not_found logged at WARN (terminal — caller marks Hosting row orphaned); 503 → da_unreachable + warn (transient — retry); generic → hard_failure + error. `changePackage` (5): forwards both `username` + `newPackage` args, the wrapper has a **5th outcome `package_not_found`** (the only DA wrapper with that branch) for config/seeding errors → ERROR log; user_not_found → ERROR log (upgrade flow already paid); 503 → da_unreachable + warn; generic → hard_failure + error. With 7di + 7dh, **all 6 DA user-op wrappers (createUser was already covered in earlier slice 7e/da-create-user.test.ts) and all 6 RC operation wrappers now have unit-test coverage** | ✅ Closed | `a77753f` |
| Batch 7dj | M14 follow-up — RC renewDomain + auth-config helpers + field-encryption (28 / 3 files): `renewDomain` wrapper (6) — RC success → outcome from classify, hard_failure response → error log, **thrown error matching BALANCE_PENDING_FRAGMENTS** ('insufficient balance' literal) → `balance_pending` (short-circuit so the cron-retry path still works when the SDK throws instead of returning {status:'error'}), thrown non-balance error → hard_failure with the message preserved, non-Error throws (string) still classified (String coercion), forwards years arg through to SDK; `auth-config/helpers` (10) — extractSocialName per-provider name parsing: google uses given_name/family_name (falls back to user.name split), github prefers profile.name → login → user.name (login alone has no space → entire string is firstName), facebook + unknown providers split user.name only, missing fields → empty strings never throws; SOCIAL_PROVIDERS=['google','facebook','github'] shape pinned; **useSecureCookies** module-load constant — true when NEXTAUTH_URL starts with `https://`, false for http://, false when unset (defensive nullish chain); `field-encryption` (12) — AES-256-GCM round-trip preserves plaintext, output starts with 'enc:' prefix + iv:tag:ciphertext layout (24+32 hex chars for IV+authTag), **two encryptions of same input produce different ciphertexts** (fresh IV per call), re-encrypting an already-encrypted value is a no-op, decrypting plaintext returns it verbatim (legacy migration), empty strings pass through, key validation requires 64-char hex (throws otherwise), isEncrypted detects the prefix, **tampered authTag fails decryption** (GCM auth check), malformed payload throws 'Invalid encrypted field format' | ✅ Closed | `2cf378d` |
| Batch 7dk | M14 follow-up — RC transferDomain + redis cache + settings-service shim (30 / 3 files): RC `transferDomain` wrapper (9) — forwards all 4 args (domainName + authCode + customerId + optional contacts), success → outcome via classify, **transfer_rejected response logs at WARN (not error)** because rejection is the customer's problem (bad EPP code / transfer lock / 60-day window) not ours, hard_failure response logs at ERROR, thrown error matching BALANCE_PENDING_FRAGMENTS → balance_pending short-circuit, thrown error matching TRANSFER_REJECTED_FRAGMENTS ('auth code' literal, hyphenated 'auth-info' does NOT match — fragments use 'auth code'/'auth-code'/'authcode') → transfer_rejected, other throws → hard_failure, String coercion for non-Error throws — **closes all 6 RC operation wrappers**; `lib/redis` (15) — REDIS_HOST unset → null + warn log on module-load + cache methods become no-ops; configured → ioredis instance constructed with `lazyConnect`+`enableOfflineQueue:false`+5s connect/3s command timeouts; redisCache.get JSON-parses, returns null on miss, catches connection errors + JSON-parse failures + logs; redisCache.set JSON-stringifies with default 120s TTL via 'EX' command, respects custom TTL; redisCache.del → redis.del(key); all 3 catch errors + log without throwing (callers can't break on cache outages); `lib/settings-service` (8) — class-style shim delegating to functional API at `@/lib/services/settings`: getSetting → getSettingValue(key, defaultValue=null), setSetting → upsertSetting(key, value, {description:'', category:'general', updatedBy:'system'} defaults), **clearCache is no-op + info log** (caching is deliberately disabled — pinned so a future "optimisation" that adds caching isn't silently masked by the shim), getAllSettings maps the docs to a flat array with all metadata fields | ✅ Closed | `1af0146` |
| Batch 7dl | M14 follow-up — RC registerDomain + DA createUser orchestration wrappers (15 / 2 files): closes 2 wrappers whose underlying classify functions were tested in earlier slices (rc-register-domain.test.ts and da-create-user.test.ts) but whose **orchestration layers** weren't. RC `registerDomain` (7): forwards all 6 args (domainName, years, customerId, optional nameServers, contacts, tldAttributes) verbatim or as `undefined`, success → outcome via classify, hard_failure response → error log, thrown error matching BALANCE_PENDING_FRAGMENTS short-circuits to balance_pending (the axios-throw-instead-of-typed-error fallback), other throws → hard_failure, non-Error throws → String coerced. DA `createUser` (8) — the username-collision retry loop: **empty candidate list → 'no username candidates' hard_failure immediately** (defensive — no SDK call), first-candidate success → `{kind:'created', username}` (no retries), first-candidate collision + second-candidate success → tries the second + warn log on each retry attempt; **all-candidates-collide → `username_collision_exhausted`** (final warn log with the count, not error — collisions are expected); **status=503 short-circuits → `da_unreachable` (no further candidates tried** — the DA backend is down, retrying other usernames just wastes calls); generic Error short-circuits → `hard_failure`; `ip` arg defaults to DA_SERVER_IP from the SDK module, custom `ip` overrides. **Closes all 6 RC + 6 DA operation orchestration wrappers** | ✅ Closed | `3d5cdf8` |
| Batch 7dm | M14 follow-up — request-context + DA error unwrap + email transporter (27 / 3 files): `lib/request-context` (12) — AsyncLocalStorage-backed per-request ctx propagation: outside-scope getRequestContext/getCurrentRequestId both undefined; withRequestContext binds ctx for sync + async work (Promise.resolve, setTimeout) inside the run; **two concurrent runs keep their own ctx (no bleed)**; ctx unbound after run completes; withRequestLogContext extracts x-request-id from Request, missing header → fresh UUID v4 (pattern asserted), extra handler args forwarded; **`__requestContextStorage` published on globalThis** so server-logger can read without re-importing this module (avoids pulling `node:async_hooks` into the Edge runtime); `lib/directadmin/types` (7) — unwrapDAError: AxiosError with response → {status, data, code, message} all populated, AxiosError without response (network failure) → status+data undefined but code+message kept, AxiosError with string body (HTML auth-bounce case) → data is raw string, generic Error → message only, non-Error string/object/null/undefined coerced; `lib/email/transporter` (8) — module-load throws on missing SMTP_HOST/FROM_EMAIL env, **SPF alignment warning when FROM_EMAIL domain differs from SMTP_USER domain**, getTransporter creates with right config + verify(), caches across calls, throws after verify() rejects, sendEmail rejects invalid recipient (returns false, no SDK call), success → sendMail with `"NAME" <FROM>` shape + true return, sendMail rejection swallowed → false return (no throw) | ✅ Closed | `bd949c1` |
| Batch 7dn | M14 follow-up — email barrel + client logger + device fingerprint (37 / 3 files): `lib/email/index` (23) — `EmailService` class shim re-exporting sendEmail + 22 per-topic helpers (welcome / password-reset / activation / purchase-order / order-confirmation / admin-notification / low-balance / domain-purchase / domain-registration + failure / renewal-invoice / domain-booking-status / service-reminder / expiry-today / suspension / grace-period / available / hosting-provisioned), all asserted via `it.each` tables; sendEmail referentially identical at module + class level; `lib/logger` (12) — client-side dev-only console + production /api/v1/log forwarding: dev NODE_ENV → all 5 methods write to console.*; prod → no console.log/info/debug; **error + warn forward to `/api/v1/log` via `navigator.sendBeacon`** (payload contains level + sanitized messages); log/info/debug do NOT forward (avoid noise); Error objects serialised to {message, stack, name}; **circular objects replaced with '[Circular Object]'** (defensive JSON.stringify); fetch fallback when sendBeacon is unavailable (`keepalive:true` for page-lifecycle safety); `lib/device-fingerprint` (5) — SHA-256-hashed client signals: 64-hex-char hash on first call, **cached across calls within a module load** (no re-paint of the canvas), different navigator profiles → different hashes (module reset between cases), **'fb_' prefixed non-crypto fallback** when subtle.digest throws, canvas-throws path still returns a full hash (canvas signal goes empty, others remain) | 🔄 Committed, deploy pending | `pending` |

**All four HIGHs + 11 MEDIUMs + 11 LOWs + 2 architectural suggestions (S2 + S3) cleared, M6 / L12 cleared, **M14 COMPLETE** (every component file + every hook has a test); S1 effectively closed.** 124 vertical slices shipped across batches 7a–7dn (6 RC ops + 6 DA ops + vocab unification + M13 cleanup + L1 Razorpay typed-client + fifty-nine M14 slices + 11 lib-utility slices + L8 error boundaries + L12 jsx-a11y lint + M6 leaf demotion + S2 pre-commit hooks + S3 complete twelve-slice sweep + twenty-four S1 frontend-apiClient slices). Bonus catches:
- 7e: M3's tightened `BookingStep` type uncovered a real save-validation bug — `provisioner-hosting.ts:379` emits `step: "hosting_deferred"` but the schema enum didn't include it.
- 7f: L6's `Redis | null` typing exposed 3 latent null-deref sites (rate-limit, razorpay webhook, tld-pricing-cache) — each guarded.
- 7g–7r: M1 vertical slices establish the `lib/integrations/{resellerclub,directadmin}/` pattern. ~35 `toLowerCase().includes()` chains removed from app code (down from ~50); the inner/outer classification layers now share one fragment vocabulary; payment + admin + cron paths all branch on typed outcomes instead of message strings.
- 7s: With the typed outcomes in place, the 7-arm `error.message.includes` chain in verification-error.ts turned out to be matching strings nobody throws — deleted (−65 LOC).

Remaining work: 2 MEDIUMs (M4 1000+ line page components, M6 deeper client-component refactors; M8 deferred pending prod data audit). **S1 effectively closed** (46/~58 frontend files on `apiClient`; rest is by-design raw fetch). **M14 substantially addressed** — cart-validation helpers + cartStore + 14 component/page suites across the cart-page subtree, checkout redirect gating, admin invoice-diagnostics triage, auth password-reset forms, public-facing forms, operational status badges, profile-completion form, and the full domain-search subtree (775 tests across 48 files); remaining gaps are the large multi-step `MultiStageRegisterForm`, the admin/dashboard layout shells, and the deliberately-out-of-scope payment-handler / SSE flows.

---

## Production-impacting (HIGH) — RESOLVED

### ✅ [H1] Cart store server-sync silently broken for NextAuth users
**File:** [store/cartStore.ts](store/cartStore.ts) — lines 158, 190, 205, 214, 296, 328, 356, 413
**Problem:** Every server-sync path (`addItem`, `removeItem`, `updateItem`, `clearCart`, `loadFromServer`, `saveToServer`, `mergeWithServerCart`, the rehydrate hook) gates on `safeLocalStorage.getItem("token")`. After Batch 6d's NextAuth-only auth migration, `token` is never written by the credentials login path — only by `activate/page.tsx` and `RegisterForm.tsx`. Users who log in via `/login` have `token === null`, so their cart never persists server-side even though `/api/cart` happily accepts the NextAuth cookie.
**Fix:** Replace the localStorage gate with `useSession().status === "authenticated"`, or just always attempt the fetch with `credentials: "include"` and fail-soft on 401.
**Effort:** ~1 hour.

### ✅ [H2] PendingDomain unique index is global-per-domainName but the L5 upsert was scoped to (domainName, userId)
**File:** [models/PendingDomain.ts](models/PendingDomain.ts) — `index({ domainName: 1 }, { unique: true, partialFilterExpression: { isArchived: { $ne: true } } })` + [lib/services/payment/provisioner-verification.ts:151-154](lib/services/payment/provisioner-verification.ts)
**Problem:** Rescan-1's L5 fix scoped the bulk-upsert filter to `(domainName, userId)` so two users' failed registrations stay as separate audit rows. But the schema's unique index was left global. So when user B's registration for the same name fails, the upsert filter doesn't find user A's row, tries to insert a new one, and the unique index throws E11000 — a worse outcome than the pre-fix silent overwrite. The fix is half-wired.
**Fix:** Change the partial unique index to `{ domainName: 1, userId: 1 }` (still partial on `isArchived: {$ne: true}`).
**Effort:** ~10 min + migration to drop/recreate the index.

### ✅ [H3] Dead Bearer-token plumbing across 25+ frontend files
**Files:** Rough list — `app/checkout/page.tsx`, `app/dashboard/{page,dns-management,orders,settings,domains/[id],hosting}/page.tsx`, `app/cart/page.tsx`, `app/admin/{support-tickets,support-tickets/[id],dns-management,hosting,hosting/pending,hosting/packages,domains,settings,system-settings,pending-domains,invoices/[id]/view}/page.tsx`, `components/{HostingUpgradeModal,HostingRenewalModal,DomainRenewalModal,ProfileCompletionForm}.tsx`, `lib/fetcher.ts`.
**Problem:** Batch 6d's L2 only swept 5 admin pages. The remaining files still construct `Authorization: Bearer ${safeLocalStorage.getItem("token")}` headers. For NextAuth-credential-logged-in users, `token` is null → the header is literally `Bearer null`. Auth still works because `AuthService.getUserFromRequest` falls through to the NextAuth cookie, so it's cargo-cult — but it masks any future bug where the cookie fallback breaks.
**Fix:** Sweep the remaining files, dropping the localStorage-token branch and standardising on `credentials: "include"`.
**Effort:** ~3 hours.

### ✅ [H4] MongoDB `maxPoolSize: 10` is too low for Cloud Run concurrency
**File:** [lib/mongodb.ts:48](lib/mongodb.ts)
**Problem:** Cloud Run defaults to 80 concurrent requests per instance. Every concurrent request that hits Mongo queues behind a 10-connection pool. Under steady load the latency floor for any DB call becomes `(queue depth × avg query duration)`.
**Fix:** Bump `maxPoolSize` to 30–50 and document the relationship with Cloud Run `--concurrency`.
**Effort:** ~15 min + a load test.

---

## Real flaws (MEDIUM)

### Architecture

#### ✅ [M1] No anti-corruption layer between routes/services and DirectAdmin / ResellerClub wire shapes — CLOSED (12 slices shipped, batches 7g–7r)
**Files:** 57 callsites of `DirectAdminService.*` and 65 of `ResellerClubAPI.*`/`ResellerClubWrapper.*` directly inside `app/api/**` + `lib/services/payment/`. 51 `toLowerCase().includes(...)` chains parsing upstream English across the codebase.
**Problem:** Routes and provisioners read raw upstream response shapes (`result.status === "success"`, `result.message.toLowerCase().includes("insufficient balance")`). Any wording change at ResellerClub silently flips success → failed.
**Fix:** Introduce `lib/integrations/{resellerclub,directadmin}/` modules that translate raw responses into typed `Result<Outcome, ErrorVariant>` unions. Callers branch on the variant, not the message text. Multi-week effort; can be incremental starting with the provisioner-domain helpers.

### TypeScript / Schema drift

#### ✅ [M2] Schema drift on `User.cart` and `Order.domains.hostingPlan`
**Files:** [models/User.ts:60-72](models/User.ts) (interface) vs `:291-330` (schema); [models/Order.ts](models/Order.ts) vs [lib/types.ts:31](lib/types.ts).
**Problem:** TS interface declares `cart: {…}` while the schema accepts `periodUnit` and `linkedDomain` that the interface omits. Three different `hostingPlan` shapes across cart (`{name, period, features, serverPackage?}`), Order subdoc (`{planId, name, serverPackage}`), and `CartItem` in `lib/types.ts`.
**Fix:** Single `CartItem`/`HostingPlan` type defined once, shared by Mongoose schema, `lib/types.ts`, and the cart store. ~2-3 hours.

#### ✅ [M3] `IOrder.domains.bookingStatus.step` is a tight enum in the schema but `string` in OrderDomain
**Files:** [models/Order.ts:31-39](models/Order.ts) (8-variant enum) vs [lib/services/payment/provisioner.ts:48](lib/services/payment/provisioner.ts) (`step: string`).
**Fix:** Define `BookingStep` once, share with both. ~30 min.

### Frontend

#### [M4] Five page components have crossed the 1000-line threshold
**Files:** `app/dashboard/dns-management/page.tsx` (1597), `app/admin/user-management/page.tsx` (1520), `app/admin/system-settings/page.tsx` (1387), `app/admin/hosting/page.tsx` (1375), `app/admin/dns-management/page.tsx` (1192), `app/admin/settings/page.tsx` (1008). Each carries 30-35 `useState` hooks.
**Fix:** Extract per-domain hooks + sub-components, starting with `dns-management/page.tsx` as the template. ~1 day per page.

#### ✅ [M5] `<Invoice />` is dead but static-imports jsPDF + html2canvas (~500KB)
**File:** [components/Invoice.tsx:5-6](components/Invoice.tsx)
**Problem:** Zero importers exist. 250-line dead component pulling 500KB of vendor code into any page that transitively touches `components/index.ts`. Same class as the M4 orphan-model deletion from rescan-1.
**Fix:** Delete the file. If retained for future use, dynamic-import jsPDF/html2canvas only on click.

#### 🔄 [M6] 78% of components are `'use client'` — PARTIALLY ADDRESSED (slice 7x demoted 14 leaves)
**Problem:** 134 of 172 page/component .tsx files are client-rendered, defeating App Router's perf model. Navigation header, footer, FAQ-item, EmptyState, Card, StatsCard, Skeletons could mostly render on the server.
**Fix:** Audit `components/*.tsx` for `'use client'` directives that don't actually need state/effects; demote the leaves. ~1 day.
**Slice 7x update:** 14 leaf components demoted to server. Static-render targets identified by absence of `useState` / `useEffect` / `useRef` / `useContext` / event handlers / browser APIs / `framer-motion`: `Footer`, `HeroSection`, `Section`, `AuthShell`, `Header`, `ContactInfo`, `MessageAttachments`, and the seven skeleton modules (`PageSkeletons`, `_primitives`, `DashboardSkeletons`, `AdminLayout`, `AdminPages`, `UserDashboard`, `PaymentPages`). `SessionProvider` correctly kept as client (wraps NextAuth's client provider). `FeatureCard` + `StatsCard` kept as client (use `motion.*` from framer-motion). Verified with `next build` (all routes built clean) + full unit suite (580/580 green). Remaining: ~110 client components remain — further demotions need per-component review since some use less obvious client features (refs, context, or transitive imports).

### Database

#### ✅ [M7] Redundant indexes in three models
**Files:**
- [models/Domain.ts:143](models/Domain.ts) (`{ next_action_at: 1 }`) is a prefix of `:147` (`{ next_action_at: 1, processing_until: 1 }`)
- [models/Domain.ts:142](models/Domain.ts) (`{ userId: 1, status: 1 }`) is a prefix of `:150` (`{ userId: 1, status: 1, expiresAt: 1 }`)
- [models/SystemLog.ts:29](models/SystemLog.ts) (`service: { index: true }`) is a prefix of `:44` (`{ service: 1, createdAt: -1 }`)
**Fix:** Drop the prefix-redundant single-field indexes. ~30 min + migration.

#### ⏸ [M8] `PendingDomain._id` is `Schema.Types.Mixed` — DEFERRED (needs prod data audit before backfill)
**File:** [models/PendingDomain.ts:34](models/PendingDomain.ts)
**Problem:** Accepts both ObjectId and String. Every read/write must remember which kind of id this row has.
**Fix:** Pick ObjectId; backfill any String-keyed rows. ~2 hours + migration.

### Operational

#### ✅ [M9] `/api/health` is shallow — passes when Mongo is unreachable
**File:** [app/api/health/route.ts](app/api/health/route.ts)
**Problem:** Returns 200 immediately with no DB/Redis/DA/RC probe. Cloud Run keeps promoting a process whose deps are broken.
**Fix:** Add `/api/health/deep` with `connectDB()` ping + Redis `PING` + last-DA-heartbeat-age. Point readiness probes at deep, keep `/api/health` shallow for liveness.

#### ✅ [M10] `AuthService.getUserFromRequest` emits `warn` on every unauthenticated request
**File:** [lib/auth.ts](lib/auth.ts) — `serverLogger.warn` lines on session fallthrough.
**Fix:** Demote to `debug` or remove — route layer already returns 401.

#### ✅ [M11] Hardcoded prod DA IP `136.115.64.54` as fallback default in 5 places
**Files:** `lib/services/payment/{renewal,provisioner-hosting}.ts`, `lib/services/pending-hostings.ts`, `lib/directadmin/users.ts:109`, `app/api/admin/hosting/provision/route.ts:265`
**Problem:** Forgotten env in staging/dev silently provisions onto the prod DA box.
**Fix:** Single `DA_DEFAULT_IP` constant in `config/constants.ts`; throw if missing in production. ~15 min.

### Code Quality

#### ✅ [M12] Webhook renewal-order does `new Order(...).save()` directly + `Math.random()` suffix
**File:** [lib/services/payment/webhook-handlers.ts:253](lib/services/payment/webhook-handlers.ts)
**Problem:** Last `new Order(...)` outside the service layer (audit said H1 closed this class). `orderId = \`ORD-RNW-${Date.now()}-${Math.floor(Math.random() * 1000)}\`` — same Math.random class that batch 1's [M3] fixed for invoice numbers but only ~1000 suffixes, will collide under burst.
**Fix:** `createRenewalOrder(input)` helper + `crypto.randomBytes(4).toString("hex")`. ~30 min.

#### ✅ [M13] String-message-based error dispatch in `verification-error.ts` — CLOSED (slice 7s removed the dead chain)
**File:** [lib/services/payment/verification-error.ts:219-266](lib/services/payment/verification-error.ts)
**Problem:** `else if (error.message.includes("Invalid payment signature"))` × 7. Any upstream wording change flips errors into the generic 500. 20 such `error.message.includes` patterns across `lib/`.
**Fix:** Throw typed `PaymentError("signature_error")` / `PaymentError("amount_mismatch")` from inner helpers; dispatch on `.code`. ~3 hours.
**Slice 7s update:** All seven `error.message.includes(...)` matchers in `verification-error.ts` (plus the mirrored `isPaymentError` narrowing in `guest/verify/route.ts`) were checking against strings nobody in the codebase throws — verification helpers return `NextResponse`s, RC/DA wrappers now return typed outcomes, the Razorpay SDK throws its own wording. The chain was dead defensive code. Removed (-65 LOC); kept a single generic 500 fallback for cases where the failure-state recording itself fails. The "typed PaymentError" rewrite is no longer needed since the surface to dispatch against doesn't exist.

### Testing

#### 🔄 [M14] Zero component / page / cart-store tests — SUBSTANTIALLY ADDRESSED (783 tests / 49 files: cart-validation + cartStore + 15 component suites incl. the full domain-search subtree, cart-page subtree, admin invoice-diagnostics, auth + registration forms, public-facing forms, status badges; remaining: layout shells)
**Problem:** 588 tests are all model/service/integration. 0 of 172 .tsx files have a sibling `.test.tsx`. 1500-line admin pages and the cart store (more complex than several services by line count) are entirely unverified.
**Fix:** Start with the cart store (pure logic) and the checkout `useEffect` redirect logic. Vitest + @testing-library/react is already transitively available. ~3 hours harness + incremental tests.
**Slice 7u update:** Pure cart-validation logic (`clampRegistrationPeriod` + `validateAndCorrectCartItems`) extracted from `store/cartStore.ts` into `store/cart-validation.ts` so it can be unit-tested without zustand / persistence / toast mocks. 21 new tests pin the clamping window (per-TLD min/max, hosting [1,60]), the legacy hosting-10 yearly back-fix, and the (domainName, itemType) dedup.

**Slice 7v update:** Store-level cart tests landed — 20 cases covering the restricted-TLD gate on `addItem`, the linked-hosting cascade on domain removal (with the info-toast assertion), all five getters (`getTotalPrice` / `getSubtotalPrice` / `getItemCount` / `hasDomainItems` / `hasHostingItems`), `loadFromServer` (dropped-restricted-toast + 401 no-op + validation-on-load), and the three `syncWithServer` happy paths (local-only / server-only / merge-with-dedup) plus the `isInitialized` short-circuit. Uses `vi.hoisted` + jsdom + `global.fetch` stubs; mocks `react-hot-toast` + `@/lib/logger`. Remaining: the `<Cart>` / `<CartCount>` component tests + checkout `useEffect` redirect logic (deferred — separate slice).

**Slice 7ay update — first React component tests:** The audit note's claim that `@testing-library/react` was "transitively available" was stale — it wasn't installed. Added it (+ `@testing-library/dom`, `@testing-library/jest-dom`, `@testing-library/user-event`) as devDeps; React 19 / vitest 4-compatible versions. `@testing-library/jest-dom/vitest` matchers registered once in the shared `tests/unit/setup.ts` (harmless to the model/service/store suites). First two component suites under `tests/unit/components/cart/`: `<EmptyCart>` (empty-state copy + the two CTA hrefs) and the logic-rich `<CartOrderSummary>` — item-count pluralisation, the GST-inclusive subtotal/GST/total math (`total / 1.18`), the three checkout-label states (Login / Proceed / Complete-Profile), the `onCheckout`/`onClearCart` callbacks via `user-event`, guest-option visibility gating, and the guest-email validation flow incl. the `router.push("/checkout/guest?email=…")` on a valid address (with `next/navigation` mocked). 12 new tests; full suite 614 green. There were no standalone `<Cart>`/`<CartCount>` files — the real cart UI is `app/cart/page.tsx` + `components/cart/*`, so this targets the extractable presentational pieces. Remaining M14: `CartItemCard`, the `app/cart` page-level wiring, and the checkout `useEffect` redirect logic.

**Slice 7dm update — request-context + DA error unwrap + email transporter:** 27 tests across 3 files. **`lib/request-context`** (12): the AsyncLocalStorage-backed per-request ctx that drives requestId log correlation. Outside-scope getters → undefined; `withRequestContext` binds ctx for both sync + async work spawned inside the run (asserted across Promise.resolve + setTimeout boundaries); **two concurrent runs keep their own ctx — no bleed** (drives correctness of structured logs under parallel requests); ctx is unbound after the run completes (verified by re-reading getRequestContext post-run). `withRequestLogContext` extracts `x-request-id` from the Request, missing header → fresh UUID v4 (pattern asserted), extra handler args (route params, etc.) forwarded. **`__requestContextStorage` is published on `globalThis`** as a deliberate decoupling — server-logger reads it directly there instead of importing this module, because the import would pull `node:async_hooks` into the Edge runtime bundle (middleware) and webpack rejects the `node:` scheme there. **`lib/directadmin/types`** (7): the `unwrapDAError` type-narrowing helper. AxiosError with response → all 4 fields ({status, data, code, message}) populated; AxiosError WITHOUT response (network failure) → status+data undefined but code+message preserved; AxiosError with a string body (the HTML auth-bounce case when DA's login page intercepts the request) → data is the raw string; generic Error → message only; non-Error string/object/null/undefined coerced via String(). **`lib/email/transporter`** (8): module-load env validation throws on missing SMTP_HOST/FROM_EMAIL (fail-fast at boot); **SPF alignment warning** when FROM_EMAIL domain differs from SMTP_USER domain — DKIM/DMARC reminder, deliberately a warn (not throw) so deploys still work if ops accidentally splits domains during a migration; no warn when domains match. getTransporter creates with the right config + verify()s, caches across calls (asserted by calling twice and counting createTransport invocations), throws after verify() rejects. sendEmail rejects invalid recipient addresses (validator.isEmail check) before any SDK call → returns false + error log. Success → sendMail called with `"NAME" <FROM>` shape + returns true. sendMail rejection swallowed → returns false (no throw — caller branches on bool).

**Slice 7dl update — RC registerDomain + DA createUser orchestration wrappers:** Closes 2 wrappers whose underlying **classify** functions had been tested in earlier slices (`tests/unit/lib/rc-register-domain.test.ts` and `tests/unit/lib/da-create-user.test.ts`, both from the original M1 batches) but whose **orchestration** layers — the actual wrapper files at `lib/integrations/*/register-domain.ts` and `lib/integrations/*/create-user.ts` — weren't. 15 tests across 2 files. **RC `registerDomain`** (7): the most-args wrapper in the RC layer — forwards all 6 args verbatim (`domainName`, `years`, `customerId`, optional `nameServers`, `contacts`, `tldAttributes`); passes `undefined` through for the omitted optionals (asserted explicitly, since a future refactor that defaults them to `{}` could change the RC SDK contract); success → outcome via `classifyRegisterDomainResponse`; hard_failure response → error log; thrown error matching `BALANCE_PENDING_FRAGMENTS` → `balance_pending` short-circuit (this is the axios-throws-instead-of-returning-typed-error fallback — RC's SDK has both behaviours depending on which upstream service rejects); other throws → `hard_failure` with message preserved; non-Error throws coerced via `String()`. **DA `createUser`** (8) — the username-collision retry loop, the most complex of the DA wrappers. **Empty candidate list → `'no username candidates provided'` hard_failure immediately** (defensive guard — no SDK call at all); first-candidate success → `{kind:'created', username}` with no retries; **first-candidate collision + second-candidate success** → tries the second candidate (warn log on the first failure recording the collision); **all-candidates-collide → `username_collision_exhausted`** with a final warn log containing the count — collisions are expected operational events, not errors, hence warn-level; **status=503 short-circuits** → `da_unreachable` with NO further candidates tried (the DA backend is down — retrying with a different username just wastes the calls and risks timing out); generic Error short-circuits → `hard_failure` (also no further candidates — different username won't help if it's a permission/config issue); the `ip` arg defaults to `DA_SERVER_IP` from the SDK module, custom `ip` overrides. **With this slice, all 6 RC + all 6 DA operation orchestration wrappers in the M1 anti-corruption layer have unit-test coverage**.

**Slice 7dk update — RC transferDomain + redis cache + settings-service shim:** 30 tests across 3 files. **`transferDomain`** wrapper (9): forwards all 4 args verbatim (`domainName + authCode + customerId` + optional `contacts`); RC success → outcome via classify; **`transfer_rejected` response logs at WARN, not ERROR** — the asymmetry is intentional: a registry-rejected transfer is the customer's problem (bad EPP code / transfer-lock / 60-day window), not ours, so it shouldn't page on-call; `hard_failure` response logs at ERROR as usual; thrown errors matching `BALANCE_PENDING_FRAGMENTS` short-circuit to `balance_pending` (same cron-retry path as `renewDomain`); thrown errors matching `TRANSFER_REJECTED_FRAGMENTS` → `transfer_rejected` with the message as reason — note the fragments use `'auth code'`/`'auth-code'`/`'authcode'` so a hyphenated `Auth-Info` does NOT match (pinned explicitly to catch a future "harmonize the vocab" PR that breaks downstream classification). Other throws → `hard_failure`; non-Error throws coerced via `String()`. **Closes all 6 RC operation wrappers** (register + renew + transfer + getOrderId + getDetails + getDNSRecords). **`lib/redis`** (15): tested via `vi.resetModules()` so each test re-imports with the right env stubs. **`REDIS_HOST` unset**: `redis` is null + a warn log fires on module-load + all 3 cache methods become no-ops (the underlying `RedisInstance.get/set/del` are NEVER called, asserted explicitly). Configured: ioredis constructed with `lazyConnect:true + enableOfflineQueue:false + connectTimeout:5000 + commandTimeout:3000` (the connection options are the rate-limiting / call-budget contract — a future "make redis sync" would break loud here). `redisCache.get` JSON-parses, returns null on miss, **catches both connection errors AND JSON.parse failures** + logs (the parse-failure path matters because a row written with a broken serializer would otherwise crash every consumer). `redisCache.set` JSON-stringifies + applies 120s default TTL via the `'EX'` flag (pin both the default and the custom-TTL override). `redisCache.del` calls `redis.del(key)`. **All 3 catch errors and log without throwing** — callers must keep working through cache outages, which is the whole point of the wrapper. **`lib/settings-service`** (8): the deprecated class-style shim. Pins delegation to `getSettingValue` (with `null` default), `upsertSetting` (with `'general'`/`'system'` defaults for category/updatedBy), `listSettings` mapped into the flat array shape. `clearCache` is **deliberately a no-op + info log** ("caching is disabled") — pinned so a future "add caching for performance" PR can't silently introduce stale reads without surfacing here.

**Slice 7dj update — RC renewDomain + auth-config helpers + field-encryption:** 28 tests across 3 files. **`renewDomain`** wrapper (6): RC success → outcome from `classifyRenewDomainResponse`; hard_failure response → error log; **thrown error matching `BALANCE_PENDING_FRAGMENTS`** ('insufficient balance' literal) takes a special path: classified as `balance_pending` even though it came from a throw — short-circuit so the cron-retry path keeps working when the underlying SDK throws instead of returning the typed `{status:'error'}` shape; thrown non-balance error → `hard_failure` with the message preserved; non-Error throws (raw string) still classified via `String()` coercion; the `years` arg is forwarded through verbatim. **`auth-config/helpers`** (10): the social-name extraction logic for NextAuth callbacks. `extractSocialName` per-provider: google uses `given_name`+`family_name` from the OAuth profile (falls back to `user.name` split when absent); **github prefers `profile.name`** → `login` → `user.name` (a github login like `ada-lovelace` has no space → the entire string lands in `firstName`, lastName stays empty); facebook + unknown providers fall through to splitting `user.name` only. Missing fields never throw — defensive empty-string returns. `SOCIAL_PROVIDERS=['google','facebook','github']` shape pinned. **`useSecureCookies`** is a module-load constant (not a function), so it's tested via dynamic imports + `vi.resetModules()`: true when `NEXTAUTH_URL` starts with `https://`, false for `http://`, false when unset (the `?? false` defensive nullish chain). **`field-encryption`** (12): AES-256-GCM transparent field encryption used for GST numbers etc. Round-trip pinned, output format pinned (`enc:` prefix + iv:tag:ciphertext, IV=12 bytes=24 hex chars, auth tag=16 bytes=32 hex chars). **Two encryptions of the same input produce different ciphertexts** — fresh `crypto.randomBytes(IV_BYTES)` per call, so ciphertexts don't leak that two rows hold the same plaintext. Re-encrypting an `enc:`-prefixed value is a no-op (the source short-circuits — important for the legacy-migration path where Mongoose's post-save hook would otherwise double-wrap). Decrypting a plaintext (no prefix) returns it verbatim — same migration path going the other way. Key validation throws when `FIELD_ENCRYPTION_KEY` isn't a 64-char hex string. **Tampered auth tag → decryption throws** (GCM's auth check is what makes this transparent encryption — if it failed silently, an attacker could flip bits in the DB and corrupt fields invisibly). Malformed `enc:` payloads throw 'Invalid encrypted field format' before the cipher even runs.

**Slice 7di update — DA delete/get-config/change-package wrappers:** Closes the 3 remaining DA user-op wrappers from M1. 13 tests across 3 files. **`deleteUser`** (4): happy `{kind:'deleted'}` no log; **user_not_found logged at INFO** with the copy "already gone, treating as success at callsite" — distinct from get-user-config's WARN because cleanup intent + already-deleted converges on success at the callsite, so it's not even worth a warn; status=503 → `da_unreachable` + warn; generic Error → `hard_failure` + error. **`getUserConfig`** (4): happy → `{kind:'found', config}` with the full DA config payload echoed back to the caller; user_not_found logged at WARN (terminal — caller marks the Hosting row orphaned, so the elevated log level matches the operational severity); 503 → da_unreachable + warn (transient — caller can retry); generic → hard_failure + error. **`changePackage`** (5) is the hosting-upgrade flow's plumbing — forwards both `username` + `newPackage` args (the only DA op with two arity), and has a **5th outcome kind, `package_not_found`** (no other DA wrapper has it) for config/seeding errors where the target package doesn't exist on the DA server — logged at ERROR because it's a deploy/config bug, not a runtime situation. user_not_found is also logged at ERROR because the upgrade flow has already collected payment; 503 → da_unreachable + warn; generic → hard_failure + error. With this slice, **all 6 DirectAdmin user-op wrappers AND all 6 ResellerClub operation wrappers** in the M1 anti-corruption layer have unit-test coverage (createUser was the first to land back in the original M1 batch).

**Slice 7dh update — RC read-ops + DA suspend/unsuspend wrappers:** 20 tests across 5 files — 3 RC reads + 2 DA writes. The wrappers themselves are thin: each calls into an SDK, classifies the result, logs per outcome, returns the typed union. The classify functions themselves were tested in earlier slices; here we pin the **delegation contract + logger calls**. **`getDomainOrderId`** (4): success → `{kind:'found', orderId}`; the **`data === 'null'` literal case** (RC sometimes echoes the string `"null"` as data) hits the empty-order-id branch → `not_found` + info log; an explicit 'no matching' error message → `not_found` + info; generic ERROR → `hard_failure` + error log. **`getDomainDetails`** (3): same shape — `success` with record → `found`, `'no orders found'` → `not_found` + info, generic → `hard_failure` + error. **`getDNSRecords`** (4): forwards both `domainName` + `customerId` (the only RC read-op with two args), success → `found`, `'404'`/`'not found'` → `not_found` + info, auth-failed → `hard_failure` + error. **`suspendUser`** (5): delegates with the optional `reason` arg (forwarded to DA's audit log), matches `USER_NOT_FOUND_FRAGMENTS` → `user_not_found` + warn log, **`DirectAdminError.status=503` → `da_unreachable`** (mapped specifically because 503 is DA's nginx-up-but-backend-down signature; other 5xx codes fall through to `hard_failure`), generic Error → `hard_failure` + error log. **`unsuspendUser`** (4): mirror of suspendUser, **but `user_not_found` is logged at ERROR (not warn)** because the renewal flow has already collected payment — the renewal worker logs at error so ops sees the "took money, account missing in DA" mismatch.

**Slice 7dg update — auth-secret + mongoose alias + thin DB services:** 18 tests across 6 files. **`lib/auth-secret`** (3): exports `AUTH_SECRET = process.env.NEXTAUTH_SECRET.trim()` and **throws at module-load when the env is unset** — fail-fast at boot vs silently producing broken JWT signatures at runtime. The whitespace trim matters because the secret is commonly pasted from `.env` files with trailing newlines. Tested via dynamic `import('@/lib/auth-secret')` with `vi.resetModules()` between cases. **`lib/mongoose`** (2): thin alias around `connectDB` for the legacy `connectToDatabase` import surface — pins delegation + error propagation. **`lib/services/domains`** (4): the dashboard's domain helpers. `listDomainsForUser` chains `Domain.find({userId}).sort({createdAt:-1})` (newest-first is load-bearing for the index view); `getDomainById` returns null when not found (avoids throwing on race-deleted rows). **`lib/services/payments`** (2): `createPaymentInTransaction(data, session)` uses the array form `Payment.create([data], {session})` — pinned because **this is the only Mongoose `create` signature that attaches to an in-flight transaction**; the source unwraps the returned array (single-element). Write-conflict errors propagate up so the calling transaction can abort. **`lib/services/system-logs`** (3): `recordSystemLog` defaults `level='error'`, `source='Unknown'`, `user=null` so callers only have to supply `{message}`. User-supplied values override the defaults; optional fields (url, stack, metadata, requestId, statusCode, ip) pass through verbatim or stay undefined. **`lib/services/ip-checks`** (4): `recordIPCheck` stamps `checkedAt: new Date()` server-side (the row is the source of truth for "when did we last probe"); `getLatestIPCheck` chains `findOne().sort({checkedAt:-1}).populate('checkedBy', ..., User)` and **lazy-imports the User model** for the populate call's typed 4th arg — pinned because Mongoose's typed overload doesn't include the 4th arg but the runtime needs it; a future "cleanup" that drops it would silently break population.

**Slice 7df update — protocol consts + time service + cron auth + hosting dates + guest JWT:** 43 tests across 5 files — chunkiest lib-utility slice yet. **`lib/razorpay-checkout-protocol`** (6): pins `RAZORPAY_FRAME_PATH='/razorpay-checkout'`, the open-ended `RazorpayCheckoutOptions` (minimum {key}, plus pass-through of unknown keys via `[k: string]: unknown`), the `RazorpaySuccessPayload` required fields, and the parent↔iframe message-shape contracts (compile-time checks via TS narrowing). **`lib/time-service`** (10): the dev/staging simulated-time helper. Pins the **resolution priority** — explicit parameter wins over everything, then in production the simulation is gated off entirely (NODE_ENV=production + ENABLE_TIME_SIMULATION=false → header + env ignored), in dev the x-simulated-time header wins over the SIMULATED_TIME env var, and `ENABLE_TIME_SIMULATION=true` overrides the production guard. `daysUntil` **uses Math.floor** — 9d23h returns 8 (no rounding up; matters because the renewal-prompt thresholds at 30/15 days would otherwise fire a day early). **`lib/cron-auth`** (6): the constant-time secret compare for cron worker routes. Pins the length-mismatch short-circuit (defence against `crypto.timingSafeEqual` throwing on different-length buffers), missing-env / missing-header / wrong-secret all return `false` (never throws), matching secret returns `true`. **`lib/hosting-dates`** (10): `calculateHostingDates(12)` returns `expiresAt = registeredAt + 12 months` via `setMonth` (1 year exactly, leap-day-safe); 'days' uses setDate; 'minutes' uses setMinutes (for the trial test plans); `registeredAt` and `expiresAt` are **distinct Date objects** (no aliasing — the source clones via `new Date(registeredAt)` so mutating one doesn't shift the other); `validateRegistrationPeriod` **only accepts the magic value 12**, everything else (0, 1, 2, 3, 6, 24, -1, undefined) → 1. **`lib/guest-token`** (6): the JWT helpers for guest checkout. Sign+verify round-trips the full registrant payload (name + phone + address); the 1h TTL is baked in (`exp - iat ≈ 3600`, asserted with a 5-second wiggle); verifyGuestToken **never throws** — tampered signature, wrong purpose (`purpose !== 'guest_checkout'` is asserted explicitly because a forged token with a valid signature but a different purpose could otherwise be replayed), garbage input, and an `alg=none` forgery (the `algorithms:['HS256']` whitelist in verify rejects it) all return null.

**Slice 7de update — request-id + auth cookies + zoho axios + 2 integration barrels:** 23 tests across 5 files. **`lib/request-id`** (6): the request-ID extractor that drives structured log correlation on Cloud Run. **Priority** — X-Cloud-Trace-Context (`abc123def/SPAN_ID;o=1` → `abc123def`) wins over x-request-id; absence of trace → x-request-id verbatim; absence of both → fresh **UUID v4** via `crypto.randomUUID()` (asserted with the canonical 8-4-4-4-12 pattern AND a freshness check — two calls without headers produce different UUIDs); exports `REQUEST_ID_HEADER = 'x-request-id'`. **`lib/auth-config/cookies`** (6): three NextAuth cookie groups — sessionToken (`httpOnly + Lax + path:/`), callbackUrl (**NOT httpOnly** because the client reads it for post-OAuth redirects — pinned as a deliberate security tradeoff), and csrfToken (`httpOnly + Lax`); **`__Secure-` vs `__Host-` prefix gating** on `useSecureCookies` — csrfToken uses `__Host-` (not just `__Secure-`) because the `__Host-` prefix mandates `path=/` + no Domain attribute, which is the stricter CSRF posture. **`lib/zohobooks/axios-client`** (3): a shared axios instance with `timeout: 30_000` matching the ResellerClub client budget — any single Zoho call past 30s is almost certainly a remote stall stalling Cloud Run request slots. **`lib/integrations/resellerclub` barrel** (4): re-exports all 6 ops (registerDomain / renewDomain / transferDomain / getDomainOrderId / getDomainDetails / getDNSRecords) + 6 corresponding `classify*` functions + `matchesAny` + 5 fragment-keyword lists (BALANCE_PENDING, PROCESSING_LOCK, ALREADY_IN_PROGRESS, TRANSFER_REJECTED, READ_NOT_FOUND). `matchesAny` works as a lower-case substring matcher. **Caveat**: the RC client throws at module-load when `RESELLERCLUB_API_URL/ID/SECRET` are missing — stubbed via `vi.hoisted` before the static import so the test file can be imported at all. **`lib/integrations/directadmin` barrel** (4): same pattern — 6 ops + 6 classify error fns + 3 fragment lists (USERNAME_COLLISION / USER_NOT_FOUND / PACKAGE_NOT_FOUND). `matchesAny` is shared between RC + DA integration layers; same behaviour pinned here.

**Slice 7dd update — small lib utilities:** Bundling 5 small previously-untested `lib/` files that other tests have been quietly relying on. 21 tests across 5 files. **`lib/utils`** (6): `cn()` — joins, filters falsy values (undefined / null / false / ''), supports the clsx object form for conditional classes, and **uses tailwind-merge to dedupe conflicting Tailwind classes** (`cn('p-4', 'p-2') === 'p-2'`, `cn('bg-red-500', 'bg-blue-500') === 'bg-blue-500'` — the latest-wins semantics are what keeps `className` props from accumulating broken duplicate classes). `getSupportEmail()` — returns `process.env.SUPPORT_EMAIL` when set, falls back to `support@anutech.in`. **`lib/fetcher`** (4): the SWR fetcher used by every dashboard `useSWR` call. Pins `fetch(url, { headers: {'Content-Type':'application/json'}, credentials:'include' })` (cookie auth, not Bearer); returns parsed JSON; **non-2xx throws a typed Error** with `.status` + `.info` populated from the response JSON (consumed downstream by SWR's onError); a non-JSON error body falls back to `.info={}` (defence — Cloud Run can return HTML error pages). **`lib/constants`** (4): `INDIAN_STATES` has exactly 36 entries (28 states + 8 UTs), all canonical names present, **sorted alphabetically** (the register form's state-select reads this array in order). **`lib/tld-min-periods`** (3): thin backwards-compat wrapper around `lib/tld-policies` — `TLD_MIN_PERIODS` only includes TLDs whose `minYears > 1` (asserted bidirectionally — every entry has matching `TLD_POLICIES[tld].minYears`, and no TLD with `minYears <=1 || undefined` is in the map); `getMinRegistrationPeriod` delegates verbatim to `getMinYears`. **`lib/security-headers`** barrel (3): `addSecurityHeaders` / `addCorsHeaders` / `buildPreflightResponse` all referentially identical (`===`) to the implementations at `lib/security/headers` — pins the backwards-compat shim so a future rename surfaces.

**Slice 7dc update — nested domain-search hooks:** `components/domain-search/hooks/useDomainPricing.ts` + `useDomainSearch.ts`. 24 tests across 2 files. **useDomainPricing** (3): a thin `Intl.NumberFormat('en-IN', {style:'currency'})` wrapper — pins the ₹ glyph + Indian-locale grouping (`1,00,000` for one lakh, distinct from the western `100,000`), the optional currency override (USD renders with the leading dollar sign), and the zero-fraction-digits guarantee (1234.99 → never a decimal). **useDomainSearch** (21): the 342-line state machine plus the two exported pure helpers. **validateDomainInput** (7 — pure): `'example.com'` → valid with `baseDomain='example'` + `suggestedTld='com'`; `'example.co.uk'` → multi-segment TLD preserved; bare `'example'` → valid with `suggestedTld=null` (triggers multi-TLD search mode); `'ab'` (the regex's 2-char minimum) accepted; single `'a'` → invalid with `warning` copy; `'foo!'` → invalid (special chars); **whitespace is stripped before validation** so `'  example  .com '` still parses correctly. **getSuggestedTlds** (2): returns `TOP_TLDS` filtered through `isRestrictedTLD`, capped at 20 — the cap is enforced even if upstream adds more TLDs. **State machine** (12): initial state (empty), **localStorage rehydration** from `domainSearchState` on mount, `initialSearchTerm` + `autoSearch=true` triggers a search via a 100ms `setTimeout`, invalid input → `toast.error` + early return (no API call), **`redirectOnSearch=true`** routes via `router.push('/domains/search?q=…')` instead of fetching (the home-page search box pattern), happy-path Phase-1 quick → `setResults` + the second Phase-2 suggestions+hostingExists fetch fires after, **quick `restricted_tld` error short-circuits** with toast.error + `setError` AND skips the Phase-2 suggestions fetch (no point spending an extra API call when the search is hard-blocked), quick network error → 'Network error' toast + error state, `handleInputChange` branches for bare/with-TLD/invalid inputs each setting the right `searchMode` + `baseDomain`, and `clearSearch` resets all state. With this slice, **every component AND every hook in the project has a corresponding test file**.

**Slice 7db update — top-level hooks:** All 3 top-level hooks now covered (`hooks/useModalScroll.ts`, `hooks/useDomainRequirements.ts`, `hooks/useUser.ts`). 18 tests across 3 files. **useModalScroll** (6 — including `useScrollToTop`): isOpen=true → body locked (overflow=hidden, position=fixed, top=-scrollY, width=100%) at the captured offset; isOpen=false leaves the styles cleared; the **true→false transition restores styles but does NOT call window.scrollTo** — pinned as a known quirk because the prior effect's cleanup wipes `body.style.top` before the false-branch reads it. Mounting with isOpen=false AND a pre-existing `body.style.top='-150px'` DOES call `scrollTo(0, 150)` (the cleanup-via-other-means path). Unmount-while-open cleanup restores styles. useScrollToTop scrolls (0,0) on mount. **useDomainRequirements** (4): non-restricted TLD → empty arrays + `requiresVerification=false` (the data fns aren't called, asserted via `expect(mock).not.toHaveBeenCalled`); restricted TLD → arrays populated from `getDomainRequirements` + `generateAlternativeDomains` with the right args; `openModal`/`closeModal` toggle `isModalOpen`; `handleSelectAlternative` closes the modal (and `console.log`s the choice — spy'd to suppress test output). **useUser** (8) — the shared dashboard auth hook. Mocks next-auth/react, next/navigation, swr (default export), fetcher, safeLocalStorage. status='loading' → isLoading=true + user=null; unauthenticated + no token → `router.push('/login')` guard; unauthenticated WITH a legacy `token` in localStorage → push NOT called (avoids redirect loops on the credential-login flow that pre-dates NextAuth); authenticated session before SWR resolves → fallback `DashboardUser` derived from `session.user` fields (name split on space, role default 'user'); **`/api/auth/me` data wins** over the sessionUser fallback (the canonical DB record); **admin role** → `window.location.replace('/admin/dashboard')` (full-page swap, not router.push — admin can't be in user dashboards); **localStorage 'user' is the legacy fallback** when there's neither session nor SWR data; **defensive `useSession()` undefined race** doesn't crash and is treated as loading.

**Slice 7da update — M14 COMPLETE 🎉:** `<ChatWidget>` — the 260-line streaming-SSE chat widget, the last untested component in `components/`. 10 tests / 1 file. With this slice, **every component file under `components/` (and its subdirectories) now has a corresponding test file** — the M14 sweep that started back in slice 7ay is closed. The widget itself uses a `ReadableStream` reader to consume server-sent-event chunks; the test mocks `global.fetch` with a hand-rolled stream shim that yields chunks via a counter-incrementing `read()` and stubs `Element.prototype.scrollIntoView` (jsdom doesn't implement it). Pins: closed by default; toggle opens panel + shows greeting; **sessionStorage hydration** restores prior conversation on mount, **dropping a trailing-empty-assistant placeholder** if the last stream was interrupted (defensive — otherwise the 'Typing…' shell would reappear forever); Close (X) hides panel (uses `getAllByRole` because when the panel is open, the toggle button also carries `aria-label='Close chat'`); Send button is disabled when input is empty; **the streaming flow** is the meatiest assertion — POST to `/api/v1/chat`, then SSE-style `data: {"text":"..."}` chunks accumulate into the trailing assistant message via a functional `setMessages((prev) => ...)` updater, and `data: [DONE]` terminates; **malformed chunks** (interleaved `data: this-is-not-json` line) are silently swallowed by the inner `try/catch` and the stream continues; network failure path (`ok:false` from fetch) → assistant message replaced with 'Sorry, something went wrong… contact support@anutech.in'; sessionStorage persists each settled exchange (the user message + the accumulated assistant reply); **Enter without shift submits**; Shift+Enter does NOT — pinned because Shift+Enter is the conventional multi-line shortcut and a future patch could accidentally trigger submit.

**Slice 7cz update:** `<RegisterForm>` — the 486-line registration orchestration, the biggest single component in the M14 sweep so far. 11 tests / 1 file. Subform sections (PersonalInfo / Address / Credentials) already have standalone tests from slices 7cg/7ch; this slice focuses on the **4-step state machine + submit paths**. Mocks: `useRouter`, `react-hot-toast`, `safeLocalStorage` (Map-backed), `apiClient.post`, and shims for GoogleRecaptcha / Card / Logo / Button / SocialLoginButtons. **Critical mock detail**: the three subform sections render **real `<input>` elements** (testid-prefixed) so `user.type()` drives one change event per character. This is necessary because the source's `handleChange` uses **non-functional `setFormData({...formData, [name]: value})`** — multiple synchronous onChange calls would clobber each other since they all read the closure-captured stale formData. Pins: heading + 'sign in to your existing account' link + 3 sections render; submit on step 1 with empty fields → 'fill in all required fields' toast (no advance, no API call); **password length 1–7 disables Create-account button, ≥8 re-enables** (gating for half-typed passwords); fill+submit chain through 4 steps → final `apiClient.post('/api/v1/auth/register', …)` body has the full register payload (firstName/lastName/email/password/phone/phoneCc='+91'/companyName/address.{line1,city,state,country:'IN',zipcode}); successful register → token + user persisted to localStorage, **token cookie set with `SameSite=Lax`** (asserted via `document.cookie`), 'Registration successful!' toast, `registerFormData` cleared from localStorage, 100ms-delayed `router.push('/dashboard')`; **password mismatch on final step → toast.error + NO apiClient.post call** (so a typo doesn't burn a registration attempt at /register on the server); apiClient fail with server-supplied message → that exact message surfaces in toast.error; apiClient fail with `status=0` (network/fetch failure) → generic 'An error occurred. Please try again.' toast (avoids leaking 'Failed to fetch' to the user); `registerFormData` persists to localStorage as the form fills (passwords excluded); **rehydrates on mount** — pre-seeded localStorage is NOT wiped by the initial empty-state render; detectLocation: when `navigator.geolocation` is undefined → 'Geolocation is not supported' toast.

**Slice 7cy update:** `<DomainSearch>` orchestrator — the 292-line search-page parent that ties SearchInput + SearchResults + DomainRequirementsModal together. 14 tests / 1 file. Heaviest mock set yet — cartStore, useRouter, toast helpers, `apiClient.post`, the `domainRequirements` helpers (`requiresAdditionalDetails` + `isDomainSupported`), the `useDomainSearch` hook, and all three subcomponents replaced with thin stubs exposing the orchestration callbacks (onAddToCart / onShowRequirements / onWatch) via test buttons so we can drive each handler in isolation. Pins SearchInput + SearchResults + hero copy by default; `showHeroText={false}` hides the hero heading. **addToCart paths** (4): standard domain → `cartStore.addItem({…, itemType:'domain'})` + success toast + **1s-delayed `router.push('/cart')`** (the user-feedback pause before redirect); requires-additional-details TLD (e.g., `.au`) → opens the requirements modal (no cart write); unsupported domain → error toast (no cart write); missing price/available → 'missing required data' error toast. handleShowRequirements opens the modal directly; close button hides it. **handleWatch paths** (5): 200 → 'we'll email you when X becomes available' success toast; 401 → opens the sign-in prompt with **the Sign In + Create Account anchors both carrying `?returnUrl=/domains/search?q=<base>`** (the base domain before the dot — so a user signing in to enable the watch returns to the same search context); 409 → 'Already watching X' error toast (no sign-in prompt); 400 → error toast with server-supplied message (per-route 400s carry the right copy); default 500 → generic 'Failed to watch domain' toast.

**Slice 7cx update:** `<LoginForm>` — the 391-line credentials-login orchestration component, the highest-traffic auth surface. 15 tests / 1 file. Heaviest mock cocktail in the cycle so far: `signIn`, `useRouter`+`useSearchParams`, `showSuccessToast`/`showErrorToast`/`showAccountDeactivated`, `safeLocalStorage` (Map-backed), `logger`, plus GoogleRecaptcha + AuthShell + SocialLoginButtons replaced with light shims so the test stays focused on the orchestration logic. Pins the form chrome (email/password/Remember-me/Forgot-password), the 'Create an account' link respecting the existing `?returnUrl=`, the **three banner types** (green activation banner from `?message=`, the checkout-required banner from `?returnUrl=/checkout`, the deactivated banner from `?error=AccessDenied` which **also cleans up the URL** via `history.replaceState` so a refresh doesn't redisplay it), and the password eye-toggle. The **signIn result paths** are pinned individually: CredentialsSignin → 'Invalid email or password'; **TotpRequired reveals the 2FA step WITHOUT an error toast** (correct password, just missing 2FA — important UX so users don't think their password was wrong); InvalidTotpCode → 'Invalid authenticator code' toast; AccountNotActivated → toast.error + 1s-delayed `router.push('/activate?email=…')`; AccountDeactivated → the `showAccountDeactivated` helper; ok=true → success toast + **100ms-delayed `window.location.href = safeReturnUrl`** (full reload is intentional — set in code comments to ensure the session cookie is sent on the next request, avoiding the middleware redirect loop back to /login). Remember-me persistence: checked → `setItem('rememberMe','true')` + `setItem('savedEmail', email)`; unchecked → `removeItem` both. **Open-redirect defence pinned explicitly** — `?returnUrl=https://evil.example.com/steal` falls back to `/dashboard` because the `safeReturnUrl` guard requires the URL to start with `/`.

**Slice 7cw update:** `<NameServerManagement>` — the 349-line localStorage-backed nameserver CRUD panel. 11 tests. Mocks `safeLocalStorage` (a `vi.hoisted` Map-backed store so each test gets a fresh state) + `react-hot-toast`. Pins: on mount, the hard-coded RESELLERCLUB default appears + any persisted customs are prepended; **hydration parses the stored JSON** (custom entries restored with their full shape); 'Add Custom' opens the inline editor + Cancel closes it without persisting; empty-name save → toast.error 'Please enter a configuration name'; all-empty servers save → toast.error 'at least one nameserver'; **Save success path** persists ONLY the customs to localStorage (the RESELLERCLUB default is hard-coded in the file, NOT persisted — pinned explicitly by asserting `persisted.find(c => c.isDefault) === undefined` so a future change accidentally double-persisting the default surfaces here); 'Add Another Nameserver' grows the input list (ns1/ns2/ns3 placeholder progression); Delete on a custom row removes + persists; **the default row does NOT render a Delete button at all** (asserted via `getAllByTitle('Delete Configuration').length === 1` with one custom present — defensive coding pattern is to not even expose the action vs error-toasting on click); Set Active flips `isActive` across configs + persists the new active state; Reset to Default Nameservers restores the default as the active config; **malformed localStorage JSON** ('{not-valid-json') gracefully falls back to the default-only list (caught by the try/catch in the load effect — pinned because corrupt localStorage from a partial write can break the whole panel otherwise).

**Slice 7cv update:** Public `<Navigation>` — 354-line component covering all three variants. 11 tests / 1 file. Mocks: `usePathname` + `useSession` (typed to model the undefined-race safe-destructure) + `useCartStore` with its static `subscribe`. **Default variant** (7): 4 desktop nav links (Home, Hosting, About Us, Contact Us — both desktop and mobile menus render their own copies so `getAllByRole` is used to acknowledge both); unauthenticated session → Login link visible + Dashboard link absent; authenticated session → Dashboard link visible + Login link absent; active-link blue colour applied to the matching pathname (matched via the `google-blue` substring in the className since the component uses CSS vars); cart-count badge appears once `getItemCount() > 0`; mobile menu button labelled 'Toggle mobile menu' and click toggles the menu state (smoke); **defensive `useSession()` undefined race** does not throw — falls back to the unauthenticated branch with Login visible (matches the same defensive pattern in `<CookieConsentBanner>` from 7cd). **Dashboard/admin variants** (4): when onLogout is supplied, the Logout button renders and click invokes the callback; **role-chip palette** swaps from blue (`bg-blue-100` for `role=user`) to red (`bg-red-100` for `role=admin`) — the operational signal that an admin session is active in the chrome; Shopping Cart link is always present with the aria-label and href=/cart, count badge follows the same > 0 gate; the dashboard/admin shells **do NOT render** the public-site nav links (Hosting, About Us, Contact Us) — pinned so a future refactor accidentally exposing those on /admin surfaces in tests.

**Slice 7cu update:** `<HostingRenewalModal>` — the hosting variant of `<DomainRenewalModal>` (slice 7ct). 10 tests / 1 file. Where DomainRenewalModal uses a mock paymentId, HostingRenewalModal goes through the full **Razorpay iframe checkout** flow + a verify POST + a router.push to /payment-success — so the mock setup is heavier: `global.fetch`, `next-auth/react.useSession`, `next/navigation.useRouter`, `safeSessionStorage` (the post-success message hand-off), `useRazorpayCheckout` (open + Frame), and `react-hot-toast.toast`. Coverage: isOpen=false renders nothing + skips the fetch; isOpen=true → `/api/v1/user/hosting/renew-info?domainName=…` fetch with `credentials:'include'` and the header chrome renders; successful renew-info → plan name 'Business' + ₹12,000 cost visible; **renew-info HTTP error → toast.error + onClose** (auto-close on load failure, distinct from DomainRenewalModal which keeps the modal open); fetch throws → generic toast.error + onClose; click Pay & Renew Now → POST `/api/v1/user/hosting/renew` then `razorpay.open(…)`; **razorpay dismissed → soft-cancel** (isProcessing flips back, no toast — important UX); POST fail → toast.error with server message and no razorpay.open call; **non-dismissed razorpay error → toast.error(tagged.message)** ('Card declined' in the test); **happy path** order POST → razorpay success → verify POST success → 'Hosting renewed successfully!' toast + `safeSessionStorage.setItem('paymentResult', …)` written + onClose + `router.push('/payment-success')` (the post-payment redirect, verified directly against the pushMock); verify failure → toast.error('Signature mismatch') + modal stays open so the user can retry without re-opening.

**Slice 7ct update:** `<DomainRenewalModal>`. 9 tests. Mocks `global.fetch` + `react-hot-toast.toast`; uses `vi.setSystemTime('2026-05-30')` so the `daysUntilExpiry` math is deterministic across CI runs. Pins: isOpen=false renders nothing AND does NOT issue the fetch (the useEffect is gated on isOpen — important to avoid a spurious server call on every page render); isOpen=true → shows the 'Loading renewal information...' copy + calls fetch with `/api/v1/domains/renew?domainName=anutech.com&years=1` and `credentials:'include'`; successful response renders the Current Status block + Renewal Cost (₹1,500 via formatIndianCurrency) + all **6 renewal-period buttons** (1/2/3/4/5/10 — anchored exact-match incl. the **'1 Year' vs '2 Years' pluralisation** boundary); HTTP error path → toast.error with the server-supplied message; **expiry ≤ 30 days** (10 days from "now" in the test) surfaces the red 'Your domain is expiring soon! Renew now to avoid service interruption.' warning block; clicking a different period button refetches with the new `?years=N` query param (the loadRenewalInfo dep array picks up `selectedYears`); Cancel button fires onClose; **successful renew POST** → 'Domain renewed successfully for N year(s)!' toast + onClose called; **failed renew POST** → toast.error with server message + modal stays open (no onClose call — so the user can retry without re-opening).

**Slice 7cs update:** `user/UserLayout` + `<GoogleRecaptcha>`. 21 tests across 2 files. **user/UserLayout** (14): mocks `usePathname`, `useCartStore` (with the `subscribe` static), `ProfileCompletionWarning`, and `DataLoading`. Pins the 6-item nav (Dashboard/Domains/Hosting/Invoices/Support/Account Settings — Invoices uses RupeeIcon and ₹ ends up in the link's accessible name, so a substring match is used for that one specifically while the others are anchored exact-match). Active-link routing applies `text-blue-700 border-blue-700` to the matching pathname; the top-bar heading reflects the active nav item and falls back to 'Dashboard' for unmatched routes. User-info block shows name + email when user present; 'Loading...' + 'Please wait' fallback when null. **Three logout-button states** — onLogout + user → active button (`data-testid="logout-button-active"`, not disabled); onLogout + user=null → disabled 'Loading...' button (`data-testid="logout-button-disabled"`); onLogout absent → inactive 'No logout handler' block (`data-testid="logout-button-inactive"`). Click awaits async onLogout. ProfileCompletionWarning is always rendered. isLoading=true swaps children for the DataLoading skeleton (children removed from the DOM). `hideFloatingButtons=true` removes the floating Home link; default render includes it. **GoogleRecaptcha** (7) — heaviest browser-integration component in the cycle. Mocks `RecaptchaClient.render` + `RecaptchaClient.reset` + `apiClient.get` + `logger`; `vi.stubEnv` controls `NEXT_PUBLIC_RECAPTCHA_SITE_KEY`. **captcha-status `enabled=false`** → onSuccess('captcha-disabled') and no widget render (the admin-kill-switch path used during dev/test environments). Missing/placeholder site key (`your-recaptcha-site-key`) → onSuccess('manual-pass') — the local-dev escape hatch. Happy path: `RecaptchaClient.render` invoked with the supplied theme + size; the widget's `callback` arg forwards tokens to onSuccess. `expired-callback` fires onExpire + `reset(widgetId)`. `error-callback` fires onError + a visible 'reCAPTCHA verification failed' message. **'already been rendered' render-error is swallowed** (no onError, no visible error — pinned because React 19 StrictMode double-mounts can otherwise fire spurious errors). Other render errors → onError + 'Failed to render verification widget' visible message.

**Slice 7cr update:** `<HostingUpgradeModal>` orchestration — the full 272-line modal that drives the hosting-upgrade payment flow. 11 tests focused on the **step state machine** rather than the visual chrome (SelectPlanStep + ConfirmStep were covered as standalone units in 7cg/7ch). Mock setup is the heaviest in the cycle so far: `global.fetch` via `vi.stubGlobal`, `next-auth/react.useSession`, `next/navigation.useRouter`, `@/lib/storage.safeSessionStorage`, `useRazorpayCheckout` (the entire hook return — `open` + a stub `Frame`), and both subcomponents replaced with thin mocks that expose `onSelectPlan`/`onCancel`/`onBack`/`onPay` via test-only buttons. Pins: isOpen gate; initial `/api/v1/user/hosting/upgrade-info?domainName=…` fetch with `credentials:'include'`; success + `eligiblePlans>0` → 'select' step; **`eligiblePlans=0` → 'error' step with 'highest available plan' copy** (this is the operational hint that drives the customer-support flow); HTTP error → 'error' with server-supplied message; fetch throws → generic 'Failed to load upgrade options' error; plan-pick transitions to 'confirm' step (SelectPlanStep unmounts, ConfirmStep mounts); back from confirm → 'select'; **`razorpay.open()` rejecting with `{kind:'dismissed'}` is a soft-cancel back to 'confirm'** (NOT 'error' — important UX); upgrade order POST fail → 'error' with server message; Retry from error step re-issues the upgrade-info fetch. Verification + success/redirect path is intentionally out of scope here — it touches `safeSessionStorage` + a 2.5s `setTimeout` + `router.push('/payment-success')` and would dominate the test setup. The `paying`/`verifying`/`success` UI strings stay covered by the smoke-style 'visible chrome' assertions implicit in the other transitions.

**Slice 7cq update:** Two domain-related modals — `<DomainRequirementsModal>` (the verification-required dialog for restricted TLDs) + `<DomainSelectionModal>` (the hosting-plan-needs-domain dialog). 16 tests across 2 files. **DomainRequirementsModal** (8): isOpen gate, intro + Important Notice block, optional Required Information list, **Restrictions per-type colour fork** (error→`text-red-600`, warning→`text-orange-600`, info→`text-blue-600` — pinned because each restriction type drives a separate operational signal), Alternative Options list rendering with the **`onSelectAlternative(domain)`** callback receiving the exact domain string (the click goes on the entire row container, not a button), alt.available true/false swaps a CheckCircle vs X trailing icon, Close fires onClose, **Contact Support button opens `mailto:?subject=Domain Registration Support` via `window.open`** (asserted with `vi.spyOn(window, 'open')`), and the **defensive `Array.isArray()` guards** — passing `"oops"` or `null` for requirements/restrictions does NOT crash (these props arrive from an API and could be malformed in production). **DomainSelectionModal** (8) — cartStore + react-hot-toast mocked via vi.hoisted. isOpen gate; empty input → 'Please enter a domain name' error (no addItem); invalid format ('not-a-domain') → 'Please enter a valid domain name' (no addItem); valid domain + no existingDomainName → **`addItem` called with the exact `hostingItem` shape** (lowercased domain via `.toLowerCase()`, 12-month period, `itemType:'hosting'`, hostingPlan nested object); `existingDomainName='hosting-abc'` placeholder leaves input empty (the `startsWith('hosting-')` defensive cleanup so the user enters their real domain); user-supplied existing value pre-fills the input; **existingDomainName + matching cart item → `updateItem(oldDomain, ...)` (NOT addItem)** to swap the placeholder in-place; typing after an error clears the error message inline.

**Slice 7cp update — slice 100 🎉:** admin `<AdminLayout>` + admin `<InvoiceDiagnostics>` — the rescan-4 cycle's hundredth slice, and the first of the bigger admin orchestration components. 19 tests across 2 files. **`admin/AdminLayout`** (10) — distinct from `skeletons/AdminLayout` (the loading-state shell in 7cl). Mocks `next/navigation.usePathname` via vi.hoisted. Pins: the **11-item nav** rendered as anchored exact-match links (Dashboard / Users / Orders / Invoices / Payments / Pending Domains / Support Tickets / Hosting / Domains / TLD Pricing / Settings — anchored because 'Domains' is a substring of 'Pending Domains'), the children slot into the main content area, the **active-link routing** (matching pathname picks up `text-blue-700 border-blue-700`), the **two special cases** — `pathname='/admin'` (no /dashboard suffix) still treats Dashboard as active, `pathname='/admin/dns-management'` treats Domains as active. User-info display: initials from `firstName.charAt(0) + lastName.charAt(0)` ('AL' for Ada Lovelace), 'AU' fallback when user=null. `onLogout` prop renders + fires the Logout button; absent prop hides it. Mobile menu button labelled `Open navigation menu`. **`admin/InvoiceDiagnostics`** (9) — heavy mock setup: `apiClient.get/post`, `confirmDialog`, `showSuccessToast`/`showErrorToast`, plus all 3 subcomponents (`DiagnosticsHeader`, `ConflictsTable`, `StuckOrdersTable`) replaced with thin mock components exposing their props via `data-testid`. Pins the loading skeleton, the **auto-expand logic** (empty issues → collapsed; conflicts > 0 or stuck > 0 → opens automatically — important UX behaviour), the clear-invoice-number flow (`confirmDialog` cancelled → no POST; confirmed → POST to `/api/v1/admin/orders/{id}/clear-invoice-number` + success toast + refetch — `apiGetMock` is called twice), single re-sync flow, the **empty-stuck-list short-circuit** (no confirm dialog opens), the all-succeed bulk path (success toast with count), and the **partial-failure bulk path** ('1 succeeded, 1 failed' error-toast copy — the in-test stuck list is sequenced as one ok + one fail to exercise the mixed-outcome branch).

**Slice 7co update:** UserDashboard + AdminPages skeleton families — closing out the last two big skeleton barrels. 22 tests across 2 files. **`skeletons/UserDashboard`** (13): DashboardLayoutSkeleton slots children into `<main>` (asserted via a `data-testid` on a child div) and renders > 15 `.skeleton` placeholders in the sidebar/topbar shell (logo 2 + 9 nav rows × 2 + user block 3 + mobile topbar 3 = 26). The 11 page-level skeletons (DashboardHome, Orders, Domains, Invoices, Hosting, Support, Referrals, Settings, DNS, TicketDetail, DetailPage) are covered by an `it.each` smoke table: each renders without throwing + has > 5 placeholders. **`skeletons/AdminPages`** (9): 9 admin-page skeletons (AdminUsers, AdminPayments, AdminSupport, AdminPendingDomains, AdminHosting, AdminPricing, AdminGeneric, AdminDashboard, AdminSettings) covered by the same `it.each` smoke pattern — > 5 placeholders each. Intentionally smoke-level rather than placeholder-count pinning: the goal is to catch a future refactor accidentally yielding an empty skeleton (e.g. an early-return on a bad condition) without locking down internal layout decisions.

**Slice 7cn update:** Barrel re-exports + payment skeletons + the example component — 12 tests across 4 files. **`skeletons/PageSkeletons` barrel** (4): namespace-imports the barrel and asserts a representative entry from each topical file (AdminLayout, AdminPages, UserDashboard, PaymentPages) is `typeof === 'function'` — pins the re-export contract so a future rename of any underlying skeleton (e.g. `AdminLayoutSkeleton` → `AdminShellSkeleton`) breaks loudly here, even when other consumers haven't been updated. **`components/DomainSearch` barrel** (1): the 4-line backwards-compat shim's default export is **referentially identical** (`===`) to the refactored module's default at `domain-search/DomainSearch` — a stronger pin than just `typeof function`. **`PaymentPages` skeletons** (3): CheckoutPageSkeleton renders > 10 `.skeleton` placeholders and the `lg:grid-cols-6` desktop content grid (load-bearing for the checkout layout); PaymentSuccessPageSkeleton renders the `min-h-screen` success shell + > 5 placeholders; CartPageSkeleton renders > 5 placeholders. **`<DomainRequirementsExample>`** (4) — example component that demos the modal pattern via the `useDomainRequirements` hook. Both the hook and the `<DomainRequirementsModal>` are mocked via `vi.hoisted` (the modal mock is a fake that exposes `data-domain` / `data-tld` data attrs from its props). Trigger button always renders; the **`requiresVerification` gate** ensures the modal is absent even when `isModalOpen=true` if the hook said the TLD doesn't need verification (defensive coding for non-restricted TLDs); when the gate is true and the modal is open, the modal receives `domain="anutech"` and `tld=".au"` from the hook's hardcoded args; button click invokes the hook's openModal callback.

**Slice 7cm update:** Dashboard skeleton family + cart-page skeleton — 14 tests across 2 files. **DashboardSkeletons** (11) — these are the user-dashboard-side skeletons (different namespace from `skeletons/_primitives` covered in 7cl, which uses the global `.skeleton` class; these use Tailwind `animate-pulse` directly on each `<SkeletonBar>`): SkeletonBar default w-full/h-4 + the width/height props override the Tailwind classes verbatim; SectionCardSkeleton renders one title bar + N body rows (defaults to 1+3=4); TableSkeleton renders one header bar + rows×cols data cells (defaults 1+30=31); ListSkeleton renders 2 bars per item (title + subtitle); FormSkeleton uses a fixed 8-field × 2-bar layout + a submit button (17); DNSManagementSkeleton composes a 2-row SectionCardSkeleton + a 4×5 TableSkeleton (1+2 + 1+20 = 24); OrdersSkeleton/DomainsSkeleton/AdminTableSkeleton are thin wrappers over TableSkeleton with their specific row/col dimensions (6×6 / 6×5 / 8×6); and the **default export `<DashboardSkeletons />` is intentionally `null`** — the file is barrel-only, the default exists for backwards-compat with any router-fallback wiring that used to render it. **SkeletonCart** (3): a 55-placeholder snapshot of the cart-page loading shell (drift from this count signals a real shell refactor that should be audited), the 4-col trust-indicator strip pinned via `md:grid-cols-4`, and the main 6/7/8-col content grid pinned via `lg:grid-cols-6`.

**Slice 7cl update:** SocialLoginButtons + skeleton primitives + admin skeleton — 17 tests across 3 files. **SocialLoginButtons** (7) — heaviest mock setup so far: `next-auth/react.signIn`, `react-hot-toast.default.{error,success}`, and `@/lib/toast.showAccountDeactivated` all stubbed via `vi.hoisted`. Asserts the env-gated provider list (Facebook + GitHub env vars are off in tests, so only Google renders), the 'Or continue with' divider, the exact `signIn('google', {redirect:false, callbackUrl:'/dashboard'})` call shape, the **`result.error='OAuthSignin'` mapped-message** ('Failed to sign in. Please try again.') plus toast.error + onError fan-out, the **`result.error='AccessDenied'` deactivated-account branch** that calls `showAccountDeactivated(SUPPORT_EMAIL)` with the `'Account deactivated'` onError code instead of toast.error, the `result.ok=true` success path (toast.success + onSuccess — uses fake timers to dodge the 2s `window.location.href` redirect setTimeout), and the **catch-all** that surfaces 'An unexpected error occurred' when signIn throws. **`_primitives`** (7): Sk → div + 'skeleton' class + className passthrough, PageHeader 3 placeholders with `wide=true` swapping `w-48` → `w-64`, TableSkeleton placeholder math (toolbar 2 + header cols + rows×cols data — defaults to 6×5 → 37 total), FormSection title=true vs false toggles the title bar (4 fields × 2 + 1 title = 9; without title = 6). **`skeletons/AdminLayout`** (3): AdminTableRowsSkeleton row math (header cols + per-row 8-cell signature: icon-tile + 2 lines + middle cells + 2-button tail), AdminLayoutSkeleton renders sidebar (logo block + 8 nav rows) + topbar + slots children into `<main>` (20 skeleton placeholders total in the shell).

**Slice 7ck update:** Footer + AuthShell — the two big public-page chrome leaves still untested. 13 tests across 2 files. **Footer** (6): the company-info paragraph + the 5 Quick Links (Home / About Us / Contact Us / Login / Register) each asserted by exact name + their target href; the Services section (Domain Registration / DNS Management / SSL Certificates) using **exact-text matches** because the company intro paragraph also mentions 'DNS management' (lowercase) — the regex `/dns management/i` was double-matching, exact strings disambiguate; Web Hosting linked to `/hosting`; the four policy links in the bottom strip (`/privacy`, `/terms-and-conditions`, `/data-deletion`, `/cancellation-refund`); current-year copyright line; the three social-media buttons each carrying their accessible `aria-label`; className passthrough to the outer `<footer>`. **AuthShell** (7): the form-side title rendered as an `<h1>` (so a future swap to h2 surfaces); the `subtitle` optional render gate (present vs absent); the children render inside the form panel; the **default brand-panel eyebrow** ('Anutech Digital') + **default panelTitle** ('The clean way to own your online identity') — note 'Anutech Digital' also appears in the footer copyright, so the test asserts at least 2 matches; the `panelEyebrow` + `panelTitle` props override the defaults; the footer copyright includes the current year; and the brand-panel Logo is wrapped in a Link back to `/`.

**Slice 7cj update:** EnhancedToast + Razorpay iframe — two of the trickier untested components. 16 tests across 2 files. **EnhancedToast** (8) + **ToastContainer** (1): title + optional message render, **per-type icon + colour palette** — success → green (`bg-green-50` + `text-green-600` CheckCircle), error → red (XCircle), warning → yellow (AlertCircle), info → blue (Info). Close button click sets internal `isVisible=false`, then after the 300ms exit-animation `setTimeout` fires `onClose(id)` — pinned with fake timers and an explicit `advanceTimersByTime(300)`. **Auto-dismiss** is the same: after `duration + 300ms` total, `onClose(id)` fires; `duration=0` disables the timer entirely. ToastContainer renders one EnhancedToast per item in the `toasts` array. **`useRazorpayCheckout`** (8) — the iframe-based checkout host. Test uses `@testing-library/react`'s renderHook + a `MessageEvent` dispatcher to drive the postMessage protocol: `Frame()` is null pre-open, and renders an `<iframe>` overlay once `open()` is called. Iframe `'ready'` → parent posts `{type:'open', options}` to its `contentWindow.postMessage` (asserted via a stub on the iframe's contentWindow). `'success'` resolves the open Promise with the payload, `'dismiss'` rejects with `{kind:'dismissed'}`, `'error'` rejects with `{kind:'error', message}`. **A second `open()` while one is active rejects immediately** with 'already in progress'. **Foreign-origin postMessages are ignored** (a `dismiss` from `https://evil.example.com` doesn't settle the Promise — important CSP defence since the iframe loads the eval-using `checkout.razorpay.com/v1/checkout.js`).

**Slice 7ci update:** PricingCard + CenteredLoading + CustomToast — 21 tests across 3 files. **PricingCard** (6): title/subtitle/price + currency + period default (₹/mo), the optional rows (`originalPrice` rendered with line-through, `renewalPrice` copy, `discountBadge` → 'X% OFF'), `isPopular=true` adds the 'Most Popular' banner + the `border-indigo-600` border on the outer wrapper, **button-vs-link fork** keyed on `onButtonClick` presence (button branch fires the callback; link branch picks up `buttonLink` as href), feature list rendering with the `highlight=true` font-semibold class branch. **CenteredLoading + InlineLoader** (8): default 'Loading...' + dots, custom message override, `showMessage=false` drops both text + dots, the `size` → spinner h/w class map (xl → `h-20 w-20`), `fullScreen=true` wraps in a `fixed inset-0` overlay vs `fullScreen=false` using the `min-h-[200px]` inline wrapper; InlineLoader's sm default → `h-4 w-4`, lg → `h-6 w-6`, plus className passthrough. **CustomToast** (7) — imperative API. `react-hot-toast.toast.custom` is mocked via `vi.hoisted`; the captured render fn is then mounted standalone with a fabricated `Toast` arg to assert the rendered toast chrome: per-type **border-l-4 colour stripe** (green/red/blue), per-type icon (svg checkmark vs svg X vs spinning div), title rendering, **dismiss button calls `toast.dismiss(t.id)`**, `showLoadingToast` + `showPermanentToast` both pin `duration=Infinity` in the options passed to `toast.custom`, and `dismissible=false` omits the X button entirely.

**Slice 7ch update:** Session provider + hosting-upgrade SelectPlanStep + register AddressSection — three composite touchpoints. 14 tests across 3 files. SessionProvider (1) — a one-line wrapper around next-auth's SessionProvider; the test mocks `next-auth/react` via `vi.hoisted` and pins both the children render and that the next-auth wrapper is actually invoked, so a future swap to a different auth library surfaces. SelectPlanStep (7) — step 1 of the hosting-upgrade modal (companion of `<ConfirmStep>` from slice 7cg): current-plan name + price + remaining days, the **`hasSubscription` warning gate** (shown only when true), the eligible-plan list rendered with name + prorated charge via `formatIndianCurrency`, the **feature pills capped at 3** per plan (the 4th feature is asserted *absent*), plan-button click forwards the full `EligiblePlan` object to `onSelectPlan`, and the Cancel button fires `onCancel`. AddressSection (6) — the address subform of the register flow: the 4 visible fields (line1 + city + state select + zipcode) populated from `formData.address.*`, the **fixed Indian country display** with a hidden `address.country=IN` form-payload field, the INDIAN_STATES set populating the state select, the **Auto-fill** button forwarding to `onDetectLocation` and being disabled + showing the custom-Button loading spinner when `isDetectingLocation=true`, and the dotted-path field names (`address.city`, `address.state`) flowing through onChange unchanged so the parent's nested-state setter receives them.

**Slice 7cg update:** Register-form subforms + hosting-upgrade `<ConfirmStep>`. 14 tests across 3 files. CredentialsSection (4): both Password + Confirm-password fields render masked by default with `type="password"`, **the eye-icon toggles independently per field** (clicking password eye doesn't reveal confirm and vice versa), and onChange forwards the right `name` (`password` vs `confirmPassword`). PersonalInfoSection (4): the 5 visible fields (firstName/lastName/email/companyName/phone) render with their parent-supplied values, email is `type=email` + phone is `type=tel` (so the custom Input's autocomplete inference covered in slice 7ce keeps working), the **fixed Indian country-code** display (🇮🇳 +91 (India)) + a **hidden `phoneCc=+91` input** in the form payload pinned explicitly so a future country-add change surfaces, plus onChange wiring. ConfirmStep (6) — the third hosting-upgrade modal step: target plan name + From-current + 'X days' remaining render, the prorated charge appears via `formatIndianCurrency` (₹1,234 grouping), **the subscription-cancel amber warning is gated by `upgradeInfo.hasSubscription`** (shown when true, absent when false — pins the manual-renewal-billing wording), Back fires `onBack` and Pay fires `onPay`, and the **Pay button label includes the formatted charge amount** ('Pay ₹1,234' — matters because the visible amount is part of the payment-flow audit trail).

**Slice 7cf update:** Contact/loading + order timeline + floating cart — `<ContactMap>` + `<LoadingPage>` + `<OrderTimeline>` + `<FloatingCart>`. 19 tests across 4 files. ContactMap (4): 'Find Us' heading + the Anutech Digital address + Rohini Sector-5 line, the **OpenStreetMap iframe src embeds the company coords** (lat 28.7406, lon 77.0884) in both the bbox and marker query params (so a future coord change breaks loud), the Get-Directions external link with `target=_blank + rel=noopener`, and the 'Loading map...' indicator visible on initial render before the iframe's onLoad fires. LoadingPage (3): default 'Loading...' + animated dots, custom message override, `showDots=false` omits the trailing dots span. OrderTimeline (5): the **base 2-step set** (Payment + Confirmation) when both `hasDomains` and `hasHosting` are false, `hasHosting=true` inserts the 'Hosting account set up' step with the '2–5 minutes' timing label, `hasDomains=true` inserts 'Domain registered' with 'Up to 24 hours', the **strict order** payment→hosting→domain→email when both flags are on, and the `userEmail` is interpolated into the confirmation step's detail copy. FloatingCart (7) — mocks `next/navigation.usePathname`, `@/lib/storage`, and `@/store/cartStore` via vi.hoisted: cart link visible on a public route for a non-admin user, badge with the live count when > 0, **'9+' cap above 9**, no badge when count is 0, and the **four hide gates** — admin route, admin user (localStorage `user.role=admin`), invoice `/view` page (`/dashboard/invoices/.../view` substring), plus the SSR-mounted guard (`isMounted` is observed via the visible cart link after the effect flush).

**Slice 7ce update:** Modal + custom Button + custom Input — three of the highest-frequency form/dialog primitives still untested, plus the custom Input's autocomplete-inference heuristic. 27 tests across 3 files. **Modal** (6): `isOpen={false}` renders nothing (no title leak, no children leak), `isOpen={true}` exposes the title as an `<h3>` heading + the body, the close (X) button and the overlay div both fire `onClose`, **`closeOnOverlayClick={false}`** turns the overlay click into a no-op (defence against accidental dismiss on big-form modals), and the `size` prop maps to `max-w-sm`/`max-w-2xl`/`max-w-4xl` on the panel. AnimatePresence keeps exit-animating nodes around, so tests use fresh mounts via `unmount()` between size checks. **Button** (custom, 8) — distinct from the shadcn `ui/button` in 7bz: default primary blue gradient + md padding, the danger red gradient + outline white-bordered surface variants, the sm size padding scale, fullWidth → `w-full`, the **loading state** disables the button and embeds the spinner svg, an explicit `disabled` prop also suppresses onClick, and **`animate={false}` returns a plain `<button>`** (no motion.button wrapper) with onClick still firing. **Input** (custom, 13) — the meatiest of the three because of the **autocomplete inference**: explicit `autoComplete` always wins; otherwise `type=email` → `email`, `type=password` defaults to `current-password`, `name` containing 'confirm' → `new-password`, `name=password` + a 'create' substring in the placeholder → `new-password` (the register-form heuristic), `name=phone` → `tel`. Also pins label render, **error vs helperText mutual-exclusion** (error wins and adds `border-red-300` to the input itself), `forwardRef` to the underlying `<input>`, `icon` → `pl-10` padding, and that typing updates the value end-to-end.

**Slice 7cd update:** shadcn card primitives + admin/stats + consent — the shadcn `ui/card` family + `<AdminTable>` (lightweight sibling of the heavier `<AdminDataTable>` covered in 7bt) + `<StatsCard>` + `<CookieConsentBanner>`. 24 tests across 4 files. **ui/card** (8): Card with rounded-lg/border/bg-card + cn() className merging + ref forwarding; CardHeader flex-col p-6; CardTitle as `<h3>` with text-2xl font-semibold; CardDescription as `<p>` with text-sm/text-muted-foreground; CardContent p-6 pt-0; CardFooter flex items-center p-6 pt-0. **AdminTable** (5): loading=true renders the spinner + 'Loading...' copy *without* the table, the default 'No data available' empty copy + the `emptyMessage` override, header + key→value cell mapping for the row set, and `column.render(value, row)` winning over the default lookup. **StatsCard** (5): value + label + icon, the **three trend variants** — `up` → `text-green-600` + ↗, `down` → `text-red-600` + ↘, `neutral` → `text-gray-600` + → — and the **both-trend-and-trendValue-required** gating (one alone doesn't render the trend row). **CookieConsentBanner** (6) — `next-auth/react` useSession + `@/lib/storage` safeLocalStorage mocked via `vi.hoisted`: anon + no consent → banner appears, prior consent → hidden, **authenticated user → auto-accept** writes 'accepted' to localStorage and stays hidden, both the 'Accept & Continue' and 'X' dismiss buttons persist consent + hide, and the **defensive `useSession()` undefined race** (`sessionResult?.data` short-circuit, observed in hydration logs) does not throw — banner still shows the anon flow.

**Slice 7cc update:** Public-page primitives + motion shell — `<Card>` + `<HeroSection>` + `<FeatureCard>` + `<MotionProvider>`. 16 tests across 4 files. Card (7): the `rounded-lg` wrapper class set, all three `variant`s (`default` → `bg-white border`, `elevated` → `shadow-lg`, `outlined` → `border-2`), all three `padding` sizes, the `hover=true` opt-in (adds `hover:shadow-xl`), and the **`animate` fork** — `animate=false` returns the bare inner div as the root, `animate=true` (default) wraps it in a framer-motion `motion.div` (the outer wrapper has NO `rounded-lg` class; only its child does). HeroSection (5): the primary gradient default (`from-primary-600 to-primary-800`), the secondary (gray-600→gray-800) and dark (gray-800→gray-900) variant swaps, the `background='solid'` branch (drops `bg-gradient-to-r` for a single `bg-primary-600`), and the `backgroundImage` prop applying an inline `background-image: url(...)` style. FeatureCard (3): icon + h4 title + description render, className passthrough to the underlying Card, and the default `hover=true` propagating `hover:shadow-xl` to the Card's inner div. MotionProvider (1): one-line wrapper around `MotionConfig reducedMotion="user"` — pinned by children render passthrough.

**Slice 7cb update:** Loading + skeleton variants — `<LoadingSpinner>` (the plain primitive, distinct from the richer `components/user/LoadingSpinner` covered in 7bv) + three skeleton layouts. 13 tests across 4 files. LoadingSpinner (4): default md size mapping to `h-8 w-8`, sm → `h-4 w-4`, lg → `h-12 w-12`, and className passthrough on the outer `flex items-center justify-center` wrapper. SkeletonHero (2): exact placeholder count (4 — icon + title + subtitle + search-box) and the circular icon placeholder. SkeletonContact (2): the `md:grid-cols-2` form/info shell with **21 SkeletonBase children** — form column has 4 label+input pairs + a submit button (9), info column has 4 rows × 3 placeholders (12). SkeletonSection (5): the default render at `title=true / cards=3 / columns=3` produces 4 title placeholders + 3 × 5 card placeholders = 19 total in an `lg:grid-cols-3` grid; `title={false}` drops the title block entirely (15 placeholders); the `cards` prop multiplies through (`cards=5` → 25); the `columns` prop maps cleanly (`columns=4` → `md:grid-cols-4`; `columns=2` → `sm:grid-cols-2`). Each card has 5 internal SkeletonBase children (icon + title + 3 description bars), matching `<SkeletonCard>` in 7ca.

**Slice 7ca update:** Tiny presentational leaves — 5 tests across the skeletons folder, the rupee icon, and the page-mount motion wrapper. 12 tests total. SkeletonBase (3): default `bg-gray-200 + rounded + animate-pulse`, `animate=false` opt-out drops the pulse class, className passthrough via `cn()`. SkeletonCard (2): 5-placeholder composition (icon + title + 3 description bars), the circular icon placeholder. SkeletonStats (2): 4 cards × 4 placeholders = 16 SkeletonBase children in a grid. RupeeIcon (3): SVG element render, default + custom className mapping, the ₹ glyph present in the rendered text content. PageTransition (2): children passthrough + className applied to the motion.div wrapper (the framer-motion easing tuple is presentational-only, not asserted).

**Slice 7bz update:** Public chrome + ui primitives — `<ContactInfo>` + `<ScrollToTop>` + `<Switch>` + `<Button>`. 23 tests across 4 files. ContactInfo (5): the 'Get in touch' intro + the three contact cards (call/email/visit) each pinning its heading + the contact value + a context line (business hours, response window, address), plus className passthrough. ScrollToTop (4): the **> 300px visibility threshold** — hidden by default at offset 0, appears once `window.pageYOffset > 300`, **hides again** when scrolled back below the threshold (asserted via a manual `Object.defineProperty(window, 'pageYOffset', …)` + `fireEvent.scroll`), and a click calls `window.scrollTo({top:0, behavior:'smooth'})`. Switch (5): Radix Switch role + `aria-checked` binding to the controlled `checked` prop, `onCheckedChange` wiring with the new value, **disabled gate suppresses callbacks**, className merging. Button (9): the default render as `<button>` with `bg-primary + h-10`, all 5 variant class branches (`destructive`/`outline`/`ghost`/`secondary`/`link`), the 4 sizes (`default`/`sm`/`lg`/`icon`), the **asChild + Radix Slot composition** rendering the child element directly (anchor-as-button still picks up the variant classes — important: the test pins `tagName === "A"` so a future Slot replacement that wraps in a `<button>` surfaces), ref forwarding, and disabled-button suppresses onClick.

**Slice 7by update:** Layout + form primitives — `<Section>` + `<Header>` + `<Textarea>` + `<Badge>` (the shadcn primitive, not the dashboard `ExpiryBadge`). 21 tests across 4 files. Section (6): children + `id` propagation to the `<section>` element, the default `white` background + `lg` padding, and the four `background` variants (`white`/`gray`/`primary`/`dark` — with `dark` getting both `bg-gray-900` and `text-white`) + the four padding-size variants. Header (3): the fixed-top `z-50` wrapper + className passthrough. Textarea (6): label rendering, the **error vs helperText mutual-exclusion** (error wins precedence + adds the `border-red-300` red-ring class; helperText hidden when error is set), `forwardRef` to the underlying textarea element, and arbitrary props pass-through (placeholder/rows/name). Badge — the shadcn primitive at `components/ui/badge.tsx` (6): default variant gets `bg-primary`, the three other variants (`secondary`/`destructive`/`outline`) with `outline` notably **dropping the background entirely** and using `text-foreground`, plus the `cn()` className-override merging.

**Slice 7bx update:** Admin display + brand chrome — `<AdminCard>` + `<AdminTabs>` + `<Logo>`. 17 tests across 3 files. AdminCard (5): title + value, three change-variant colour classes (positive→green / negative→red / neutral→gray) + the no-change-prop hide. AdminTabs (4): tab list rendering with per-tab labels, the active-tab class fork (`border-primary-500` vs `border-transparent`), per-tab `onClick` wiring with non-active tabs unaffected, and crash-safety for tabs without an `onClick` handler (defensive — admin pages occasionally pass href-only navigation tabs). Logo (8): the **default-linked render** wrapping in Next/Link at `'/'`, custom `href`, **unlinked path when `href=''` is falsy** (the `if (href)` check), the conditional company-name label under `showText`, the **variant fork** (`light` → `/black-logo.png` + dark `text-gray-900` label; `dark` → `/logo-white.png` + `text-white` label), and the size→class mapping. Pins the asset-name swap explicitly so a future logo-rename surfaces in tests.

**Slice 7bw update:** Four small UI leaves — `<ClientOnly>` + `<EmptyState>` + `<RefreshButton>` + `<ExpiryBadge>`. 21 tests across 4 files. ClientOnly (3) pins the hydration-deferred render — under RTL, effects flush synchronously so we observe the post-mount state; the test pins the children-render-once-mounted + the null default fallback. EmptyState (4) — title + description, optional action button (rendered only when supplied; click fires onClick), className passthrough. RefreshButton (5) — default Refresh label + icon, showText=false icon-only mode, isLoading swaps the icon to `animate-spin` and disables the button, click wiring, title attribute defaults to 'Refresh Data' and honours a custom prop. **ExpiryBadge (9)** is the meatiest of the four — pins the null/NaN render-nothing, the **three-tier copy + colour fork** (> 60d → green + "Expires DATE"; 15-60d → yellow + "Expires in N days"; ≤ 14d → red + "Expires in N day(s) — Renew Now" with `animate-pulse`), the **expired path** (0 or negative days → "Expired" with red but **no pulse** — important: the pulse is only for the urgent-but-not-expired window 1-14d), the **singular 'day' boundary** at exactly 1 day, and the button-vs-span fork on the `onRenew` prop. Uses `vi.setSystemTime` for deterministic day-count math; `fireEvent.click` instead of user-event under fake timers to dodge the hang we've hit in earlier slices.

**Slice 7bv update:** Loading + skeleton primitives — `<Skeleton>` + the four prebuilt compositions, plus `<LoadingSpinner>` / `<PageLoading>` / `<DataLoading>`. 23 tests across 2 files. EnhancedSkeleton (10): the **variant-routing fork** on the base `Skeleton` (`animation: 'wave'` renders the relative-wrapper + gradient overlay path; `'pulse'` renders the simple box + `animate-pulse` class; `'none'` skips both), default `100%/200px` for rectangular vs `40px` circle for circular, explicit `width`/`height` overrides, and the prebuilt composition counts — `SkeletonText` defaults to 3 lines + honours the `lines` prop, `SkeletonCard` carries a circular avatar, `SkeletonTable` renders `1 header + N body` rows, `SkeletonList` renders the requested item count. LoadingComponents (13): `LoadingSpinner` message + size→class mapping + fullScreen container swap; `PageLoading`'s **page→message lookup** with the 'content' fallback for unknown keys (the audit-MD-style "magic key map" pattern — pinning all 4 known keys + the unknown-fallback so a future rename surfaces); `DataLoading`'s type=`table`/`card`/`list` count routing.

**Slice 7bu update:** `<ErrorBoundary>` — the L8 / batch-7w island-survival error boundary that wraps `<FloatingCart>`, `<ChatWidget>`, the cart-page body, etc. 7 tests pin: the happy-path passthrough (children render when nothing throws); the default inline fallback (`role="alert"` + "Something went wrong in this section." copy + "Try again" reset button); the ReactNode `fallback` prop override; the **function `fallback` form** — `fallback(err, reset)` is called with the captured `Error` and a `reset: () => void` callback, both verified by spy + signature shape; the labelled `console.error` on capture (`[ChatWidget] render error caught:` style prefix); and the default `[ErrorBoundary]` label fallback when no `label` prop is provided. `console.error` is stubbed to suppress React's own "uncaught error in render" warning *and* exposes the boundary's own log calls for assertion via the same spy. Class-component lifecycle (`getDerivedStateFromError`, `componentDidCatch`) exercised end-to-end via a `ThrowOnce` test harness.

**Slice 7bt update:** `<AdminDataTable>` — the generic admin-table workhorse consumed by 5+ admin pages. 10 tests cover the rendering surface (title + column headers + custom `column.render` callbacks executing per cell), the empty + loading rows, **client-side search** filtering (per-keystroke), the **sortable-column toggle** (first click sorts asc and reorders the rows, second click flips to desc), **client-side pagination** (page-2 button swaps the visible 10-row window), the **server-side pagination mode** when `totalItems` + `currentPage` + `onPageChange` are provided (uses external math for "Showing N to M of T" and fires `onPageChange` instead of mutating internal state), the **`onSearch` hook** firing per keystroke in server-side mode (so the parent can debounce server requests), the `searchable={false}` hide, and the **right-click → `onRowContextMenu`** wiring with the row data forwarded as the 2nd arg (used by admin pages that swap row clicks for context menus). The generic-`T` typing on the component is preserved at test boundary via a local `Row` interface so each call-site's `Column<Row>` shape gets type-checked.

**Slice 7bs update:** Admin display leaves — `<AdminStatsCard>` + `<AdminQuickActions>` + `<ActionMenu>`. AdminStatsCard (7): title + value render, the change panel with its three variants — `increase` shows TrendingUp + green-600 text, `decrease` shows TrendingDown + red-600 text, `neutral` shows the value with **no trend icon** (worth pinning so a "always render an icon" tidy-up doesn't slip in), plus "vs last month" suffix; the change panel hides entirely when the prop is omitted; colour variant maps to the right `bg-{c}-50` icon background incl. the default-to-blue fallback. AdminQuickActions (3): empty-actions → zero links; per-action anchor with href + heading + description; colour-variant `bg-{c}-50` classes. ActionMenu (7): isOpen render gate; per-item button with label; **item-click fires both its own onClick AND onClose** — pins the menu's contract that selecting an action also dismisses; click-outside via `mouseDown` on a sibling fires onClose; Escape fires onClose, other keys are ignored; the **danger-variant divider** above the danger item only renders when the danger item is non-first (so a single danger item or a leading danger has no orphan divider).

**Slice 7br update:** `<AttachmentPicker>` — the support-ticket image attachment picker. 10 tests pin: the trigger + helper-text render (label + the "PNG, JPG, WebP, GIF · max 2 MB · up to 4 images" hint); the custom `label` prop override; the thumbnail grid for existing attachments with filename caption + remove button; the index-correct removal via the X button; **the four validation gates each surfacing a distinct toast and short-circuiting `onChange`** — count > MAX_FILES (4) blocks the whole batch, non-image MIME is rejected per-file, MIME outside the JPEG/PNG/WebP/GIF allowlist is rejected (defence-in-depth behind the `accept` attribute), and files > MAX_BYTES (2 MB) are rejected; the happy-path upload runs the real `FileReader` to a `data:image/png;base64,…` URL and forwards `{filename, mimeType, size, dataUrl}` via `onChange`; the trigger is disabled when attachments are at capacity. The mime + non-image tests bypass user-event's `accept`-attribute filtering via a direct `fireEvent.change` + `Object.defineProperty` on `input.files` — important because both jsdom and user-event apply the `accept` attr by default, hiding the component's JS-side validation that we actually want to pin (the input attr is the first line of defence, the JS check is the second).

**Slice 7bq update:** Two small leaves — `<FAQItem>` + `<MessageAttachments>`. FAQItem (4): rendering both question and answer; the default-closed state (chevron not rotated); the **uncontrolled** mode (no `onToggle` → click flips internal `useState`, chevron gains `rotate-180`, click again closes); and the **controlled** mode (`onToggle` provided → click fires the callback but doesn't flip the component's own state; the parent's `isOpen` prop drives the chevron rotation). MessageAttachments (6): null/empty → renders nothing; the anchor shape (`href=dataUrl`, `download=filename`, `target="_blank"` + `rel="noopener noreferrer"` — pinning the cross-origin safety attrs so a future refactor can't silently drop them; `title` carries filename + `Math.round(size / 1024) KB` + the click hint); the inner img's `src=dataUrl` + `alt=filename`; multiple attachments each get their own anchor; the align-prop → justify-class mapping (`left`/`right`/`center` + the unspecified default falling to `justify-start`).

**Slice 7bp update:** `<MultiStageRegisterForm>` — the multi-field registration form with client validation, server Zod-error field mapping, and the requiresActivation success branch. 8 tests pin: the rendering (heading + 5 fields), the empty-fields validation gate (shows "Please confirm your password" + skips the API call — exercised via `fireEvent.submit` to bypass HTML5 required), the mismatched-confirmPassword inline error short-circuits the API call, the happy-path POST shape (`{firstName, lastName, email, password}` — confirmPassword is **not** forwarded), the two success branches with `requiresActivation:true` → `/login?message=…&returnUrl=…` vs `:false` → `/login?returnUrl=%2Fdashboard` (the latter pins that the component's `returnUrl` fallback to `/dashboard` is always appended — a future "bare /login when no returnUrl" tweak would surface), the Zod field-error tree from `result.error.body.details` mapped back onto per-field inline errors (recursive walk down to `_errors`), the generic toast on errors without `details`, and the inline-error clearing on subsequent keystrokes (with framer-motion's exit animation handled via `vi.waitFor`). With this the auth-form trio (Register + ResetPassword + AdminPasswordReset) is fully covered. AuthShell + SocialLoginButtons mocked as thin shells so the test stays focused on form behaviour.

**Slice 7bo update:** `<HeroResultCard>` + `<CompactResultCard>` — the leaf result cards that `<SearchResults>` orchestrates. 10 tests across both: HeroResultCard (5) pins the EXACT MATCH + BEST VALUE badges and "Available to register" copy on the available fork, the strike-through original at `price × 1.5` next to the main `formatPrice`, the "MAKE IT YOURS" → `onAdd` wiring; the TAKEN badge + "Already registered" + the disabled NOT AVAILABLE button on the taken fork; the conditional NOTIFY ME button — present when `onWatch` is provided, absent otherwise. CompactResultCard (5) pins the domain name + Available + BUY NOW → `onAdd`, the **"SAVE N%" computation** from `originalPrice` (asserted with the `Math.round((1500 - 999) / 1500 * 100) = 33` case to lock the rounding semantics in case future "always floor" or "always ceil" tweaks land), the "Live Price" badge under `pricingSource === 'live'`, the WATCH callback on the taken fork with `onWatch`, and the disabled N/A fallback when `onWatch` is missing. Real `useDomainPricing` hook is used (it's a pure Intl.NumberFormat wrapper, no need to mock). With this the entire `components/domain-search/` subtree is now component-tested (`SearchInput` + `SearchResults` + `DomainCard`).

**Slice 7bn update:** `<SearchResults>` — the orchestrator that turns search state into the result panel. 10 tests pin the AnimatePresence pre-render gate (no DOM until `isSearching || hasSearched`), the "Analyzing Availability" loading splash, the hero+compact split (HeroResultCard for `results[0]`, CompactResultCards for `results.slice(1)`), the gate on the "Other Popular Extensions" section (only rendered when there's > 1 result — pinning that boundary so a future "always show secondaries" tweak surfaces), the inline error banner, the Clear Search callback wiring, the suggestions-loading skeleton (asserted via `.animate-pulse` count), the category-tab filtering (All passes everything through; switching to Tech filters by `s.category === 'Tech'`), the Load More button visibility under `canLoadMore`, the click wiring, and the `isLoadingMore` swap to "Loading More..." with the button disabled. The `HeroResultCard` / `CompactResultCard` children from `./DomainCard` are mocked as deterministic stubs so the test focuses on this component's orchestration rather than the leaf cards' rich content.

**Slice 7bm update:** `<SearchInput>` — the registrar-style two-piece search bar (input pill + attached blue button) used on the domain-search hero. 9 tests pin: the value mirroring (`searchTerm` → input `.value`); onChange firing per keystroke; onSearch firing both from the button click and from Enter inside the wrapping container; the empty-term disable on the button and the re-enable once a value is present; the `isSearching` state — input + button disabled and the button swaps to a Loader2 + "Searching..." label; and the `searchMode === 'multiple' && baseDomain && !hasSearched` gate on the "We'll check .com, .net, .in and more for …" prompt — visible when all three are satisfied, suppressed once a search has fired and suppressed entirely in single mode. The latter three cases are deliberately separate render calls rather than `rerender` chains — framer-motion's `AnimatePresence` keeps elements in the DOM during exit animations under jsdom, so a fresh mount is the only reliable way to observe the absent state.

**Slice 7bl update:** `<ProfileCompletionForm>` — the post-checkout profile-completion gate. 6 tests cover: the disabled `firstName` / `lastName` / `email` inputs sourced from the `user` prop (those fields aren't editable in this step — pinning that contract); the happy-path POST shape with the **forced India-only invariants** (`phoneCc: "+91"` and `address.country: "IN"` ignore whatever's in the form state — important because the `<input type="hidden">` markers are not authoritative if a client tampered with state); the localStorage merge — read existing user, spread the response user on top, write back — plus the `profileUpdated` window event dispatch (asserted via a real `window.addEventListener` spy); the router.push destination — `/dashboard` by default, the URL's `?returnUrl=` honoured when present (a cart→checkout→profile→cart loop relies on this); the failure path surfacing `result.error.message` via toast with no `router.push` so the user stays on the form; and the GST input's `e.target.value.toUpperCase()` transform on every keystroke.

**Slice 7bk update:** Operational status leaves — `<OutboundIPBadge>` (5) + `<DomainBookingProgress>` (5). OutboundIPBadge pins the loading-pulse badge on mount, the single-IP green-dot path, the multi-IP orange-dot + `"*"` suffix path with the `title` tooltip carrying `"All IPs: …"`, and both failure modes (response `success:false` + apiClient `!ok`) folding to the "IP Error" red badge. DomainBookingProgress pins the loading state, the empty-`bookingStatus` "No booking status information available" fallback, the rendered progress % + the per-step messages, the `domain_registered` last-step path firing `onComplete()` and rendering the "Domain Registration Complete!" success panel with the domain name highlighted, and the Refresh-Status button re-fetching. **Quirk surfaced:** `DomainBookingProgress`'s `useEffect` deps include `isComplete`, so when it flips to true the effect re-runs and re-fires `onComplete` — the test asserts called-at-least-once with an inline comment so a future deps cleanup is free to drop the double-fire without breaking the suite. Polling intervals on both components (`OutboundIPBadge`'s 30-min refresh, `DomainBookingProgress`'s 3s `autoRefresh`) aren't asserted — `autoRefresh={false}` keeps the latter deterministic.

**Slice 7bj update:** `<TrialOtpModal>` — the phone-OTP step rendered when the admin's `hosting_trial_otp_required` toggle is on. 10 tests pin: the `isOpen` render gate (returns null when closed); the `defaultPhone` normalisation pipeline — strip non-digits → drop leading "91" → keep last 10 chars (`"+91 99887 76655"` → `"9988776655"`); the 10-digit Send-OTP enable gate; happy-path `/trial-otp/send` with `{phone}` swaps to the code stage with the phone mirrored at the user; failure-path surfaces the route's `error.message` without swapping; happy-path `/trial-otp/verify` with `{phone, code}` stashes the returned signed token in `sessionStorage.trial-otp-token` and calls `onVerified()`; a **2xx response with no token is treated as a verification failure** — pinning that contract so a future "trust 2xx" simplification surfaces; the "Change number" link returns to the phone stage and clears the code input; Close fires `onClose`. The 60s Resend cooldown countdown is intentionally not asserted (timer-driven, prone to the same jsdom-timeout class flagged in 7bg/7bh).

**Slice 7bi update:** `<LivePricingIndicator>` — the live-pricing chip on the domain-search results. 5 tests: mount-fetch hits `/api/v1/domains/pricing?tlds={tld}`, the nested `data[tld].customer.addnewdomain["1"]` is parsed as float and surfaced as the Intl en-IN INR display + the `onPriceUpdate(price, "INR")` callback fires; the "Live" green-badge appears next to the heading once a price is loaded and is suppressed in failure states; the two distinct error messages — "Live pricing not available" when the response is OK but the `tld` key is missing from `data` (an upstream-shape quirk worth pinning so a future "simplify error copy" refactor doesn't merge them), and "Failed to fetch live pricing" when apiClient itself returns `ok:false`; and the Refresh button re-fetches and updates to the new price.

**Slice 7bh update:** Public-facing forms — `<ContactForm>` (4 tests) + `<ForgotPasswordForm>` (6 tests). ContactForm: full-form render, happy-path POST to `/api/v1/contact` with `{name, email, subject, message, recaptchaToken: null}` and swap to the "Message Sent!" success screen, "Send Another Message" button bringing the empty form back, and `showErrorToast` mirroring the route's `error.message` on failure (no success swap). ForgotPasswordForm: default vs isSetup heading + button-label swap, the `prefilledEmail` prop pre-populating the input (used in the guest-conversion flow), happy-path POST + sent-screen swap with the email **mirrored back at the user** in the confirmation copy, the isSetup-flavoured "Setup link sent" toast + sent-screen wording, and the failure path surfacing the route's message via both the inline red error-bar AND the toast (no swap). The 60s "Send Another Email" cooldown countdown is intentionally not asserted — pure timer-driven UI, prone to the same jsdom timeout class that bit `ResetPasswordForm` last slice.

**Slice 7bg update:** Auth password-reset forms — `<AdminPasswordReset>` and `<ResetPasswordForm>`, 11 tests across 2 files. `AdminPasswordReset` (5): empty-fields / mismatched-passwords disable the submit; matching short passwords (button enabled) hit the < 8-chars guard and toast; happy path POSTs `{newPassword, confirmPassword}` to `/api/v1/admin/reset-password`, fires the success toast, and clears both fields; failure path surfaces the route's `error.message`. `ResetPasswordForm` (6): default vs `isSetup` headings + button labels swap correctly; mismatch and length validation toasts fire and short-circuit the apiClient call; happy path POSTs `{token, password, recaptchaToken}` to `/api/v1/auth/reset-password` and swaps to the success screen; the `isSetup` variant renders the "Account Activated" heading and "account is ready" toast. Four nice-to-have flows on `ResetPasswordForm` (3s redirect timer, Go-to-Login click, fallback-copy on failure, eye-icon password toggles) were dropped — they tripped jsdom's default 5s timeout under user-event's cumulative state across multi-render tests, and the load-bearing logic (validation, payload, success-screen swap, both isSetup variants) is already covered.

**Slice 7bf update:** Admin invoice-diagnostics subtree — three small focused tables that triage the Zoho-Books reconciliation bugs surfaced by the slice-7ad migration. 21 tests across 3 files: `<DiagnosticsHeader>` (6) pins the issue-vs-clear icon and copy, singular/plural grammar ("1 conflict, 1 stuck order" vs "3 conflicts, 2 stuck orders"), the **row-click → onToggle vs Refresh-chip-click → onRefresh** with `e.stopPropagation` suppressing toggle on the chip, and the spinner class while loading; `<ConflictsTable>` (7) pins the empty-null-render, the group header (`invoiceNumber` + "N orders" count chip), the per-row fields, the `isDeleted` tag, the long-zohoInvoiceId truncation with full value preserved via `title` attribute, the Clear # callback wiring, and the row-targeted `pendingId` disable; `<StuckOrdersTable>` (8) pins the empty-null-render, the missing-zoho "missing" fallback, the **`length > 1` gate on the Re-sync-all button** (single order → no bulk button; multi → "Re-sync all (N)"), the per-row `onResync` callback, the bulk-in-flight progress bar + "X synced · Y failed · Z remaining" line + "Re-syncing X/Y…" button label, and the per-row `pendingId` disable.

**Slice 7be update:** `<DomainSetup>` — the "Connect a Domain" card shown for unattached hosting items (hosting placeholder with no `linkedDomain`). 11 tests across both tabs: heading + the plan-name `||` fallback to "Hosting Plan"; **Link Existing tab** — empty input → "Required", malformed → "Invalid domain format", typing clears the inline error, valid input → `onUpdateDomain(placeholder, newDomain)` after the 500ms `setTimeout` plus the success toast; **Buy New tab** — tab switch surfaces the search UI, `.com` auto-append on a bare label (asserted against `apiClient.post`), available + taken panels, apiClient failure → `toast.error`, and the Add & Link flow firing **both** callbacks atomically — `onAddDomainToCart` with a `CartItem` at `registrationPeriod: 1` (different from `<HostingUpsell>`'s 12-month default — pinning that intentional divergence) and `onUpdateDomain(placeholder, boughtDomain)` to link the freshly-added domain to the hosting placeholder, followed by the search state being cleared. With this the entire cart-page subtree is now component-tested: store + helpers upstream, the EmptyCart / CartItemCard / CartOrderSummary trio in the middle, HostingUpsell + DomainCrossSell + DomainSetup on the upsell rail, ProfileCompletionWarning gating, and CheckoutPage's redirect useEffects downstream.

**Slice 7bd update:** `<DomainCrossSell>` — the search-and-add card shown when the cart has no domain. 8 tests: disabled-when-empty submit; the `.com` auto-append when the user types a bare label (asserted against the `apiClient.post` argument); the three result shapes — available (green panel + Add button), taken (red panel, no Add), `success:false` (red panel with the route's error mirrored to the unavailable copy); the apiClient failure → `toast.error("Failed to search domain")` and no panel; the exact-match preference when the API returns multiple entries (the matching `domainName` wins over the first array element); the typing-clears-result behaviour driven by the `onChange` reset; and the add-to-cart `CartItem` shape (`domainName`, `price`, `currency`, `registrationPeriod: 12`, `itemType: 'domain'`) with the success toast.

**Slice 7bc update:** `<HostingUpsell>` — the "Add Standard Hosting" upsell card on the cart page. 5 tests: the static render (heading + ₹/original-strikethrough/Save-50%/features list incl. the "30-Day Money-Back Guarantee" tail entry + the button); the add-to-cart shape (`itemType:"hosting"`, `registrationPeriod:12`, `billingCycle:"yearly"`, `currency:"INR"`, `price` from the standard plan, `hostingPlan.id` matches the catalog, `domainName` starts with `"hosting-"` — the unique-id pattern); the auto-link to an existing domain via `linkedDomain`; the dedupe path (same `hostingPlan.id` already in cart → error toast, `addItem` not called); and a regression-pin that legacy domain items without an `itemType` field still count for the auto-link lookup (`!item.itemType || item.itemType === 'domain'`).

**Slice 7bb update:** `<ProfileCompletionWarning>` — the amber banner that sits above the cart and pushes users to `/dashboard/settings` when their phone or address is missing. 12 tests covering: the no-user → null path; the `profileCompleted === true` early-exit; the three missing-field shapes (both missing → "phone number and address … are missing"; only phone → "phone number … is missing"; only address → "address … is missing"); the whitespace-only trim guard; the session-vs-localStorage merge **precedence** — local overrides session because the component does `{ ...session.user, ...local }` so a stale-but-incomplete cache surfaces the warning even when the session looks complete; localStorage as the user source when no session; Complete-now `router.push` to `/dashboard/settings?returnUrl=%2Fcart` with a returnUrl and to bare `/dashboard/settings` without; Dismiss hiding the warning via the `isDismissed` latch; and the `profileUpdated` window-event handler re-evaluating and hiding the warning once localStorage holds a complete user.

**Slice 7ba update — checkout-redirect gating (the deferred M14 piece):** First page-level component test under `tests/unit/app/checkout/`. 9 tests against `<CheckoutPage>`'s two redirect `useEffect`s — the explicit deferred M14 item. The page pulls in a forest of unrelated subcomponents/hooks, so each is stubbed at the module boundary (Navigation/Footer/OrderTimeline/ClientOnly/CheckoutPageSkeleton → `() => null`; `useRazorpayCheckout` → `{ open, Frame: () => null }` after the first run surfaced the page's `<razorpay.Frame />` render; logout/storage/device-fingerprint/logger as no-op spies; toast as a callable-with-method spy; `useSession`/`useCartStore`/`global.fetch`/`useRouter` as configurable `vi.hoisted` mocks). The first `useEffect`: status-loading bails before fetch; admin role → `router.push("/admin/dashboard")`; profileCompleted not strictly `=== true` (incl. the string `"true"` — pinning the strict-boolean coercion) → toast + `/cart`; non-ok `/auth/me` → `/login`; thrown fetch → `/login`; completed profile → no redirect, `syncWithServer` runs. The second `useEffect`: after the user is set and the cart finishes loading empty (and no payment in progress) → `router.replace("/dashboard")`; suppressed while `isLoading` is still true. Full unit suite 637 green; typecheck clean.

**Slice 7az update:** `<CartItemCard>` — 14 tests against the cart line-item component, the rendering workhorse for both domain and hosting items. Covers the domain-vs-hosting display fork (heading uses `domainName` for domain, `hostingPlan.name` for hosting); the period-label formatting (domain "N year(s) registration", hosting "N month(s) subscription", hosting-with-12-months "1 year subscription"); the `.ai`-style min-period annotation (selected option labelled "(Minimum)" + the amber ".AI requires min 2 year registration" notice — uses the real `getMinRegistrationPeriod` via the actual TLD-policies table, not a mock); the billing-cycle lock (`yearly` → fixed "1 Year" display, no `<select>`; `monthly` → "1 Month"); the hosting-placeholder "Domain Required" warning visibility (shown for `hosting-*` items lacking a `linkedDomain`, gone once one is attached, replaced by the "Domain: foo.com" line); the price math (`price × registrationPeriod`); the multi-year domain "Price locked for N years" + "No renewal needed until …" badges; the annual hosting "Save N%" + "₹X off vs monthly billing" badges with the 50% saving math; and the two callbacks — `onPeriodChange` fires with `(domainName, parsedYears, itemType, "months")`, `onRemove` fires with `(domainName, itemType)`. Full unit suite 628 green; typecheck clean.

#### ✅ [M15] Integration test suite runs serially (`fileParallelism: false`)
**File:** [vitest.integration.config.ts:30](vitest.integration.config.ts)
**Problem:** 159 tests across 16 files run serially. Will scale linearly as coverage grows.
**Fix:** Per-file `beforeAll` spinning up a fresh `MongoMemoryServer` on a random port. ~2 hours.

---

## Cleanups (LOW)

### ✅ [L1] Razorpay SDK escape-hatch casts repeated 10× — CLOSED in batch 7t
**Files:** `lib/razorpay.ts:41,223,244,257,303,338,354`, `lib/razorpay-payments.ts:54,77,93`
**Fix:** Wrap once in a typed `lib/razorpay-client.ts`; collapse the 10 casts to 1.
**Resolution:** New `lib/razorpay-client.ts` constructs the SDK once, sets the 30s timeout, and exposes a strongly-typed `TypedRazorpayClient` facade (`orders` / `payments` / `subscriptions` / `plans` with our `RazorpayPaymentDetails` etc. on the return types). The single `as unknown as TypedRazorpayClient` cast at module scope is the only type-bridging cast left; all seven callsites in `razorpay.ts` and two of the three in `razorpay-payments.ts` are now clean. The third callsite (`getPaymentById`) had zero consumers and was deleted.

### ✅ [L2] Three orphan/duplicate types in `lib/types.ts`
**File:** [lib/types.ts:1-11](lib/types.ts) (`User` duplicates `IUser` and is wrong), `:65-76` (`Payment` — `IPayment` is used directly), `:69-78` (`DNSRecord` — model deleted in M4 batch).
**Fix:** Delete all three; add ESLint rule to flag duplicate `I*`-name interfaces.

### ✅ [L3] Magic numbers `=== 10`, `=== 12` for hosting billing cycle inference (5 sites)
**Files:** `lib/services/payment/post-tasks.ts:68`, `app/checkout/page.tsx:486`, `app/checkout/guest/page.tsx:446`, `app/api/payments/create-order/route.ts:182`, `store/cartStore.ts:44-47`
**Fix:** `lib/billing.ts` exports `inferPeriodUnit(item: CartItem)` returning a strict enum.

### ✅ [L4] `puppeteer` in `dependencies` but only used by test scripts
**File:** `package.json` (puppeteer ^24.28.0, used only in `tests/browser/`)
**Problem:** Adds ~200MB to production node_modules + cold-start working set.
**Fix:** Move to `devDependencies`.

### ✅ [L5] Two parallel client-side loggers with different signatures
**Files:** `lib/logger.ts` (27 consumers, `(...args)` signature) and `lib/client-logger.ts` (2 consumers, `(message, details)` signature). Both POST to `/api/v1/log`.
**Fix:** Pick one shape; migrate; delete the loser. ~2 hours.

### ✅ [L6] `Redis = null as unknown as Redis` when REDIS_HOST is unset
**File:** [lib/redis.ts:22](lib/redis.ts)
**Problem:** Structural lie — every caller of `redis.foo()` will NPE. Currently OK because all callers are inside try/catch, but fragile.
**Fix:** Export typed `Redis | null`; force callers to narrow.

### ✅ [L7] `findUserOrder` still uses `$or` despite Batch 4 [L4] saying it was fixed
**File:** [lib/services/orders.ts:276-281](lib/services/orders.ts)
**Problem:** Sibling `getOrderByIdOrOrderId` was correctly fixed with a single-filter branch; this one was missed. Behaviour fine due to `userId` scope, but description/code disagree.
**Fix:** Mirror `getOrderByIdOrOrderId`. ~10 min.

### ✅ [L8] React error boundaries — CLOSED in batch 7w
**Problem:** A single React render error inside (say) the cart UI tears the whole page tree down to the root `error.tsx`. No mid-tree isolation.
**Fix:** Add `<ErrorBoundary>` around cart, chat widget, floating cart count. ~3 hours.
**Resolution:** Generic `<ErrorBoundary>` (class component, since React's error lifecycles only exist on class components) added at `components/ErrorBoundary.tsx`. Three integrations: `FloatingCart` in the root layout (fallback=null — silent failure preferred to a visible warning on every page), `ChatWidget` on the homepage (fallback=null), and the full cart-page body in `app/cart/page.tsx` (default fallback — the visible reload prompt, since the cart is the primary content). Navigation and Footer stay outside the boundary so a cart crash doesn't tear those down too.

### ✅ [L9] `app/error.tsx` still reads the dead localStorage `token` for "Was user logged in" display
**File:** [app/error.tsx:49-50](app/error.tsx)
**Fix:** Read from `useSession()` (client component, available).

### ✅ [L10] `Order.paymentId` `required:true, unique:true` is write-only/fallback-only after H1 migration
**File:** [models/Order.ts:14,16,122-126](models/Order.ts)
**Problem:** Every renewal-webhook construction fabricates a unique `paymentId` (using Razorpay payment id) just to satisfy the index. `razorpayPaymentId` is the real source of truth and is already indexed.
**Fix:** Drop `required` + `unique`; reads use `razorpayPaymentId ?? paymentId` defensively. ~30 min + migration.

### ✅ [L11] `getUserByIdSafe` is functionally identical to `getUserById` after `password: select:false`
**File:** [lib/services/users.ts:22,33-37](lib/services/users.ts)
**Problem:** Comment claims one is dangerous; in reality neither returns password without explicit `.select("+password")`. Misleads new contributors.
**Fix:** Delete the duplicate; pick the surviving name. ~30 min.

### 🔄 [L12] Accessibility coverage is thin — 10/62 components carry any aria/role attribute — PARTIALLY ADDRESSED (lint setup landed in 7w.2)
**Problem:** No keyboard navigation tests; modals likely lack focus traps + `aria-modal`.
**Fix:** Add `eslint-plugin-jsx-a11y`; run axe-core in CI. ~1 day for lint setup; remediation per component.
**Slice 7w.2 update:** `plugin:jsx-a11y/recommended` added to `.eslintrc.json` (the plugin was already installed transitively via `next/core-web-vitals`, but only 6 of the recommended ~30 rules were active). Enabling the full preset surfaced 144 issues across 7 rules: `label-has-associated-control` (105 — most form labels are wrapping `<input>` instead of using `htmlFor`), `no-static-element-interactions` (17 clickable `<div>`s), `click-events-have-key-events` (10 same family), `anchor-is-valid` (6), `no-autofocus` (4), `html-has-lang` (1), `heading-has-content` (1). Downgraded those 7 specific rules from the preset's `error` to `warn` so new code is flagged but existing patterns don't break the build; remediation is per-component as the audit notes. `npm run lint` still exits 0. Runtime axe-core in CI deferred — needs `@testing-library/react` first.

---

## Architectural Suggestions (no current bug, structural improvement)

### ✅ [S1] No shared API client in the frontend — 58 files do raw `fetch("/api/...")` — EFFECTIVELY CLOSED (helper in 7aa + 24-slice sweep 7aa–7ax; 46/~58 files migrated, remainder is by-design raw fetch)
**Problem:** Only 8 files use `lib/fetcher.ts`; the rest hand-roll. Every endpoint addition touches N callsites; response error handling is per-file; no central place to attach request-id / tracing.
**Suggestion:** Thin typed `apiClient.get(url, schema)` returning a `Result<T, ApiError>`. Migrate incrementally; pair with Zod schemas at the boundary.
**Slice 7aa update:** Helper landed at `lib/api-client.ts` — `apiClient.{get, post, put, patch, delete}` returning `ApiResult<T> = {ok: true; data} | {ok: false; error: ApiError}`. ApiError normalises to `{status, message, code?, body?}` and pulls the route-side `error` + `code` fields (matched pair with the slice 21–32 `validatedBody` helper). Optional Zod schema parses the response body and surfaces "Response schema mismatch" on failure. Network errors (fetch threw) surface as `status=0`. `credentials: "include"` is automatic. 12 new tests pin the 200/4xx/network/schema-mismatch/method-routing paths. 3 representative callsites migrated to validate the API: `OutboundIPBadge` (GET typed against IPData), `ContactForm` (POST), `ForgotPasswordForm` (POST).

**Slice 7ab update:** 5 more callsites migrated — `AdminPasswordReset` (POST), `ProfileCompletionForm` (POST), `DomainBookingProgress` (GET, typed against a `BookingStatusResponse` shape; the 3s-interval polling now goes through the client), `DomainCrossSell` (POST search), `GoogleRecaptcha` (GET captcha-status — the fail-soft security posture is preserved: only an explicit `{enabled:false}` skips the captcha; both non-ok responses and network errors now map cleanly to `result.ok=false` and keep the captcha shown). Each conversion dropped a try/catch + manual `res.json()` + `response.ok` ladder. Total: 8 of ~58 frontend files now use `apiClient`.

**Slice 7ac update:** 4 more callsites — `ResetPasswordForm` (POST), `LivePricingIndicator` (GET pricing, typed against a `PricingResponse` shape), `TrialOtpModal` (both the OTP send + verify POSTs), `DomainSetup` (POST search). Streaming endpoints like `ChatWidget` (SSE) and the Razorpay-flow modals (DomainRenewalModal / HostingRenewalModal / HostingUpgradeModal — their POST lands mid-payment-handler) are intentionally deferred to a dedicated grouping. Total: 12 of ~58 frontend files now use `apiClient`.

**Slice 7ad update:** 3 more components (6 fetches) — `MultiStageRegisterForm` (POST register, with the Zod `details` field-error tree now read from `result.error.body`), `DomainSearch` (POST watch — the 401/409/400 status branching now switches on `result.error.status`), `admin/InvoiceDiagnostics` (all 4 fetches: GET conflicts + clear-invoice-number POST + re-sync POST + the sequential bulk re-sync loop). The `useDomainSearch` hook (3 fetches incl. generation-superseding + a fire-and-forget Phase-2 suggestions call) and `RegisterForm` (4 fetches) are deferred — both have enough control-flow nuance to warrant focused slices. Total: 15 of ~58 frontend files now use `apiClient`. Remaining: ~43.

**Slice 7ae update:** First `app/` pages migrated, plus a helper enhancement — added an optional `cache?: RequestCache` to `apiClient`'s `RequestOptions` (passed through to `fetch`) so always-fresh polling reads can request `cache: "no-store"`. `app/maintenance/page.tsx` (2 status-poll GETs, fail-closed on error → assume maintenance on), `app/activate/page.tsx` (3 fetches: `auth/me` GET with a Bearer header via the new `headers` option + activate POST with its expired/invalid-token branching now keyed on `result.error.message` + resend POST; dropped the now-unused `logger` import). Total: 17 of ~58 frontend files now use `apiClient`.

**Slice 7af update:** 2 admin pages — `app/admin/invoices/page.tsx` (the paginated invoices-list GET; the sibling PDF-download fetch stays raw since it uses `res.blob()`, not JSON), `app/admin/pricing-management/page.tsx` (TLD-pricing GET + cache-purge DELETE — the latter shows off `apiClient.delete`). Confirmed the binary-download routes (`invoices/[id]/view`, the invoice PDF endpoints) are intentionally left on raw `fetch` — `apiClient` is JSON-only by design. Total: 19 of ~58 frontend files now use `apiClient`. Remaining: ~39 (minus the handful of blob/stream endpoints that will stay on raw fetch permanently).

**Slice 7ag update:** 3 more pages — `app/dashboard/domains/transfer/page.tsx` (POST transfer), `app/domains/bulk-search/page.tsx` (POST bulk-search; network vs server error split via `result.error.status === 0`), `app/admin/domains/page.tsx` (domains list GET + per-row sync POST; dropped the now-unused `logger` import). Total: 22 of ~58 frontend files now use `apiClient`. Remaining: ~36.

**Slice 7ah update:** Dashboard support pages — `app/dashboard/support/page.tsx` (NewTicketForm create-ticket POST, typed against `{ ticket: { ticketNumber } }`) and `app/dashboard/support/[id]/page.tsx` (close-ticket PATCH + reply POST). All three mutations dropped their try/catch + manual `res.json()` + `res.ok` ladder; network vs server errors now split on `result.error.status === 0`. The SWR list/detail GETs stay on `useSWR(fetcher)` — apiClient only replaces the imperative mutation fetches here. Total: 24 of ~58 frontend files now use `apiClient`. Remaining: ~34.

**Slice 7ai update:** Admin support-tickets pages (the admin-side twins of 7ah) — `app/admin/support-tickets/page.tsx` (auth/me GET + ticket-list GET) and `app/admin/support-tickets/[id]/page.tsx` (auth/me GET + ticket-detail GET + reply POST + status PATCH + priority PATCH). 9 fetches total. Unlike the dashboard twins these had no SWR — all raw `fetch` with `credentials: "include"` (now automatic). Note on the auth/me path: the old code routed a network `throw` straight to `/login` but an HTTP-error to the session fallback; apiClient collapses both into `result.ok === false` → session fallback → `/login`, so a transient network blip now attempts the NextAuth-session fallback before bouncing to login (strictly more resilient, same end state when no valid session). `setTicket(... ?? null)` keeps the `Ticket | null` state type now that the response is typed. Total: 26 of ~58 frontend files now use `apiClient`. Remaining: ~32.

**Slice 7aj update:** Admin hosting pages — `app/admin/hosting/pending/page.tsx` (pending-list GET, retry POST, delete) and `app/admin/hosting/packages/page.tsx` (packages-list GET, package PATCH). The pending page dropped its now-unused `logger` import (the only `logger.error` lived in the migrated catch). Two preserved nuances: (1) the retry handler refreshes the list on both success and server-rejected outcomes but NOT on a network error — mapped to `if (!result.ok) { toast; return }` ahead of the refresh; (2) the packages-list page's DA-server-down sentinel can arrive either as HTTP 503 / `code: "DA_SERVER_DOWN"` on a non-ok response OR as `code: "DA_SERVER_DOWN"` inside a 200 body — both branches kept, the former via `result.error.{status,code}`, the latter via `result.data.code`. Total: 28 of ~58 frontend files now use `apiClient`. Remaining: ~30. (Slices 7ai + 7aj committed; deploy batched for later per user.)

**Slice 7ak update:** Admin transactions pages — `app/admin/order-management/page.tsx` (paginated orders GET with its prefetch/cache machinery untouched, delete/archive DELETE, unarchive PATCH) and `app/admin/payment-management/page.tsx` (latest-5-payments GET). The orders GET previously swallowed errors via a `logger.error` in the catch and otherwise no-op'd; since apiClient never throws, the whole try/catch/finally collapsed to a plain `if (result.ok) {...}` followed by the unconditional `fetching.current.delete` + loading-flag reset (same cleanup the old `finally` did) — dropped the now-unused `logger` import there. The delete handler also dropped a dead `const data = await response.json()` it never read. payment-management kept its `logger` import (still logs load failures, now off `result.error.message`). Total: 30 of ~58 frontend files now use `apiClient`. Remaining: ~28.

**Slice 7al update:** Admin 2FA page `app/admin/settings/security/page.tsx` — all 4 fetches (totp/setup GET in the mount effect, totp/setup POST to start enrolment, totp/confirm POST, totp/disable POST). The three handlers dropped their `throw new Error(data.error) … catch(showErrorToast)` pattern for the flat `if (result.ok)` shape; error text now reads `result.error.message` (apiClient already lifts the route's `error` field into it). The mount GET's `.then/.catch` promise chain became a small async IIFE with the same fail-to-`false` default. Note this page targets the shared `auth/totp/*` routes (not admin-scoped), so the dashboard-side security page — if one exists — could reuse the exact same conversion later. Total: 31 of ~58 frontend files now use `apiClient`. Remaining: ~27.

**Slice 7am update:** Admin hosting main page `app/admin/hosting/page.tsx` — all 7 fetches (DA stats GET, the provision-deps pair `hosting/packages` + `users/no-hosting`, provision POST, actions POST, hosting-details GET, change-package POST). This was the most guard-heavy page so far: the old stats fetch hand-rolled a `content-type` sniff to catch 502/503 HTML from Nginx before `res.json()`. apiClient already parses best-effort and leaves `data === undefined` on an unparseable 200 body, so that guard became a clean `if (!result.data)` branch, while the 503/`DA_SERVER_DOWN`/network paths fold into `!result.ok` (checking `error.status === 503 || error.code === 'DA_SERVER_DOWN' || error.status === 0`). The DA-down sentinel is still also honoured when it rides inside a 200 body (`result.data.code === 'DA_SERVER_DOWN'`). `fetchProvisionDeps` went from two sequential awaits to a single `Promise.all` (no dependency between the two GETs); its DA-down check now covers both the non-ok-error and the 200-body-sentinel forms via one combined predicate. `logger` stays imported (still used in the actions + change-package error paths). One behaviour normalisation worth noting: a non-503 HTTP error that previously returned `{success:false}` JSON used to show "Failed to fetch hosting data"; it now routes through `!result.ok` and shows the server-supplied `error.message` while marking the DA banner — acceptable given the DA-down detection (the load-bearing part) is preserved. Total: 32 of ~58 frontend files now use `apiClient`. Remaining: ~26.

**Slice 7an update — plus a helper enhancement:** Admin user-management `app/admin/user-management/page.tsx` — all 8 fetches: the 3-way parallel user-list load (active / deactivated / services), reset-password POST, reactivate POST, reset-2fa POST, and the two DELETEs (soft-deactivate + `?permanent=true` hard-delete). The parallel load previously used `Promise.allSettled` + per-result `.ok`/`.json()` unwrapping; since apiClient never throws it became a plain `Promise.all` of three `ApiResult`s with `result.ok ? data.users : []` each (the service-users branch still `logger.warn`s on failure, so `logger` stays imported). **Helper enhancement:** both DELETEs send a JSON body (`{ userId }`), but `apiClient.delete(url, schema?, opts?)` had no body slot — so `delete` now takes `(url, body?, schema?, opts?)` mirroring post/put/patch (the underlying `request` already gates on `body !== undefined`, so the three existing no-body `delete` callers — pricing-management, hosting/pending, order-management — are unaffected; verified by full `tsc`). Total: 33 of ~58 frontend files now use `apiClient`. Remaining: ~25.

**Slice 7ao update:** Admin pending-domains `app/admin/pending-domains/page.tsx` — all 10 fetches across every HTTP verb the helper now covers: balance GET, auth/me GET (same session-fallback normalisation as 7ai/7an), the paginated list GET, register + verify POSTs, archive + `?permanent=true` DELETEs, mark-resolved + retry PUTs. The retry handler keeps its two-step dependency (PUT reset-to-pending → POST register) — the early `if (!resetResult.ok) return` now resets `actionLoading` before bailing (previously the `finally` did it). The two DELETEs use the new bodyless `apiClient.delete` (no `{userId}` here — the id is in the path/query). No `logger` in this file, so nothing to drop. Total: 34 of ~58 frontend files now use `apiClient`. Remaining: ~24.

**Slice 7ap update:** Admin system-settings `app/admin/system-settings/page.tsx` — 12 of its 13 fetches: razorpay-mode GET + save-keys/switch-mode POSTs, the three `admin/settings` GET loaders (IP-whitelist, captcha, CORS — all typed against `{ settings?: Record<string, { value?: unknown }> }`), the four `admin/settings` save POSTs, and the check-ip GET. **The backup fetch stays raw** — it's a `res.blob()` download with bespoke 401/403/500 status-code messaging, exactly the kind of binary/stream endpoint apiClient is not meant for (same call made for the invoice-PDF routes in 7af). Notable: the save-whitelist and save-CORS handlers each fire two sequential POSTs that previously relied on a shared try/catch to surface any failure; since apiClient never throws, each now captures both `ApiResult`s and shows the success toast only when *both* `.ok`, logging the first failing `.error.message` otherwise (the union is narrowed via `!x.ok ? x.error.message : …` so TS is happy). `logger` stays (used across every loader's failure path + the untouched backup handler). Total: 35 of ~58 frontend files now use `apiClient`. Remaining: ~23.

**Slice 7aq update:** Dashboard settings `app/dashboard/settings/page.tsx` — 8 of its 9 fetches: the user-settings GET loader + its two PUT savers (change-password, update-profile), the auth/me GET, and the four totp endpoints (setup GET in the security-tab effect, setup/confirm/disable POSTs — same shape as the admin 2FA page in 7al). **The nominatim.openstreetmap.org reverse-geocode stays raw** — it's a cross-origin third-party call with a custom `User-Agent`, and apiClient is for same-origin `/api/*` routes (it force-sets `credentials: include`, wrong for a public CORS endpoint). Two response-typing snags surfaced and were fixed cleanly rather than papered over: the settings GET is typed `UserSettings & { profile?: Record<string,string> }` so `setSettings(data)` stays assignable, and the `/auth/me` merge keeps its original `any`-era spread semantics via a single `as User` cast on the updater return (the server user carries extra `password`/`provider` fields not on the local `User` interface). Total: 36 of ~58 frontend files now use `apiClient`. Remaining: ~22.

**Slice 7ar update:** Admin dns-management `app/admin/dns-management/page.tsx` — all 10 fetches: the domains-list GET, dns-records GET (with its 404-driven "zone still propagating" 30s-retry loop preserved — now keyed on `result.error.status === 404` instead of `response.status`), the nameservers GET with default-NS auto-detect, the default + custom nameserver POSTs, activate-dns POST, record-add POST, and the two record DELETEs that ship a `recordData` body (delete-record + the delete-then-add edit flow) — these lean on the new `apiClient.delete(url, body?)` from 7an. Dropped the now-unused `logger` import (its only use was the domains-load catch) plus a dead `initialNsSnapshot` local that was captured but never read. The DNS GET's `errorData = await response.json().catch(...)` was also dead (parsed, never used) and is gone. Total: 37 of ~58 frontend files now use `apiClient`. Remaining: ~21.

**Slice 7as update — DNS pair complete:** Dashboard dns-management `app/dashboard/dns-management/page.tsx` (the user-facing twin of 7ar) — all 12 fetches: services-status GET (its catch was the only `logger` use, so the import is gone), domains-list GET, dns-records GET (404 propagation retry preserved), nameservers GET (keeps the `data.success`-false branch + the 404/500-specific toasts via `result.error.status`), the default + custom nameserver POSTs with their 10-attempt verification polling loops intact, record-add POST, and the record DELETEs-with-body (delete + the edit's delete-then-add **plus its best-effort restore-POST on add failure**) — all on `apiClient.delete(url, body?)` / `apiClient.post`. With 7ar this closes the DNS-management pair. Total: 38 of ~58 frontend files now use `apiClient`. Remaining: ~20.

**Slice 7at update — biggest single page:** Admin settings `app/admin/settings/page.tsx` — all 22 fetches. The page's `authHeaders = (extra) => extra` helper was a no-op (it never added a Bearer token — H3 in 7c confirmed none is written), so it's deleted outright and apiClient's automatic `credentials: include` + Content-Type covers everything. Breakdown: 9 GET loaders (incl. the `Promise.all` cache+settings pair, which became two `ApiResult`s), the cache PUT + DELETE, two check-ip GETs, the IP-whitelist + CORS save pairs (now `Promise.all` of two POSTs, success toast only when both `.ok`), and the captcha/hosting-trial/trial-OTP/test-plan/maintenance save POSTs. The many `try {…} catch {}`-swallow loaders collapse to flat `if (result.ok)` since apiClient never throws. Three response-typing snags fixed by typing the GET generics to the existing state shapes (`IPData` for both check-ip + ip-status, the inline cache-status shape) rather than casting. Total: 39 of ~58 frontend files now use `apiClient`. Remaining: ~19.

**Slice 7au update:** Two user-facing service pages — `app/dashboard/hosting/page.tsx` (cancel-trial POST + auto-renew PATCH; SWR GET untouched) and `app/dashboard/domains/[id]/page.tsx` (the two sequential loadDomainDetails GETs — services-status then user/domains — plus the nameservers GET and the NS-update POST). The latter dropped its now-unused `logger` import (all three uses were in the migrated catches). The two-step domain load keeps its semantics: services-status is best-effort (`statusResult.ok ? data.hostedDomains : []`) and the domains GET drives the not-found redirect. Total: 41 of ~58 frontend files now use `apiClient`. Remaining: ~17.

**Slice 7av update:** Three one-/two-fetch reads/syncs — `app/hosting/page.tsx` (trial-eligibility POST keeping its OTP_REQUIRED / otpRequired modal branches + the public hosting-test-plan GET, now an async IIFE; `logger` stays for an unrelated auto-link log), `app/dashboard/page.tsx` (the domains/sync POST — its nuanced handling preserved: success/auto-import toasts, the `NO_LINKED_ACCOUNT` "register your first domain" path, network-vs-server split; since apiClient lifts `error` not `message`, the message/code are read back from `result.error.body` so the route's `message`-field copy and `code` survive), and `app/dashboard/invoices/page.tsx` (only the invoices/sync POST — the pay + payments/verify stay raw as part of the deferred Razorpay payment-handler flow, and the invoice-PDF `res.blob()` download stays raw by design). Total: 44 of ~58 frontend files now use `apiClient`. Remaining: ~14 — almost entirely the intentionally-raw set (payment-handler POSTs, blob downloads, ChatWidget SSE, cross-origin nominatim, the JWT-branch error boundaries) plus the deferred `RegisterForm` + `useDomainSearch` hook.

**Slice 7aw update — first 7ad-deferred item cleared:** `components/domain-search/hooks/useDomainSearch.ts` — all 3 `/api/v1/domains/search` POSTs. This was deferred in 7ad for its control-flow nuance; the key piece is the **generation-superseding guard** (`searchGenRef` bumped per search; each callback bails if `thisGen !== searchGenRef.current`). That survives the migration unchanged because the guard runs *after* the single `await` — so Phase-1 quick now does one gen-check covering both the old success-path and catch-path checks. Phase-2 (suggestions + hostingExists) was a fire-and-forget `.then/.catch/.finally`; it's now a `void (async () => …)()` IIFE that keeps both gen-checks and the "suggestions are non-critical" silent-fail. Load-more keeps its TLD-batch accounting. Total: 45 of ~58 frontend files now use `apiClient`. Remaining: ~13 (RegisterForm is the last non-deferred-by-design holdout; the rest are payment-handler/blob/SSE/cross-origin/JWT-boundary fetches that stay raw).

**Slice 7ax update — S1 incremental sweep effectively complete:** `components/RegisterForm.tsx` register POST → `apiClient`, storing the returned token in localStorage + the JS-readable `token` cookie exactly as before; network vs server errors split on `result.error.status === 0` (preserving "An error occurred. Please try again." vs the route's error message). The handler's try/catch/finally collapsed since the register call was its only throw source. **Its other 3 fetches stay raw by design** — they're cross-origin reverse-geocoding calls (BigDataCloud primary → OpenStreetMap Nominatim fallback, with a third targeted Nominatim postcode lookup), each with custom `Accept-Language`/`User-Agent` headers; apiClient is same-origin-`/api/*`-only and force-sets `credentials: include`, so these correctly remain on `fetch`. **This was the last cleanly-migratable callsite.** What remains on raw `fetch` is now entirely by-design: payment-handler POSTs (checkout/cart/Razorpay renewal+upgrade modals/invoice pay+verify), blob downloads (invoice + order PDFs, backup), the ChatWidget SSE stream, cross-origin geocoding (RegisterForm + dashboard settings nominatim), and the JWT-branch error boundaries (`app/error.tsx`, `app/global-error.tsx`). Total: 46 of ~58 frontend files now use `apiClient`; the **S1 architectural suggestion is effectively closed** — every internal JSON `/api/*` callsite that should migrate has migrated.

### ✅ [S2] No pre-commit hooks (husky/lefthook); CI is the only gate — CLOSED in batch 7y
**Suggestion:** Husky + lint-staged running `tsc --noEmit` on staged files and `eslint --fix`. ~30 min.
**Resolution:** `husky` + `lint-staged` installed as devDeps; `prepare` script wired so `npm install` auto-installs the git hook. `.husky/pre-commit` runs `npx lint-staged` (eslint --fix on staged `.ts/.tsx/.js/.jsx` per the `lint-staged` config in `package.json`) followed by `npx tsc --noEmit` (project-wide typecheck). Skipping with `--no-verify` is supported but discouraged — the same checks gate CI.

### ✅ [S3] Most API routes parse JSON bodies with no validation — CLOSED (helper + 12-slice sweep across batches 7z–7z.12, all 73 routes validated)
**Files:** `app/api/admin/users/route.ts`, `app/api/domains/dns/route.ts`, `app/api/admin/users/reset-2fa/route.ts` use `Schemas.*.safeParse`. The other 130+ routes do `const { x, y } = await request.json()`.
**Suggestion:** `validatedBody<T>(req, schema)` helper. Sweep highest-risk routes (user mutations, admin writes, payment webhooks) first. Helper ~30 min; sweep is route-by-route.
**Slice 7z update:** Helper landed at `lib/api-validation.ts` — `validatedBody(req, schema)` + `validatedQuery(req, schema)`, both returning a `{ok: true, data} | {ok: false, response}` discriminated union with a uniform 400 + `code: "VALIDATION_ERROR"` / `code: "INVALID_JSON"` shape. 10 new tests pin the parse-fail + multi-issue join + coercion paths. 5 high-risk routes migrated: `payments/cancel-subscription` (hostingId required), `payments/create-order` (cart-items array shape), `user/domains/watch` (domain-name shape + length), `user/complete-profile` (nested address shape), `user/hosting/check-eligibility` (optional domainName).

**Slice 7z.2 update:** 6 more routes migrated — `payments/create-subscription` (planId/interval/domainName), `user/hosting/renew` (domainName), `user/hosting/cancel-trial` (hostingId via Schemas.id), `user/hosting/[id]/auto-renew` (boolean autoRenew flag), `user/domains/nameservers` (with a Zod `.refine` linking method=custom to ≥2 nameservers, replacing 20 lines of inline validation), `user/settings/change-email` (Schemas.email + currentPassword, replacing 4 hand-rolled checks).

**Slice 7z.3 update:** 5 more routes — `admin/razorpay-mode` (Zod `discriminatedUnion("action")` typing the save_keys / switch_mode variants distinctly; replaces a runtime `if (action === …)` re-check), `user/hosting/upgrade` (domainName + targetPlanId), `contact` (full structural shape; existing InputValidator still runs after Zod for content safety + sanitization), `user/support` (subject/category/message + attachments shape; replaces 4 hand-rolled length checks). Plus a latent-typing fix in `contact/route.ts` — Zod giving `email: string` revealed that the `InputValidator.*.sanitized` return is `string | Record<string,string>`; coerced via `String()` at the boundary.

**Slice 7z.4 update:** 6 more routes — `user/support/[id]` (PATCH close-only + POST reply, with attachment shape + 5000-char cap), `admin/support-tickets/[id]` (PATCH status/priority via `.enum`, POST admin reply), `user/hosting/trial-otp/send` (optional phone — body or User record), `user/hosting/trial-otp/verify` (phone + 6-digit code regex), `user/hosting/trial-eligibility` (planId / deviceFingerprint / recaptchaToken / otpToken — `z.infer` re-exports the input type so the shared `runEligibility` helper keeps a single type alias), `chat` (LLM endpoint — array shape + role enum + bounded content per message; replaces 14 lines of `(m as unknown)`-style filtering).

**Slice 7z.5 update:** 6 admin write routes — `admin/hosting/change-package` (username + newPackage caps), `admin/hosting/actions` (discriminated union per action — suspend/unsuspend require username because the typed DA wrappers don't accept undefined; delete accepts either username or hostingId; surfaced + fixed a latent bug where the previous `any`-typed body would crash at the wrapper boundary when only hostingId was sent for suspend/unsuspend), `admin/hosting/assign` (userId via Schemas.id + package + domain), `admin/users/reactivate` (userId via Schemas.id), `admin/users/reset-password` (userId + newPassword min-6 + optional sendEmail with default true), `admin/log-error` (full system-log entry shape — replaces an unauthenticated `await req.json()` that fed straight into recordSystemLog).

**Slice 7z.6 update:** 6 more admin write routes — `admin/domains/nameservers` (same refine-based shape as the user twin from 7z.2; replaces 20 lines of inline validation), `admin/domains/activate-dns` (domainName + optional force flag), `admin/domains/sync` (single domain OR domainNames array, batch capped at 100), `admin/diag-da/cleanup` (usernames bulk-delete, capped at 100), `admin/reset-password` (admin self-reset — Zod refine validates newPassword === confirmPassword so the path-tagged error attaches to confirmPassword, replacing 3 hand-rolled checks), `admin/tld-pricing/cache` (PUT — enabled / ttlMinutes with refine: at least one required; ttlMinutes capped at 30 days).

**Slice 7z.7 update:** 6 admin routes — `admin/hosting/test-plan` (action enum + optional plan id), `admin/hosting/packages` (POST + PATCH — POST uses `.passthrough()` for arbitrary DA flags, PATCH validates the partial-update shape against Schemas.id), `admin/hosting/provision` (userId/domain/package/daUsername/validity/price/periodUnit — moved validation ahead of the admin auth check; the script's existing `let body: ProvisionBody = {}; try {...} catch {}` early-parse pattern was kept for the catch-handler's later `body` reference), `admin/backup` (password required, replacing a 2-step manual JSON-parse + missing-password check), `admin/users/[id]` (PUT updates firstName/lastName/email/role/isActive partial shape, Schemas.email + role enum), `admin/pending-domains/verify` (domainIds bulk-array, capped at 100). Plus a latent typing fix in `admin/hosting/packages` — Zod giving `quota: string | number | undefined` surfaced that the DA wrapper expects `string`; coerced via `String(...)` at the boundary.

**Slice 7z.8 update:** 6 more routes spanning admin + auth + workers — `admin/settings` (key + value as `z.unknown()` to preserve the arbitrary-JSON-value contract; description + category caps), `admin/pending-domains` POST (full create-row shape — userId via Schemas.id, currency / period defaults handled downstream, numeric-or-string customer/contact IDs preserved), `auth/activate` (single activation token, length-capped), `workers/sync-zoho-invoice` (full Cloud Tasks payload — orderId/userId/serviceType/domainName/hostingPlanId/amount/currency/razorpayPaymentId/durationMonths; replaces a 4-field hand-rolled check that was reachable only after destructure), `workers/sync-hosting-status` (userId), `workers/process-hosting-expiry` (hostingId). The workers are particularly nice — Cloud Tasks payloads now fail-fast with a structured 400 instead of crashing midway through processing.

**Slice 7z.9 update:** 6 more routes finishing the workers + the security-critical auth surface — `workers/process-service-expiry` (serviceId + hosting/domain enum + optional simulatedTime string-only — TimeService.parse rejects raw ms, which the previous `any` body silently coerced), `auth/check-account-status` (Schemas.email), `auth/totp/confirm` (6-8 digit regex), `auth/totp/disable` (code 6..64 chars accepting TOTP or backup code; runtime branches downstream; + password), `auth/resend-activation` (Schemas.email), `admin/domains/dns` POST + DELETE (finishes the slice 11 GET-only migration — required record envelope with `.passthrough()` for per-type extras; the previous `request.json().catch(() => ({}))` pattern in DELETE meant `recordData` was effectively `any`, masking that RC's wrapper requires all four fields).

**Slice 7z.10 update:** 6 user-facing domain routes — `domains/search` (domain + tlds-as-union-of-array-or-CSV-string for legacy clients + quick flag), `domains/activate-dns` (domainName + optional force), `domains/renew` (GET via `validatedQuery` with `z.coerce.number()` defaulting years to 1; POST with paymentId required — was silently allowed empty before), `domains/booking-status` (GET refine "orderId OR domainName"; POST extracts a `bookingStepSchema` enum mirroring the model's BookingStep — surfaces a real shape contract that the old `string` accept would only have hit at Mongoose enum-validation save time), `domains/verify-status` (domainName), `domains/transfer` (domainName + authCode capped at 128, no regex pin since EPP code formats vary by registry).

**Slice 7z.11 update:** 6 more routes finishing nearly all the remaining surface — `log` (client log payload — level enum + bounded message + z.unknown() details), `domains/bulk-search` (array of strings, route dedupes downstream), `domains/nameservers` (same refine-based shape as the user + admin twins from 7z.2/7z.6), `cart` (.passthrough() envelope; the existing TLD-clamp + restricted-TLD-drop logic runs after Zod's structural gate), `test/automation/trigger` (optional serviceId/serviceType/now), `admin/pending-domains/[id]` PATCH (status enum mirroring the PendingDomain model's allowed values — exposed a latent bug where the route's downstream order-sync compared `status === "registered"` but the model never allows that value, so every pending → completed admin action silently set the Order domain to `status=failed`; fixed by switching to the actual enum mapping `completed → registered`, anything-else → failed).

**Slice 7z.12 update — S3 closed:** 3 payment routes — `payments/verify` (cartItems[] + order_id OR subscription_id refine + payment_id + signature; replaces a 3-condition post-destructure check), `payments/guest/verify` (same envelope + guestToken with its own min(1) for "token required"), `payments/guest/create-order` (envelope-only `.passthrough()` schema; the route has two modes — with guestToken: signed token is source of truth, body's registrant fields are ignored; without: deeper per-field validation runs inside the route to preserve precise error wording). **73 of 73 routes now validate** — every API route that parses a JSON body now does so through `validatedBody` or an equivalent Zod-based path. Helper landed in slice 21 (`067da79`); the 12-slice sweep ran 21 → 32 across admin writes, payment flows, user mutations, auth + workers, user-facing domain ops, and the payment-verification surface. S3 architectural suggestion closed.

---

## What surprised me

**Half-finished NextAuth migration.** Batch 6d closed 5 admin pages but ~30% of the frontend still wires Bearer tokens that nobody writes. Three callsites are load-bearing: cart-store (H1), `app/error.tsx`, `app/checkout/page.tsx`'s "credentials login uses JWT token" branch. The system limps along because every API route falls through to a NextAuth-cookie path that hides the bug.

**The PendingDomain L5 fix is half-wired.** App-layer filter was scoped correctly, DB-layer unique index was not — worse outcome than the pre-fix silent overwrite.

**Zero React tests.** 588 tests, 0 component/page/store tests. The 1500-line admin pages and the cart store (more complex than several services) have no automated coverage at all.

**The anti-corruption-layer gap** is structurally the most expensive piece. 51 `toLowerCase().includes(...)` chains + 122 direct upstream-SDK callsites. Every prior rescan flagged some flavor of "RC/DA error leaked / string-typed". The per-finding fixes are correct but they're a Hydra. A proper `lib/integrations/` boundary would close that class of bug permanently.

---

## Suggested batching

- ~~**Batch 7a** — H1 (cart breakage)~~ ✅ shipped `c4aec87`
- ~~**Batch 7b** — H2 (PendingDomain index + prod migration)~~ ✅ shipped `45e8e85` + `caece03`
- ~~**Batch 7c** — H3 (Bearer cleanup sweep, 38 files)~~ ✅ shipped `6bdd43b`
- ~~**Batch 7d** — H4 (Mongo pool sizing)~~ ✅ shipped `9b60a9f`
- ~~**Batch 7e** — M2 + M3 + M5 + M7 + M9 + M10 + M11 + M12 + M15 + L2~~ ✅ shipped `8aee422`
- ~~**Batch 7f** — L3 + L4 + L5 + L6 + L7 + L9 + L10 + L11~~ ✅ shipped `5ee9c94`
- ~~**Batch 7g** — M1 slice 1: RC registerDomain anti-corruption~~ 🔄 shipped `67390e4`
- ~~**Batch 7h** — M1 slice 2: DA createUser anti-corruption~~ 🔄 shipped `6391de2`
- ~~**Batch 7i** — M1 slice 3: RC renewDomain anti-corruption~~ 🔄 shipped `2fca12a`
- ~~**Batch 7j** — M1 slice 4: inner/outer RC fragment vocab unified~~ 🔄 shipped `c31bf1e`
- ~~**Batch 7k** — M1 slice 5: DA suspendUser anti-corruption~~ 🔄 shipped `292802c`
- ~~**Batch 7l** — M1 slice 6: DA unsuspendUser + webhook suspend migration~~ 🔄 shipped `90ac494`
- ~~**Batch 7m** — M1 slice 7: RC transferDomain~~ 🔄 shipped `facfb87`
- ~~**Batch 7n** — M1 slice 8: DA getUserConfig + sync-worker migration~~ 🔄 shipped `23fda8e`
- ~~**Batch 7o** — M1 slice 9: DA deleteUser + admin/hosting/actions sweep~~ 🔄 shipped `ba451f2`
- ~~**Batch 7p** — M1 slice 10: RC getDomainOrderId / getDomainDetails~~ ✅ shipped `c76ce35`
- ~~**Batch 7q** — M1 slice 11: RC getDNSRecords~~ ✅ shipped `a4b4422` (DA modifyDomain dropped — no callers in codebase; updateDNS is permanently disabled)
- ~~**Batch 7r** — M1 slice 12: DA changePackage + upgrade-flow + admin route~~ ✅ shipped `26d201f`
- ~~**Batch 7s** — M13 (delete dead `error.message.includes` chain — typed PaymentError unnecessary, no callers throw those strings)~~ ✅ shipped `299f84c`
- ~~**Batch 7t** — L1 (typed Razorpay SDK client, 10 casts → 1)~~ ✅ shipped `9b6d018`
- ~~**Batch 7u** — M14 partial: pure cart-validation helpers + 21 unit tests~~ ✅ shipped `0011477`
- ~~**Batch 7v** — M14 continued: store-level cart tests (addItem/removeItem cascade, getters, syncWithServer happy path)~~ ✅ shipped `12d2cbd`
- ~~**Batch 7w** — L8 (React error boundaries)~~ ✅ shipped `2339a2a`
- ~~**Batch 7w.2** — L12 (jsx-a11y eslint surface)~~ ✅ shipped `7c466b5` (lint setup; runtime axe + per-component remediation deferred)
- ~~**Batch 7x** — M6 partial: 14 leaf components demoted from `'use client'` to server~~ ✅ shipped `0f35e5f`
- ~~**Batch 7y** — S2: pre-commit hooks (husky + lint-staged + tsc --noEmit)~~ ✅ shipped `2660cfe`
- ~~**Batch 7z** — S3 helper (`validatedBody` / `validatedQuery`) + 5-route sweep + 10 tests~~ ✅ shipped `067da79`
- ~~**Batch 7z.2** — S3 continued: 6 more routes (payments/create-subscription, hosting/renew + cancel-trial + auto-renew, domains/nameservers, settings/change-email)~~ ✅ shipped `e87ad6a`
- ~~**Batch 7z.3** — S3 continued: 5 more (admin/razorpay-mode discriminated-union, hosting/upgrade, contact, user/support, latent `.sanitized` typing fix)~~ ✅ shipped `8b4aac3`
- ~~**Batch 7z.4** — S3 continued: 6 more (user/support/[id], admin/support-tickets/[id], trial-otp send + verify, trial-eligibility, chat)~~ ✅ shipped `1dfc073`
- ~~**Batch 7z.5** — S3 continued: 6 admin write routes (hosting/change-package, hosting/actions discriminated-union + latent bug fix, hosting/assign, users/reactivate, users/reset-password, log-error)~~ ✅ shipped `a978849`
- ~~**Batch 7z.6** — S3 continued: 6 more admin write routes (domains/nameservers, domains/activate-dns, domains/sync, diag-da/cleanup, reset-password, tld-pricing/cache)~~ ✅ shipped `60cf32f`
- ~~**Batch 7z.7** — S3 continued: 6 admin routes (hosting/test-plan, hosting/packages POST + PATCH, hosting/provision, backup, users/[id] PUT, pending-domains/verify) + latent quota-NaN fix~~ ✅ shipped `ba4f8d5`
- ~~**Batch 7z.8** — S3 continued: 6 routes (admin/settings, admin/pending-domains POST, auth/activate, 3 workers)~~ ✅ shipped `d524fd9`
- ~~**Batch 7z.9** — S3 continued: 6 routes (process-service-expiry, auth/check-account-status, totp/confirm + disable, resend-activation, admin/domains/dns POST + DELETE)~~ ✅ shipped `33ee8ff`
- ~~**Batch 7z.10** — S3 continued: 6 user-facing domain routes (search, activate-dns, renew, booking-status, verify-status, transfer)~~ ✅ shipped `993272a`
- ~~**Batch 7z.11** — S3 continued: 6 routes (log, bulk-search, nameservers, cart, test-automation, pending-domains/[id]) + latent pending-domain status bug fix~~ ✅ shipped `a9317ec`
- ~~**Batch 7z.12** — S3 COMPLETE: 3 payment routes (verify, guest/verify, guest/create-order) — **73/73 routes validated**~~ ✅ shipped `5355331` (+ follow-up fix `d7cd9cb`)
- ~~**Batch 7aa** — S1: typed frontend `apiClient` (`ApiResult<T, ApiError>` + Zod-aware) + 3 callsites + 12 tests~~ ✅ shipped `bd4edbf`
- ~~**Batch 7ab** — S1 continued: 5 more frontend callsites migrated to `apiClient`~~ ✅ shipped `6d6b714`
- ~~**Batch 7ac** — S1 continued: 4 callsites (ResetPasswordForm, LivePricingIndicator, TrialOtpModal ×2, DomainSetup)~~ ✅ shipped `e9c5458`
- ~~**Batch 7ad** — S1 continued: 3 components / 6 fetches (MultiStageRegisterForm, DomainSearch watch, InvoiceDiagnostics ×4)~~ ✅ shipped `ba0602c`
- ~~**Batch 7ae** — S1 continued: first app/ pages (maintenance, activate ×3) + `cache` option on apiClient~~ ✅ shipped `cbe3301`
- ~~**Batch 7af** — S1 continued: 2 admin pages (invoices list-GET, pricing-management GET + DELETE)~~ ✅ shipped `02b2d7f`
- ~~**Batch 7ag** — S1 continued: 3 pages (domains/transfer, bulk-search, admin/domains GET + sync POST)~~ ✅ shipped `d390f38`
- ~~**Batch 7ah** — S1 continued: dashboard support pages (create-ticket POST, close PATCH, reply POST)~~ ✅ shipped `9bd22ea`
- ~~**Batches 7ai–7ax** — S1 sweep through admin + dashboard pages + 7ad-deferred holdouts (useDomainSearch, RegisterForm). 16 slices closing the S1 architectural suggestion: 46/~58 frontend files on `apiClient`; remainder is by-design raw `fetch` (payment-handler POSTs, blob downloads, ChatWidget SSE, cross-origin geocoding, JWT-branch error boundaries). Helper enhancement: `apiClient.delete` now accepts an optional body.~~ ✅ shipped through `4d3ac2c`
- ~~**Batches 7ay–7bo** — M14 component-test sweep. `@testing-library/react` + jest-dom installed (audit's "transitively available" note was stale). 17 slices building 775 tests across 48 files: the cart-page subtree (EmptyCart / CartOrderSummary / CartItemCard / HostingUpsell / DomainCrossSell / DomainSetup / ProfileCompletionWarning), CheckoutPage redirect gating, admin invoice-diagnostics triage (DiagnosticsHeader / ConflictsTable / StuckOrdersTable), auth password-reset forms (AdminPasswordReset / ResetPasswordForm), public-facing forms (ContactForm / ForgotPasswordForm), status leaves (LivePricingIndicator / TrialOtpModal / OutboundIPBadge / DomainBookingProgress), ProfileCompletionForm, and the full domain-search subtree (SearchInput / SearchResults / DomainCard hero+compact).~~ ✅ shipped through `ab35b6c`

### Remaining open work (no batch numbers assigned yet)
- **M14 remaining** — `MultiStageRegisterForm` (multi-step + Zod field-error tree), admin/dashboard layout shells (UserLayout / AdminLayout / DashboardSidebar — mostly presentational), and the deliberately-out-of-scope payment-handler / SSE / cross-origin / JWT-boundary flows that stay un-tested by design.
- **M4** — 5 page components > 1000 lines (multi-day per page).
- **M6 deeper** — per-component refactors to extract interactive bits so static shells can render on the server. The easy leaf demotions landed in 7x; the remaining ~110 client components genuinely need client features (framer-motion, event-handler props, forwardRef with refs, Radix), so further M6 progress requires component-splitting refactors, not one-shot demotions.
- **axe-core in CI** — runtime accessibility checks (now unblocked — the M14 harness is in place).
- **M8** (PendingDomain._id ObjectId) — deferred pending prod data audit.
